//! Placeholder entry point. Task 3 replaces this with the full clap dispatch
//! and exit-code mapping. This minimal stub exists so `cargo build` succeeds at
//! the Task 1 checkpoint while still compiling the core/commands modules.

#[allow(dead_code)]
mod cli;
#[allow(dead_code)]
mod commands;
#[allow(dead_code)]
mod core;

fn main() {}
