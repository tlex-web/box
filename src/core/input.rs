//! Shared stdin/arg reader for `box` commands (D-04/D-05).
//!
//! The full implementation (`read_input` / `read_input_bytes` with the TTY
//! no-block guard) lands in plan 02-01 Task 3. This placeholder exists so the
//! module registers and the crate builds while the four Phase-2 crates are
//! verified.
