---
phase: 04-terminal-visuals
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - Cargo.toml
  - src/cli.rs
  - src/main.rs
  - src/commands/mod.rs
  - src/commands/ascii/mod.rs
  - tests/ascii.rs
  - tests/cmd/ascii.in/tiny.png
  - tests/cmd/ascii.in/tiny.jpg
autonomous: true
requirements: [ASCI-01]
nyquist_compliant: true

must_haves:
  truths:
    - "D-00: ascii follows the established slice — Ascii unit variant in src/cli.rs swapped for Ascii(AsciiArgs), the not_implemented(\"ascii\") arm at src/main.rs:78 deleted, input via a positional path arg, data→stdout / messages→stderr, exit 0/1/2, assert_cmd tests with checked-in fixtures; unit tests run via cargo test --bin box (NOT --lib)"
    - "D-01: the rendering engine is hand-rolled on the image crate 0.25.10, NOT artem — artem is REJECTED (its unconditional deps drag clap/colored/terminal_size/log/env_logger/once_cell/ureq and bypass terminal_width()); image is the single sanctioned new crate (the image-decoding hand-roll exception)"
    - "D-02: pipeline is image::open(path) → resize_exact(cols, rows, FilterType::Triangle) → to_luma8() → map each luma byte through a dark→light ASCII ramp; cols = core::output::terminal_width() (falls back to 80 when piped); rows = cols * src_h / src_w / 2 (the /2 corrects the ~2:1 terminal cell aspect); source dims via img.dimensions() (needs use image::GenericImageView) or img.width()/img.height()"
    - "D-03: monochrome v1 — plain ASCII to stdout, NO color path (colored ASCII is VIS-V2-01); the module does not import owo_colors/is_color_on; the ramp emit is the clean v2-color seam; input path (positional or --file) routes the user's on-disk image"
    - "User runs box ascii ./photo.jpg and sees an ASCII art rendering fitted to the current terminal width; PNG and JPEG inputs both work (ROADMAP Phase 4 success criterion 3)"
  artifacts:
    - path: "src/commands/ascii/mod.rs"
      provides: "AsciiArgs + RunCommand impl + pure luma_to_char(byte, ramp) + rows formula + decode/resize/ramp pipeline"
      contains: "fn luma_to_char"
    - path: "tests/ascii.rs"
      provides: "ASCI-01 integration: real PNG + real JPEG render exit 0 non-empty; missing/bad file → exit 1 no panic"
    - path: "Cargo.toml"
      provides: "image dependency (0.25.10)"
      contains: "image ="
  key_links:
    - from: "src/main.rs"
      to: "AsciiArgs::run"
      via: "Commands::Ascii(args) => args.run()"
      pattern: "Commands::Ascii\\(args\\) => args.run\\(\\)"
    - from: "src/commands/ascii/mod.rs"
      to: "image::open"
      via: "decode the user's image (exit 1 on error, no panic)"
      pattern: "image::open"
    - from: "src/commands/ascii/mod.rs"
      to: "core::output::terminal_width"
      via: "cols source (80 when piped)"
      pattern: "terminal_width\\(\\)"
---

<objective>
Ship `box ascii` as a complete vertical slice (ASCI-01): pass a PNG or JPEG path, get an ASCII-art rendering fitted to the current terminal width (80 cols when piped). Monochrome v1. A missing/bad file errors cleanly (exit 1, no panic).

Purpose: Third Phase-4 slice. Establishes the `image` crate (the one sanctioned hand-roll exception, D-01) and the decode→resize→luma→ramp pipeline.
Output: A live `box ascii` command, a pure unit-tested `luma_to_char` + `rows` formula, checked-in tiny PNG/JPEG fixtures, and ASCI-01 integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-terminal-visuals/04-CONTEXT.md
@.planning/phases/04-terminal-visuals/04-RESEARCH.md
@.planning/phases/04-terminal-visuals/04-PATTERNS.md
@.planning/phases/04-terminal-visuals/04-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use directly, no exploration needed. -->

From src/commands/mod.rs:
```
pub trait RunCommand { fn run(self) -> anyhow::Result<()>; }
```

From src/core/output.rs (cols source — already falls back to 80 when piped, D-02/Pitfall 8):
```
pub fn terminal_width() -> usize      // :215  — REUSE THIS; do NOT call crossterm::terminal::size() directly
```

image 0.25.10 (every symbol VERIFIED docs.rs for this pinned version):
```
image::open(path) -> ImageResult<DynamicImage>     // P: AsRef<Path>; detects format from FILE EXTENSION, not content
DynamicImage::resize_exact(nw: u32, nh: u32, FilterType) -> DynamicImage   // exact size, ignores aspect (D-02 computes rows)
DynamicImage::to_luma8() -> GrayImage              // GrayImage = ImageBuffer<Luma<u8>, Vec<u8>>
GrayImage::as_raw() -> &Vec<u8>                    // row-major luma bytes (x 0..w then y 0..h) — cleanest ramp source
image::imageops::FilterType::Triangle              // linear; good cost/quality default (discretion)
// .dimensions() -> (u32,u32) is a GenericImageView TRAIT method — `use image::GenericImageView;`
//   OR use inherent .width()/.height() (no import). Pitfall 1.
```

The locked pure helper (D-02 / RESEARCH:196-199), crate-free & unit-testable:
```
fn luma_to_char(luma: u8, ramp: &[u8]) -> u8 {
    let idx = (luma as usize * (ramp.len() - 1)) / 255;
    ramp[idx]
}
```
</interfaces>

<!-- Analogs — render+width reproducibility precedent and the path-arg/fixture shapes (read in full): -->
@src/commands/cowsay/mod.rs
@tests/tree.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 failing tests + tiny PNG/JPEG fixtures for ASCI-01</name>
  <read_first>
    - tests/tree.rs (the `tree()` runner at :29-38 + the temp-dir / checked-in fixture pattern + the friendly-error assertion `.failure().stderr(contains(...))` at :121-142)
    - tests/hash.rs (the `assert_fs` child-file fixture shape around :36-42, for an on-disk image path)
    - .planning/phases/04-terminal-visuals/04-VALIDATION.md (the two ASCI-01 rows + Wave 0 Requirements: tests/cmd/ascii.in/{tiny.png,tiny.jpg})
    - .planning/phases/04-terminal-visuals/04-PATTERNS.md (the `tests/ascii.rs` analog section: checked-in fixtures + bad/missing file → exit 1)
  </read_first>
  <action>
    Generate two TINY checked-in image fixtures at `tests/cmd/ascii.in/tiny.png` and `tests/cmd/ascii.in/tiny.jpg` (e.g. a 4×4 or 8×8 gradient — small enough to commit, large enough that the ramp produces non-empty rows). Create them programmatically and write them once (a throwaway `image`-crate snippet run via `cargo run --example` is fine, OR any tool that emits a valid PNG and a valid JPEG with matching extensions — the extension MUST match the format, Pitfall 2). Commit both as binary fixtures.
    Create `tests/ascii.rs` (integration) with FAILING tests pinning ASCI-01 (fail now — `box ascii` is the stub). Mirror the `tree()` runner: `Command::cargo_bin("box").arg("ascii").env("NO_COLOR","1")`. Cases:
    (1) renders the PNG fixture: `.arg("tests/cmd/ascii.in/tiny.png")` → exit 0, stdout non-empty (`predicate::str::is_empty().not()`), stdout is valid UTF-8 (ASCII ramp chars).
    (2) renders the JPEG fixture: same with `tiny.jpg` → exit 0, non-empty stdout (proves both formats work, ROADMAP criterion 3).
    (3) missing file → exit 1, no panic: `.arg("tests/cmd/ascii.in/does-not-exist.png")` → `.failure().code(1)`, stderr non-empty, NO panic/backtrace text (assert stderr does not contain `panicked`).
    Optionally also assert an 80-col render is produced when piped (RESEARCH:285 — terminal_width() falls back to 80). These are RED until Task 2; the `luma_to_char` + `rows` unit tests live in Task 2's `#[cfg(test)]`.
  </action>
  <verify>
    <automated>test -f tests/cmd/ascii.in/tiny.png &amp;&amp; test -f tests/cmd/ascii.in/tiny.jpg &amp;&amp; (cargo test --test ascii 2>&1 | grep -qE "(FAILED|error\[|cannot find|not_implemented|test result: FAILED)" &amp;&amp; echo "RED as expected" || echo "UNEXPECTED: tests passed before impl")</automated>
  </verify>
  <acceptance_criteria>
    - `tests/cmd/ascii.in/tiny.png` and `tests/cmd/ascii.in/tiny.jpg` exist, are valid images with matching extensions, and are small (each well under a few KB)
    - `tests/ascii.rs` exists and compiles; `cargo test --test ascii` shows the ASCI-01 cases FAILING (binary still stubs `ascii`)
    - The three behaviors (PNG render, JPEG render, missing-file→exit-1-no-panic) are each a distinct `#[test]`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ascii slice — AsciiArgs + decode/resize/ramp pipeline + pure helpers (ASCI-01)</name>
  <read_first>
    - src/commands/cowsay/mod.rs (the render + the FIXED-width-vs-terminal_width reproducibility precedent; ascii intentionally DIVERGES to use terminal_width() per D-02 — document the divergence) and its pure-helper + `#[cfg(test)]` seam at :70-154 / :156-249
    - src/commands/hash/mod.rs:42-49 (the path-arg field shape `pub path: ...`)
    - src/cli.rs:71-72 (the `Ascii` unit variant) and src/cli.rs:44-48 (data-carrying variant shape)
    - src/main.rs:78 (the `Commands::Ascii => Err(not_implemented("ascii"))` arm) and src/main.rs:65 (the `=> args.run()` precedent)
    - src/commands/mod.rs:7-22 (add `pub mod ascii;` — alpha FIRST in the list)
    - src/core/output.rs:212-220 (`terminal_width()` — 80 fallback when piped)
    - Cargo.toml:11-47 (the `[dependencies]` block + annotated feature-trim style)
  </read_first>
  <behavior>
    - Pure `luma_to_char(luma: u8, ramp: &[u8]) -> u8`: `idx = (luma * (ramp.len()-1)) / 255`; monotonic — 0 → ramp[0] (darkest of a dark→light ramp), 255 → ramp[last] (lightest); never indexes out of bounds for luma 0..=255. Unit-test: `luma_to_char(0, RAMP)` == RAMP[0]; `luma_to_char(255, RAMP)` == RAMP[last]; monotonic across the byte range.
    - Pure `rows` formula `rows = (cols * src_h / src_w / 2).max(1)`: unit-test a couple of (cols, src_w, src_h) triples (e.g. square source at cols=80 → rows=40; wide source → fewer rows); always ≥ 1 (no zero-height render even for extreme aspect ratios).
    - Decode error: `image::open` on a missing/bad file → `.with_context(...)?` → exit 1, never a panic.
  </behavior>
  <action>
    Add to `Cargo.toml` under `[dependencies]` (annotated like chrono/uuid): `image = "0.25.10"` with a comment citing ascii D-01/D-02 (the sanctioned image-decoding hand-roll exception; artem rejected). Trimming to `default-features = false, features = ["png", "jpeg"]` for binary size is DISCRETION (RESEARCH A2) — if you trim, verify the build still resolves the decode/resize/luma path; otherwise keep full defaults. Document the chosen feature set in the comment.
    Create `src/commands/ascii/mod.rs` (NO owo_colors / NO is_color_on import — monochrome v1, D-03): `//!` doc citing ASCI-01 + D-01/D-02/D-03 and noting `image::open` is EXTENSION-based (PNG/JPEG only, document in clap `///` help, Pitfall 2) and that width uses `terminal_width()` (80 when piped) which intentionally diverges from cowsay's fixed width (D-02). Imports `clap::Args`, `anyhow::Context`, `image::GenericImageView` (for `.dimensions()`; or use `.width()`/`.height()` and skip the import), `crate::commands::RunCommand`. Define `#[derive(Debug, Args)] pub struct AsciiArgs { /// Path to a PNG or JPEG image (extension must match the format). pub path: String }`. In `run`: `let img = image::open(&self.path).with_context(|| format!("failed to read image {}", self.path))?;` (→ exit 1); `let (sw, sh) = img.dimensions();`; `let cols = crate::core::output::terminal_width() as u32;`; `let rows = (cols * sh / sw / 2).max(1);`; `let gray = img.resize_exact(cols, rows, image::imageops::FilterType::Triangle).to_luma8();`; `const RAMP: &[u8] = b" .:-=+*#%@";` (dark→light; exact ramp & FilterType are discretion, RESEARCH OQ-1); `let buf = gray.as_raw();` then nested loops `for y in 0..rows { let mut line = String::with_capacity(cols as usize); for x in 0..cols { line.push(luma_to_char(buf[(y*cols+x) as usize], RAMP) as char); } println!("{line}"); }`. Implement the pure `luma_to_char` and a `compute_rows(cols, sw, sh)` helper, with a `#[cfg(test)]` block covering the behaviors above. Guard `sw == 0` defensively if image::open could yield a 0-width image (clamp to avoid divide-by-zero — surface as a clean error, not a panic).
    Wire the registry: `src/cli.rs` swap `Ascii,` (line 71-72) for `Ascii(crate::commands::ascii::AsciiArgs),` keeping the `/// Render an image as ASCII art` doc; `src/main.rs` replace line 78 with `Commands::Ascii(args) => args.run(),`; `src/commands/mod.rs` add `pub mod ascii;` (alpha first). Leave qr/clip/pomodoro/weather (and matrix if not yet shipped) stub arms untouched.
  </action>
  <verify>
    <automated>cargo test --bin box ascii &amp;&amp; cargo test --test ascii &amp;&amp; cargo clippy -- -D warnings &amp;&amp; cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test ascii` exits 0: PNG fixture renders (exit 0, non-empty); JPEG fixture renders (exit 0, non-empty); missing file → exit 1 with no `panicked` in stderr
    - `cargo test --bin box ascii` exits 0 (luma_to_char monotonic + bounds + compute_rows unit tests green)
    - `cargo run -q -- ascii tests/cmd/ascii.in/tiny.png` exits 0 with non-empty stdout; `cargo run -q -- ascii nope.png` exits 1 without panicking
    - `Cargo.toml` contains `image = "0.25.10"` (or the documented feature-trimmed form); `artem` is NOT present anywhere
    - The ascii module imports neither `owo_colors` nor `is_color_on` (monochrome v1, D-03)
    - `cargo clippy -- -D warnings` and `cargo fmt --check` pass; the `ascii` not_implemented arm is gone
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user path → image decoder | An untrusted/large image file is decoded via `image::open` + `resize_exact` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04A-01 | Denial of Service | decompression bomb / very large image | accept | `image::open` uses default (effectively unlimited) `Limits`; `resize_exact` cost scales with SOURCE pixels before downscale (Pitfall 6). This is a LOW-severity, local single-user dev tool reading the user's OWN files (ASCI-01 scope) — a hard limit is NOT required at ASVS L1. Documented as accepted; `ImageReader::set_limits` is the seam if ever escalated. |
| T-04A-02 | Denial of Service | malformed image → panic | mitigate | `image::open` returns `Result`; `.with_context(...)?` maps a decode/format error to a clean exit-1 anyhow error — never `.unwrap()`/panic (FOUND-05). Verified by the missing/bad-file integration test asserting no `panicked` in stderr. |
| T-04A-03 | Information Disclosure | arbitrary path read via the positional arg | accept | Reads exactly the path the user passes — no traversal logic, same posture as hash/json `--file`. Out of threat scope for a single-user local tool. |
| T-04-SC | Tampering | `image` crate install | mitigate | RESEARCH Package Legitimacy Audit: `image` 0.25.10 = image-rs canonical, slopcheck OK, [VERIFIED: crates.io], mature 0.25.x line. No `[ASSUMED]`/`[SUS]` → no blocking human checkpoint. |

No high-severity threats: local terminal tool, no network, no secrets, no file writes.
</threat_model>

<verification>
- `cargo test --test ascii` green (ASCI-01 integration: PNG + JPEG render, missing-file→exit-1-no-panic)
- `cargo test --bin box ascii` green (luma_to_char + compute_rows unit tests)
- `cargo test` (full suite) green — no regression
- `cargo clippy -- -D warnings` + `cargo fmt --check` clean
</verification>

<success_criteria>
- `box ascii` is live (the `not_implemented("ascii")` arm is deleted)
- A PNG and a JPEG both render to width-fitted ASCII art (cols = terminal_width(), 80 when piped; rows aspect-corrected via /2, D-02)
- Monochrome v1 — no color path (D-03); engine is hand-rolled on `image`, artem rejected (D-01)
- A missing/bad image errors cleanly (exit 1, no panic, FOUND-05)
</success_criteria>

<output>
Create `.planning/phases/04-terminal-visuals/04-03-SUMMARY.md` when done
</output>
