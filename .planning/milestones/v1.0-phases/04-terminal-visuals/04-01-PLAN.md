---
phase: 04-terminal-visuals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/cli.rs
  - src/main.rs
  - src/commands/mod.rs
  - src/commands/json/mod.rs
  - tests/json.rs
  - tests/cmd/json.trycmd
autonomous: true
requirements: [JSON-01]
nyquist_compliant: true

must_haves:
  truths:
    - "D-00: json follows the established slice — Json unit variant in src/cli.rs swapped for Json(JsonArgs), the not_implemented(\"json\") arm at src/main.rs:79 deleted, input via core::input, color via is_color_on(), data→stdout / messages→stderr, exit 0/1/2, assert_cmd + (optional) trycmd tests; unit tests run via cargo test --bin box (NOT --lib)"
    - "D-04: serde_json is added with the preserve_order feature ON (and arbitrary_precision OFF); {\"b\":1,\"a\":2} pretty-prints with b before a (input key order preserved, not alphabetized)"
    - "D-05: JSON syntax coloring is a hand-rolled colorizer over serde_json::Value using owo-colors, gated through is_color_on() — no colored_json crate, no second color stack, no set_override toggle; the non-color and --compact paths delegate to serde_json::to_string_pretty / to_string so piped output is byte-identical minus ANSI"
    - "D-06: invalid JSON prints an error containing the 1-based line and column to stderr and exits 1; valid JSON exits 0; pretty default is 2-space indent; --compact minifies"
    - "User pipes invalid JSON to box json and gets exit code 1 with a line/column error on stderr; valid JSON pretty-prints with syntax coloring; --compact minifies (ROADMAP Phase 4 success criterion 4)"
  artifacts:
    - path: "src/commands/json/mod.rs"
      provides: "JsonArgs + RunCommand impl + pure colorize(&Value) walker + parse-error→exit-1"
      contains: "impl RunCommand for JsonArgs"
    - path: "tests/json.rs"
      provides: "JSON-01 integration: invalid→exit1+line/col, valid pretty 2-space, --compact minify, key-order, piped-no-ANSI"
    - path: "Cargo.toml"
      provides: "serde_json dependency with preserve_order feature"
      contains: "preserve_order"
  key_links:
    - from: "src/main.rs"
      to: "JsonArgs::run"
      via: "Commands::Json(args) => args.run()"
      pattern: "Commands::Json\\(args\\) => args.run\\(\\)"
    - from: "src/commands/json/mod.rs"
      to: "serde_json::from_str::<Value>"
      via: "parse + validate"
      pattern: "from_str::<serde_json::Value>"
    - from: "src/commands/json/mod.rs"
      to: "core::output::is_color_on"
      via: "color gate on the colorize path"
      pattern: "is_color_on\\(\\)"
---

<objective>
Ship `box json` as a complete vertical slice (JSON-01): pipe or pass JSON text, get a 2-space pretty-printed, syntax-colored result in a TTY; pipe invalid JSON and get exit 1 with a 1-based line/column error on stderr; `--compact` minifies. Input key order is preserved (`preserve_order`).

Purpose: First of the four Phase-4 terminal-visual slices. Establishes the serde_json dependency and proves the parse→validate→colorize-or-delegate pattern that reuses the locked `is_color_on()` color gate.
Output: A live `box json` command (no more `not_implemented` stub), a pure unit-tested `colorize(&Value)` walker, and JSON-01 integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-terminal-visuals/04-CONTEXT.md
@.planning/phases/04-terminal-visuals/04-RESEARCH.md
@.planning/phases/04-terminal-visuals/04-PATTERNS.md
@.planning/phases/04-terminal-visuals/04-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use directly, no exploration needed. -->

From src/commands/mod.rs (the dispatch trait — implement this on JsonArgs):
```
pub trait RunCommand { fn run(self) -> anyhow::Result<()>; }
```

From src/core/input.rs (text input, D-04 precedence: arg → piped stdin → exit-2 on no-arg TTY):
```
pub fn read_input(arg: Option<String>) -> anyhow::Result<String>
```

From src/core/output.rs (the ONE color gate — .truecolor() is unconditional, so consult this):
```
pub fn is_color_on() -> bool          // :32
```

serde_json 1.0.150 (VERIFIED docs.rs for this pinned version):
```
serde_json::from_str::<serde_json::Value>(&s) -> Result<Value, Error>
serde_json::to_string_pretty(&Value) -> Result<String>   // default indent = exactly 2 spaces
serde_json::to_string(&Value) -> Result<String>          // compact / minified
Error::line() -> usize    // 1-based
Error::column() -> usize  // 1-based
// Value variants (the 6 colorizer match arms): Null, Bool(bool), Number(Number),
//   String(String), Array(Vec<Value>), Object(Map<String,Value>)
// With preserve_order, Map = IndexMap (insertion order kept, NOT alphabetized).
```

owo-colors 4.3 (already a dep; UNCONDITIONAL — always gate on is_color_on()):
```
use owo_colors::OwoColorize;   //  "text".truecolor(r,g,b) / "text".blue() etc.
```
</interfaces>

<!-- Canonical analog — the ONE color path, gated (read in full before implementing): -->
@src/commands/color/mod.rs
@tests/color.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 failing tests + json fixture for JSON-01</name>
  <read_first>
    - tests/color.rs (the runner shape `Command::cargo_bin("box")` + the malformed-input `.failure().code(1).stdout(is_empty()).stderr(is_empty().not())` assertion at color.rs:67-74 — reuse verbatim for the invalid-JSON case)
    - tests/cmd/color.trycmd (plain-text trycmd snapshot shape, for the optional json.trycmd)
    - .planning/phases/04-terminal-visuals/04-VALIDATION.md (the JSON-01 rows of the Per-Task Verification Map + Wave 0 Requirements)
    - .planning/phases/04-terminal-visuals/04-PATTERNS.md (the `tests/json.rs` analog section: tree_piped_no_ansi byte-scan + line/column predicates)
  </read_first>
  <action>
    Create `tests/json.rs` (integration) with FAILING tests that pin JSON-01 — they must fail now because `box json` is still the stub. Mirror the `tests/color.rs` runner: a helper that builds `Command::cargo_bin("box").arg("json")` with `.env("NO_COLOR","1")` and feeds JSON via `.write_stdin(...)` (stdin path) or a positional arg. Write these cases:
    (1) invalid JSON `{bad` piped → `.failure().code(1)`, `stdout(predicate::str::is_empty())`, and stderr containing both `line` and `column` (use `predicate::str::contains("line").and(predicate::str::contains("column"))`) — the D-06 contract.
    (2) valid pretty default: pipe `{"a":1}` → exit 0, stdout contains 2-space-indented `  "a": 1` (D-06 2-space indent).
    (3) `--compact`: pipe `{ "a" : 1 }` → exit 0, stdout is the single-line minified `{"a":1}` with no embedded newline before EOF.
    (4) key-order (D-04): pipe `{"b":1,"a":2}` → assert the byte offset of `"b"` is LESS than the offset of `"a"` in stdout (input order preserved, NOT alphabetized).
    (5) piped-no-ANSI (D-05): run WITHOUT `NO_COLOR` but with piped stdout (assert_cmd pipes by default) on valid input → assert stdout bytes contain no `\x1b[` window (mirror the tree_piped_no_ansi byte-scan `!out.stdout.windows(2).any(|w| w == b"\x1b[")`).
    Optionally add `tests/cmd/json.trycmd` snapshotting the locked 2-space pretty layout for a tiny object (mirror tests/cmd/color.trycmd plain shape). Do NOT add a colorizer unit test here — that lives in Task 2's `#[cfg(test)]` block. These are RED on purpose: leave them in until Task 2 turns them GREEN.
  </action>
  <verify>
    <automated>cargo test --test json 2>&1 | grep -qE "(FAILED|error\[|cannot find|not_implemented|test result: FAILED)" &amp;&amp; echo "RED as expected" || echo "UNEXPECTED: tests passed before impl"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/json.rs` exists and compiles against the current binary
    - Running `cargo test --test json` shows the JSON-01 cases FAILING (the binary still routes `json` to `not_implemented`, so exit code / output assertions do not hold yet) — this confirms the tests actually exercise the contract
    - The five behaviors above are each represented by a distinct `#[test]` fn with a behavior-anchored assertion (no `assert!(true)` placeholders)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement json slice — JsonArgs + colorizer + registry wiring (JSON-01)</name>
  <read_first>
    - src/commands/color/mod.rs:17-61 (module skeleton: imports, `#[derive(Debug, Args)]` struct with `///`-doc fields, `impl RunCommand`, the gated color block at :53-58 — the EXACT precedent)
    - src/cli.rs:73-74 (the `Json` unit variant to swap) and src/cli.rs:44-48 (the data-carrying variant shape to mirror)
    - src/main.rs:79 (the `Commands::Json => Err(not_implemented("json"))` arm to replace) and src/main.rs:65 (the `=> args.run()` precedent)
    - src/commands/mod.rs:7-22 (the alphabetized `pub mod` list — add `pub mod json;`)
    - Cargo.toml:11-47 (the `[dependencies]` block + the chrono/uuid feature-trim comment style at :32-34)
    - src/core/input.rs:47-50 (`read_input` signature) and src/core/output.rs:32 (`is_color_on`)
  </read_first>
  <behavior>
    - Pure `colorize(&Value, indent: usize) -> String`: matches all 6 Value variants (Null/Bool/Number/String/Array/Object); emits 2-space indentation per nesting level; on Object, iterates in IndexMap insertion order (preserve_order) so `{"b":1,"a":2}` keeps `b` before `a`; applies owo-colors per token type ONLY (the caller has already checked is_color_on()).
    - Key-order unit test: `colorize` (or a plain `to_string_pretty`) over a parsed `{"b":1,"a":2}` yields `b` before `a`.
    - Invalid input: `from_str::<Value>` Err → `anyhow::bail!("at line {} column {}: {e}", e.line(), e.column())` (1-based, → exit 1 via main()).
    - --compact delegates to `serde_json::to_string`; plain (non-color) pretty delegates to `serde_json::to_string_pretty` (2-space); colored pretty uses the hand-rolled `colorize`.
  </behavior>
  <action>
    Add to `Cargo.toml` under `[dependencies]` (mirror the chrono/uuid annotated style): `serde_json = { version = "1.0.150", features = ["preserve_order"] }` with a comment noting preserve_order keeps input key order (D-04) and that arbitrary_precision is deliberately OFF (D-04 landmine, serde-rs/json #505/#721/#845). serde is pulled transitively; Value needs no derive so do NOT add a direct `serde` dep.
    Create `src/commands/json/mod.rs` following the color/mod.rs skeleton: `//!` doc citing JSON-01 + D-04/D-05/D-06; imports `clap::Args`, `owo_colors::OwoColorize`, `crate::commands::RunCommand`, `crate::core::output::is_color_on`. Define `#[derive(Debug, Args)] pub struct JsonArgs { /// JSON text to format; omit to read piped stdin. pub input: Option<String>, /// Minify instead of pretty-printing. #[arg(long)] pub compact: bool }`. In `impl RunCommand for JsonArgs::run`: `let text = crate::core::input::read_input(self.input)?;` then `match serde_json::from_str::<serde_json::Value>(&text)` — Err arm `anyhow::bail!("at line {} column {}: {e}", e.line(), e.column())` (D-06); Ok arm: if `self.compact` → `println!("{}", serde_json::to_string(&value)?)`; else if `is_color_on()` → `print!("{}", colorize(&value, 0))`; else → `println!("{}", serde_json::to_string_pretty(&value)?)`. Implement the pure `colorize(&serde_json::Value, indent: usize) -> String` walker (token colors are discretion per RESEARCH OQ-3: e.g. key=blue, string=green, number=yellow, bool/null=magenta, punctuation=plain). Add a `#[cfg(test)]` block with the key-order test and at least one colorize-shape test (parse `{"a":[1,true,null]}`, assert structure/indent). Keep `to_string_pretty`'s trailing-newline behavior consistent with the integration test's expectation.
    Wire the registry: in `src/cli.rs` swap `Json,` (line 73-74) for `Json(crate::commands::json::JsonArgs),` keeping the `/// Pretty-print and validate JSON` doc; in `src/main.rs` replace line 79 with `Commands::Json(args) => args.run(),`; in `src/commands/mod.rs` add `pub mod json;` in alpha order (between `hash` and `passgen`). Do NOT touch the `qr`/`clip`/`pomodoro`/`weather` stub arms — the `not_implemented` import at main.rs:22 must stay.
  </action>
  <verify>
    <automated>cargo test --bin box json &amp;&amp; cargo test --test json &amp;&amp; cargo clippy -- -D warnings &amp;&amp; cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test json` exits 0: all five Task-1 cases now PASS (invalid→exit1 with line+column on stderr & empty stdout; valid pretty 2-space; --compact single-line; key-order b-before-a; piped→no `\x1b[`)
    - `cargo test --bin box json` exits 0 (colorize key-order + shape unit tests green)
    - `printf '%s' '{bad' | cargo run -q -- json` exits 1, stderr mentions `line` and `column`, stdout empty
    - `printf '%s' '{"b":1,"a":2}' | cargo run -q -- json` keeps `b` before `a` in stdout (preserve_order)
    - `Cargo.toml` contains `serde_json` with `preserve_order` and NOT `arbitrary_precision`
    - `cargo clippy -- -D warnings` and `cargo fmt --check` both pass; the `json` not_implemented arm is gone from src/main.rs
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin/arg → json parser | Untrusted JSON text crosses into `serde_json::from_str` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04J-01 | Denial of Service | `serde_json::from_str` on deeply-nested / huge input | accept | serde_json enforces a default recursion limit (128) on nested structures and returns `Err`, not a stack overflow; the Err maps to a clean exit-1 anyhow error (no panic). Local single-user dev tool — no hard input-size cap needed at ASVS L1. |
| T-04J-02 | Denial of Service | malformed JSON → panic | mitigate | `from_str` returns `Result`; the Err arm `bail!`s (exit 1) — no `.unwrap()` on user input (FOUND-05). Verified by the `{bad` integration test. |
| T-04J-03 | Tampering (output) | colorizer emitting attacker-controlled ANSI | mitigate | Color comes ONLY from owo-colors `.truecolor`/named methods over parsed `Value` tokens, gated on `is_color_on()`; the raw input string is never echoed verbatim into the colored path. Piped output is byte-identical minus ANSI (D-05). |
| T-04-SC | Tampering | `serde_json` crate install | mitigate | RESEARCH Package Legitimacy Audit: `serde_json` 1.0.150 = dtolnay canonical, slopcheck OK, [VERIFIED: crates.io], >1B downloads. No `[ASSUMED]`/`[SUS]` package → no blocking human checkpoint required. |

No high-severity threats: local terminal tool, no network, no secrets, no file writes.
</threat_model>

<verification>
- `cargo test --test json` green (JSON-01 integration: invalid/valid/compact/key-order/piped-no-ANSI)
- `cargo test --bin box json` green (colorize + key-order unit tests)
- `cargo test` (full suite) green — no regression in the 98 existing tests
- `cargo clippy -- -D warnings` + `cargo fmt --check` clean
</verification>

<success_criteria>
- `box json` is live (the `not_implemented("json")` arm is deleted from src/main.rs)
- Invalid JSON → exit 1 + 1-based line/column on stderr, empty stdout (D-06)
- Valid JSON → 2-space pretty (colored in TTY, plain when piped — byte-identical minus ANSI, D-05); `--compact` minifies (D-06)
- Input key order preserved (D-04, preserve_order); arbitrary_precision NOT enabled
- All four ROADMAP-Phase-4-criterion-4 behaviors demonstrably pass
</success_criteria>

<output>
Create `.planning/phases/04-terminal-visuals/04-01-SUMMARY.md` when done
</output>
