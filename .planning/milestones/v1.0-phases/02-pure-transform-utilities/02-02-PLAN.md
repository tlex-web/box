---
phase: 02-pure-transform-utilities
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/commands/uuid/mod.rs
  - src/commands/base64/mod.rs
  - src/commands/mod.rs
  - src/cli.rs
  - src/main.rs
  - tests/uuid.rs
  - tests/base64.rs
autonomous: true
requirements: ["UUID-01", "B64-01"]
must_haves:
  truths:
    - "User runs `box uuid` and gets one lowercase v4 UUID; `box uuid -n 5` prints 5 distinct UUIDs; `box uuid --upper` prints uppercase"
    - "User pipes a string through `box base64` to encode, then `box base64 --decode` recovers the original bytes exactly"
    - "`box base64 --url-safe` produces the URL-safe alphabet with no padding"
    - "`box base64` with no arg on an interactive TTY exits 2 (via core::input), never hangs"
  artifacts:
    - path: "src/commands/uuid/mod.rs"
      provides: "UuidArgs { count, upper } implementing RunCommand; v4 generation"
      contains: "impl RunCommand for UuidArgs"
    - path: "src/commands/base64/mod.rs"
      provides: "Base64Args { decode, url_safe, input } implementing RunCommand; STANDARD/URL_SAFE_NO_PAD engines via core::input::read_input_bytes"
      contains: "impl RunCommand for Base64Args"
    - path: "tests/uuid.rs"
      provides: "UUID-01 integration tests (v4 regex, count, uniqueness, --upper)"
      contains: "cargo_bin"
    - path: "tests/base64.rs"
      provides: "B64-01 round-trip + url-safe + stdin + no-input-exit-2 integration tests"
      contains: "cargo_bin"
  key_links:
    - from: "src/main.rs"
      to: "uuid/base64 .run()"
      via: "Commands::Uuid(args) => args.run(); Commands::Base64(args) => args.run()"
      pattern: "Commands::Uuid\\(args\\)|Commands::Base64\\(args\\)"
    - from: "src/commands/base64/mod.rs"
      to: "core::input::read_input_bytes"
      via: "read_input_bytes(self.input)?"
      pattern: "read_input_bytes"
---

<objective>
Two slices on the foundation: `uuid` (arg/flag-only, no stdin) and `base64`
(the first `core::input::read_input_bytes` consumer). Each swaps its unit
variant in `cli.rs` for a real Args struct, removes its `not_implemented` arm in
`main.rs`, registers its module, and ships integration tests. base64 is the
end-to-end proof that the Plan-01 exit-2 wiring works (no-arg on TTY → exit 2).

Purpose: uuid is the lowest-risk command — it proves the RunCommand clone on a
random generator. base64 proves the binary-exact `core::input` path and the
0.22 engine API (the free functions were removed in 0.21).

Output: two working commands, two integration test files, registry edits, and
the first real exit-2 integration assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pure-transform-utilities/02-CONTEXT.md
@.planning/phases/02-pure-transform-utilities/02-RESEARCH.md
@.planning/phases/02-pure-transform-utilities/02-PATTERNS.md
@.planning/phases/02-pure-transform-utilities/02-VALIDATION.md
@.planning/phases/02-pure-transform-utilities/02-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. From the live repo + Plan 01 output. -->

RunCommand trait, src/commands/mod.rs:13-15:
```rust
pub trait RunCommand {
    fn run(self) -> anyhow::Result<()>;
}
```

Args-struct + impl shape to clone, src/commands/flatten/mod.rs:25-46,93-94:
```rust
use clap::Args;
use crate::commands::RunCommand;

#[derive(Debug, Args)]
pub struct FlattenArgs { /* doc-commented fields become --help text */ }

impl RunCommand for FlattenArgs {
    fn run(self) -> anyhow::Result<()> { /* acquire → transform → println! */ Ok(()) }
}
```

cli.rs variant swap (src/cli.rs:37-40) — keep the `///` doc comment byte-identical (help.trycmd locks it, RESEARCH A5):
```rust
/// Generate a random UUID
Uuid(crate::commands::uuid::UuidArgs),
/// Encode or decode base64 text
Base64(crate::commands::base64::Base64Args),
```

main.rs dispatch arm swap (src/main.rs:62-63):
```rust
Commands::Uuid(args) => args.run(),
Commands::Base64(args) => args.run(),
```

base64 0.22 engine API (RESEARCH Pattern 5):
```rust
use base64::Engine as _;
use base64::engine::general_purpose::{STANDARD, URL_SAFE_NO_PAD};
let engine = if url_safe { &URL_SAFE_NO_PAD } else { &STANDARD };
let encoded = engine.encode(&bytes);
let decoded = engine.decode(input.trim())?;   // trim trailing newline (Pitfall 3)
```

core::input (from Plan 01): `pub fn read_input_bytes(arg: Option<String>) -> anyhow::Result<Vec<u8>>`.

Integration test harness, tests/flatten.rs:15-24 (NO_COLOR=1) + tests/cli.rs:66-75 (exit/stream assertions).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: uuid command + tests (UUID-01)</name>
  <files>src/commands/uuid/mod.rs, src/commands/mod.rs, src/cli.rs, src/main.rs, tests/uuid.rs</files>
  <behavior>
    - `box uuid` → exactly one line matching the v4 regex `^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$`.
    - `box uuid -n 5` → 5 lines, all distinct, all matching the v4 regex.
    - `box uuid --upper` → the uppercase form of a valid v4 UUID.
    - `box uuid -n 100` → 100 distinct lines (uniqueness via HashSet).
  </behavior>
  <read_first>
    - src/commands/flatten/mod.rs:25-46 (Args + imports), :93-94 (impl block), :331-365 (pure-fn + #[cfg(test)] discipline)
    - src/cli.rs:33-55 (the Commands enum variants to swap; keep doc comments verbatim)
    - src/main.rs:60-84 (dispatch arms; remove the uuid not_implemented arm)
    - src/commands/mod.rs:7-8 (the `pub mod` registry)
    - tests/flatten.rs:1-24 (harness + NO_COLOR helper), tests/cli.rs:42-75 (exit-code + stream-discipline assertions)
    - .planning/phases/02-pure-transform-utilities/02-RESEARCH.md:213-231 (UuidArgs reference impl), :512 (uniqueness HashSet + v4 regex)
    - .planning/phases/02-pure-transform-utilities/02-PATTERNS.md:261-264 (uuid pattern assignment)
  </read_first>
  <action>
    Create src/commands/uuid/mod.rs with `UuidArgs { count: u8 (#[arg(short='n', long="count", default_value_t = 1)]), upper: bool (#[arg(long)]) }` implementing `RunCommand`. `run()` loops `count` times, generates `uuid::Uuid::new_v4().to_string()` (Display gives RFC-4122 lowercase-hyphenated), uppercases when `self.upper`, and `println!`s one per line to stdout. No `core::input` (uuid takes no input). Doc-comment every field (becomes `box uuid --help`). `uuid` flag spelling beyond `-n`/`--upper` is Claude's discretion (D-discretion).
    Register `pub mod uuid;` in src/commands/mod.rs. In src/cli.rs swap the unit `Uuid,` variant (cli.rs:38) for `Uuid(crate::commands::uuid::UuidArgs)`, keeping the `/// Generate a random UUID` doc comment byte-identical (help.trycmd lock, RESEARCH A5). In src/main.rs replace `Commands::Uuid => Err(not_implemented("uuid"))` (main.rs:62) with `Commands::Uuid(args) => args.run()`.
    Create tests/uuid.rs (analog tests/flatten.rs + tests/cli.rs): assert `box uuid` prints one v4-regex line; `box uuid -n 5` prints 5 distinct regex-matching lines; `box uuid --upper` prints an uppercase valid v4; `box uuid -n 100` yields 100 distinct lines (collect stdout lines into a HashSet, assert len==100). Force `NO_COLOR=1` per the harness. Use `predicates`/regex on stdout; assert stderr empty + exit 0.
  </action>
  <verify>
    <automated>cargo test --test uuid 2>&1 | tail -8 && cargo clippy -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test uuid` exits 0.
    - CLI assertion: `box uuid -n 5` stdout is 5 lines each matching `^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$`, all distinct.
    - CLI assertion: `box uuid --upper` matches the uppercase v4 regex.
    - Source assertion: `grep -c 'Commands::Uuid(args) => args.run()' src/main.rs` returns 1; no `not_implemented("uuid")` arm remains.
    - `cargo clippy -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`box uuid` and its flags produce correctly-formatted, unique v4 UUIDs; the uuid stub is gone; integration tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: base64 command + round-trip/url-safe/stdin/exit-2 tests (B64-01)</name>
  <files>src/commands/base64/mod.rs, src/commands/mod.rs, src/cli.rs, src/main.rs, tests/base64.rs</files>
  <behavior>
    - encode(bytes) then decode → original bytes, byte-exact, for STANDARD and URL_SAFE_NO_PAD, including a non-UTF-8 byte like 0xFF and a zero byte.
    - `box base64 "hello"` → base64 of `hello` on stdout, no trailing line-wrap.
    - `echo -n hello | box base64 | box base64 --decode` → `hello`.
    - `box base64 --url-safe` of bytes that would contain `+`/`/` in STANDARD uses `-`/`_` and has no `=` padding.
    - `box base64` with no arg and stdin a TTY → exit 2 with the MissingInput message on stderr (proves Plan-01 wiring).
    - decoding non-alphabet input → exit 1 with a clean stderr error, no panic (V5 input validation).
  </behavior>
  <read_first>
    - src/commands/flatten/mod.rs:25-46,93-94 (Args + impl shape)
    - src/core/input.rs (the read_input_bytes signature shipped in Plan 01)
    - src/cli.rs:39-40 (the Base64 unit variant to swap), src/main.rs:63 (the arm to replace)
    - tests/flatten.rs:1-24, tests/cli.rs:42-75 (harness + exit/stream assertions)
    - .planning/phases/02-pure-transform-utilities/02-RESEARCH.md:313-325 (Pattern 5 engine API + decode-whitespace caveat), :396-400 (Pitfall 3 trailing newline), :402-406 (Pitfall 4 — write_stdin proves the Rust reader, NOT the PS7 pipe), :531-532 (round-trip test rows)
    - .planning/phases/02-pure-transform-utilities/02-PATTERNS.md:266-269 (base64 pattern assignment)
  </read_first>
  <action>
    Create src/commands/base64/mod.rs with `Base64Args { decode: bool (#[arg(long)]), url_safe: bool (#[arg(long = "url-safe")]), input: Option<String> }` implementing `RunCommand`. `run()` calls `crate::core::input::read_input_bytes(self.input)?` to get `Vec<u8>`, selects the engine (`&URL_SAFE_NO_PAD` when `self.url_safe`, else `&STANDARD`) via `use base64::Engine as _; use base64::engine::general_purpose::{STANDARD, URL_SAFE_NO_PAD}`. Encode path: `engine.encode(&bytes)` → `println!` (no line-wrapping — B64-01). Decode path (`self.decode`): interpret the input bytes as text, `.trim()` trailing whitespace/CRLF before `engine.decode(...)` (Pitfall 3), then write the decoded bytes to stdout via `std::io::stdout().write_all(&decoded)?` (use `Write`) so binary output is byte-exact — do NOT `println!` decoded bytes through a String. A decode error propagates as `anyhow::Error` → exit 1 (Pattern D, FOUND-05); never `.unwrap()` on user input.
    Add a pure encode/decode helper unit-test in-module: `#[cfg(test)]` asserting `decode(encode(x)) == x` for a `Vec<u8>` including `0x00` and `0xFF` for BOTH engines.
    Register `pub mod base64;` in commands/mod.rs. Swap cli.rs:40 `Base64,` → `Base64(crate::commands::base64::Base64Args)` (keep `/// Encode or decode base64 text` verbatim). Replace main.rs:63 with `Commands::Base64(args) => args.run()`.
    Create tests/base64.rs: round-trip via `write_stdin(b"...")` (raw bytes, bypasses the shell — Pitfall 4); url-safe alphabet assertion; decode-non-alphabet → `.failure().code(1)` with stderr non-empty + stdout empty; no-arg with stdin NOT redirected is the piped-empty branch in CI, so to assert the exit-2 TTY branch deterministically rely on the Plan-01 unit downcast test PLUS add an integration test that `box base64` given an explicit empty arg behaves per spec — and add a comment that the true interactive-TTY exit-2 is unit-proven in core::input (RESEARCH:540-543). Force `NO_COLOR=1`.
  </action>
  <verify>
    <automated>cargo test --test base64 2>&1 | tail -8 && cargo test base64:: 2>&1 | tail -5 && cargo clippy -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test base64` and `cargo test base64::` both exit 0.
    - Unit assertion: `decode(encode(x)) == x` for a `Vec<u8>` containing `0x00` and `0xFF`, for STANDARD and URL_SAFE_NO_PAD.
    - CLI assertion: `write_stdin(b"hello")` → encode → pipe back → `--decode` recovers exactly `hello`.
    - CLI assertion: decoding `"!!!not-base64!!!"` → `.failure().code(1)`, stderr contains an error, stdout empty (no panic).
    - Source assertion: `grep -c 'read_input_bytes' src/commands/base64/mod.rs` returns ≥1; `grep -c '% ' src/commands/base64/mod.rs` is irrelevant here (no random pick). No `not_implemented("base64")` arm remains.
    - `cargo clippy -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`box base64` round-trips byte-exact through the engine API, `--url-safe` uses the URL alphabet with no padding, malformed input fails cleanly with exit 1, and the base64 stub is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arg/stdin → base64 decode | Untrusted text decoded against the base64 alphabet; non-alphabet bytes must be rejected cleanly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service (self) | base64 decode | mitigate | `engine.decode` returns `Result`; the error propagates via `anyhow` to exit 1 — no `.unwrap()` on user input, no panic (FOUND-05, V5). Trailing-newline footgun handled by `.trim()` before decode (Pitfall 3). |
| T-02-04 | Tampering / data corruption | base64 encode/decode I/O | mitigate | Decoded bytes written via `stdout().write_all`, not through a lossy String; input read via `read_input_bytes` (read_to_end) — byte-exact both directions, unit-tested with `0x00`/`0xFF`. |
| (note) | — | uuid | accept | Trivial surface — pure random generation, no input, no network/auth/storage. |

PS7 pipe boundary (D-06): byte-exactness of the *PowerShell* pipeline is a shell property `assert_cmd` cannot reproduce (Pitfall 4) — it is a manual/human-verify item in 02-VALIDATION.md, not an automated gate in this plan.
</threat_model>

<verification>
- `cargo test --test uuid`, `cargo test --test base64`, `cargo test base64::` all green.
- `cargo clippy -- -D warnings` clean; `cargo fmt --check` clean.
- `box --help` still lists all 23 commands unchanged (doc comments preserved); if any `about` text drifted, regenerate `help.trycmd` with `TRYCMD=overwrite cargo test` and review the diff (RESEARCH A5).
</verification>

<success_criteria>
- `box uuid`/`-n`/`--upper` produce correct, unique v4 UUIDs.
- `box base64` encode/decode round-trips byte-exact; `--url-safe` correct; bad input → exit 1.
- uuid and base64 `not_implemented` arms removed; both modules registered and dispatched.
- Integration + unit tests green; clippy/fmt clean.
</success_criteria>

<output>
Create `.planning/phases/02-pure-transform-utilities/02-02-SUMMARY.md` when done.
</output>
