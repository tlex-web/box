---
phase: 02-pure-transform-utilities
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/commands/passgen/mod.rs
  - src/commands/cowsay/mod.rs
  - src/data/eff_large_wordlist.txt
  - src/commands/mod.rs
  - src/cli.rs
  - src/main.rs
  - Cargo.toml
  - tests/passgen.rs
  - tests/cmd/cowsay-single.trycmd
  - tests/cmd/cowsay-multi.trycmd
autonomous: true
requirements: ["PASS-01", "COW-01"]
must_haves:
  truths:
    - "User runs `box passgen` and gets a 16-char cryptographically random password from the curated paste-safe charset"
    - "User runs `box passgen --words 4` and gets a four-word passphrase from the EFF list; `box passgen --count 10` prints 10 distinct passwords; all output is stdout-only"
    - "User runs `box passgen --no-symbols` and gets a password with no symbol characters"
    - "User runs `box cowsay \"hello\"` and gets the classic cow + single-line speech bubble; long text wraps at 40 cols (multi-line border); a word longer than the width hard-breaks"
  artifacts:
    - path: "src/commands/passgen/mod.rs"
      provides: "PassgenArgs implementing RunCommand; hand-rolled on rand not the passwords crate (D-02); OsRng CSPRNG, unbiased IndexedRandom::choose/random_range, NO % len (D-08); curated paste-safe symbol const, stdout-only, default-16/--words/--count/--no-symbols (D-14); EFF wordlist via include_str! (D-07)"
      contains: "OsRng"
    - path: "src/commands/cowsay/mod.rs"
      provides: "CowsayArgs { text, width } implementing RunCommand; hand-rolled greedy wrap + hard-break (D-03), fixed-40 default not terminal_width (D-11), single/multi-line bubble"
      contains: "impl RunCommand for CowsayArgs"
    - path: "src/data/eff_large_wordlist.txt"
      provides: "7776-word EFF Large Diceware wordlist (dice codes stripped at load), CC-BY 3.0 US attribution required (D-09)"
      contains: "abacus"
    - path: "tests/passgen.rs"
      provides: "PASS-01 tests (length, charset membership, --no-symbols, --words N, --count N distinct, stdout-only, wordlist==7776)"
      contains: "cargo_bin"
  key_links:
    - from: "src/commands/passgen/mod.rs"
      to: "rand::rngs::OsRng + IndexedRandom::choose / random_range"
      via: "unbiased CSPRNG selection — never `% len`"
      pattern: "OsRng|IndexedRandom|random_range"
    - from: "src/commands/passgen/mod.rs"
      to: "src/data/eff_large_wordlist.txt"
      via: "include_str!(\"../../data/eff_large_wordlist.txt\")"
      pattern: "include_str!"
    - from: "src/main.rs"
      to: "passgen/cowsay .run()"
      via: "Commands::Passgen(args) => args.run(); Commands::Cowsay(args) => args.run()"
      pattern: "Commands::Passgen\\(args\\)|Commands::Cowsay\\(args\\)"
---

<objective>
The phase's one real threat surface plus a text-layout slice: `passgen`
(CSPRNG passwords + EFF passphrases, D-02/D-08/D-14) and `cowsay` (greedy
40-col word-wrap + hard-break + classic bubble, D-11). This plan creates the
net-new `src/data/` directory (the EFF wordlist), wires the `include_str!`
embed, and locks the cowsay layout via two trycmd snapshots.

Purpose: passgen is the security-critical command — it MUST use `OsRng`
(getrandom→ProcessPrng) and unbiased `IndexedRandom::choose`/`random_range`,
NEVER `% len` (modulo bias, D-02). The bias-freedom + CSPRNG-source guarantees
are a code-review gate, not a flaky statistical test (T-V6). cowsay proves the
hand-rolled wrap/hard-break math with deterministic snapshots.

Output: two working commands, the EFF wordlist asset + attribution, passgen
integration + unit tests, cowsay unit tests + two snapshots, registry edits, and
(conditionally) `rand_core` added to Cargo.toml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pure-transform-utilities/02-CONTEXT.md
@.planning/phases/02-pure-transform-utilities/02-RESEARCH.md
@.planning/phases/02-pure-transform-utilities/02-PATTERNS.md
@.planning/phases/02-pure-transform-utilities/02-VALIDATION.md
@.planning/phases/02-pure-transform-utilities/02-03-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. From the live repo + prior plans + RESEARCH. -->

OsRng wrapper for passgen — OsRng implements TryRngCore, NOT Rng (RESEARCH Pattern 4):
```rust
use rand::rngs::OsRng;
use rand::TryRngCore;          // brings .unwrap_err(); Task 1 verifies this resolves via rand 0.9 — if NOT, Task 1 adds rand_core and Task 2 imports rand_core::TryRngCore instead
use rand::seq::IndexedRandom;  // brings .choose() onto slices
use rand::Rng;                 // brings .random_range()
let mut rng = OsRng.unwrap_err();              // UnwrapErr<OsRng>: impls RngCore → Rng
let ch: &char = CHARSET.choose(&mut rng).expect("non-empty charset");
let idx: usize = rng.random_range(0..WORDLIST.len());
```
ANTI-PATTERNS (forbidden, RESEARCH:358-366): `slice[n % slice.len()]`, `gen_range`, `thread_rng()`, calling `.choose()` on bare `OsRng`, any fixed/manual seed.

include_str! path is relative to THE SOURCE FILE (Pitfall 6): from src/commands/passgen/mod.rs →
`include_str!("../../data/eff_large_wordlist.txt")`. EFF lines are `<5-digit-dice>\t<word>` — strip with `line.rsplit('\t').next()` / `split_whitespace().last()` (Pitfall 7); assert 7776 clean entries, no digit/tab.

cowsay single-line target (CONTEXT specifics):
```
 _____________
< Hello, box! >
 -------------
        \   ^__^
         \  (oo)\_______
            (__)\       )\/\
                ||----w |
                ||     ||
```
Multi-line border: `/ \` (top line), `| |` (middle lines), `\ /` (bottom line); shorter lines space-padded to the longest. Hard-break any word longer than width into width-sized chunks (Pitfall 5). Fixed 40 cols default, NOT terminal_width() (D-11).

Args + impl shape, src/commands/flatten/mod.rs:25-46,93-94. const table style, src/commands/flatten/rename.rs:18-26 (model for the curated symbol const).
core::input (Plan 01): `read_input(arg) -> Result<String>` (cowsay text or stdin).
cli.rs swaps (src/cli.rs:45-48): `Passgen,`→`Passgen(crate::commands::passgen::PassgenArgs)`, `Cowsay,`→`Cowsay(crate::commands::cowsay::CowsayArgs)`. main.rs arms (src/main.rs:66-67): `Commands::Passgen(args) => args.run(); Commands::Cowsay(args) => args.run()`.
EFF attribution line (CONTEXT:98 / canonical refs): "Passphrase wordlist: EFF Long Wordlist, © Electronic Frontier Foundation, CC-BY 3.0 US." — add to README and/or `box passgen --help` long help and/or a LICENSE-THIRD-PARTY note.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the EFF wordlist asset + resolve the OsRng/rand_core import (A1 → checked fact)</name>
  <files>src/data/eff_large_wordlist.txt, Cargo.toml</files>
  <read_first>
    - .planning/phases/02-pure-transform-utilities/02-RESEARCH.md:420-424 (Pitfall 7 EFF dice-code format), :293-311 (Pattern 4 OsRng wrapper + A1 rand_core note), :574 (rand_core conditional-add)
    - .planning/phases/02-pure-transform-utilities/02-CONTEXT.md:44-48 (D-09 content/counts), :97-99 (EFF licensing/attribution)
    - .planning/phases/02-pure-transform-utilities/02-PATTERNS.md:306-310 (data files — NO ANALOG; include_str! relative-path rule)
    - Cargo.toml:11-27 (the [dependencies] block — rand 0.9 already added in Plan 01)
  </read_first>
  <action>
    Create the directory `src/data/` and add `src/data/eff_large_wordlist.txt`: the canonical EFF Large (Diceware) wordlist — 7776 words (CC-BY 3.0 US, https://www.eff.org/dice). The canonical file ships as `<5-digit-dice-code>\t<word>` lines; you may store it verbatim (and strip dice codes at load in Task 2) OR store words-only — either is acceptable as long as the loader in Task 2 yields exactly 7776 clean words with no digits/tabs. Do NOT use the short EFF lists (weaker entropy per D-09). If network fetch of the file is unavailable, reconstruct the 7776-word list from a bundled/known-good source; the binding contract is the 7776-count + no-dice-code unit test in Task 2.
    DECISION STEP — resolve A1 to a checked fact (RESEARCH Pattern 4 / Assumption A1, LOW risk): determine whether `use rand::TryRngCore;` (the import that brings `OsRng.unwrap_err()`) resolves under rand 0.9. Run a one-line build probe — e.g. add a throwaway `use rand::TryRngCore;` to a scratch location (or attempt `cargo build` after Task 2 wires it, but the deterministic check is to compile a one-line probe) — and observe whether it resolves. If it RESOLVES: leave Cargo.toml unchanged (the re-export path works; Task 2 uses `rand::TryRngCore`). If it does NOT resolve: add `rand_core = "0.9"` to the Cargo.toml `[dependencies]` block (Task 2 will then import `rand_core::TryRngCore`). Record the outcome explicitly in the SUMMARY so A1 is closed as fact, not assumption. Primary RNG construction is `OsRng.unwrap_err()` (D-08 literal reading, RESEARCH Open Question 2 RESOLVED) — note this choice.
    The 7776-count assertion is NOT verified here by a shell line-count; it is owned by the Task-2 Rust unit test (`assert_eq!(eff_wordlist().len(), 7776)`). Task 1's gate is simply that the project builds.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/eff_large_wordlist.txt` exists and is non-empty.
    - `cargo build` exits 0 (cross-platform gate — no POSIX `test`/`grep` dependency; the 7776-count is asserted by the Task-2 Rust unit test, not a shell line-count).
    - A1 is resolved to a checked fact: SUMMARY records whether `use rand::TryRngCore;` resolved via rand 0.9 (Cargo.toml unchanged) or `rand_core = "0.9"` was added, and that the passgen RNG path is `OsRng.unwrap_err()`.
  </acceptance_criteria>
  <done>The EFF wordlist is embedded-ready under src/data/, the project builds, and the OsRng import path is a checked fact (rand_core added only if `rand::TryRngCore` did not resolve).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: passgen command + CSPRNG/charset/passphrase tests (PASS-01, T-V6)</name>
  <files>src/commands/passgen/mod.rs, src/commands/mod.rs, src/cli.rs, src/main.rs, tests/passgen.rs</files>
  <behavior>
    - `box passgen` → one 16-char password; every char ∈ the curated four-class set (lower+upper+digits+curated symbols).
    - `box passgen --length 24` → a 24-char password.
    - `box passgen --no-symbols` → a password (sampled large) with NO char in the symbol set.
    - `box passgen --words 4` → 4 words joined by the separator; each word ∈ the parsed EFF list.
    - `box passgen --count 10` → 10 lines, all distinct (HashSet len == 10).
    - the parsed EFF wordlist has exactly 7776 entries, none containing a digit or tab — asserted by a unit test `assert_eq!(eff_wordlist().len(), 7776)` (this is the real 7776 gate, replacing the removed Task-1 shell line-count).
    - the curated symbol const excludes shell/quoting-hostile chars: backtick, single quote, double quote, dollar, backslash, semicolon, pipe, ampersand, angle brackets, parens, and space.
    - all output is stdout-only; nothing is written to stderr on success (D-14).
  </behavior>
  <read_first>
    - src/commands/flatten/mod.rs:25-46,93-94 (Args + impl), src/commands/flatten/rename.rs:18-26 (the const table model for the curated symbol set)
    - src/cli.rs:45 (Passgen unit variant), src/main.rs:66 (the arm to replace)
    - tests/flatten.rs:1-24, tests/cli.rs:42-75 (harness), RESEARCH.md:512-513 (HashSet distinctness)
    - .planning/phases/02-pure-transform-utilities/02-RESEARCH.md:275-311 (Patterns 3+4 unbiased selection + OsRng wrapper), :358-366 (anti-patterns), :515-527 (crypto-sampling testable-vs-review split), :639,:644-647 (V6 + threat patterns)
    - .planning/phases/02-pure-transform-utilities/02-PATTERNS.md:281-284 (passgen pattern assignment)
    - .planning/phases/02-pure-transform-utilities/02-CONTEXT.md:30 (D-02 no % len), :43 (D-08 RNG split), :65 (D-14 charset/flags/stdout-only)
  </read_first>
  <action>
    Create src/commands/passgen/mod.rs with `PassgenArgs { length: usize (#[arg(long, default_value_t = 16)]), count: usize (#[arg(long, default_value_t = 1)]), words: Option<usize> (#[arg(long)]), no_symbols: bool (#[arg(long = "no-symbols")]) }` implementing `RunCommand`. The exact restriction-flag surface beyond `--no-symbols` is Claude's discretion (D-14 discretion), as long as the default is the four-class curated-symbol set.
    Define a curated paste-safe symbol constant (e.g. `const SYMBOLS: &[char] = &[ ... ];`) that EXCLUDES backtick, single quote, double quote, dollar, backslash, semicolon, pipe, ampersand, angle brackets, parens, and space (D-14, paste-safe in PowerShell). Build the default charset = lowercase a-z + uppercase A-Z + digits 0-9 + SYMBOLS; `--no-symbols` drops SYMBOLS.
    RNG: construct `let mut rng = OsRng.unwrap_err();` (RESEARCH Pattern 4 primary path, D-08 literal; `use rand::rngs::OsRng;`). For the `TryRngCore` import, use `rand::TryRngCore` IF it resolves under rand 0.9 (the default, per Task 1's A1 check); OTHERWISE — i.e. if Task 1 added `rand_core = "0.9"` to Cargo.toml because the re-export did not resolve — use `rand_core::TryRngCore` instead. The construction (`OsRng.unwrap_err()`) is identical and locked in both cases (D-08, RESEARCH Open Question 2 RESOLVED); only the import path differs based on Task 1's checked outcome. Sample EVERY character with `charset.choose(&mut rng)` (`use rand::seq::IndexedRandom;`) — NEVER `charset[x % charset.len()]`. For passphrase mode, pick words with `WORDLIST.choose(&mut rng)` or `rng.random_range(0..WORDLIST.len())` (`use rand::Rng;`). NO `% len` anywhere; NO fixed seed (D-02/D-08).
    Passphrase: when `self.words` is `Some(n)`, parse the EFF wordlist via `include_str!("../../data/eff_large_wordlist.txt")` into `&[&str]` stripping the dice-code prefix (`line.rsplit('\t').next()` or `split_whitespace().last()`, Pitfall 7), pick `n` words, join with a separator (separator choice is discretion). Otherwise generate `self.length` random chars from the charset. Expose the parsed list via a `fn eff_wordlist() -> Vec<&'static str>` (or cached slice) so the unit test can assert its length.
    Loop `self.count` times, `println!` each result. ALL output to stdout; NEVER log a generated password to stderr (D-14, info-disclosure mitigation).
    Add the EFF attribution to `box passgen --help` long help (and note it for README/LICENSE-THIRD-PARTY): "Passphrase wordlist: EFF Long Wordlist, © Electronic Frontier Foundation, CC-BY 3.0 US."
    Add `#[cfg(test)] mod tests`: `assert_eq!(eff_wordlist().len(), 7776)` AND none of those entries contains a digit or tab (this is the authoritative 7776-count gate that the cross-platform Task-1 verify hands off to); default password length == 16 and every char ∈ the four-class set; the SYMBOLS const contains none of the banned shell chars; a `--no-symbols` sample (build many) contains no symbol.
    Register `pub mod passgen;`; swap cli.rs:45 (keep `/// Generate secure passwords and passphrases` verbatim); replace main.rs:66 with `Commands::Passgen(args) => args.run()`.
    Create tests/passgen.rs: `box passgen` length 16; `box passgen --length 24` length 24; `box passgen --no-symbols` (sample many, assert no symbol); `box passgen --words 4` → 4 EFF words; `box passgen --count 10` → 10 distinct lines (HashSet); success runs write nothing to stderr. NO_COLOR=1.
    CODE-REVIEW GATE (T-V6, NOT a statistical test): the bias-freedom + CSPRNG-source are construction guarantees — a reviewer (and the grep below) must confirm no `% ` modulo selection and that the RNG is `OsRng`-backed. Do NOT add a flaky chi-square statistical test as the gate.
  </action>
  <verify>
    <automated>cargo test --test passgen 2>&1 | tail -8 && cargo test passgen:: 2>&1 | tail -6 && cargo clippy -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test passgen` and `cargo test passgen::` both exit 0.
    - Unit assertion (the authoritative 7776 gate): `assert_eq!(eff_wordlist().len(), 7776)` passes and none of the parsed entries contains a digit or tab.
    - Unit assertion: a default password is 16 chars and every char ∈ the four-class curated set; the SYMBOLS const contains none of the banned shell chars.
    - CLI assertion: `box passgen --words 4` → 4 EFF words; `box passgen --count 10` → 10 distinct lines; `box passgen --no-symbols` over a large sample has no symbol char.
    - CODE-REVIEW gate (T-V6): `grep -n 'OsRng' src/commands/passgen/mod.rs` returns ≥1 (CSPRNG source); `grep -nE '\[[^]]*%[^]]*\]' src/commands/passgen/mod.rs` returns no modulo-indexed selection (no `% len`). These greps are the bias/CSPRNG gate, not a statistical test.
    - `cargo clippy -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`box passgen` generates CSPRNG passwords/passphrases via OsRng + unbiased selection (no `% len`), honors length/count/words/no-symbols, embeds the EFF list (7776, attributed, unit-asserted), outputs stdout-only, and the passgen stub is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cowsay command + wrap/hard-break unit tests + two trycmd snapshots (COW-01)</name>
  <files>src/commands/cowsay/mod.rs, src/commands/mod.rs, src/cli.rs, src/main.rs, tests/cmd/cowsay-single.trycmd, tests/cmd/cowsay-multi.trycmd</files>
  <behavior>
    - `box cowsay "Hello, box!"` → the classic cow + single-line `< Hello, box! >` bubble (top `_`-border length matches the text+padding, bottom `-`-border).
    - text longer than 40 cols → multi-line bubble with `/ \`, `| |`, `\ /` borders; shorter lines space-padded to the longest.
    - a single word longer than the width hard-breaks into width-sized chunks; no output line exceeds the width.
    - `echo "piped" | box cowsay` (no arg) reads stdin via core::input.
    - `--width N` overrides the fixed 40-col default; `width=0` does not panic.
  </behavior>
  <read_first>
    - src/commands/flatten/mod.rs:25-46,93-94 (Args + impl)
    - src/core/output.rs:174-190 (truncate_middle — the closest char-count string-manipulation analog)
    - src/core/input.rs (read_input signature from Plan 01)
    - src/cli.rs:47-48 (Cowsay unit variant), src/main.rs:67 (the arm to replace)
    - tests/cmd/help.trycmd:1-38 (transcript format + box[EXE] placeholder), tests/cli.rs:100-103 (glob registration)
    - .planning/phases/02-pure-transform-utilities/02-RESEARCH.md:408-411 (Pitfall 5 hard-break off-by-one)
    - .planning/phases/02-pure-transform-utilities/02-PATTERNS.md:286-289 (cowsay pattern assignment)
    - .planning/phases/02-pure-transform-utilities/02-CONTEXT.md:60 (D-11 cowsay layout), :134-144 (single-line target art)
  </read_first>
  <action>
    Create src/commands/cowsay/mod.rs with `CowsayArgs { text: Option<String>, width: usize (#[arg(long, default_value_t = 40)]) }` implementing `RunCommand`. Acquire text via `crate::core::input::read_input(self.text)?` (arg or piped stdin; no-arg on a TTY → exit 2 via Plan-01 wiring).
    Pure `fn wrap(text: &str, width: usize) -> Vec<String>`: greedy word-wrap to `width` columns; when a single word's char-count exceeds `width`, hard-break it into `width`-sized chunks BEFORE wrapping (Pitfall 5 — unit-test that no returned line exceeds `width`; guard `width == 0` to avoid a panic/infinite loop, e.g. clamp to ≥1). Use char-count, not byte-length (mirror truncate_middle's char handling).
    Pure `fn bubble(lines: &[String]) -> String`: single content line → top ` _…_ ` border, `< text >`, bottom ` -…- ` border (D-11 single-line form). Multiple lines → top `/ \`, middle `| line |` with each line space-padded to the longest, bottom `\ /`. Append the fixed classic cow art (exact bytes are Claude's discretion per D-discretion; match the CONTEXT single-line target). Pure ASCII only (Phase-1 glyph-is-source-of-truth). Print the assembled bubble+cow to stdout.
    Add `#[cfg(test)] mod tests`: `wrap` of a long word asserts no line exceeds width and the word is chunked; `wrap("a b c", 40)` stays one line; `wrap` with `width=0` does not panic; `bubble` of one line vs many produces the correct border characters and equal-length padded middle lines.
    Register `pub mod cowsay;`; swap cli.rs:47-48 `Cowsay,` → `Cowsay(crate::commands::cowsay::CowsayArgs)` (keep `/// Wrap text in an ASCII-art speech bubble` verbatim); replace main.rs:67 with `Commands::Cowsay(args) => args.run()`.
    Create two snapshots under NO_COLOR (deterministic — Pattern G): tests/cmd/cowsay-single.trycmd for `box cowsay "Hello, box!"` (single-line bubble) and tests/cmd/cowsay-multi.trycmd for a string that wraps past the width (multi-line `/ \` bubble). Generate with `TRYCMD=overwrite cargo test`, review the diff for Windows CRLF (RESEARCH A4) before committing. Auto-registered by the existing glob in tests/cli.rs:101-103.
  </action>
  <verify>
    <automated>cargo test cowsay:: 2>&1 | tail -6 && cargo test --test cli 2>&1 | tail -6 && cargo clippy -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test cowsay::` exits 0 (wrap/hard-break/bubble unit tests).
    - `cargo test --test cli` exits 0 (both cowsay snapshots match under NO_COLOR).
    - Unit assertion: for a word longer than `width`, no wrapped line exceeds `width`; `wrap(_, 0)` does not panic.
    - CLI assertion: `box cowsay "Hello, box!"` produces the single-line `< … >` bubble; a long string produces the `/ \`…`\ /` multi-line bubble with equal-length padded middle lines.
    - Source assertion: `grep -c 'terminal_width' src/commands/cowsay/mod.rs` returns 0 (fixed 40, NOT terminal_width — D-11). No `not_implemented("cowsay")` arm remains.
    - `cargo clippy -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`box cowsay` renders the classic single- and multi-line bubbles, hard-breaks over-long words within a fixed/overridable width, reads arg-or-stdin, and the cowsay stub is gone — locked by two snapshots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS CSPRNG → passgen output | Generated secrets must be unpredictable and unbiased; output must not leak off stdout. |
| arg/stdin → cowsay wrap | Untrusted text wrapped to a fixed width; over-long words must not break the layout or panic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V6-rng | Information Disclosure | passgen RNG construction | mitigate | Use `OsRng` (getrandom→ProcessPrng CSPRNG); NO fixed/manual seed (D-08). Code-review gate: `grep 'OsRng'` confirms the source; statistical test is explicitly NOT the gate. |
| T-V6-bias | Information Disclosure | passgen char/word selection | mitigate | Unbiased selection via `IndexedRandom::choose` / `random_range` ONLY; NEVER `% len` (D-02). Code-review gate: grep finds no modulo-indexed selection. |
| T-V6-leak | Information Disclosure | passgen output channel | mitigate | All passwords to stdout only; never logged to stderr; no `--clip` in v1 (D-14). Integration test asserts stderr is empty on success. |
| T-02-08 | (usability/safety) | passgen symbol set | mitigate | Curated paste-safe symbol const excludes shell/quoting-hostile chars (D-14); unit-tested that the banned chars are absent. |
| T-02-09 | Denial of Service (self) | cowsay wrap/hard-break | mitigate | Hard-break splits over-long words into width chunks; `width=0` clamped — no infinite loop/panic (Pitfall 5); unit-tested. |
| T-02-SC | Tampering | crates.io install (rand_core, conditional) | accept | `rand_core` is the rust-random canonical crate (same org as `rand`, slopcheck `[OK]` lineage); added only if the `rand::TryRngCore` re-export check (Task 1) fails. No `[ASSUMED]`/`[SUS]`/`[SLOP]` package — no blocking checkpoint. |
</threat_model>

<verification>
- `cargo test --test passgen`, `cargo test passgen::`, `cargo test cowsay::`, `cargo test --test cli` all green.
- T-V6 code-review gate: grep confirms `OsRng` present and no `% len` selection in passgen.
- `cargo clippy -- -D warnings` clean; `cargo fmt --check` clean.
- EFF attribution present in `box passgen --help` (and README/LICENSE-THIRD-PARTY note).
- Manual (02-VALIDATION.md): paste a generated `box passgen` into a PS7 prompt as a quoted string — confirms paste-safety (shell-interaction property, not automatable).
</verification>

<success_criteria>
- passgen: 16-char default, `--length`/`--count`/`--words`/`--no-symbols`, OsRng + unbiased selection (no `% len`), EFF list embedded+attributed, stdout-only.
- cowsay: classic single/multi-line bubble, fixed-40 wrap + hard-break, arg-or-stdin, two snapshots.
- Both stubs removed; modules registered and dispatched; unit + integration + snapshot tests green; T-V6 gate satisfied.
</success_criteria>

<output>
Create `.planning/phases/02-pure-transform-utilities/02-04-SUMMARY.md` when done.
</output>
</content>
</invoke>
