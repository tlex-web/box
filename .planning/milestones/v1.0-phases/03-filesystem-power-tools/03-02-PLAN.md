---
phase: 03-filesystem-power-tools
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/commands/tree/mod.rs
  - src/commands/mod.rs
  - src/core/output.rs
  - src/commands/flatten/mod.rs
  - src/cli.rs
  - src/main.rs
  - tests/tree.rs
  - tests/cmd/tree.trycmd
autonomous: true
requirements: [TREE-01]
must_haves:
  truths:
    - "User runs `box tree ./src` and sees a box-drawing tree (├── └── │) with directory names colored vs files (D-09, D-10)"
    - "User runs `box tree ./src --sizes` and sees a per-file size column; directories show a blank size (D-10)"
    - "User runs `box tree ./src --depth N` and the displayed tree is capped at depth N (D-10)"
    - "User sees a trailing `N directories, M files` summary line (D-10)"
    - "Tree entries are ordered directories-first, then files, each case-insensitive alphabetical (D-08)"
    - "Piping `box tree ./src` to a file yields plain text with no ANSI escape sequences (D-10)"
  artifacts:
    - path: "src/commands/tree/mod.rs"
      provides: "TreeArgs + dir-first recursive box-drawing render (D-08/D-09) + --sizes/--depth + count summary (D-10) + shared-walk hidden prune (D-06) + RunCommand impl"
      contains: "impl RunCommand for TreeArgs"
      min_lines: 90
    - path: "src/core/output.rs"
      provides: "human_size promoted from flatten (1024-based, B/KB/MB/GB/TB), shared by tree + du (D-12)"
      contains: "pub fn human_size"
    - path: "tests/tree.rs"
      provides: "TREE-01 integration tests (sizes/depth/summary/no-ansi)"
      contains: "NO_COLOR"
    - path: "tests/cmd/tree.trycmd"
      provides: "end-to-end box-drawing transcript snapshot"
      contains: "box tree"
  key_links:
    - from: "src/main.rs"
      to: "crate::commands::tree::TreeArgs::run"
      via: "Commands::Tree(args) => args.run()"
      pattern: "Commands::Tree\\(args\\)"
    - from: "src/commands/tree/mod.rs"
      to: "core::output::human_size"
      via: "the promoted shared size helper (D-12)"
      pattern: "human_size"
    - from: "src/commands/tree/mod.rs"
      to: "core::fs::is_hidden"
      via: "the shared walk filter reused verbatim — skip hidden by default (D-06)"
      pattern: "is_hidden"
    - from: "src/commands/flatten/mod.rs"
      to: "core::output::human_size"
      via: "flatten's call site updated to the promoted fn (no local copy, D-12)"
      pattern: "human_size"
---

<objective>
Ship `box tree` end-to-end (TREE-01): a depth-first, directories-first box-drawing tree render with colored directory names, an optional per-file `--sizes` column, a `--depth N` cap, and an `N directories, M files` summary. This plan also performs the shared-core change `human_size` promotion — moving flatten's tested `human_size` fn into `core::output` so tree (this plan) and du (Plan 03-03) share one formatter (D-12).

Purpose: TREE-01 is the first size-formatting consumer, so it is the natural home for the `human_size` promotion. Folding the shared-core change into its first consumer (rather than a standalone infrastructure plan) keeps the slice vertical.
Output: a working `box tree` command, the promoted shared `human_size`, flatten re-pointed at it, and Wave-0 tests + a trycmd transcript.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-filesystem-power-tools/03-CONTEXT.md
@.planning/phases/03-filesystem-power-tools/03-RESEARCH.md
@.planning/phases/03-filesystem-power-tools/03-PATTERNS.md
@.planning/phases/03-filesystem-power-tools/03-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. -->

From src/core/fs.rs — the walk filter every read-only walker reuses (D-06); never re-implement (it exempts depth-0 so a dotted root isn't pruned — walkdir#142):
```rust
pub fn is_hidden(entry: &walkdir::DirEntry) -> bool;     // fs.rs:46-61
pub fn normalize_path(p: &Path) -> std::io::Result<PathBuf>;  // fs.rs:34-36 (dunce, UNC-safe)
```

From src/core/output.rs — the color gate to route every styled token through (so piped output is byte-identical minus ANSI):
```rust
pub fn is_color_on() -> bool;        // output.rs:32-34
pub fn terminal_width() -> usize;    // output.rs:195-200
```

The fn being PROMOTED (currently private in src/commands/flatten/mod.rs:332-344, with its unit test at 350-357) — move it VERBATIM into core::output, make it `pub`, and re-point flatten's one call site (flatten/mod.rs:199):
```rust
fn human_size(bytes: u64) -> String {
    const UNITS: &[&str] = &["B", "KB", "MB", "GB", "TB"];
    if bytes < 1024 { return format!("{bytes} B"); }
    let mut size = bytes as f64; let mut unit = 0;
    while size >= 1024.0 && unit < UNITS.len() - 1 { size /= 1024.0; unit += 1; }
    format!("{size:.1} {}", UNITS[unit])
}
```

Walk skeleton to copy verbatim (flatten/mod.rs:212-215):
```rust
let walker = WalkDir::new(root).follow_links(false).into_iter().filter_entry(|e| !is_hidden(e));
```

Box-drawing glyph constants (RESEARCH Pattern 3 — STRUCTURE, so Unicode is correct here, distinct from flatten's ASCII status glyphs):
```rust
const TEE: &str = "├── "; const ELL: &str = "└── "; const PIPE: &str = "│   "; const GAP: &str = "    ";
```

Dir-first sort (D-08): directories before files, each case-insensitive alphabetical:
```rust
children.sort_by(|a, b| b.is_dir().cmp(&a.is_dir())
    .then_with(|| a.name().to_lowercase().cmp(&b.name().to_lowercase())));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote human_size to core::output + Wave-0 tree tests</name>
  <files>src/core/output.rs, src/commands/flatten/mod.rs, tests/tree.rs, tests/cmd/tree.trycmd</files>
  <read_first>
    - src/core/output.rs (the file being modified — place the promoted fn alongside `terminal_width`/`truncate_middle`, lines 171-200; follow the existing `pub fn` + co-located `#[cfg(test)] mod tests` style, lines 227-329)
    - src/commands/flatten/mod.rs (lines 332-344 — the `human_size` fn to MOVE; line 199 — the single call site `real_run_summary(..., &human_size(bytes_written))` to re-point to `core::output::human_size`; lines 350-357 — the `human_size_scales` unit test that MOVES with the fn into output.rs's test mod)
    - tests/flatten.rs (lines 14-24 — the `assert_cmd` runner + `NO_COLOR=1` analog `tests/tree.rs` copies); tests/cmd/cowsay-single.trycmd (the fenced `$ box <cmd>` transcript format); tests/cli.rs (lines 100-103 — the `trycmd()` runner already globs `tests/cmd/*.trycmd`, so a new `tree.trycmd` is auto-picked up — no test-code change needed)
    - .planning/phases/03-filesystem-power-tools/03-VALIDATION.md (the TREE-01 rows — exact test names)
    - .planning/phases/03-filesystem-power-tools/03-PATTERNS.md (the `src/core/output.rs (edit: promote human_size)` section — verbatim-move instructions)
  </read_first>
  <action>
    Move `human_size` from `src/commands/flatten/mod.rs:332-344` into `src/core/output.rs` VERBATIM (1024-based, decimal-style `B`/`KB`/`MB`/`GB`/`TB` labels, stop at TB), make it `pub`, and place it alongside `terminal_width`/`truncate_middle`. Move its `human_size_scales` unit test (flatten/mod.rs:350-357) into `output.rs`'s `#[cfg(test)] mod tests`. Update flatten's one call site (flatten/mod.rs:199) to `crate::core::output::human_size(...)` and delete the now-orphaned private fn + test from flatten so `clippy -D warnings` stays clean (no dead code). Do NOT add the `humansize` crate (D-12).

    Scaffold `tests/tree.rs` as the Wave-0 gate: copy the runner shape from `tests/flatten.rs:14-24` (`Command::cargo_bin("box")`, `.arg("tree")`, `cmd.env("NO_COLOR", "1")`). Write one `#[test]` per VALIDATION TREE-01 row, named exactly: `tree_sizes_and_depth`, `tree_count_summary`, `tree_piped_no_ansi`. Build a fixed temp tree (distinct, known file byte-sizes so `--sizes` is deterministic). Assert: `--sizes` shows the per-file `human_size` and a blank size for directories (D-10); `--depth N` caps displayed depth; the `N directories, M files` summary line is present; and (mirroring `tests/cli.rs::piped_help_has_no_ansi`) piped output contains no `\x1b[`. Also create `tests/cmd/tree.trycmd` with a `$ box tree <fixture>` transcript — author expected paths with FORWARD SLASHES (trycmd normalizes `\`→`/` on Windows; do not fight the harness). These fail until Task 2 lands (RED gate).
  </action>
  <verify>
    <automated>cargo test --lib core::output 2>&1 | grep -q 'test result: ok' && cargo build 2>&1 | grep -v '^#' | grep -c 'error\[' | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/output.rs` contains `pub fn human_size(bytes: u64) -> String` and the migrated `human_size_scales` test; `cargo test --lib core::output` shows `test result: ok`.
    - `src/commands/flatten/mod.rs` no longer defines a local `human_size` (the private fn and its in-module test are removed) and its call site uses `crate::core::output::human_size`; `cargo build` reports 0 `error[` lines and no dead-code warning.
    - `tests/tree.rs` exists with `tree_sizes_and_depth`, `tree_count_summary`, `tree_piped_no_ansi`, each setting `NO_COLOR=1`; `tests/cmd/tree.trycmd` exists with a `box tree` transcript using forward-slash paths.
  </acceptance_criteria>
  <done>`human_size` lives in `core::output` (flatten re-pointed, no duplicate), and the Wave-0 tree tests + trycmd transcript exist and compile.</done>
</task>

<task type="auto">
  <name>Task 2: tree command — dir-first box-drawing render + --sizes/--depth + summary + wiring</name>
  <files>src/commands/tree/mod.rs, src/commands/mod.rs, src/cli.rs, src/main.rs</files>
  <read_first>
    - src/commands/flatten/mod.rs (lines 29-33 imports; 212-215 the walk skeleton to copy; 93-94 the `impl RunCommand` shell; 100-103 the `normalize_path` call site to mirror)
    - src/core/output.rs (the now-promoted `human_size`; `is_color_on` at 32-34; `terminal_width` at 195-200; the `is_color_on()` gate pattern at 121-129 — gate `.blue().bold()` on directory names the same way)
    - src/core/fs.rs (lines 46-61 `is_hidden` — reuse verbatim, never re-implement; 34-36 `normalize_path`)
    - src/cli.rs (lines 35-36 model `Tree(TreeArgs)` on `Flatten`; line 59 the unit `Tree,` variant to replace)
    - src/main.rs (line 61 model the dispatch arm; line 72 the `Commands::Tree => Err(not_implemented("tree"))` arm to remove)
    - src/commands/mod.rs (lines 7-17 register `pub mod tree;`)
    - .planning/phases/03-filesystem-power-tools/03-RESEARCH.md (Pattern 3 deterministic dir-first tree render — glyph constants, the dir-first sort, color-only-on-dir-names, summary to stdout)
    - .planning/phases/03-filesystem-power-tools/03-PATTERNS.md (the `src/commands/tree/mod.rs` section)
  </read_first>
  <action>
    Create `src/commands/tree/mod.rs` and register `pub mod tree;` in `src/commands/mod.rs`. Define `TreeArgs` (`#[derive(Debug, Args)]`): a positional `path: PathBuf` (default `.`), `#[arg(long)] sizes: bool`, and `#[arg(long)] depth: Option<usize>`. In `impl RunCommand for TreeArgs`: `normalize_path` the target (dunce, `.context(...)`-wrapped per FOUND-06), then render depth-first.

    Render: for each directory read its children, partition into dirs/files, sort each with the D-08 dir-first case-insensitive comparator, and recurse depth-first. Compute each row's prefix from "is this the last child at this level" — `└── ` (ELL, last) vs `├── ` (TEE, non-last); the continuation prefix from ancestors is `│   ` (PIPE) where the ancestor was non-last and `    ` (GAP) where it was last (the four Unicode box-drawing constants are RESEARCH Pattern 3 — STRUCTURE, distinct from flatten's ASCII status glyphs, D-09). Reuse the `WalkDir`-free recursion OR a depth-aware walk, but apply the SAME hidden-prune as the shared walk (skip `is_hidden` children; never re-implement the predicate, D-06) and `follow_links(false)`. Color ONLY directory names via `.blue().bold()` gated on `is_color_on()` so piped output is byte-identical minus ANSI (D-10); file names and branch glyphs are default-colored.

    `--sizes` (D-10): show `core::output::human_size(file_len)` per FILE only; directories show a BLANK size column (recursive dir totals are du's job, not tree's). `--depth N` limits the DISPLAYED depth. Print the `N directories, M files` summary line to stdout after the tree (count every shown dir/file; GNU `tree` convention, satisfies TREE-01's count summary). Send all tree rows + summary to stdout; any messages/errors to stderr (FOUND-03).

    Swap `src/cli.rs:59` `Tree,` → `Tree(crate::commands::tree::TreeArgs)` (model on `Flatten`), keep its doc comment. Remove `Commands::Tree => Err(not_implemented("tree"))` (main.rs:72) → `Commands::Tree(args) => args.run()`. Leave du/dupes/bulk-rename stubs untouched. Add co-located `#[cfg(test)] mod tests` for the pure formatting fns (glyph-prefix computation, dir-first sort) so byte-exact renders are locked by units while trycmd covers the end-to-end shape. `cargo fmt`; `cargo clippy --all-targets -- -D warnings` clean.
  </action>
  <verify>
    <automated>cargo test --test tree 2>&1 | grep -q 'test result: ok' && cargo test --test cli trycmd 2>&1 | grep -q 'test result: ok' && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `src/commands/tree/mod.rs` contains `impl RunCommand for TreeArgs`, the four Unicode box-drawing constants, the D-08 dir-first sort, and color gated on `is_color_on()` for directory names only.
    - `cargo test --test tree` is green: `--sizes` shows per-file `human_size` and blank dir sizes, `--depth N` caps depth, the `N directories, M files` summary appears, and piped output has no `\x1b[`.
    - `tests/cmd/tree.trycmd` passes via the existing `trycmd()` runner (`cargo test --test cli trycmd` green) with forward-slash-normalized paths.
    - `src/cli.rs` `Tree` carries `TreeArgs`; `src/main.rs` dispatches `Commands::Tree(args) => args.run()` with no `not_implemented("tree")` arm; `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check` clean.
  </acceptance_criteria>
  <done>`box tree` renders a colored, dir-first Unicode box-drawing tree with `--sizes`/`--depth` and a count summary — verified by green `cargo test --test tree` + the trycmd transcript.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → tree | untrusted directory tree walked read-only |
| user argv (`path`/`--depth`) → tree | untrusted path + numeric arg |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Denial of Service | symlink loop during walk | mitigate | `follow_links(false)` on every walker (D-06); symlinks are not descended. |
| T-03-06 | Denial of Service | dotted-root pruned to zero (walkdir#142) | mitigate | Reuse `core::fs::is_hidden` verbatim (exempts depth-0); never re-implement (RESEARCH Pitfall 7). |
| T-03-07 | Tampering / Info Disclosure | `std::fs::canonicalize` UNC `\\?\` leak | mitigate | `core::fs::normalize_path` (dunce) on the target path (FOUND-06). |
| T-03-08 | Information Disclosure | ANSI leaking into piped output | mitigate | All color gated on `is_color_on()`; a dedicated `tree_piped_no_ansi` test asserts no `\x1b[` when piped (D-10). |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependency is added by this plan (it only PROMOTES an existing fn and reuses `walkdir`/`owo-colors`, already vetted in RESEARCH.md § Package Legitimacy Audit). No install-time legitimacy checkpoint required. |
</threat_model>

<verification>
- `cargo test --test tree` green (all TREE-01 rows).
- `cargo test --test cli trycmd` green (tree.trycmd transcript).
- `cargo test --lib core::output` green (promoted `human_size`).
- `cargo build` 0 errors; `cargo clippy --all-targets -- -D warnings` clean; `cargo fmt --check` clean.
- Manual sanity (PS7, MANUAL — color is terminal-dependent): `box tree ./src` (colored dir names), `box tree ./src --sizes --depth 2`, `box tree ./src | cat` (no ANSI).
</verification>

<success_criteria>
- ROADMAP Phase-3 success-criterion #2 is met: `box tree ./src` shows a box-drawing tree with colored directory names vs files, optional `--sizes`, `--depth N`, and a file/dir count summary.
- TREE-01 satisfied; `human_size` is now a shared `core::output` helper ready for du (Plan 03-03).
</success_criteria>

<output>
Create `.planning/phases/03-filesystem-power-tools/03-02-SUMMARY.md` when done.
</output>
