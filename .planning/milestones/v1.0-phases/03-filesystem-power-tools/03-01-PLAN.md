---
phase: 03-filesystem-power-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/hash/mod.rs
  - src/commands/mod.rs
  - src/core/input.rs
  - src/core/errors.rs
  - src/cli.rs
  - src/main.rs
  - Cargo.toml
  - tests/hash.rs
autonomous: true
requirements: [HASH-01]
must_haves:
  truths:
    - "User runs `box hash <file>` and gets the file's SHA-256 in `<hash>  <filename>` format (two spaces) (D-01)"
    - "User runs `box hash --algo blake3 <file>` and the algorithm switches to BLAKE3; sha512 and md5 likewise (D-02, D-03)"
    - "User pipes bytes to `box hash` (no arg or `-`) and the digest is computed over the piped bytes, labelled `-` (D-05)"
    - "User runs `box hash --verify <expected> <file>` and the process exits 0 on match, 1 on mismatch (D-04)"
    - "User runs `box hash --verify <40-hex> <file>` (unsupported length) and the process exits 2 (usage error) (D-04)"
  artifacts:
    - path: "src/commands/hash/mod.rs"
      provides: "HashArgs + Algo ValueEnum (sha256/blake3/sha512/md5, D-02) + enum-dispatch Hasher (RustCrypto + native streaming blake3, D-03) + length-autodetect verify logic (D-04) + RunCommand impl"
      contains: "impl RunCommand for HashArgs"
      min_lines: 120
    - path: "src/core/input.rs"
      provides: "the deferred --file PATH branch in resolve_bytes, ahead of the stdin branch; stdin label `-` (D-05)"
      contains: "file"
    - path: "src/core/errors.rs"
      provides: "UnsupportedHashLength typed variant for exit-2 mapping on an unsupported --verify length (D-04)"
      contains: "UnsupportedHashLength"
    - path: "tests/hash.rs"
      provides: "HASH-01 known-answer + verify exit-code integration tests"
      contains: "NO_COLOR"
  key_links:
    - from: "src/main.rs"
      to: "crate::commands::hash::HashArgs::run"
      via: "Commands::Hash(args) => args.run()"
      pattern: "Commands::Hash\\(args\\)"
    - from: "src/main.rs"
      to: "BoxError::UnsupportedHashLength"
      via: "downcast arm mapping the variant to exit 2 (D-04)"
      pattern: "UnsupportedHashLength"
    - from: "src/commands/hash/mod.rs"
      to: "blake3::Hasher::update_reader"
      via: "the native (non-traits-preview) streaming blake3 arm (D-03)"
      pattern: "update_reader"
---

<objective>
Ship `box hash` end-to-end as the phase's first vertical slice (HASH-01): a streaming, enum-dispatch checksum tool (SHA-256 default; blake3/sha512/md5 via `--algo`), `--verify` with length auto-detection and the correct 0/1/2 exit codes, and the `--file PATH` input layer this command is the first consumer of (D-05). This plan also stands up the `Hasher` infrastructure that the `dupes` slice (Plan 03-04) reuses for its BLAKE3 content-equality hash.

Purpose: HASH-01 is the only Phase-3 command with genuinely new ecosystem surface (the RustCrypto digest 0.11 + native blake3 stack). Building it first de-risks the hashing core for `dupes` and exercises the `--file` extension point that was deliberately stubbed in Phase 2.
Output: a working `box hash` command, a typed exit-2 error variant, the `--file` input branch, and Wave-0 integration tests with known-answer vectors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-filesystem-power-tools/03-CONTEXT.md
@.planning/phases/03-filesystem-power-tools/03-RESEARCH.md
@.planning/phases/03-filesystem-power-tools/03-PATTERNS.md
@.planning/phases/03-filesystem-power-tools/03-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use directly; no exploration. -->

From src/commands/mod.rs — the dispatch trait every command implements:
```rust
pub trait RunCommand {
    fn run(self) -> anyhow::Result<()>;
}
```

From src/core/input.rs — the byte resolver the `--file` branch extends (the documented extension point is at lines 24-25). Public signature must NOT change:
```rust
pub fn read_input_bytes(arg: Option<String>) -> anyhow::Result<Vec<u8>>;
fn resolve_bytes<R: Read>(arg: Option<String>, is_tty: bool, mut reader: R) -> anyhow::Result<Vec<u8>>;
// existing precedence: Some(s) if s != "-" => arg bytes; _ if is_tty => MissingInput; _ => read_to_end(stdin)
```

From src/core/errors.rs — the typed error enum `main()` downcasts for exit codes:
```rust
pub enum BoxError {
    NotImplemented { cmd: &'static str },   // -> exit 1
    MissingInput,                            // -> exit 2 (downcast in main.rs:97-103)
}
```

From src/main.rs:97-103 — the downcast arm that maps a typed variant to exit 2:
```rust
if let Some(crate::core::errors::BoxError::MissingInput) =
    e.downcast_ref::<crate::core::errors::BoxError>()
{ ExitCode::from(2) } else { ExitCode::from(1) }
```

From Cargo.toml (lines 37-43) — deps ALREADY present (do NOT re-add): `blake3 = "1.8.5"`, `sha2 = "0.11.0"`, `md-5 = "0.11.0"`, `base16ct = "1.0.0"` (currently WITHOUT `alloc`), `const-hex = "1.19.1"`.

Verified API facts (RESEARCH Pattern 1, docs.rs):
- RustCrypto (sha2/md-5): `digest::Digest` 0.11 — `new()` → `update(impl AsRef<[u8]>)` → `finalize() -> Output<Self>` (a hybrid-array that behaves like `[u8; N]`; NO `.as_slice()` needed — digest 0.11 dropped `GenericArray`).
- Hex for RustCrypto output: `base16ct::lower::encode_string(&out) -> String` (needs `alloc` feature) OR `const-hex::encode(&out)` (already on hand, no feature change) OR no-alloc `base16ct::lower::encode_str(&out, &mut buf)`.
- blake3: native stable `blake3::Hasher::new()` → `update_reader(impl Read) -> io::Result<&mut Self>` (reader BY VALUE) → `finalize() -> Hash`; `Hash::to_hex() -> ArrayString` already lowercase 64-hex. Do NOT use blake3's `digest::Digest` impl (it is behind the unstable `traits-preview` feature — D-03).
- `--verify` length map (D-04): 32→md5, 64→**sha256** (wins the sha256/blake3 tie), 128→sha512; `--algo` is the explicit override. Compare case-insensitively with plain `==` (`eq_ignore_ascii_case`) — NOT constant-time (a checksum is a public value).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 hash tests + --file input branch + exit-2 error variant</name>
  <files>tests/hash.rs, src/core/errors.rs, src/core/input.rs, src/main.rs</files>
  <read_first>
    - src/core/input.rs (the file being modified — the documented `--file` extension point is at lines 24-25; the byte resolver to extend is `resolve_bytes` at lines 93-109; mirror the inner-resolver `is_tty`/`Read` injection so the new branch is unit-testable without a terminal, lines 111-153)
    - src/core/errors.rs (the file being modified — add `UnsupportedHashLength` next to `MissingInput`, lines 23-24, following the `#[error(...)]` thiserror style)
    - src/main.rs (lines 97-103 — the downcast arm to extend so BOTH `MissingInput` and `UnsupportedHashLength` map to exit 2)
    - tests/flatten.rs (lines 14-24 — the `assert_cmd` runner + `NO_COLOR=1` analog every new `tests/<cmd>.rs` copies); tests/cli.rs (lines 43-61 — the `.failure().code(1)` / `.code(2)` exit-code assert style)
    - .planning/phases/03-filesystem-power-tools/03-VALIDATION.md (the HASH-01 rows — the exact test names to scaffold)
  </read_first>
  <action>
    Scaffold `tests/hash.rs` as the Wave-0 failing-test gate. Copy the runner shape from `tests/flatten.rs:14-24`: a `fn hash_cmd(args: &[&str]) -> assert_cmd::assert::Assert` builder using `Command::cargo_bin("box")`, `.arg("hash")`, and `cmd.env("NO_COLOR", "1")`. Write one `#[test]` per VALIDATION HASH-01 row, named exactly: `hash_default_sha256`, `hash_algo_blake3`, `hash_algo_sha512_md5`, `hash_stdin_dash_label`, `hash_verify_match_mismatch`, `hash_verify_autodetect`, `hash_verify_bad_len_exit2`. Use KNOWN-ANSWER vectors: embed the literal lowercase hex of a fixed input (the empty-file digests, or the digests of `b"box"`) as string literals so each is a true known-answer test, not a round-trip. Assert `<hash>  <filename>` format (two spaces, coreutils style — D-01). Assert exit codes explicitly via `.success()`/`.code(0)`, `.failure().code(1)` (verify mismatch), and `.code(2)` (a 40-hex sha1-length `--verify`). These tests fail to compile/run until Task 2 lands — that is the RED gate.

    Add the `UnsupportedHashLength { len: usize }` variant to `BoxError` in `src/core/errors.rs` next to `MissingInput`, with a `#[error("unsupported --verify hash length: {len} (expected 32/64/128 hex)")]` attribute. Extend the downcast arm in `src/main.rs:97-103` so it matches BOTH `BoxError::MissingInput` and `BoxError::UnsupportedHashLength` → `ExitCode::from(2)`; every other error stays exit 1 (a mismatched `--verify` is a plain `Err`/exit 1, NOT this variant — RESEARCH Pitfall 1).

    Add the `--file PATH` branch to `core::input::resolve_bytes` AHEAD of the stdin branch, per the documented extension point (D-05/D-06). Keep `read_input_bytes`'s public signature unchanged; thread the `--file` value in without reshaping it (Discretion D allows a new internal helper or an added param to the inner resolver — the constraint is that the `-` sentinel and `MissingInput`→exit-2 semantics stay inherited and the inner resolver remains `is_tty`/`Read`-injectable for unit tests). For a large file, expose a streaming path (a `File`/`impl Read` handle), NOT a whole-file `read_to_end` `Vec` — `hash` must stream (no whole-file buffering, RESEARCH anti-pattern). Add a unit test in `core::input`'s `#[cfg(test)] mod tests` proving the `--file` branch reads named-file bytes ahead of stdin and that a missing/unreadable `--file` path surfaces a clean `anyhow` error (FOUND-05), not a panic.
  </action>
  <verify>
    <automated>cargo build 2>&1 | grep -v '^#' | grep -c 'error\[' | grep -qx 0 && cargo test --lib core::input 2>&1 | grep -q 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/hash.rs` exists and contains the 7 named tests from the VALIDATION HASH-01 rows, each setting `NO_COLOR=1`.
    - `src/core/errors.rs` contains `UnsupportedHashLength` with a `#[error(...)]` message; `src/main.rs` downcast arm maps it (and `MissingInput`) to exit 2.
    - `src/core/input.rs` contains a `--file`-handling branch placed ahead of the stdin branch; its inner resolver stays `is_tty`/`Read`-injectable; a `core::input` unit test for the `--file` branch passes (`cargo test --lib core::input` shows `test result: ok`).
    - The crate still compiles (`cargo build` reports 0 `error[` lines) with `tests/hash.rs` present (the test bodies reference `box hash`, which exits via the stub until Task 2; that is allowed — they are not yet asserted-green).
  </acceptance_criteria>
  <done>The Wave-0 hash tests, the exit-2 error variant + its `main.rs` mapping, and the `--file` input branch all exist and compile; the `core::input` `--file` unit test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hash command — streaming Hasher enum + --verify + wiring</name>
  <files>src/commands/hash/mod.rs, src/commands/mod.rs, src/cli.rs, src/main.rs, Cargo.toml</files>
  <read_first>
    - src/commands/base64/mod.rs (the `core::input` consumer + `clap::Args` struct shape + `#[derive(Debug, Args)]` + the `RunCommand` shell + the co-located `#[cfg(test)] mod tests` known-answer style — the closest analog; `hash` streams instead of buffering but the input-acquisition precedence and struct shape are base64's)
    - src/commands/flatten/mod.rs (lines 29-33 — the `use crate::commands::RunCommand;` + error-context style; the `human_size`/`.context(...)` conventions)
    - src/cli.rs (lines 35-36 — the live `Flatten(FlattenArgs)` variant to model `Hash(HashArgs)` on; line 57 — the unit `Hash,` variant to replace)
    - src/main.rs (line 61 — the live `Commands::Flatten(args) => args.run()` arm to model on; line 71 — the `Commands::Hash => Err(not_implemented("hash"))` arm to remove; line 22 `use ...stub::not_implemented;` STAYS — other stubs still use it)
    - src/commands/mod.rs (lines 7-17 — register `pub mod hash;` alphabetically alongside the existing modules)
    - .planning/phases/03-filesystem-power-tools/03-RESEARCH.md (Pattern 1 streaming enum-dispatch hasher + Pattern 2 `--verify` length auto-detection — the VERIFIED signatures; this is the net-new capability with no in-repo analog)
    - .planning/phases/03-filesystem-power-tools/03-PATTERNS.md (the `src/commands/hash/mod.rs` section — analogs + the typed exit-2 mapping)
    - Cargo.toml (lines 37-43 — deps already present; resolve the `base16ct` `alloc` open item here)
  </read_first>
  <behavior>
    - Test (hash_default_sha256): `box hash <file-of-known-bytes>` prints `<known-sha256-hex>  <filename>` (two spaces) and exits 0.
    - Test (hash_algo_blake3 / hash_algo_sha512_md5): `--algo blake3|sha512|md5` switches to the matching known-answer hex.
    - Test (hash_stdin_dash_label): piping bytes with no path arg (or `-`) hashes the piped bytes and labels the row `-`.
    - Test (hash_verify_match_mismatch): `--verify <correct-hex> <file>` exits 0; `--verify <wrong-hex> <file>` exits 1.
    - Test (hash_verify_autodetect): a 64-hex `--verify` with no `--algo` is treated as sha256 (wins the 64-tie over blake3); `--algo blake3 --verify <64hex>` disambiguates to blake3; 32→md5, 128→sha512.
    - Test (hash_verify_bad_len_exit2): a 40-hex `--verify` (sha1 length, unsupported) exits 2 via `UnsupportedHashLength`.
  </behavior>
  <action>
    Create `src/commands/hash/mod.rs` and register `pub mod hash;` in `src/commands/mod.rs`. Define `HashArgs` with `#[derive(Debug, Args)]`: an `#[arg(long, value_enum, default_value_t = Algo::Sha256)] algo: Algo` field (a `#[derive(Clone, Copy, ValueEnum)]` `Algo` enum with `Sha256`/`Blake3`/`Sha512`/`Md5` — Discretion D fixes the value spellings to `sha256`/`blake3`/`sha512`/`md5`), an `#[arg(long)] verify: Option<String>`, and the file/`--file` input field routed through `core::input` (Discretion D: positional path vs a `--file` flag — pick one and route it through the Task-1 `--file` branch so `-`/stdin/TTY precedence is inherited; stdin label is `"-"`, D-05).

    Implement the `Hasher` as enum-dispatch (D-03), NOT `dyn Digest`: a generic helper `fn hash_rustcrypto<D: digest::Digest, R: Read>(hasher: D, reader: R) -> anyhow::Result<String>` that loops `read` into a 64 KiB buffer → `hasher.update(&buf[..n])` → `finalize()` → hex-encode the hybrid-array output (resolve the open item: enable `base16ct`'s `alloc` feature for `encode_string`, OR use the already-present `const-hex::encode`, OR no-alloc `base16ct::lower::encode_str` into a stack buffer — pick one, do not add a duplicate dep line). The blake3 arm is separate: `blake3::Hasher::new().update_reader(reader)?.finalize().to_hex().to_string()` on the STABLE native `Hasher` (never `traits-preview`). Sha256/Sha512/Md5 dispatch into `hash_rustcrypto`; Blake3 into the blake3 arm. Stream every algorithm — no whole-file buffering.

    Implement `--verify` per Pattern 2 (D-04): if `--algo` is explicitly set, use it; else `algo_from_len(expected.trim().len())` mapping 32→Md5, 64→Sha256, 128→Sha512, else return `BoxError::UnsupportedHashLength { len }.into()` (→ exit 2). Compute the digest, compare `computed.eq_ignore_ascii_case(expected.trim())` (plain, not constant-time); on match print nothing extra and `Ok(())` (exit 0), on mismatch surface a clear stderr message and exit 1 (a plain `bail!`/`Err`, NOT the typed exit-2 variant). When NOT verifying, print `<hash>  <label>` (two spaces) to stdout.

    Swap `src/cli.rs` line 57 `Hash,` → `Hash(crate::commands::hash::HashArgs)` (model on `Flatten` at line 36), keeping its `/// Compute and verify file hashes` doc comment. Remove the `Commands::Hash => Err(not_implemented("hash"))` arm in `src/main.rs:71` and replace it with `Commands::Hash(args) => args.run()` (model on line 61). Leave the other four phase stubs (tree/du/dupes/bulk-rename) untouched — they are later waves. Co-locate `#[cfg(test)] mod tests` in `mod.rs` with known-answer unit tests for the `Hasher` arms and `algo_from_len` (mirroring base64's co-located test style). Run `cargo fmt` and ensure `cargo clippy --all-targets -- -D warnings` is clean.
  </action>
  <verify>
    <automated>cargo test --test hash 2>&1 | grep -q 'test result: ok' && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `src/commands/hash/mod.rs` contains `impl RunCommand for HashArgs`, an `Algo` `ValueEnum` with the four locked algorithms, and a `Hasher` enum-dispatch (a `hash_rustcrypto` generic helper + a separate blake3 native-`Hasher` arm; NO `dyn Digest`, NO `traits-preview`).
    - `cargo test --test hash` is fully green: all 7 named tests pass, including `<hash>  <filename>` two-space format, `--algo` switching to known-answer vectors, stdin labelled `-`, `--verify` exit 0 (match) / 1 (mismatch), and a 40-hex `--verify` exiting 2.
    - `src/cli.rs` `Hash` variant carries `HashArgs`; `src/main.rs` dispatches `Commands::Hash(args) => args.run()` with no `not_implemented("hash")` arm remaining; `not_implemented` import retained for the other stubs.
    - `cargo clippy --all-targets -- -D warnings` is clean and `cargo fmt --check` passes.
  </acceptance_criteria>
  <done>`box hash` is a live command: SHA-256 default, `--algo blake3|sha512|md5`, stdin/`-`/`--file` input, and `--verify` with the 0/1/2 exit contract — all verified by a green `cargo test --test hash`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user argv / `--verify` value → hash | untrusted string whose hex length selects an algorithm |
| filesystem / stdin → hash | untrusted file or piped bytes streamed into a hasher |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure / V6 | `--verify` compare | accept | A file checksum is a PUBLIC integrity value, not a secret — plain `eq_ignore_ascii_case` is correct; constant-time compare would be cargo-culting (D-04). MD5 offered for legacy interop only, never presented as a security guarantee. |
| T-03-02 | Denial of Service / V5 | bad `--verify` length / unreadable `--file` / non-UTF-8 path | mitigate | Unsupported length → typed `UnsupportedHashLength` → clean stderr + exit 2 (never a panic); unreadable file → `.context(...)`-wrapped `anyhow` error → exit 1 (FOUND-05). Unit + integration tests assert both. |
| T-03-03 | Denial of Service | multi-GB file buffered whole | mitigate | Stream via `update`/`update_reader` (D-03); no `read_to_end` of the hashed payload. Code-review + the streaming `--file` handle enforce it. |
| T-03-04 | Tampering | path normalization of the input path | mitigate | Input path resolved through `core::fs`/`core::input` conventions (no `std::fs::canonicalize` UNC leak); reading is non-mutating. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Deps already vetted in RESEARCH.md § Package Legitimacy Audit (all six `[OK]`, first-party RustCrypto/BLAKE3-team); no NEW dep is added by this plan (only the `base16ct` `alloc` feature flag is toggled), so no install-time legitimacy checkpoint is required. |
</threat_model>

<verification>
- `cargo test --test hash` green (all 7 HASH-01 rows).
- `cargo test --lib core::input` green (the `--file` branch unit test).
- `cargo build` 0 errors; `cargo clippy --all-targets -- -D warnings` clean; `cargo fmt --check` clean.
- Manual sanity (optional, PS7): `box hash Cargo.toml`, `box hash --algo blake3 Cargo.toml`, `echo hi | box hash`, `box hash --verify <hex> Cargo.toml`.
</verification>

<success_criteria>
- ROADMAP Phase-3 success-criterion #1 is met: `box hash somefile` gives SHA-256 in `HASH  filename` format; `--algo blake3` switches; `box hash --verify EXPECTEDHASH somefile` exits 0 on match and 1 on mismatch (and 2 on an unsupported length).
- HASH-01 requirement satisfied and the `Hasher`/blake3 infra is ready for `dupes` (Plan 03-04) to reuse.
</success_criteria>

<output>
Create `.planning/phases/03-filesystem-power-tools/03-01-SUMMARY.md` when done.
</output>
