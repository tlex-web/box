---
phase: 05-windows-platform-integration
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]   # shares cli.rs/main.rs/commands/mod.rs/Cargo.toml registry — serialize after pomodoro; LAST plan
files_modified:
  - Cargo.toml
  - src/cli.rs
  - src/main.rs
  - src/commands/mod.rs
  - src/commands/weather/mod.rs
  - tests/weather.rs
  - tests/fixtures/weather/forecast_metric.json
  - tests/fixtures/weather/forecast_imperial.json
  - tests/fixtures/weather/geocode_hit.json
  - tests/fixtures/weather/geocode_no_match.json
autonomous: true   # all auto-testable (offline/non-2xx exit codes + bad --units exit 2); a live-fetch human-verify is OPTIONAL, run as part of phase UAT
requirements: [WTHR-01]
user_setup: []

must_haves:
  truths:
    - "User runs `box weather \"London\"` and sees current temperature, conditions, wind, and humidity from Open-Meteo (keyless)"
    - "A `lat,lon` positional (e.g. `51.5,-0.13`) is used directly; any other string is geocoded as a city name (D-12)"
    - "`--units imperial` switches to Fahrenheit (server-side params); the default is metric °C/km/h (D-10/D-11)"
    - "Offline / DNS / connect failure → `error: could not reach weather service (offline?)` to stderr + exit 1"
    - "Non-2xx HTTP → `error: weather service returned {status}` + exit 1; a bad `--units` value → clap exit 2"
    - "Zero geocoding results (absent `results` key) → `error: no location found for \"{name}\"` + exit 1"
  artifacts:
    - path: "src/commands/weather/mod.rs"
      provides: "WeatherArgs + Units ValueEnum + parse_lat_lon/wmo_to_str/build_forecast_url + ureq fetch error-split + serde Deserialize structs; established slice (variant swap, arm removal incl. final stub-import deletion, exit 0/1/2, assert_cmd via cargo test --bin box) (D-00/D-10..D-13)"
      contains: "wmo_to_str"
      min_lines: 110
    - path: "tests/weather.rs"
      provides: "WTHR-01 integration: offline/non-2xx exit-1 + bad --units exit-2"
      contains: "weather"
    - path: "tests/fixtures/weather/forecast_metric.json"
      provides: "Captured live Open-Meteo forecast (metric) for the Deserialize unit test"
      contains: "current_units"
  key_links:
    - from: "src/cli.rs"
      to: "crate::commands::weather::WeatherArgs"
      via: "Weather(WeatherArgs) variant swap (was unit `Weather,`)"
      pattern: "Weather\\(crate::commands::weather::WeatherArgs\\)"
    - from: "src/main.rs"
      to: "args.run()"
      via: "Commands::Weather(args) => args.run() (was Err(not_implemented(\"weather\"))); LAST stub gone → delete the now-dead not_implemented import"
      pattern: "Commands::Weather\\(args\\)"
    - from: "src/commands/weather/mod.rs"
      to: "ureq::get(...).call()"
      via: "blocking HTTPS GET to Open-Meteo; non-2xx = Err(StatusCode) match arm (Pitfall WTHR-1)"
      pattern: "ureq::get"
    - from: "src/commands/weather/mod.rs"
      to: "serde_json::from_reader(resp.into_body().into_reader())"
      via: "Deserialize via from_reader (no ureq json feature, D-13)"
      pattern: "from_reader"
---

<objective>
Ship `box weather` as a complete vertical slice (WTHR-01): swap the unit `Weather` variant for a real `WeatherArgs { location, units }` (with a `Units` clap `ValueEnum`), delete the `not_implemented("weather")` arm, register `pub mod weather;`, add `ureq` + `serde` derive, and implement the only network command. Parse-shape disambiguation treats the positional as `lat,lon` only if it matches the range-checked shape, else geocodes the city name via Open-Meteo (keyless). The forecast GET reads the AUTHORITATIVE unit label from `current_units` (never hardcode °C/mph — D-11) and maps `weather_code` via a WMO `match → &'static str`. The error split is the load-bearing part: ureq 3.x returns `Err(StatusCode)` on non-2xx by default, so non-2xx is a `match` arm, never a post-success `resp.status()` check (Pitfall WTHR-1).

Purpose: The last and most complex Phase-5 command — and the final command in the v1 milestone (all 23 tools). It encodes the three live-API landmines (absent `results` on no-match, `"mp/h"` label ≠ request param, ureq non-2xx-as-Err). Because this is the LAST stub, removing its arm makes the `stub::not_implemented` import dead — delete it.

Output: A working `box weather <city|lat,lon> [--units imperial]` + `tests/weather.rs` + in-module unit tests (parse_lat_lon / wmo_to_str / build_forecast_url / Deserialize-from-fixture) + 4 checked-in Open-Meteo JSON fixtures + Cargo.toml `ureq`/`serde` deps. ALL Phase-5 stubs gone → phase feature-complete; the dead `not_implemented` import removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-windows-platform-integration/05-CONTEXT.md
@.planning/phases/05-windows-platform-integration/05-RESEARCH.md
@.planning/phases/05-windows-platform-integration/05-PATTERNS.md
@.planning/phases/05-windows-platform-integration/05-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Use directly. -->

From src/commands/mod.rs:
```rust
pub trait RunCommand { fn run(self) -> anyhow::Result<()>; }
```

From src/commands/json/mod.rs — the serde_json consumer + thin-orchestrator + `bail!`→exit-1 precedent (json.rs:60). From src/commands/color/mod.rs:53-58 — the `is_color_on()`-gated `.truecolor`/`.cyan()` precedent (for any OPTIONAL gated conditions/temperature color — discretion).

From src/cli.rs:85-92 — swap the unit `Weather,` (line 92). The `ValueEnum` flag form for `--units`.
From src/main.rs:95-98 — delete the `Commands::Weather => Err(not_implemented("weather"))` arm; mirror line 80. ⚠️ This is the LAST stub: after the swap, `use crate::commands::stub::not_implemented;` (main.rs:37) becomes DEAD — delete it (the `clippy -D warnings` gate will flag the unused import otherwise). Whether to also delete `src/commands/stub.rs` + its `pub mod stub;` (commands/mod.rs:24) is discretion — but if left, ensure it does not trip the dead-code gate (it has its own callers/tests or `#[allow]`; verify).
From src/main.rs:115-121 — the exit-2 mapping. weather adds NO `BoxError` variant: every weather runtime error is a plain `anyhow::bail!` → exit 1. Exit 2 for a bad `--units` value is raised by clap's `ValueEnum`, NOT by the command.

ureq 3.3.0 API (VERIFIED docs.rs for the pinned version):
- `ureq::get(url).call() -> Result<Response<Body>, ureq::Error>`.
- ⚠️ Non-2xx → `Err(ureq::Error::StatusCode(u16))` by DEFAULT (`http_status_as_error()` true). The `Ok` branch is ALWAYS 2xx. So non-2xx is a `match` arm, NOT a `resp.status()` check (Pitfall WTHR-1).
- Offline/DNS family: `ureq::Error::{Io(_), ConnectionFailed, HostNotFound}` (+ a catch-all `_`).
- `Response::into_body() -> Body`; `Body::into_reader(self) -> BodyReader<'static>` — pass to `serde_json::from_reader(resp.into_body().into_reader())` (D-13; do NOT enable ureq's `json` feature).

regex 1.12.4 is ALREADY a dep (Cargo.toml:41) → `parse_lat_lon` needs no new crate.
serde_json 1.0.150 is ALREADY a dep (Cargo.toml:49) → only `serde`'s `derive` macro is new.

Open-Meteo endpoints (keyless):
- Geocode: `https://geocoding-api.open-meteo.com/v1/search?name={url-encoded}&count=1&language=en&format=json` → `results[0].{latitude, longitude, name, admin1, country}`. ⚠️ no-match OMITS the `results` key entirely (Pitfall WTHR-2) → `#[serde(default)]`.
- Forecast: `https://api.open-meteo.com/v1/forecast?latitude={lat}&longitude={lon}&current=temperature_2m,relative_humidity_2m,weather_code,wind_speed_10m` (+ `&temperature_unit=fahrenheit&wind_speed_unit=mph` ONLY on `--units imperial`, D-11).

Deserialize structs (from the LIVE shapes, RESEARCH:454-466 — encode the three landmines):
```rust
#[derive(Deserialize)] struct GeoResp { #[serde(default)] results: Vec<GeoHit> }
#[derive(Deserialize)] struct GeoHit { latitude: f64, longitude: f64, name: String,
                                       #[serde(default)] admin1: Option<String>, #[serde(default)] country: Option<String> }
#[derive(Deserialize)] struct ForecastResp { current: Current, current_units: CurrentUnits }
#[derive(Deserialize)] struct Current { temperature_2m: f64, relative_humidity_2m: f64, weather_code: u32, wind_speed_10m: f64 }
#[derive(Deserialize)] struct CurrentUnits { temperature_2m: String, wind_speed_10m: String }  // "°C"/"°F", "km/h"/"mp/h"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 scaffold — pure helpers + Deserialize structs + JSON fixtures + tests/weather.rs (RED for integration, GREEN for pure)</name>
  <files>src/commands/weather/mod.rs, src/commands/mod.rs, tests/weather.rs, tests/fixtures/weather/forecast_metric.json, tests/fixtures/weather/forecast_imperial.json, tests/fixtures/weather/geocode_hit.json, tests/fixtures/weather/geocode_no_match.json</files>
  <read_first>
    - src/commands/json/mod.rs (the serde_json consumer + thin orchestrator + `bail!`→exit-1 shape)
    - src/commands/color/mod.rs (the pure parse-helper seam color.rs:69-167 + the gated-color precedent color.rs:53-58)
    - tests/color.rs (the exit-1 clean-error shape, color.rs:67-74) + tests/json.rs (the `bail!`→exit-1 stderr predicate) + tests/cli.rs (the bad-flag → clap exit-2 shape, cli.rs:42-50)
    - src/commands/mod.rs (add `pub mod weather;` in alpha order — after `uuid`, at the end)
    - Cargo.toml (line 41 `regex` already present; line 49 `serde_json` already present)
    - .planning/phases/05-windows-platform-integration/05-RESEARCH.md § Pattern 2 (`wmo_to_str` 287-299, `parse_lat_lon` 301-307), § Code Examples "weather" (452-492 — the structs + the live JSON samples to save as fixtures), § Pitfalls WTHR-1/2/3 (382-398)
  </read_first>
  <action>
    Add `pub mod weather;` to `src/commands/mod.rs` in alpha order (after `uuid`, at the end). Create `src/commands/weather/mod.rs` with the `//!` doc (WTHR-01 + D-10..D-13), imports (`anyhow::Context`, `clap::{Args, ValueEnum}`, `serde::Deserialize`, `crate::commands::RunCommand`, and `crate::core::output::is_color_on` if a gated color path is used), the `Units` enum (`#[derive(Debug, Clone, Copy, ValueEnum)] pub enum Units { Metric, Imperial }`), the `WeatherArgs` struct (`pub location: String`, `#[arg(long, value_enum, default_value_t = Units::Metric)] pub units: Units`), the five `#[derive(Deserialize)]` structs (exact shapes from the interfaces block — encode `#[serde(default)] results` for Pitfall WTHR-2), and the THREE pure helpers:
    - `wmo_to_str(code: u32) -> &'static str` — the WMO `match` (exact bands from RESEARCH:287-299: 0→"Clear sky", 1|2|3→"Partly cloudy", 45|48→"Fog", 51|53|55→"Drizzle", 61|63|65→"Rain", 71|73|75→"Snow", 80|81|82→"Rain showers", 95→"Thunderstorm", `_`→"Unknown"; exact labels are discretion, the `_` arm is mandated by D-13).
    - `parse_lat_lon(s: &str) -> Option<(f64, f64)>` — regex `^\s*(-?\d+(?:\.\d+)?)\s*,\s*(-?\d+(?:\.\d+)?)\s*$` over `s.trim()`, parse both, return `Some` ONLY if `lat ∈ [-90,90]` and `lon ∈ [-180,180]` (RESEARCH:301-307; D-12).
    - `build_forecast_url(lat: f64, lon: f64, units: Units) -> String` — base forecast URL with `latitude`/`longitude`/`current=...`; append `&temperature_unit=fahrenheit&wind_speed_unit=mph` ONLY for `Units::Imperial` (D-11/D-13). Pure → the imperial branch is unit-testable without a network call.
    Give `WeatherArgs` a placeholder `RunCommand` impl that `anyhow::bail!("not implemented")` so the crate compiles (the real orchestration lands in Task 2; the network integration tests are RED until then).
    Save the FOUR captured live JSON samples (from RESEARCH § Code Examples lines 484-492) as checked-in fixtures under `tests/fixtures/weather/`: `forecast_metric.json` (the metric forecast with `current_units.wind_speed_10m == "km/h"`), `forecast_imperial.json` (an imperial variant where `current_units.wind_speed_10m == "mp/h"` — note the label ≠ the request param `mph`, Pitfall WTHR-3; if a live imperial sample is not captured, build one with the `"mp/h"` label per the research note), `geocode_hit.json` (the London hit), `geocode_no_match.json` (`{"generationtime_ms":0.458}` — NO `results` key, Pitfall WTHR-2).
    Write the in-module `#[cfg(test)] mod tests`: `wmo_to_str` for each mapped band + `wmo_to_str(9999)=="Unknown"`; `parse_lat_lon("51.5,-0.13")==Some`, `parse_lat_lon("London")==None`, `parse_lat_lon("91,0")==None` (out of range), `parse_lat_lon("1,2,3")==None`; `build_forecast_url` metric does NOT contain `temperature_unit=fahrenheit`, imperial contains BOTH `temperature_unit=fahrenheit` AND `wind_speed_unit=mph`; and Deserialize tests using `serde_json::from_str` over `include_str!("../../../tests/fixtures/weather/...")` (or the appropriate relative path) — the metric forecast parses into `ForecastResp` (assert `current.weather_code` maps via `wmo_to_str`, `current_units.wind_speed_10m == "km/h"`), `geocode_hit.json` parses with a non-empty `results`, and `geocode_no_match.json` parses into `GeoResp` with an EMPTY `results` (the `#[serde(default)]` proves no "missing field" error — Pitfall WTHR-2). These pure tests should pass immediately (GREEN).
    Write `tests/weather.rs` (analog: color.rs exit-1 shape + cli.rs bad-flag exit-2): a `box weather "London" --units bogus` → clap exit 2 (the `ValueEnum` rejects the value, cli.rs:42-50 shape); an offline/unreachable path → exit 1 with stderr containing "offline" or "service" and empty stdout. Make the offline test deterministic: either a `BOX_WEATHER_BASE_URL`-style test seam pointing at a bogus host, OR `#[ignore]` a true live-network test and assert the offline-shape via a guaranteed-unreachable host (document the chosen approach). These behavior tests are RED until Task 2.
    Run `cargo test --bin box weather` (pure + Deserialize tests GREEN). Commit: `test(05-04): add weather parse/wmo/url + Deserialize-from-fixture unit tests + offline/bad-units integration (pure green, behavior red)`.
  </action>
  <verify>
    <automated>cargo build --bin box 2>&1 | tail -5 && cargo test --bin box weather 2>&1 | tail -18</automated>
  </verify>
  <acceptance_criteria>
    - `src/commands/weather/mod.rs` exists with `Units` ValueEnum, `WeatherArgs { location, units }`, the five Deserialize structs (incl. `#[serde(default)] results`), and the three pure helpers `wmo_to_str` / `parse_lat_lon` / `build_forecast_url`.
    - `pub mod weather;` present in alpha order (end of list).
    - Four fixtures exist under `tests/fixtures/weather/` (forecast metric + imperial, geocode hit + no-match).
    - `#[cfg(test)]` covers: `wmo_to_str` bands + `_`→"Unknown"; `parse_lat_lon` Some/None/out-of-range/3-component; `build_forecast_url` metric-omits / imperial-includes both unit params; Deserialize of all relevant fixtures incl. the no-match-empty-results proof.
    - `tests/weather.rs` exists with the bad-`--units`→exit-2 and offline→exit-1 tests.
    - Crate COMPILES; `cargo test --bin box weather` shows the pure + Deserialize tests GREEN.
  </acceptance_criteria>
  <done>Scaffold + fixtures committed; pure + Deserialize tests green; network behavior RED; crate compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement weather orchestration — Cargo deps + ureq fetch error-split + geocode/forecast + format + wiring + delete dead stub import (GREEN)</name>
  <files>Cargo.toml, src/commands/weather/mod.rs, src/cli.rs, src/main.rs</files>
  <read_first>
    - src/commands/weather/mod.rs (the scaffold from Task 1 — fill in `run` + the `fetch`/`geocode`/`fetch_forecast` helpers + the format block)
    - src/commands/json/mod.rs (serde_json + `bail!`→exit-1) + src/commands/color/mod.rs (gated color, color.rs:53-58)
    - src/cli.rs (lines 85-92; mirror line 44 for the Weather swap)
    - src/main.rs (lines 95-98 → mirror line 80; ⚠️ delete the now-dead `use crate::commands::stub::not_implemented;` at main.rs:37 — this is the LAST stub)
    - Cargo.toml (the `[dependencies]` block; mirror the feature-trim comment style)
    - .planning/phases/05-windows-platform-integration/05-RESEARCH.md § Code Examples "weather: geocode + forecast" (452-482), § API Verification ureq table (200-208), § Pitfalls WTHR-1/2/3
    - .planning/phases/05-windows-platform-integration/05-PATTERNS.md § "src/commands/weather/mod.rs" (the run orchestration 448-461, the error split 463-473, the Deserialize structs 475-490)
  </read_first>
  <action>
    Add to `Cargo.toml`: `ureq = "3.3.0"` (comment: blocking HTTPS, default `["rustls","gzip"]` = no OpenSSL/DLL; do NOT enable the `json` feature — deserialize via `serde_json::from_reader`, D-13) and `serde = { version = "1", features = ["derive"] }` (comment: only the derive macro is new; `serde_json` is already a dep). Build to confirm both resolve.
    Implement the `fetch` helper (the SINGLE most important weather pattern — RESEARCH:469-477 / Pitfall WTHR-1): a generic `fn fetch<T: serde::de::DeserializeOwned>(url: &str) -> anyhow::Result<T>` that `match`es `ureq::get(url).call()`: `Ok(resp) => serde_json::from_reader(resp.into_body().into_reader()).context("parse weather response")` (D-13 `into_reader`, no `json` feature); `Err(ureq::Error::StatusCode(code)) => anyhow::bail!("weather service returned {code}")` (non-2xx → exit 1, Pitfall WTHR-1); `Err(_) => anyhow::bail!("could not reach weather service (offline?)")` (offline/DNS/connect → exit 1). If a `BOX_WEATHER_BASE_URL` test seam was introduced in Task 1, honor it here so the offline integration test is deterministic.
    Implement `WeatherArgs::run(self)`:
    - Parse-shape disambiguation (D-12): `match parse_lat_lon(&self.location) { Some((lat, lon)) => (lat, lon, format!("{lat:.4},{lon:.4}")), None => geocode(&self.location)? }` where `geocode` GETs the geocoding URL (URL-encode the city name — see below), `fetch`es a `GeoResp`, takes `results.into_iter().next()` and on `None` → `anyhow::bail!("no location found for \"{}\"", self.location)` (exit 1, Pitfall WTHR-2 — the absent `results` deserializes to empty via `#[serde(default)]`), returning `(latitude, longitude, formatted-label-from-name/admin1/country)`.
    - URL-encode the city `name` in the geocode query (D-12). ureq's transitive `percent-encoding` is NOT re-exported; either build the query with a tiny manual encode of the name (percent-encode reserved chars) or document the chosen approach (Pitfall WTHR-2). Do NOT add a new crate solely for this if a minimal inline encode suffices.
    - `eprintln!("Resolved \"{}\" → {label} ({lat:.2}, {lon:.2})", self.location);` (echo resolved location → stderr, D-12 — a wrong match is visible).
    - Forecast: `let url = build_forecast_url(lat, lon, self.units); let forecast: ForecastResp = fetch(&url)?;` (+ unit params on imperial via `build_forecast_url`, D-11).
    - Format → stdout (D-13): read the AUTHORITATIVE unit labels from `forecast.current_units.temperature_2m` / `.wind_speed_10m` (NEVER hardcode °C/mph — D-11; the imperial wind label is `"mp/h"` ≠ the request param `mph`, Pitfall WTHR-3); conditions via `wmo_to_str(forecast.current.weather_code)`; humidity is `relative_humidity_2m` with a `%`. Emit a clean labeled block (temperature / conditions / wind / humidity — exact layout is discretion; recommend a 4-line block like `color`). Any conditions/temperature color is OPTIONAL and gated on `is_color_on()` (discretion) — if used, follow the color.rs:53-58 gated pattern so piped output is byte-identical minus ANSI.
    Swap the cli.rs `Weather,` variant (line 92) for `Weather(crate::commands::weather::WeatherArgs)` (keep the `///` doc). Swap the main.rs arm for `Commands::Weather(args) => args.run()`. ⚠️ DELETE the now-dead `use crate::commands::stub::not_implemented;` import (main.rs:37) — this is the LAST stub. Verify the build does not now flag `src/commands/stub.rs`/`pub mod stub;` as dead (if it does, either delete `stub.rs` + its `pub mod stub;` at commands/mod.rs:24, or confirm its existing tests keep it live — discretion, but `clippy -D warnings` MUST pass).
    Re-run the suite — pure + Deserialize + the offline-exit-1 + bad-`--units`-exit-2 integration tests MUST pass (GREEN). Run `cargo clippy -- -D warnings` + `cargo fmt --check`. Commit: `feat(05-04): implement box weather (WTHR-01) — Open-Meteo geocode+forecast, ureq error-split, server-side units; remove dead stub import`.
  </action>
  <verify>
    <automated>cargo test --bin box weather 2>&1 | tail -8 && cargo test --test weather 2>&1 | tail -8 && cargo test 2>&1 | tail -6 && cargo clippy -- -D warnings 2>&1 | tail -3 && cargo fmt --check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` contains `ureq = "3.3.0"` (no `json` feature) and `serde = { version = "1", features = ["derive"] }`.
    - `fetch` matches `ureq::get().call()`: `Ok` → `serde_json::from_reader(resp.into_body().into_reader())`; `Err(Error::StatusCode(code))` → `bail!("weather service returned {code}")`; `Err(_)` → `bail!("could not reach weather service (offline?)")`. NO post-success `resp.status()` check (Pitfall WTHR-1).
    - `run` does parse-shape disambiguation (`parse_lat_lon` → else geocode), echoes the resolved location to stderr (D-12), and on zero geocode results bails `no location found for "{name}"` (exit 1).
    - The forecast output reads unit labels from `current_units` (no hardcoded °C/mph) and maps `weather_code` via `wmo_to_str`; the city name is URL-encoded in the geocode query.
    - `src/cli.rs` has `Weather(crate::commands::weather::WeatherArgs)`; `src/main.rs` has `Commands::Weather(args) => args.run()` AND the `use crate::commands::stub::not_implemented;` import is DELETED.
    - `cargo test --bin box weather` (pure + Deserialize) + `cargo test --test weather` (offline→exit-1, bad-`--units`→exit-2) green; `cargo test` full suite green (no regression).
    - `cargo clippy -- -D warnings` (no dead-import / dead-stub warning) + `cargo fmt --check` clean.
  </acceptance_criteria>
  <done>`box weather` live (city + lat,lon, metric/imperial, graceful offline); all weather tests green; dead stub import removed; clippy + fmt clean; ALL Phase-5 stubs gone — milestone feature-complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → weather (location/units) | City name / lat,lon string + `--units` from clap |
| weather → Open-Meteo (HTTPS) | Two outbound keyless GETs (geocode → forecast); the response is untrusted server data |
| Open-Meteo → weather (JSON) | Deserialized into strict structs; mapped + printed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-WTHR-INJ | Tampering / Input Validation | city `name` in the geocode query string | mitigate | URL-encode the city name before building the query (D-12) so a name with `&`/`=`/spaces cannot inject extra query params. lat/lon is shape+range validated by `parse_lat_lon` (D-12); `--units` is a clap `ValueEnum` (invalid → exit 2). ASVS V5. |
| T-05-WTHR-MITM | Information / Tampering | weather fetched over the network | mitigate | HTTPS-only via ureq's rustls default (no plaintext, no OpenSSL/DLL); cert/connect failures surface as a clean exit-1 error (D-13). ASVS V9. |
| T-05-WTHR-DoS | Denial of Service | hostile/malformed JSON shape → panic | mitigate | Strict `#[derive(Deserialize)]` (unknown fields ignored by default; `#[serde(default)]` on `results` tolerates the absent key — Pitfall WTHR-2); the `_` WMO arm future-proofs unknown codes; any parse failure is `.context(...)?` → exit 1, never `.unwrap()`/panic (FOUND-05). No `eval`/no shell-out from the response. |
| T-05-WTHR-ERR | DoS / Information (confusing UX) | offline mislabeled as a status error, or a dead non-2xx branch | mitigate | ureq 3.x `Error::StatusCode` vs `Io/ConnectionFailed/HostNotFound` matched EXPLICITLY (Pitfall WTHR-1); offline is an expected, graceful exit-1 path (D-13). No `resp.status()` post-success check (it would be dead code). Auto-tested via the offline integration test. |
| T-05-WTHR-LEAK | Information Disclosure | no API key, no secrets | accept | Open-Meteo is keyless (D-10/D-13) — no credential to mishandle, no env var, no persistence. Nothing to leak. |
| T-05-WTHR-SC | Tampering | `ureq` 3.3.0 + `serde` derive supply chain | accept | `[VERIFIED: crates.io]` — ureq slopcheck `status: OK` (0 flags), algesten canonical repo, current latest stable; serde is dtolnay-canonical (>2B downloads), already in the tree transitively (RESEARCH § Package Legitimacy Audit). No blocking legitimacy checkpoint required. |

No auth, no session, no access control, no persistence (ASVS V2/V3/V4 N/A — RESEARCH § Security Domain). The genuine controls (URL-encode, HTTPS-only, no-panic Deserialize, explicit error classification) are all encoded above.
</threat_model>

<verification>
- `cargo test --bin box weather` — `wmo_to_str` bands + `_`; `parse_lat_lon` Some/None/range/3-component; `build_forecast_url` metric-vs-imperial; Deserialize of all four fixtures incl. the no-match-empty-results proof.
- `cargo test --test weather` — bad-`--units`→exit-2; offline/unreachable→exit-1 with "offline"/"service" stderr + empty stdout.
- `cargo test` — full suite green (no regression in the prior box-bin unit tests + integration).
- `cargo clippy -- -D warnings` (no dead `not_implemented` import) + `cargo fmt --check` — clean.
- (Optional / phase-UAT) live fetch: `box weather "London"` and `box weather "London" --units imperial` in PS7 show temp/conditions/wind/humidity with the authoritative unit label (`mp/h` on imperial) and the resolved-location echo on stderr.
</verification>

<success_criteria>
- `box weather "London"` shows current temperature, conditions, wind, and humidity from Open-Meteo with no API key (ROADMAP Phase-5 success criterion #4).
- `lat,lon` input is used directly; other strings are geocoded; `--units imperial` switches to Fahrenheit (server-side); a graceful offline error appears when unreachable.
- Non-2xx → exit 1 with the status; bad `--units` → exit 2; zero geocode results → exit 1 with "no location found".
- WTHR-01 satisfied; weather `not_implemented` arm + the now-dead stub import gone → ALL Phase-5 stubs removed, v1 milestone (23 commands) feature-complete.
</success_criteria>

<output>
Create `.planning/phases/05-windows-platform-integration/05-04-SUMMARY.md` when done.
</output>
