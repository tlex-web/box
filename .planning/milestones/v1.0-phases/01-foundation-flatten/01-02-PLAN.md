---
phase: 01-foundation-flatten
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/core/mod.rs
  - src/core/output.rs
  - src/core/fs.rs
  - src/main.rs
  - tests/cli.rs
autonomous: true
requirements: [FOUND-04, FOUND-06]
must_haves:
  truths:
    - "User pipes `box <cmd> --help` (or any colorized output) to a file and gets clean text with no ANSI escape sequences"
    - "Color is enabled in a real PowerShell 7 terminal and disabled when stdout is not a TTY, when NO_COLOR is set, or when --no-color is passed"
    - "Paths handed to the toolbox are normalized without `\\?\` UNC prefix leakage (dunce)"
    - "D-09: reusable glyph-row helper uses ASCII status glyphs (+ ~ -) as source of truth with `->` arrow, color decoration only (pipe-safe)"
    - "D-10: color gating is is_terminal() && NO_COLOR unset (plus --no-color); plain layout byte-identical minus ANSI; arrows aligned/width-capped via crossterm, over-long paths middle-truncated with ellipsis; inline reasons [collision]/(skipped: ...)"
    - "D-11: locked dry-run and real-run summary wording emitted exactly by reusable summary helpers"
    - "D-12: hidden = dotfile OR FILE_ATTRIBUTE_HIDDEN (0x2), applied in walkdir filter_entry so hidden dirs prune their subtree (is_hidden helper)"
  artifacts:
    - path: "src/core/output.rs"
      provides: "color_enabled gate + init_color + glyph/print helpers shared by all commands"
      contains: "is_terminal"
    - path: "src/core/fs.rs"
      provides: "normalize_path (dunce), is_hidden predicate, walk wrapper, safe_copy (FileTimes)"
      contains: "dunce::canonicalize"
  key_links:
    - from: "src/main.rs"
      to: "core::output::init_color"
      via: "called after parse, before dispatch"
      pattern: "init_color"
    - from: "src/core/output::color_enabled"
      to: "std::io::stdout().is_terminal()"
      via: "TTY + NO_COLOR + --no-color gate"
      pattern: "is_terminal"
    - from: "src/core/fs::normalize_path"
      to: "dunce::canonicalize"
      via: "UNC-safe canonicalization wrapper"
      pattern: "dunce::canonicalize"
---

<objective>
Build the shared `src/core/output.rs` (color gating + reusable print/glyph helpers) and `src/core/fs.rs` (UNC-safe path normalization, hidden-entry predicate, walk wrapper, timestamp-preserving copy). Wire `init_color` into `main()`. These are the reusable surfaces every command — starting with `flatten` next plan — inherits, so color, path safety, and timestamp handling are correct in exactly one place.

Purpose: Second slice of the walking skeleton. After this plan, the toolbox honors the color contract (FOUND-04) and the path-safety contract (FOUND-06) globally; `box <cmd> --help` piped to a file is byte-identical minus ANSI, and no command can leak a `\\?\` path.
Output: `core/output.rs` + `core/fs.rs` with reusable helpers; `init_color` wired into main; an automated `piped_help_has_no_ansi` test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-foundation-flatten/01-CONTEXT.md
@.planning/phases/01-foundation-flatten/01-RESEARCH.md
@.planning/phases/01-foundation-flatten/01-VALIDATION.md
@CLAUDE.md
@.planning/phases/01-foundation-flatten/01-01-SUMMARY.md

<interfaces>
<!-- Contracts this plan CREATES that plan 03 (flatten) consumes. Defined here so the executor builds flatten against them. -->

src/core/output.rs:
    pub fn color_enabled(no_color_flag: bool) -> bool   // !flag && NO_COLOR unset && stdout.is_terminal()
    pub fn init_color(no_color_flag: bool)              // owo_colors::set_override(color_enabled(..))
    // plus reusable print helpers for the flatten glyph rows (D-09): a status glyph (`+`/`~`/`-`),
    // an aligned `->` arrow column, terminal-width capping (crossterm terminal::size()), middle-ellipsis
    // truncation for over-long paths. Keep these pure/string-returning where possible so they are testable.

src/core/fs.rs:
    pub fn normalize_path(p: &Path) -> std::io::Result<PathBuf>   // dunce::canonicalize wrapper (never std::fs::canonicalize)
    pub fn is_hidden(entry: &walkdir::DirEntry) -> bool           // depth()==0 → false; else dotfile OR FILE_ATTRIBUTE_HIDDEN (0x2)
    pub fn safe_copy(src: &Path, dst: &Path) -> anyhow::Result<u64>  // fs::copy + FileTimes(modified+accessed) preserve; returns bytes copied
    // a thin WalkDir constructor helper is optional; flatten may call WalkDir directly with filter_entry(|e| !is_hidden(e)).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: core/output.rs — color gating + reusable flatten-row helpers</name>
  <files>src/core/output.rs, src/core/mod.rs, src/main.rs</files>
  <read_first>
    - .planning/phases/01-foundation-flatten/01-RESEARCH.md § "Pattern 5" (color gating, `color_enabled`/`init_color`, owo_colors::set_override, lines ~378-394), § "Don't Hand-Roll" (use `std::io::IsTerminal` NOT `atty`/`is-terminal`; use `owo_colors::set_override` not per-println checks)
    - .planning/phases/01-foundation-flatten/01-CONTEXT.md D-09 (glyph `+ ~ -` is source of truth, color decoration only; ASCII glyphs not Unicode; `->` arrow), D-10 (gate = `is_terminal() && NO_COLOR unset` plus `--no-color`; plain layout byte-identical minus ANSI; arrows aligned, width-capped via crossterm, over-long paths middle-truncated with `…`), D-11 (summary wording — locked, both dry-run and real-run forms)
    - CLAUDE.md "What NOT to Use" (no `colored`/`termcolor`); STATE.md Architecture (output.rs = color init + print helpers + TTY/NO_COLOR gating)
  </read_first>
  <action>
    Create `src/core/output.rs`. Implement `pub fn color_enabled(no_color_flag: bool) -> bool` = `!no_color_flag && std::env::var_os("NO_COLOR").is_none() && std::io::stdout().is_terminal()` (import `std::io::IsTerminal`; do NOT add the `is-terminal` crate). Implement `pub fn init_color(no_color_flag: bool)` calling `owo_colors::set_override(color_enabled(no_color_flag))` so all `.green()`/`.yellow()` calls no-op when piped (plain layout byte-identical minus ANSI per D-10). Add reusable, mostly-pure helpers the flatten command (plan 03) will use: a function that formats one status row from a glyph char (`+`/`~`/`-`), a left source label, an optional `-> dst` mapping, and an optional inline reason (`[collision]`, `[collision x2]`, `(skipped: symlink)`, `(skipped: reserved name)`) — glyph printed unconditionally, only color wrapped via owo-colors; align the `->` arrow into a column; cap line width at `crossterm::terminal::size()` width (fall back to 80 if size() errors, e.g. piped), middle-truncating over-long paths with `…` so the filename stays visible (D-10). Also add helpers for the two locked D-11 summary strings (dry-run and real-run). Declare `pub mod output;` in `src/core/mod.rs`. In `src/main.rs`, replace the plan-01 TODO with the real `crate::core::output::init_color(cli.no_color)` call after a successful parse and before dispatch.
  </action>
  <verify>
    <automated>cargo build</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/output.rs` `color_enabled` references `std::io::stdout().is_terminal()`, `std::env::var_os("NO_COLOR")`, and the `no_color_flag` parameter
    - `src/core/output.rs` does NOT import the `is-terminal` crate (uses stdlib `std::io::IsTerminal`)
    - `src/main.rs` contains `core::output::init_color(cli.no_color)` called before the `match cli.command` dispatch
    - `cargo build` exits 0
  </acceptance_criteria>
  <done>Color is decided once at startup from --no-color ∧ NO_COLOR ∧ TTY; reusable glyph-row + summary helpers exist for flatten; init_color wired into main.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: core/fs.rs — normalize_path + is_hidden + safe_copy; piped-no-ANSI test</name>
  <files>src/core/fs.rs, src/core/mod.rs, tests/cli.rs</files>
  <read_first>
    - .planning/phases/01-foundation-flatten/01-RESEARCH.md § "Pitfall 1" (dunce::canonicalize, never std::fs::canonicalize — UNC leak, lines ~446-451), § "Pitfall 6" (timestamp preservation via std::fs::FileTimes — exact code, lines ~479-491), § "Hidden-entry predicate for filter_entry" code example (D-12, lines ~613-631), § "Pitfall 8" (filter_entry root-entry gotcha walkdir#142)
    - .planning/phases/01-foundation-flatten/01-CONTEXT.md D-12 (hidden = dotfile OR FILE_ATTRIBUTE_HIDDEN 0x2; apply in filter_entry so hidden dirs prune subtree); D-13 (auto-create output dir)
    - .planning/phases/01-foundation-flatten/01-VALIDATION.md (test name `piped_help_has_no_ansi`); STATE.md pitfall (dunce everywhere; FileTimes preserve)
  </read_first>
  <behavior>
    - `normalize_path` on an existing dir returns a path NOT beginning with `\\?\` (dunce strips the verbatim prefix)
    - `is_hidden` returns false for the root entry (depth 0) even if its name starts with `.` (walkdir#142 gotcha)
    - `is_hidden` returns true for a non-root dotfile entry and for a non-root entry with FILE_ATTRIBUTE_HIDDEN set
    - `safe_copy` copies bytes AND sets the destination modified time equal to the source modified time
    - `piped_help_has_no_ansi`: `box flatten --help` captured (not a TTY) contains no `\x1b[` escape sequence (FOUND-04)
  </behavior>
  <action>
    Create `src/core/fs.rs`. Implement `pub fn normalize_path(p: &Path) -> std::io::Result<PathBuf>` wrapping `dunce::canonicalize(p)` (NEVER `std::fs::canonicalize` — it leaks `\\?\`). Implement `pub fn is_hidden(entry: &walkdir::DirEntry) -> bool` exactly per the RESEARCH code example: return `false` when `entry.depth() == 0` (never prune the root — walkdir#142); otherwise true if the file name starts with `.` OR (on Windows, via `std::os::windows::fs::MetadataExt::file_attributes()`) the `FILE_ATTRIBUTE_HIDDEN` bit `0x2` is set; gate the Windows attribute check behind `#[cfg(windows)]` with a `#[cfg(not(windows))]` fallback of `false` so the crate still type-checks elsewhere. Implement `pub fn safe_copy(src: &Path, dst: &Path) -> anyhow::Result<u64>`: `let bytes = std::fs::copy(src, dst).with_context(|| format!("copying {}", src.display()))?;` then read source metadata and apply `std::fs::FileTimes::new().set_modified(meta.modified()?).set_accessed(meta.accessed()?)` via `File::options().write(true).open(dst)?.set_times(times)` (Pitfall 6 exact pattern) — wrap each fallible I/O call in `.context(...)` so deep-path (>260 char) failures surface loudly per-file, never silently dropped (FOUND-06, Pitfall 5); return `bytes`. Declare `pub mod fs;` in `src/core/mod.rs`. Add the `piped_help_has_no_ansi` integration test to `tests/cli.rs`: run `box flatten --help` via assert_cmd (captured output is not a TTY) and assert stdout contains no `\x1b[` byte sequence.
  </action>
  <verify>
    <automated>cargo test --test cli</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/fs.rs` `normalize_path` calls `dunce::canonicalize` and the file contains no call to `std::fs::canonicalize`
    - `src/core/fs.rs` `is_hidden` returns `false` for `entry.depth() == 0` and checks `file_attributes() & 0x2` under `#[cfg(windows)]`
    - `src/core/fs.rs` `safe_copy` calls `std::fs::FileTimes` / `set_times` and wraps copy I/O in `.context(...)`
    - `cargo test --test cli` exits 0 with `piped_help_has_no_ansi` passing (no `\x1b[` in piped help output)
  </acceptance_criteria>
  <done>Path normalization is UNC-safe, hidden-pruning honors the root gotcha, copies preserve timestamps and report deep-path errors loudly, and piped output is proven ANSI-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → box process | Path arguments and directory contents are untrusted; canonicalization + hidden filtering happen here for every command. |
| box process → terminal vs pipe | Color must auto-disable off-TTY; a stuck `\x1b[` in piped output corrupts downstream consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | output color gating | mitigate | `color_enabled` requires `is_terminal()` AND `NO_COLOR` unset; `piped_help_has_no_ansi` asserts no `\x1b[` when piped (FOUND-04). |
| T-02-02 | Tampering | path normalization | mitigate | `normalize_path` uses `dunce::canonicalize` only; no `std::fs::canonicalize` anywhere — prevents `\\?\` leakage corrupting containment guards and collision encoding (FOUND-06, Pitfall 1). |
| T-02-03 | Information disclosure / integrity | safe_copy deep paths | mitigate | All copy/metadata/set_times I/O wrapped in `.context(...)`; >260-char path errors surface per-file, never silently dropped (FOUND-06, Pitfall 5). |
| T-02-04 | Denial of service | is_hidden root gotcha | mitigate | `is_hidden` returns false at depth 0 so a dotted source root is not pruned to zero files (walkdir#142, Pitfall 8). |
</threat_model>

<verification>
- `cargo build` exits 0.
- `cargo test --test cli` exits 0 including `piped_help_has_no_ansi`.
- `cargo clippy -- -D warnings` clean (wave merge).
- Manual: in a PS7 terminal, colorized output is colored; piped to a file it is plain (deferred to plan 03's flatten output and the phase manual-verify gate, since no colored command output exists until flatten ships).
</verification>

<success_criteria>
- Color auto-disables off-TTY / under NO_COLOR / under --no-color and is enabled in PS7 (FOUND-04).
- Paths are normalized without `\\?\` leakage; deep-path failures are reported, not silent (FOUND-06).
- Reusable output (glyph rows + summaries) and fs (normalize/is_hidden/safe_copy) helpers exist for flatten to consume next plan.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-flatten/01-02-SUMMARY.md` when done.
</output>
