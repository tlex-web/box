---
phase: 09-dev-transform-visual-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/commands/uuid/mod.rs
  - src/commands/passgen/mod.rs
  - src/commands/epoch/mod.rs
  - src/commands/json/mod.rs
  - src/commands/color/mod.rs
  - tests/uuid.rs
  - tests/passgen.rs
  - tests/epoch.rs
  - tests/json.rs
  - tests/color.rs
autonomous: true
requirements: [UUID-V2-01, EPOC-V2-01, COLR-V2-01, JSON-V2-01, PASS-V2-01]

must_haves:
  truths:
    - "box uuid --v7 emits a version-7 UUID (nibble '7' at canonical position 14); --upper/--no-hyphens/--braces/--urn each reshape the printed value; --braces with --urn exits 2"
    - "box epoch <int> appends a relative-time suffix ('3 hours ago' / 'in 2 days' / 'just now') to the Local and UTC lines, while bare 'now' and date-string modes stay plain integers"
    - "box epoch --tz <IANA> adds a third zone-labeled line; an unknown zone exits 1 with an IANA hint, never a panic"
    - "box color resolves a CSS name to RGB and an RGB to its exact-or-nearest CSS name; box color 'hsl(210, 100%, 50%)' parses as HSL; JSON carries always-present name + nearest"
    - "box json --sort-keys recursively sorts object keys (arrays keep order) while a plain box json preserves input key order"
    - "box passgen prints an entropy-bits estimate on STDERR (never stdout); --no-similar drops il1Lo0O and recomputes the pool; --separator overrides the '.' passphrase join"
  artifacts:
    - path: "Cargo.toml"
      provides: "chrono-tz dependency + uuid v7 feature"
      contains: "chrono-tz"
    - path: "src/commands/uuid/mod.rs"
      provides: "v7 generation + four wrapping forms"
      contains: "now_v7"
    - path: "src/commands/passgen/mod.rs"
      provides: "entropy_bits + --no-similar + --separator"
      contains: "entropy_bits"
    - path: "src/commands/epoch/mod.rs"
      provides: "relative_for humanizer + --tz line"
      contains: "relative_for"
    - path: "src/commands/json/mod.rs"
      provides: "recursive sort_value key sort"
      contains: "sort_value"
    - path: "src/commands/color/mod.rs"
      provides: "hsl_to_rgb + CSS name table + redmean nearest"
      contains: "hsl_to_rgb"
  key_links:
    - from: "src/commands/uuid/mod.rs format_one"
      to: "both out_line and emit_json"
      via: "single-source formatted value"
      pattern: "format_one\\("
    - from: "src/commands/epoch/mod.rs relative_for"
      to: "EpochOutput.relative AND the integer-branch human suffix"
      via: "one helper feeds both paths"
      pattern: "relative_for\\("
    - from: "src/commands/passgen/mod.rs entropy"
      to: "eprintln! (STDERR) for human, entropy_bits for JSON"
      via: "secret stays sole stdout content"
      pattern: "eprintln!"
---

<objective>
Add the five dev-transform depth flags, each additive to its command's frozen Phase-7 output struct: `uuid` v7 + four wrapping forms (UUID-V2-01); `epoch` always-on relative time + `--tz` (EPOC-V2-01); `color` CSS named-color lookup (both directions) + HSL input (COLR-V2-01); `json --sort-keys` opt-in recursive sort (JSON-V2-01); `passgen` entropy estimate + `--no-similar` + `--separator` (PASS-V2-01).

Purpose: every new field slots into BOTH the human render and `emit_json` from one `#[derive(Serialize)]` struct (no-drift by construction); the only manifest edits are `chrono-tz` (epoch `--tz`) and the `uuid` `v7` feature.
Output: five extended command modules + their integration tests; two additive Cargo.toml edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dev-transform-visual-depth/09-CONTEXT.md
@.planning/phases/09-dev-transform-visual-depth/09-PATTERNS.md
@.planning/phases/09-dev-transform-visual-depth/09-RESEARCH.md
@.planning/phases/09-dev-transform-visual-depth/09-VALIDATION.md

The spine is CONSUMED, never modified: `is_json_on()` fork happens FIRST; every new field rides one `#[derive(Serialize)]` struct into both renders; `init_output` already forces COLOR_ON=false under `--json`/`--clip`. No `cli.rs`/`main.rs` changes. Locked JSON shapes are additive-only (color `{hex,rgb,hsl}` D-19, epoch `{epoch,utc,local}` D-20, uuid `{results:[{uuid,version}],count}`). Bare `u64` for large numbers (D-3); `to_string_lossy()` for non-UTF-8 paths (D-4).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: uuid v7 + wrapping forms; passgen entropy + --no-similar + --separator</name>
  <read_first>
    - src/commands/uuid/mod.rs (the `format_one` seam :88, `UuidRow`/`UuidOutput` :26-39, the `.map()` build-once fork :59-80, `UuidArgs` :42-50)
    - src/commands/passgen/mod.rs (the `phrase.join(".")` seam :138, `build_charset` :176, `PassgenOutput` :47, the `is_json_on()` fork :159-169, `PassgenArgs` :92-109)
    - Cargo.toml (the `uuid` dependency line)
    - .planning/phases/09-dev-transform-visual-depth/09-PATTERNS.md (the uuid and passgen sections — the named seams)
    - .planning/phases/09-dev-transform-visual-depth/09-RESEARCH.md (§ Code Examples — uuid v7 + the four wrapping forms; § Common Pitfalls — Pitfall 5 passgen secret/entropy)
  </read_first>
  <files>Cargo.toml, src/commands/uuid/mod.rs, src/commands/passgen/mod.rs, tests/uuid.rs, tests/passgen.rs</files>
  <behavior>
    - format_one with Plain form renders the 36-char hyphenated value; Simple form (--no-hyphens) renders 32 hex; Braces wraps in `{...}`; Urn prefixes `urn:uuid:`; --upper composes with every form (uppercases the result).
    - A --v7 UUID has version nibble '7' at canonical position 14; the `version` field reads "v7"; without --v7 it is "v4" with nibble '4'.
    - `box uuid --braces --urn` exits 2 (clap conflicts_with).
    - passgen entropy_bits = `length * log2(pool_size)` in char mode and `words * log2(7776)` (=12.925) in passphrase mode; --no-similar removes every char of `il1Lo0O` from the pool and lowers entropy_bits accordingly.
    - `box passgen --separator -` joins passphrase words with `-` instead of `.`.
    - The entropy line appears on STDERR only; stdout under `--json` parses and carries a top-level `entropy_bits` number and no entropy prose.
  </behavior>
  <action>
    Cargo.toml: change the `uuid` dependency features to `["v4", "v7"]` (add `v7`; `v4` already present). No other manifest change in this task.
    uuid: extend the pure `format_one` to take format options as well as `upper`. Introduce a private `enum Form { Plain, Simple, Braces, Urn }` chosen from the args; select the wrapping via `match form { Plain => u.hyphenated(), Simple => u.simple(), Braces => u.braced(), Urn => u.urn() }.to_string()` (all four are const fn, no feature), THEN apply `--upper` as the existing composable post-pass. In `UuidArgs` add `--v7` (bool), `--no-hyphens` (bool → Form::Simple), `--braces` (bool), `--urn` (bool); mark `--braces` and `--urn` with `conflicts_with` each other (clap exit 2). In the `.map()`, generate `if self.v7 { uuid::Uuid::now_v7() } else { uuid::Uuid::new_v4() }`; set `version` to `"v7"`/`"v4"` accordingly. The `uuid` JSON field carries the SAME `format_one` output the human line prints (single-source — default per Claude's Discretion).
    passgen: replace `phrase.join(".")` at :138 with `phrase.join(&self.separator)`. Add to `PassgenArgs`: `--separator <str>` (default `"."`), `--no-similar` (bool). In char mode, after `build_charset(self.no_symbols)`, when `--no-similar` apply `set.retain(|c| !"il1Lo0O".contains(*c))` and recompute the pool from the resulting length. Compute `entropy_bits: f64` from the active pool (`self.length as f64 * (pool_len as f64).log2()` char mode; `n as f64 * 7776f64.log2()` passphrase mode). Add `entropy_bits: f64` as a top-level field on `PassgenOutput` (per-config, not per-row). Human path: `eprintln!` a single entropy summary to STDERR, TTY-gated like the clip confirmation and suppressed when `is_json_on()`; the secret stays the ONLY stdout content (D-14). RNG (`OsRng.unwrap_err()` + `.choose`) is UNTOUCHED (T-V6) — only the charset/join change, never the draw.
  </action>
  <verify>
    <automated>cargo test --bin box format_one && cargo test --bin box entropy && cargo test --test uuid && cargo test --test passgen</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test uuid` includes an assertion that `box uuid --braces --urn` exits with code 2.
    - A unit test asserts a `--v7` value's byte at index 14 is `b'7'` and `version=="v7"`.
    - Unit tests assert each wrapping form's shape (length/prefix) and that `--upper` uppercases each.
    - A passgen unit test asserts `entropy_bits` matches the formula and drops when `--no-similar` is set; an integration test asserts `box passgen --json` stdout contains `"entropy_bits"`, parses as one JSON document, and carries no `0x1B` byte; an integration assertion confirms the entropy text is on stderr, not stdout.
    - `cargo clippy --all-targets -- -D warnings` stays clean for both modules.
  </acceptance_criteria>
  <done>uuid emits v7 + the four wrapping forms with the --braces/--urn conflict gate; passgen prints entropy on stderr, honors --no-similar and --separator with the secret as the sole stdout content; the uuid v7 Cargo.toml edit is present and both modules' tests + clippy are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: epoch relative time + --tz (chrono-tz); json --sort-keys</name>
  <read_first>
    - src/commands/epoch/mod.rs (the `epoch_output` :141 + `format_timestamp` :155 shared math, the integer-arg human branch :88-96, the `bail!` hint discipline :185, `EpochOutput` :31, `EpochArgs` :43)
    - src/commands/json/mod.rs (the pretty/compact/colorize/emit_json fork :61-90, `JsonArgs` :37-44, the `colorize_preserves_key_order` test :197+)
    - Cargo.toml (dependency table — add chrono-tz)
    - .planning/phases/09-dev-transform-visual-depth/09-PATTERNS.md (the epoch and json sections)
    - .planning/phases/09-dev-transform-visual-depth/09-RESEARCH.md (§ Code Examples — epoch --tz validation + the `json --sort-keys` recursive rewrite; § Common Pitfalls — Pitfall 2 `%Z`)
  </read_first>
  <files>Cargo.toml, src/commands/epoch/mod.rs, src/commands/json/mod.rs, tests/epoch.rs, tests/json.rs</files>
  <behavior>
    - relative_for(epoch, now) returns "just now" within the smallest threshold, "N minutes ago"/"N hours ago"/"N days ago" for past instants, and "in N ..." for future instants (assert FORMAT, not clock-dependent value).
    - `box epoch <int>` appends the relative suffix to BOTH the Local and UTC lines; `box epoch now` and a date-string arg emit a bare integer with no suffix.
    - `box epoch --tz Asia/Tokyo <int>` renders a third line containing `JST` and `+0900`; `box epoch --tz Not/AZone <int>` exits 1 with an IANA hint.
    - EpochOutput always serializes `relative`; `tz` is present only under `--tz` (skip_serializing_if).
    - sort_value recursively rebuilds every object map in sorted key order; arrays keep element order; a plain `box json` (no `--sort-keys`) preserves input key order unchanged; `box json --sort-keys --json` emits a sorted document.
  </behavior>
  <action>
    Cargo.toml: add `chrono-tz = "0.10"` (default tz-database feature; committed in REQUIREMENTS D-1).
    epoch: add a new pure helper `relative_for(epoch: i64, now: i64) -> String` — a hand-rolled threshold ladder (just now / N min / N hr / N days / weeks / months / years) with a sign branch emitting `in N ...` for future instants (~30 lines, no crate). In `EpochOutput` add `relative: String` (always-present, set to `relative_for(epoch, Utc::now().timestamp())`) and `tz: Option<String>` with `#[serde(skip_serializing_if = "Option::is_none")]`. In the integer-arg human branch ONLY (:88-96), append the relative suffix to each line returned by `format_timestamp`; leave the `now`/date-string branches emitting a bare integer (scripting-clean). Add `tz: Option<String>` to `EpochArgs`. When `--tz` is set, validate via `chrono_tz::Tz::from_str(name)` (`use std::str::FromStr`) — on Err, `bail!("unknown timezone '{name}'; expected an IANA name like 'Asia/Tokyo'")` (exit 1, mirrors the `:185` parse_date hint discipline; never panic). Build the third human line and the JSON `tz` value off the SAME `dt_utc` via `dt_utc.with_timezone(&tz).format("%Y-%m-%d %H:%M:%S %Z %z")`. Add a unit test confirming `%Z` renders the abbreviation (Asia/Tokyo @ 1700000000 contains `"JST"` and `"+0900"`) — confirms assumption A1.
    json: add a recursive `fn sort_value(v: serde_json::Value) -> serde_json::Value` that, for `Value::Object`, collects entries, recurses into each value, sorts by key (`a.0.cmp(&b.0)`), and rebuilds the ordered map in sorted order; for `Value::Array` recurses into each element preserving order; otherwise returns the value. Add `--sort-keys` (bool, opt-in) to `JsonArgs`. Apply `let value = if self.sort_keys { sort_value(value) } else { value };` BEFORE the `is_json_on()` fork at :69 so it feeds emit_json/pretty/compact/colorize identically. `preserve_order` STAYS the default — never sort implicitly.
  </action>
  <verify>
    <automated>cargo test --bin box relative_for && cargo test --bin box sort_value && cargo test --test epoch && cargo test --test json</automated>
  </verify>
  <acceptance_criteria>
    - A unit test asserts `relative_for` FORMAT across past/future/just-now branches.
    - A unit test asserts the `%Z %z` line for a known zone contains `JST` and `+0900` (A1 lock).
    - `cargo test --test epoch` asserts a bad `--tz` value exits 1, and that `box epoch now` / a date-string arg emit a bare integer (no relative suffix).
    - A unit test asserts `sort_value` sorts nested object keys and leaves arrays in order; `cargo test --test json` asserts plain `box json` preserves input order and `box json --sort-keys` sorts.
    - `cargo clippy --all-targets -- -D warnings` clean.
  </acceptance_criteria>
  <done>epoch appends always-on relative time to the integer-path lines only, adds a validated --tz third line (bad zone → exit 1) with always-present JSON `relative` and conditional `tz`; json gains opt-in recursive --sort-keys with preserve_order still the default; chrono-tz added and all tests + clippy green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: color CSS named-color (both directions) + HSL input</name>
  <read_first>
    - src/commands/color/mod.rs (the `parse_color` auto-detect ordering :131-146, the `rgb_to_hsl` inverse template :199-223, the `ColorOutput` struct :50-55 and JSON assembly :79-87, the human block :93-97, the `bail!` :143)
    - .planning/phases/09-dev-transform-visual-depth/09-PATTERNS.md (the color section — the no-collision seam)
    - .planning/phases/09-dev-transform-visual-depth/09-RESEARCH.md (§ Seam Verification — the `hsl(` branch ordering note; § Security Domain — V5 input validation)
    - .planning/STATE.md (D-19: JSON hex stays lowercase, human Hex row stays uppercase)
  </read_first>
  <files>src/commands/color/mod.rs, tests/color.rs</files>
  <behavior>
    - `parse_color("hsl(210, 100%, 50%)")` and the space form `hsl(210 100% 50%)` both parse to the expected RGB; a round-trip rgb→hsl→rgb stays within ±1 per channel.
    - A CSS name (`black`, `white`, `rebeccapurple`, `cornflowerblue`) resolves to its exact RGB.
    - For an RGB that lands exactly on a CSS keyword, JSON `name` is that keyword and `nearest` equals it; for an arbitrary RGB, `name` is null and `nearest` is the redmean-closest keyword; both fields are ALWAYS present.
    - The human block marks an exact match as `name` and an approximate as `~nearest`.
  </behavior>
  <action>
    color: in `parse_color`, insert an `hsl(` prefix check BEFORE the RGB separator branch at :132 (a bare `hsl(210, 100%, 50%)` contains commas and would otherwise mis-hit RGB) — strip the `hsl(`/`)`, parse `H` (0–360), `S`/`L` (0–100 percent, tolerating the `%` and both comma and space separators), and convert via a new pure `hsl_to_rgb(h, s, l) -> (u8,u8,u8)` mirroring `rgb_to_hsl` (:199) as the standard closed form. Insert a named-color table lookup AFTER the hex check and BEFORE the final `bail!` (:143) — no CSS name is an all-hex string of length 3/6, so there is no collision. Add a `const CSS_NAMES: &[(&str, (u8,u8,u8))]` of the ~148 CSS Color Module Level 4 keywords (including `rebeccapurple`), no crate. Add a pure `nearest_name(r,g,b) -> &'static str` using a hand-rolled weighted-RGB "redmean" distance (~10 lines — NOT plain Euclidean, NOT CIEDE2000) and a pure `exact_name(r,g,b) -> Option<&'static str>`. Extend `ColorOutput` (:50-55) with `name: Option<String>` (exact keyword or null) and `nearest: String` (redmean-closest), BOTH always-present; keep the JSON `hex` LOWERCASE-locked (D-19). Extend the human block (:93-97) to print the exact `name` or the approximate `~nearest`. Malformed `hsl(...)`/RGB/name input must `bail!` exit 1 (no panic, no index overflow — extend the existing parse discipline; V5).
  </action>
  <verify>
    <automated>cargo test --bin box hsl && cargo test --bin box nearest && cargo test --test color</automated>
  </verify>
  <acceptance_criteria>
    - A unit test asserts the four CSS anchors (`black`/`white`/`rebeccapurple`/`cornflowerblue`) map to their known RGB.
    - A unit test asserts `hsl(...)` parses and a round-trip rgb→hsl→rgb is within ±1.
    - `cargo test --test color` asserts JSON `name` is null + `nearest` non-empty for an arbitrary color, and `name`==`nearest` for an exact keyword; both fields present in every document.
    - A malformed `hsl(...)` / unknown token exits 1 (no panic).
    - `cargo clippy --all-targets -- -D warnings` clean.
  </acceptance_criteria>
  <done>color parses CSS-functional hsl(...) (routed before the RGB branch), resolves the ~148-name CSS table both directions, and emits always-present JSON name (exact|null) + nearest (redmean), with the hex field still lowercase-locked; malformed input exits 1 and tests + clippy are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI args → parser | Untrusted `hsl(...)`/RGB/CSS-name strings and `--tz` IANA names cross into `color`/`epoch` |
| CSPRNG → stdout | `passgen` secret must stay the sole stdout content; entropy must not leak the draw or contaminate the channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-IV | Tampering/DoS | `color::parse_color` (`hsl(...)`/RGB/CSS-name parse) | mitigate | Malformed input `bail!`s exit 1 with a hint; no `unwrap`/index past bounds (extend the existing parse_color V5 discipline); HSL fields range-checked |
| T-09-01-TZ | Tampering/DoS | `epoch --tz` (`chrono_tz::Tz::from_str`) | mitigate | Unknown IANA name → `bail!` exit 1 with `'Asia/Tokyo'` hint, never a panic (mirrors `parse_date` :185) |
| T-09-01-CRYPTO | Information disclosure | `passgen` RNG + secret channel | accept | RNG (OsRng + unbiased `.choose`) UNCHANGED (T-V6) — `--no-similar`/`--separator` change only charset/join, never the draw; entropy is a display estimate, not a control |
| T-09-01-LEAK | Information disclosure | `passgen` entropy output | mitigate | Entropy → STDERR (human, TTY-gated) / `entropy_bits` JSON field; the secret stays the ONLY stdout content (D-14); `--clip` copies only the secret |
| T-09-01-SC | Tampering (supply chain) | `chrono-tz` (new) + `uuid` `v7` feature | mitigate | RESEARCH § Package Legitimacy Audit cleared `chrono-tz` (Approved, crates.io verified, github.com/chronotope/chrono-tz); `uuid` v7 is a feature edit on an existing dep. No `[ASSUMED]`/`[SUS]`/`[SLOP]` package → no blocking-human checkpoint required |
</threat_model>

<verification>
- Per task: `cargo test --bin box <changed-seam>` plus the touched command's `cargo test --test <cmd>` integration file.
- Plan gate: `cargo test` green for uuid/epoch/color/json/passgen + `cargo clippy --all-targets -- -D warnings` clean.
- SC4 holds for free: these are value-producing commands already on the `is_json_on()` fork; no new colored stdout path is added here, so piped/`--json` output stays byte-identical minus ANSI.
</verification>

<success_criteria>
- ROADMAP Phase 9 SC1 satisfied: uuid v7 + four wrapping forms reshape output; epoch relative + `--tz`; color CSS both directions + HSL input; json `--sort-keys` vs preserved order; passgen entropy + `--no-similar` + `--separator`.
- All five locked JSON shapes extended additively (no field removed/renamed); `preserve_order` remains the json default.
- Both Cargo.toml edits (chrono-tz add, uuid v7 feature) present and the workspace builds.
</success_criteria>

<output>
Create `.planning/phases/09-dev-transform-visual-depth/09-01-SUMMARY.md` when done.
</output>
