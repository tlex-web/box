---
phase: 08-filesystem-depth
plan: 04
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/commands/flatten/mod.rs
  - tests/flatten_move.rs
autonomous: false
requirements: [FLAT-V2-02]

must_haves:
  truths:
    - "box flatten <src> <out> --move (no --force) writes NOTHING and previews the relocation plan"
    - "box flatten <src> <out> --move --force relocates each file via copy → verify (dest exists + size matches) → delete source"
    - "every abort path (containment refusal, dry-run, mid-batch copy error) leaves the source tree byte-for-byte unchanged"
    - "empty source directories are left in place after a move (only files relocate)"
    - "box flatten --move --json emits the plan (dry-run) or the executed result (--force) with dry_run reflecting the mode"
  artifacts:
    - path: "src/commands/flatten/mod.rs"
      provides: "--move destructive mode: dry-run default, --force, copy→verify→delete reusing safe_copy + containment guard"
      contains: "move"
      min_lines: 480
    - path: "tests/flatten_move.rs"
      provides: "move relocation + snapshot-the-tree-unchanged tests for every abort path"
      contains: "snapshot_tree"
      min_lines: 120
  key_links:
    - from: "src/commands/flatten/mod.rs"
      to: "core::fs::safe_copy"
      via: "create-new copy then verify dest size then std::fs::remove_file(source)"
      pattern: "safe_copy"
    - from: "src/commands/flatten/mod.rs"
      to: "containment guard + collision-rename"
      via: "the existing copy-path guards reused before any delete"
      pattern: "refusing to flatten|occupied"
---

<objective>
**DESTRUCTIVE — own plan + mandatory adversarial code review.**

- **FLAT-V2-02:** `flatten --move` relocates files instead of copying — copy → verify (dest exists + size matches) → delete source; dry-run default, `--force` to execute.

Purpose: this is the v1 Phase-3 destructive gate applied to `flatten`. The mechanics are pre-locked (ROADMAP SC2): reuse the existing containment guard + collision-rename + `safe_copy` (create-new, never clobbers) from the copy path, then verify the destination before deleting the source. Every abort path must leave the source tree byte-for-byte unchanged. Empty source directories are left in place (only files relocate).

Output: `--move` mode on `flatten`; new `tests/flatten_move.rs` with a snapshot-the-tree-unchanged assertion for every abort path; a blocking code-review checkpoint.

Depends on 08-01 (shares `src/commands/flatten/mod.rs` and `rename.rs` — different wave so no parallel edit; builds on the filtered `build_plan` and `FlattenOutput`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-filesystem-depth/08-CONTEXT.md
@.planning/phases/08-filesystem-depth/08-RESEARCH.md
@.planning/phases/08-filesystem-depth/08-VALIDATION.md
@.planning/phases/08-filesystem-depth/08-01-SUMMARY.md

<interfaces>
From src/core/fs.rs:
  pub fn safe_copy(src: &Path, dst: &Path) -> anyhow::Result<u64>;   // create_new — fails AlreadyExists, never clobbers
  pub fn normalize_path(p: &Path) -> std::io::Result<PathBuf>;

From src/commands/flatten/mod.rs (extend after 08-01's filters land):
  pub struct FlattenArgs { src, out, dry_run, /* 08-01: extensions, separator, include_hidden */ }
  struct Plan { items: Vec<PlanItem>, to_copy, renamed, skipped }
  struct PlanItem { src, src_label, dst_name: Option<String>, kind: ItemKind, reason }
  struct FlattenOutput { results, count, dry_run, copied, renamed, skipped, total_bytes }
  fn build_plan(src_root, occupied) -> anyhow::Result<Plan>;        // the single source of truth
  // existing copy real-run loop uses safe_copy(&item.src, &dst); containment guard is the bail! at the top of run()

From tests/dupes.rs (copy-me helper):
  fn snapshot_tree(dir: &Path) -> BTreeMap<String, (Vec<u8>, SystemTime)>;   // line 46 — copy into flatten_move.rs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold tests/flatten_move.rs — relocation + snapshot-unchanged abort paths (RED)</name>
  <files>tests/flatten_move.rs</files>
  <read_first>
    - tests/flatten.rs (the runner shape, NO_COLOR discipline, the existing dry-run/copy assertions)
    - tests/dupes.rs (the `snapshot_tree` helper at line 46 — copy it into the new file)
    - .planning/phases/08-filesystem-depth/08-RESEARCH.md (§ Validation Architecture → FLAT-V2-02 rows; § Pitfall 5 — copy→verify→delete; abort cases)
    - .planning/phases/08-filesystem-depth/08-VALIDATION.md (the snapshot-the-tree-unchanged requirement for every abort path)
  </read_first>
  <action>
    Create `tests/flatten_move.rs` with a `snapshot_tree(dir)` helper (copied from `tests/dupes.rs:46`) and these FAILING tests:
    - `move_dryrun_default_no_write`: `--move` with NO `--force` prints a relocation plan and writes nothing; the source tree snapshot before == after; the output dir is empty/unchanged.
    - `move_force_relocates`: `--move --force` copies each file to the flat output, verifies it exists with matching size, deletes the source file; after the run the output dir holds the flattened files and the source FILES are gone but the source DIRECTORIES remain (empty source dirs left in place).
    - `move_abort_containment_snapshot_unchanged`: an output dir inside the source dir is refused (the containment guard `bail!`s) and the source tree snapshot is unchanged.
    - `move_abort_midbatch_copy_error_snapshot_unchanged`: induce a mid-batch copy failure (e.g. a pre-existing destination so `safe_copy`'s create-new errors) and assert NO source file was deleted before the error — the already-copied dests may exist but every source file still exists (verify-before-delete means a failed copy never deletes its source).
    - `move_json_plan_and_executed`: `--move --json` (dry-run) emits the plan with `dry_run:true`; `--move --force --json` emits the executed result with `dry_run:false`; both are one parseable value with no `0x1B`.

    All MUST compile and FAIL.
  </action>
  <acceptance_criteria>
    - `cargo test --test flatten_move move_force_relocates` and `cargo test --test flatten_move move_abort_containment_snapshot_unchanged` COMPILE and FAIL (red).
    - Every abort-path test calls `snapshot_tree` before and after and asserts equality.
    - `tests/flatten_move.rs` exists and contains a `snapshot_tree` helper.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --test flatten_move 2>&1 | tail -20</automated>
  </verify>
  <done>tests/flatten_move.rs compiles with failing relocation + per-abort-path snapshot-unchanged tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: flatten --move = dry-run default → --force → copy→verify→delete (FLAT-V2-02)</name>
  <files>src/commands/flatten/mod.rs</files>
  <read_first>
    - src/commands/flatten/mod.rs (the FULL module post-08-01 — `run`, the containment guard `bail!`, the `occupied` seed, `build_plan`, the real-run `safe_copy` loop, `FlattenOutput`/`flatten_rows`, the `is_json_on()` fork)
    - src/core/fs.rs (`safe_copy` — create-new semantics; it returns the byte count)
    - .planning/phases/08-filesystem-depth/08-CONTEXT.md (§ carried locks — flatten --move mechanics; empty source dirs left in place)
    - .planning/phases/08-filesystem-depth/08-RESEARCH.md (§ Pattern 2 destructive template; § Pitfall 5; Assumption A6 — `move` is a keyword)
  </read_first>
  <behavior>
    - `--move` selects relocation. With `--move` the safety posture is dry-run-DEFAULT: write nothing unless `--force` (the destructive template, distinct from copy mode where `--dry-run` is opt-in).
    - `--force` per file: `safe_copy(src, dst)` (create-new) → verify `dst` exists and `metadata().len()` == source size → `std::fs::remove_file(src)`. ANY failure at any step leaves the source intact (verify-before-delete; never delete on a failed/short copy).
    - Containment refusal, dry-run, and a mid-batch copy error all leave the source tree byte-for-byte unchanged. Empty source directories are NOT pruned.
    - `--json`: dry-run → plan with `dry_run:true`; `--force` → executed result with `dry_run:false`, real `copied`/`total_bytes`.
  </behavior>
  <action>
    Add to `FlattenArgs`: `move_: bool` with `#[arg(long = "move")]` (A6 — `move` is a Rust keyword) and `force: bool` with `#[arg(long)]`. Keep the existing copy mode untouched when `--move` is absent.

    When `--move` is set, branch into a relocation path that REUSES the existing pipeline up to and including the containment guard, the `occupied` seed, and `build_plan` (the single source of truth — so `--extensions`/`--separator`/`--include-hidden` filters apply identically). Gate execution on `--force`: without it, this is a dry-run that emits the plan (human preview or, under `--json`, the `FlattenOutput` plan projection with `dry_run:true`) and writes nothing. With `--force`, execute per plan item: call `safe_copy(&item.src, &dst)` (create-new — never clobbers); then VERIFY by reading `dst` metadata and asserting it exists and its length equals the source length (`std::fs::metadata(&item.src)?.len()`); only on a successful verify call `std::fs::remove_file(&item.src)` (`.context(...)`-wrapped). On any error, `?`-propagate (exit 1, source intact) — the source file for the failing item is never deleted because the delete is strictly after a successful copy+verify. Do NOT prune emptied source directories.

    Reuse `FlattenOutput`/`flatten_rows` for the `--json` document (`dry_run` flips with `--force`; `copied`/`total_bytes` reflect the actual relocation). Keep the `is_json_on()` fork FIRST. Update the module doc comment to document the destructive `--move` template (dry-run default, `--force`, copy→verify→delete, abort-leaves-source-intact, empty dirs preserved). Reuse the existing containment guard verbatim — it already `bail!`s before any I/O when the output dir is inside the source dir.
  </action>
  <acceptance_criteria>
    - `cargo test --test flatten_move move_dryrun_default_no_write` passes (no `--force` → nothing written).
    - `cargo test --test flatten_move move_force_relocates` passes (copy→verify→delete; source files gone, source dirs remain).
    - `cargo test --test flatten_move move_abort_containment_snapshot_unchanged` passes (source tree identical after refusal).
    - `cargo test --test flatten_move move_abort_midbatch_copy_error_snapshot_unchanged` passes (no source deleted before a copy error).
    - `cargo test --test flatten_move move_json_plan_and_executed` passes (dry_run flips with --force; one parseable value, no 0x1B).
    - `cargo test --test flatten` (copy mode) stays green — non-`--move` behavior byte-identical to 08-01.
    - Source: `flatten/mod.rs` has `#[arg(long = "move")]`, a `safe_copy` → verify-size → `remove_file` sequence, and no emptied-dir pruning.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --test flatten_move --test flatten 2>&1 | tail -20</automated>
  </verify>
  <done>flatten --move is dry-run-default, --force runs copy→verify→delete, every abort path leaves the source tree byte-for-byte unchanged, empty dirs preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Adversarial code-review gate (mandatory — ROADMAP Code-review gate)</name>
  <files>src/commands/flatten/mod.rs, tests/flatten_move.rs</files>
  <action>Run the mandatory adversarial code review of the `flatten --move` destructive path AFTER Task 2's automated tests pass. Read the `--move` diff in `src/commands/flatten/mod.rs` against the destructive safety checklist below and confirm every `tests/flatten_move` snapshot-the-tree-unchanged abort-path test is green. This is a blocking human gate that verifies the data-loss surface; it does not write code.</action>
  <what-built>
    `flatten --move` destructive relocation: dry-run default, `--force` to execute, copy→verify→delete reusing `safe_copy` (create-new) + the containment guard + collision-rename, with per-abort-path snapshot-the-tree-unchanged tests.
  </what-built>
  <how-to-verify>
    Review the diff against the destructive safety checklist (the v1 Phase-3 bulk-rename gate, applied to flatten):
    1. Confirm `--move` defaults to a dry-run that writes NOTHING (run `box flatten <src> <out> --move` on a scratch tree; the output dir stays empty, the source unchanged).
    2. Confirm the delete is strictly AFTER a successful `safe_copy` + a size-verify of the destination — read the code path and confirm no `remove_file` can run on a failed/short copy.
    3. Confirm all snapshot-the-tree-unchanged tests are GREEN for every abort path: `cargo test --test flatten_move` (containment refusal, dry-run, mid-batch copy error).
    4. Confirm empty source directories are left in place (only files relocate).
    5. Confirm `--move --json` keeps stdout pure (one parseable value, no `0x1B`) and `dry_run` reflects the mode.
    Look adversarially for: a cross-volume path that could delete before confirming; a TOCTOU window between verify and delete; a containment-guard bypass under `--move`.
  </how-to-verify>
  <acceptance_criteria>
    - All `tests/flatten_move` snapshot-unchanged tests pass for every abort path.
    - Reviewer confirms the copy→verify→delete ordering and the dry-run-default + `--force` gate by reading the code.
    - No source file is ever deleted on a failed/short copy.
  </acceptance_criteria>
  <verify>
    <human-check>Reviewer confirms the destructive safety checklist (dry-run default, copy→verify→delete ordering, abort-leaves-source-intact, empty dirs preserved) after `cargo test --test flatten_move` is green.</human-check>
  </verify>
  <done>The adversarial code-review checklist is satisfied; all flatten_move abort-path snapshot tests pass; reviewer approves.</done>
  <resume-signal>Type "approved" once the destructive safety checklist is satisfied, or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI src/out + `--force` → filesystem mutation | A destructive relocation that deletes source files |
| Copy → verify → delete | The data-loss-critical sequence; a failure between steps must never lose data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-04 | Tampering / data loss | delete-before-confirm in `--move` | mitigate | `safe_copy` is create-new (never clobbers) → verify dest exists + size matches → only then `remove_file`; snapshot-tree-unchanged test for every abort path (Pitfall 5) |
| T-8-04-CONTAIN | Tampering | output dir inside source dir under `--move` | mitigate | Reuse the existing containment guard `bail!` (lowercased `starts_with`, NTFS case-fold) BEFORE any I/O; abort-snapshot test |
| T-8-04-TOCTOU | Tampering | TOCTOU between verify and delete | accept | Single-process local CLI; the window is the same as the existing copy path; verify-immediately-before-delete bounds it. Documented as accepted (low value, local user context) |
| T-8-04-INJ | Tampering | collision-renamed dest escaping the output dir | mitigate | Reuse `rename::encode_relative` (no `\`/`/`/`..` survives) + `safe_copy` create-new; covered by `tests/flatten::encode_no_separator` |
| T-8-04-ANSI | Information Disclosure | `--json` purity under the destructive path | mitigate | `is_json_on()` fork FIRST; `move_json_plan_and_executed` asserts no `0x1B`, one value, correct `dry_run` |
</threat_model>

<verification>
- `cargo test` (full suite) green; `cargo clippy --all-targets -- -D warnings` clean.
- Every `tests/flatten_move` abort path proves the source tree byte-for-byte unchanged (Code-review gate).
- Copy mode (`box flatten` without `--move`) output byte-identical to 08-01.
- The adversarial code-review checkpoint is approved.
</verification>

<success_criteria>
- FLAT-V2-02 deliverable: `--move` dry-run default + `--force` copy→verify→delete; every abort path leaves the source tree unchanged; empty dirs preserved; clean `--json`.
- The mandatory code-review gate (dry-run default, `--force`, abort-leaves-source-intact, snapshot tests per abort path) is satisfied and approved.
</success_criteria>

<output>
Create `.planning/phases/08-filesystem-depth/08-04-SUMMARY.md` when done.
</output>
