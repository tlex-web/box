# Requirements Archive: box — Rust CLI Toolbox (v1.0)

**Milestone:** v1.0 — Full Toolbox
**Status:** ✅ SHIPPED 2026-06-24
**Archived from:** `.planning/REQUIREMENTS.md`

> This is the frozen v1.0 requirements record. All 34 v1 requirements shipped and
> were verified (per-phase VERIFICATION: 5/5, 24/24, 4/4, 16/16) with human-UAT
> clearance on four of five phases. A fresh `REQUIREMENTS.md` is created for the
> next milestone via `/gsd:new-milestone`.

**Core Value:** The toolbox must be globally available and instantly usable from PowerShell 7 — type `box <command>` from anywhere and the tool just works.

## v1 Requirements — Outcomes

All 34 requirements **Complete & Validated**. Outcome notes record any adjustment from the original wording.

### Foundation & Distribution

- [x] **FOUND-01**: `box <command>` for any subcommand; `box --help` lists all with one-line descriptions; `box <cmd> --help` per-command usage — _validated, single clap-derive registry_
- [x] **FOUND-02**: `box --version` prints the semantic version — _validated_
- [x] **FOUND-03**: Shared UX — machine output to stdout, messages/errors to stderr, exit codes (0/1/2) — _validated; main() owns the strict 0/1/2 policy via try_parse_
- [x] **FOUND-04**: Color/ANSI auto-disables off-TTY and on `NO_COLOR`/`--no-color`; ANSI enabled in PS7 — _validated; gated on our own COLOR_ON flag, byte-identical-minus-ANSI when piped_
- [x] **FOUND-05**: Clear stderr message instead of panicking on bad input/missing files — _validated across all 23 commands_
- [x] **FOUND-06**: Safe Windows path normalization (no `\\?\` UNC leakage), edge-case paths handled — _validated; dunce::canonicalize everywhere_
- [x] **FOUND-07**: Global install via `install.ps1` (build → copy to bin dir → idempotent user PATH) — _validated, human-verified in PS7_
- [x] **FOUND-08**: `install.ps1` refreshes session PATH and smoke-tests with `box --help` — _validated, same-session verified_

### flatten (anchor)

- [x] **FLAT-01**: Recursive copy of a folder tree into one flat output folder, originals untouched, no output subdirs — _validated_
- [x] **FLAT-02**: Collision → encode source path (`docs_sub_report.txt`); double-collision → numeric suffix — _validated; silent-overwrite edge cases hardened post-review_
- [x] **FLAT-03**: `--dry-run` previews without writing; completion summary reports copies + collisions — _validated_
- [x] **FLAT-04**: Refuses output-inside-source loop, skips symlinks safely, preserves timestamps — _validated_

### Filesystem Tools

- [x] **HASH-01**: Hash a file (default SHA-256), choose algorithm, hash stdin, verify (exit 0/1), `HASH  filename` format — _validated; SHA-256 default per binding contract, BLAKE3 via `--algo` (BLAKE3-default → HASH-V2-01)_
- [x] **TREE-01**: Box-drawing tree, optional sizes, depth limit, colored dirs vs files, count summary — _validated_
- [x] **DU-01**: Size-sorted disk usage (biggest first), human sizes, depth limit, top-N truncation — _validated_
- [x] **DUPE-01**: Find duplicates by content (size pre-filter then hash), grouped, wasted-space summary — _validated; strictly read-only, rayon-parallel BLAKE3_
- [x] **RENM-01**: Regex bulk-rename with capture groups; dry-run default, `--force` to execute, collision abort-before-any — _validated; pre-flight is the backstop vs std::fs::rename silent overwrite_

### Dev Utilities

- [x] **UUID-01**: Generate one or more UUIDs (v4 default), lowercase, one per line — _validated_
- [x] **JSON-01**: Pretty-print JSON from stdin/file with color, validate (exit 0/1 + line/column), `--compact` minify — _validated; preserve_order, arbitrary_precision OFF_
- [x] **B64-01**: base64 encode/`--decode` from file/stdin, no line-wrapping, URL-safe option — _validated; byte-exact round-trip_
- [x] **EPOC-01**: Unix timestamp → human date (local + UTC); date string → timestamp; no-arg → now — _validated_
- [x] **COLR-01**: Convert hex ↔ RGB, all representations at once, in-terminal swatch — _validated; HSL also supported, gated swatch_
- [x] **CLIP-01**: Pipe stdin → clipboard and clipboard → stdout with correct Windows Unicode (bypassing clip.exe) — _validated; arboard/Win32, no elevation, Unicode round-trip human-verified in PS7_
- [x] **PASS-01**: CSPRNG passwords (length + charset control) and passphrases (`--words N`), `--count N` bulk, stdout only — _validated; OsRng, unbiased selection, EFF wordlist_

### Terminal Visuals

- [x] **QR-01**: Render a scannable QR code for text/URL using Unicode half-blocks, arg or stdin — _validated; qrcode 0.14 Dense1x2, phone-scan human-verified in PS7_
- [x] **LOL-01**: Pipe text through `lolcat` for a smooth truecolor rainbow; color auto-disables off-TTY — _validated; per-Unicode-scalar sine gradient, unconditional ANSI strip_
- [x] **MTRX-01**: Full-terminal Matrix digital-rain, exits cleanly on Ctrl+C, restores terminal state — _validated; ~20 FPS, RAII restore, human-verified_
- [x] **ASCI-01**: Convert a PNG/JPEG image to ASCII art fitted to terminal width — _validated; monochrome v1 (color → VIS-V2-01)_

### Fun

- [x] **COW-01**: ASCII speech bubble (cowsay) for arg or stdin, word-wrapped — _validated; fixed 40-col wrap + hard-break_
- [x] **FORT-01**: Random fortune/quote, different each run, fitted to terminal — _validated_
- [x] **8BAL-01**: Yes/no question → classic random Magic 8-Ball response (question optional) — _validated; canonical 20_
- [x] **ROST-01**: Random programmer roast, different each run — _validated_

### Whimsy with Utility

- [x] **POMO-01**: Pomodoro timer, in-place countdown, break/long-break modes, clean Ctrl+C cancel, Windows toast on completion — _validated; raw-mode countdown + tauri-winrt-notification toast, cancel→no-toast, human-verified in PS7_
- [x] **WTHR-01**: Fetch current weather for city/lat-lon (temp, conditions, wind, humidity) via keyless API, metric/imperial, graceful offline — _validated; Open-Meteo, server-side unit labels, offline error tested via fixtures_

## v2 Requirements (Deferred — carried to next milestone)

Per-command differentiators surfaced in research, tracked but not in v1 scope.

- **FLAT-V2-01**: `--separator`, `--include-hidden`, `--extensions` filter, `--json` mapping, progress bar, `--move`
- **HASH-V2-01**: BLAKE3 default, multi-file hashing, progress bar, `--json`
- **DUPE-V2-01**: Multi-stage prefix/suffix/full hashing, `--json`, `--delete`, hardlink awareness
- **RENM-V2-01**: Case transforms, sequential numbering, `--backup`, `--json` plan
- **TREE-V2-01**: `.gitignore` respect, hidden/dirs-only/ignore-pattern flags, `--json`, sort-by-size
- **DU-V2-01**: Percentage bars, color-coded size ranges, exclude patterns, `--json`, apparent-size
- **DEV-V2-01**: `--clip` across passgen/uuid/color; UUID v7 + format flags; epoch relative/timezone; JSON `--sort-keys`; named CSS colors + HSL
- **VIS-V2-01**: lolcat `--animate`/`--freq`/`--seed`; matrix color/speed/charset; QR `--save`/error-correction; ascii color/braille/invert
- **FUN-V2-01**: cowsay figures/think mode; fortune categories; 8ball ASCII art + sentiment color; roast `--language`
- **SYS-V2-01**: pomodoro session counter/auto-break/sound/`--label`; weather `--forecast`/`--json`/cache/stored location
- **PASS-V2-01**: passgen entropy estimate, `--no-similar`, `--separator`, `--clip`

## Out of Scope (v1)

| Feature | Reason |
|---------|--------|
| Cross-platform builds (macOS/Linux) | Target is Windows PowerShell 7 |
| Scoop/winget packaging | `install.ps1` is the v1 distribution path |
| `flatten` move/overwrite-silently modes | Data-loss risk; copy + collision-rename only |
| `dupes` auto-delete / interactive selection | Catastrophic if wrong; print groups, user decides |
| `bulk-rename` execute-by-default / undo log | Dry-run-first + `--force` is the safety model |
| `qr` decode/scan mode | Different tool; doubles complexity |
| `json` full jq query language | jq already exists; pretty-print + validate only |
| `du`/`dupes` interactive TUI | Scope creep; one-shot commands only |
| AI/network-backed roast/fortune/8ball | Network dependency for a fun toy; curated local lists |
| `ascii` video/GIF/URL input | Complex; file input, still images only |
| `weather` API key requirement, radar/maps | Keyless Open-Meteo; simple data display only |
| `clip` binary/image clipboard | Text only in v1 |
| Logging/data mixed on stdout | Breaks pipes; data→stdout, messages→stderr |

## Traceability — Final Status

| Requirement | Phase | Status |
|-------------|-------|--------|
| FOUND-01..08 | Phase 1 | Complete |
| FLAT-01..04 | Phase 1 | Complete |
| UUID-01, B64-01, EPOC-01, COLR-01, PASS-01, COW-01, FORT-01, 8BAL-01, ROST-01 | Phase 2 | Complete |
| HASH-01, TREE-01, DU-01, DUPE-01, RENM-01 | Phase 3 | Complete |
| LOL-01, MTRX-01, ASCI-01, JSON-01 | Phase 4 | Complete |
| QR-01, CLIP-01, POMO-01, WTHR-01 | Phase 5 | Complete |

**Coverage:** 34/34 v1 requirements complete (100%). Mapped: 34. Unmapped: 0.

---
*Requirements defined: 2026-06-22 · Archived: 2026-06-24 at v1.0 milestone close*
