---
phase: 04-terminal-visuals
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - src/cli.rs
  - src/main.rs
  - src/commands/mod.rs
  - src/commands/matrix/mod.rs
  - tests/matrix.rs
autonomous: false
requirements: [MTRX-01]
nyquist_compliant: true

must_haves:
  truths:
    - "D-00: matrix follows the established slice — Matrix unit variant in src/cli.rs swapped for Matrix(MatrixArgs), the not_implemented(\"matrix\") arm at src/main.rs:77 deleted, messages→stderr, exit 0/1/2, smoke test + (in-module) unit tests; unit tests run via cargo test --bin box (NOT --lib); crossterm/owo-colors already present so NO Cargo.toml change"
    - "D-07: glyph set is pure halfwidth katakana (U+FF66–U+FF9D) — every glyph is East-Asian-Width Narrow / single-cell so the column/drop grid math stays clean; NO fallback flag / charset option (those are VIS-V2-01); the CJK-font requirement (tofu on bare Cascadia Mono = documented cosmetic limitation, not a bug) is noted in --help"
    - "D-08: animation loop on crossterm 0.29 — enable_raw_mode() → execute!(EnterAlternateScreen, cursor::Hide) → terminal::size(); per-column drop state (head row, possibly negative-staggered start; randomized trail length ~6–20; per-column speed; reset to a fresh negative start when the head clears bottom+trail); the WHOLE frame is rendered into one buffer with queue! then stdout.flush() EXACTLY ONCE per frame (STATE.md hard pitfall — per-character flush ≈ 5 FPS); head cell bright, trail fades green→dark by distance, erase the cell past the tail"
    - "D-09: frame rate ≈ 20 FPS and the frame timer IS the input poll — event::poll(Duration::from_millis(50)) gates both the frame and the keypress read; single-threaded, no background thread, no separate sleep"
    - "D-10: exit on Ctrl+C (KeyCode::Char('c') + KeyModifiers::CONTROL — in raw mode crossterm delivers Ctrl+C as a KeyEvent, NOT SIGINT), q, and Esc; filter KeyEventKind::Press ONLY (Windows fires Press AND Release → otherwise double-counts); restoration via an RAII Drop guard built right after setup whose drop runs execute!(cursor::Show, LeaveAlternateScreen) then disable_raw_mode() (errors ignored) — fires on normal return, ? early-return, and unwinding; NO ctrlc/signal crate"
    - "User runs box matrix and sees a full-terminal green digital-rain animation that fills the terminal width; pressing Ctrl+C exits cleanly and restores the cursor and terminal state with no visual artifacts (ROADMAP Phase 4 success criterion 2)"
  artifacts:
    - path: "src/commands/matrix/mod.rs"
      provides: "MatrixArgs + RunCommand impl + RAII RawGuard + pure drop/fade model + katakana glyph table + single-flush render loop"
      contains: "impl Drop for"
    - path: "tests/matrix.rs"
      provides: "MTRX-01 smoke: binary enters/exits cleanly (fed q or non-TTY stdin, non-hanging)"
  key_links:
    - from: "src/main.rs"
      to: "MatrixArgs::run"
      via: "Commands::Matrix(args) => args.run()"
      pattern: "Commands::Matrix\\(args\\) => args.run\\(\\)"
    - from: "src/commands/matrix/mod.rs"
      to: "stdout.flush"
      via: "single flush per frame after queue!"
      pattern: "flush\\(\\)"
    - from: "src/commands/matrix/mod.rs"
      to: "KeyEventKind::Press"
      via: "Windows double-fire filter before matching the exit key"
      pattern: "KeyEventKind::Press"
---

<objective>
Ship `box matrix` as a complete vertical slice (MTRX-01): a full-terminal green digital-rain animation (pure halfwidth katakana) at ~20 FPS that exits cleanly on Ctrl+C / q / Esc and restores the cursor + terminal state with no artifacts. The pure drop/fade model and glyph table are unit-tested; the enter/exit contract is smoke-tested; the visual rain is human-verified.

Purpose: Final and hardest Phase-4 slice — the only command that owns terminal raw-mode state. Reuses the already-present crossterm (no Cargo.toml change).
Output: A live `box matrix` command with an RAII restore guard, unit-tested pure helpers, a smoke test, and a human-verify checkpoint for the animation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-terminal-visuals/04-CONTEXT.md
@.planning/phases/04-terminal-visuals/04-RESEARCH.md
@.planning/phases/04-terminal-visuals/04-PATTERNS.md
@.planning/phases/04-terminal-visuals/04-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase + RESEARCH-verified crossterm 0.29 APIs. -->

From src/commands/mod.rs:
```
pub trait RunCommand { fn run(self) -> anyhow::Result<()>; }
```

crossterm 0.29 (already a dep; every symbol VERIFIED docs.rs for this pinned version):
```
crossterm::terminal::{enable_raw_mode, disable_raw_mode}() -> Result<()>
crossterm::terminal::{EnterAlternateScreen, LeaveAlternateScreen}    // command structs for execute!/queue!
crossterm::terminal::size() -> Result<(u16, u16)>                    // (columns, rows)
crossterm::cursor::{Hide, Show, MoveTo}                              // queue!(out, MoveTo(x,y), ...)
crossterm::event::poll(Duration) -> Result<bool>                    // the 50ms poll IS the 20-FPS frame timer (D-09)
crossterm::event::read() -> Result<Event>                          // call only when poll() == true
crossterm::event::{Event, KeyEvent, KeyCode, KeyModifiers, KeyEventKind}
//   KeyEventKind has Press / Repeat / Release → filter Press ONLY (Windows double-fire, D-10)
crossterm::{queue!, execute!}                                       // queued cmds run on flush; flush ONCE/frame
```

rand 0.9 (already a dep) for per-column random start/trail/speed and per-frame head glyph:
```
use rand::Rng;  // rng().random_range(...) / IndexedRandom::choose for the katakana table
```

The locked RAII guard (D-10 / RESEARCH:202-215) + loop skeleton (RESEARCH:364-385):
```
struct RawGuard;
impl Drop for RawGuard {
    fn drop(&mut self) {
        let mut out = std::io::stdout();
        let _ = crossterm::execute!(out, crossterm::cursor::Show,
                                    crossterm::terminal::LeaveAlternateScreen);
        let _ = crossterm::terminal::disable_raw_mode();
    }
}
```
Katakana range: U+FF66..=U+FF9D (halfwidth, all single-cell). Quit = `KeyCode::Char('q') | KeyCode::Esc`, or `KeyCode::Char('c')` with `KeyModifiers::CONTROL`.
</interfaces>

<!-- Analogs — the only existing crossterm::terminal::size() caller (import style) + the pure-helper shape (read in full): -->
@src/core/output.rs
@src/commands/cowsay/mod.rs
@tests/color.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 smoke test + implement matrix slice — RawGuard, drop/fade model, katakana table, single-flush loop (MTRX-01)</name>
  <read_first>
    - src/core/output.rs:212-220 (the ONLY existing `crossterm::terminal::size()` caller — the crossterm import style to mirror)
    - src/commands/cowsay/mod.rs:70-154 + :156-249 (the pure-helper extraction + `#[cfg(test)]` shape)
    - tests/color.rs:13-32 (the `assert_cmd` runner shape for the smoke test)
    - src/cli.rs:69-70 (the `Matrix` unit variant) and src/cli.rs:44-48 (data-carrying variant shape)
    - src/main.rs:77 (the `Commands::Matrix => Err(not_implemented("matrix"))` arm) and src/main.rs:65 (the `=> args.run()` precedent)
    - src/commands/mod.rs:7-22 (add `pub mod matrix;` in alpha order)
    - .planning/phases/04-terminal-visuals/04-RESEARCH.md (Pattern 3 + Pitfalls 3/4/5/7 + the loop skeleton at :364-385; A1 on panic=abort)
    - Cargo.toml:57-61 (panic = "abort" in the release profile — keep the loop panic-free so the Drop guard is the real restore path)
  </read_first>
  <behavior>
    - Pure drop/fade model (unit-testable WITHOUT a terminal): a per-column `Drop { head: f32/i32, trail_len, speed }`; a `step()` advances the head by speed; a `fade(distance_behind_head, trail_len) -> u8` interpolates the green channel from bright (head, ~255) down to dark (~40) by distance; a `reset()` (or the step logic) puts the head at a fresh random NEGATIVE start once it clears bottom + trail. Unit-test: head advances monotonically; fade is bright at distance 0 and dark at distance == trail_len; a head past `rows + trail_len` triggers a reset to a negative row.
    - Pure katakana glyph table: a function/const yielding chars in U+FF66..=U+FF9D. Unit-test: every glyph is within that inclusive range AND `unicode_width::UnicodeWidthChar::width(c) == Some(1)` (all single-cell — keeps the grid math clean, D-07). (unicode-width is already a dep after plan 04-02.)
    - Quit logic (pure, testable): given a `KeyEvent`, return true iff `kind == Press` AND (code is q or Esc, or code is 'c' with CONTROL). Unit-test Press-vs-Release and the three quit keys.
    - The render loop itself is NOT unit-tested (terminal-bound) — covered by the smoke test + human-verify.
  </behavior>
  <action>
    Create `tests/matrix.rs` (smoke, integration): mirror the `tests/color.rs` runner — `Command::cargo_bin("box").arg("matrix")` with NON-TTY stdin (assert_cmd pipes stdin by default, so `terminal::size()`/`poll` still work but the process is launched non-interactively) and `.write_stdin("q")` plus a bounded `.timeout(Duration::from_secs(5))` posture so the test proves the binary STARTS and EXITS non-hanging without leaving artifacts (exit status is success or a clean non-hang). Keep the assertion tolerant: the goal is "does not hang / does not panic", not a pixel snapshot (MTRX-01's testable invariant is the enter/exit contract per RESEARCH:442/464). This test must compile against the stub first (RED), then pass once the loop honors `q`.
    Create `src/commands/matrix/mod.rs`: `//!` doc citing MTRX-01 + D-07/D-08/D-09/D-10, documenting the CJK-font requirement (tofu on bare Cascadia Mono is a cosmetic limitation, not a bug — D-07/Pitfall 7) in the clap `///` long help. Define `#[derive(Debug, Args)] pub struct MatrixArgs {}` (no flags in v1 — charset/speed/color are VIS-V2-01). Implement the `RawGuard` struct + `impl Drop` exactly per the locked shape (Show + LeaveAlternateScreen + disable_raw_mode, errors ignored). In `run`: `crossterm::terminal::enable_raw_mode()?;` → `let mut out = std::io::stdout(); crossterm::execute!(out, EnterAlternateScreen, cursor::Hide)?;` → `let _guard = RawGuard;` (armed immediately after setup, D-10) → `let (cols, rows) = crossterm::terminal::size()?;` → init per-column drop state (negative-staggered heads, random trail 6–20, per-column speed — exact ranges are discretion, RESEARCH OQ-2). Loop: (1) update drop state; (2) `queue!` the WHOLE frame into `out` — for each column draw the head cell bright (head RGB ~`(180,255,180)` via `.truecolor`), the trail green→dark (G interpolated 255→40 by distance), a fresh random katakana glyph for the head each frame, and erase the cell just past the tail with `queue!(out, MoveTo(x,y), Print(' '))`; (3) `out.flush()?` EXACTLY ONCE per frame (D-08 — NEVER per character, the STATE.md hard pitfall); (4) `if crossterm::event::poll(Duration::from_millis(50))?` (= 20-FPS timer, D-09) read the event, and `if let Event::Key(k) = ... { if k.kind == KeyEventKind::Press { ... quit on q/Esc/Ctrl+C ... break; } }` (Press-only filter, D-10/Pitfall 3). Keep the loop panic-free (no `.unwrap()` on terminal ops; propagate with `?`) so the Drop guard is the real restore path under `panic = "abort"` (RESEARCH A1). Add a `#[cfg(test)]` block covering the drop/fade model, the katakana table (range + width-1), and the quit-key logic.
    Wire the registry: `src/cli.rs` swap `Matrix,` (line 69-70) for `Matrix(crate::commands::matrix::MatrixArgs),` keeping the `/// Run the Matrix digital-rain screensaver effect` doc; `src/main.rs` replace line 77 with `Commands::Matrix(args) => args.run(),`; `src/commands/mod.rs` add `pub mod matrix;` in alpha order. Leave qr/clip/pomodoro/weather stub arms untouched (the `not_implemented` import stays — those four remain stubbed). NO Cargo.toml change (crossterm/owo-colors/rand/unicode-width all already present).
  </action>
  <verify>
    <automated>cargo test --bin box matrix &amp;&amp; cargo test --test matrix &amp;&amp; cargo clippy -- -D warnings &amp;&amp; cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --bin box matrix` exits 0: drop/fade model (head advance monotonic, fade bright→dark by distance, reset on clear-bottom), katakana table (all in U+FF66..=U+FF9D and all width-1), and quit-key logic (Press-only, q/Esc/Ctrl+C) unit tests green
    - `cargo test --test matrix` exits 0: the smoke test proves `box matrix` starts and exits non-hanging (fed `q` / non-TTY) without panicking
    - `src/commands/matrix/mod.rs` contains `impl Drop for RawGuard` (or equivalent) and exactly one `flush()` call site inside the frame loop (single-flush-per-frame, D-08)
    - The loop filters `KeyEventKind::Press` before matching the exit key (D-10) and has no `.unwrap()` on terminal ops
    - NO Cargo.toml change; the `matrix` not_implemented arm is gone from src/main.rs
    - `cargo clippy -- -D warnings` and `cargo fmt --check` pass
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the matrix animation + clean teardown (MTRX-01)</name>
  <what-built>
    `box matrix` — a full-terminal halfwidth-katakana green digital-rain animation at ~20 FPS with a bright head + green→dark trail, single-flush-per-frame rendering, and an RAII restore guard. Pure helpers (drop/fade, glyph table, quit logic) are unit-tested and the enter/exit contract is smoke-tested; the VISUAL animation and clean teardown are perceptual and must be confirmed by eye in PowerShell 7 (RESEARCH/VALIDATION Manual-Only Verifications).
  </what-built>
  <how-to-verify>
    1. Build + run in PowerShell 7: `cargo run --release -- matrix` (release matters — panic=abort + real FPS).
    2. Confirm the rain FILLS the terminal width (columns span the whole window) and animates smoothly (no ~5-FPS stutter — that would mean a per-character flush regression, D-08).
    3. Confirm the head cell is bright and the trail fades green→dark behind it; glyphs change each frame. (On the bare default Cascadia Mono the katakana may show as tofu □ — this is the DOCUMENTED cosmetic font limitation, D-07/Pitfall 7, NOT a bug. To see authentic glyphs use a CJK-capable font such as Cascadia Next JP.)
    4. Press Ctrl+C → the animation stops, the cursor REAPPEARS, the alternate screen is left, and your real terminal shows NO leftover rain/glyphs and NO hidden cursor (clean teardown — the MTRX-01 "no visual artifacts" criterion).
    5. Re-run and exit with `q`, then again with `Esc` — each must exit cleanly and restore the terminal identically.
    6. Confirm none of the three exit keys "double-fires" or feels sticky (the Press-only filter, D-10/Pitfall 3).
  </how-to-verify>
  <resume-signal>Type "approved" if the rain fills the width, animates smoothly, and Ctrl+C / q / Esc each restore the terminal cleanly with no artifacts; otherwise describe what you saw (stutter, leftover glyphs, missing cursor, sticky/double exit) and which key.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal raw-mode state | matrix is the only command that mutates terminal state (raw mode, alt screen, hidden cursor) and must restore it on every exit path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04M-01 | Denial of Service (left-broken terminal) | failure to restore raw mode / cursor / alt screen on exit | mitigate | RAII `RawGuard::drop` runs Show + LeaveAlternateScreen + disable_raw_mode on normal return, `?` early-return, and unwinding (D-10). Under `panic = "abort"` (Cargo.toml:61) a true panic won't unwind, so the alternate screen itself is the backstop — nothing persists in the real terminal (RESEARCH A1). Keep the loop panic-free. Verified by the smoke test (clean exit) + the human-verify teardown check. |
| T-04M-02 | (input) sticky/double exit | Windows Press+Release double-fire | mitigate | Filter `KeyEventKind::Press` only before matching the exit key (D-10/Pitfall 3). Verified by the quit-key unit test. |

No high-severity threats: local terminal tool, no network, no secrets, no file writes, no untrusted input parsing. No new crate (crossterm already audited/present) → no package-legitimacy checkpoint for this slice.
</threat_model>

<verification>
- `cargo test --bin box matrix` green (drop/fade model + katakana table + quit-key unit tests)
- `cargo test --test matrix` green (enter/exit smoke — non-hanging)
- `cargo test` (full suite) green — no regression
- `cargo clippy -- -D warnings` + `cargo fmt --check` clean
- Human-verify checkpoint: rain fills width, animates smoothly, Ctrl+C / q / Esc each restore cleanly with no artifacts
</verification>

<success_criteria>
- `box matrix` is live (the `not_implemented("matrix")` arm is deleted) — ALL FOUR Phase-4 stubs are now gone
- Full-terminal green katakana digital-rain at ~20 FPS, single flush per frame (D-08/D-09)
- Exits on Ctrl+C / q / Esc with a clean RAII teardown (cursor restored, alt screen left, no artifacts) on every path (D-10)
- Pure drop/fade + glyph table + quit logic unit-tested; visual animation human-verified
- No charset/speed/color flags (VIS-V2-01); CJK-font requirement documented in --help (D-07)
</success_criteria>

<output>
Create `.planning/phases/04-terminal-visuals/04-04-SUMMARY.md` when done
</output>
