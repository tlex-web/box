---
phase: 02-pure-transform-utilities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/core/input.rs
  - src/core/mod.rs
  - src/core/errors.rs
  - src/main.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "A command given no positional arg and piped stdin reads stdin to EOF"
    - "A command given a positional arg uses it directly (ignores stdin)"
    - "A command given no arg on an interactive TTY exits 2 with a usage message on stderr, never hangs"
    - "The four Phase-2 crates (uuid, base64, chrono, rand) resolve and the crate builds clean"
  artifacts:
    - path: "src/core/input.rs"
      provides: "read_input / read_input_bytes shared stdin-or-arg reader with TTY no-block guard (D-04/D-05)"
      contains: "pub fn read_input"
    - path: "src/core/errors.rs"
      provides: "BoxError::MissingInput variant for the exit-2 usage error"
      contains: "MissingInput"
    - path: "src/main.rs"
      provides: "Err-arm downcast that maps BoxError::MissingInput to exit code 2"
      contains: "MissingInput"
    - path: "Cargo.toml"
      provides: "uuid v4, base64, chrono (clock,std), rand 0.9 dependencies"
      contains: "uuid"
  key_links:
    - from: "src/main.rs"
      to: "core::errors::BoxError::MissingInput"
      via: "anyhow downcast_ref in the Err(e) arm → ExitCode::from(2)"
      pattern: "downcast_ref.*MissingInput|MissingInput"
    - from: "src/core/input.rs"
      to: "core::errors::BoxError::MissingInput"
      via: "branch-3 returns BoxError::MissingInput (not bail!)"
      pattern: "MissingInput"
    - from: "src/core/mod.rs"
      to: "src/core/input.rs"
      via: "pub mod input;"
      pattern: "pub mod input"
---

<objective>
Ship the one shared surface every stdin-consuming Phase-2 command needs: the
`core::input` reader (`read_input` / `read_input_bytes`) with the TTY-no-block
guard (D-04/D-05), the typed `BoxError::MissingInput` variant, and the `main.rs`
downcast that maps it to exit code 2 (D-04 branch 3 / RESEARCH Open Question 1 /
Pitfall 2). Also add the four locked Phase-2 crates to `Cargo.toml` once, here,
so the Wave-2 command plans never touch the manifest.

Purpose: This is the foundation slice. base64/epoch/color/cowsay all call
`core::input`; without the exit-2 wiring the no-input case would wrongly map to
exit 1. Centralizing the crate adds removes `Cargo.toml` from every later plan's
file-ownership footprint, keeping Wave-2 command slices parallel-clean.

Output: `src/core/input.rs` (new, registered), `BoxError::MissingInput`,
`main.rs` exit-2 downcast, four crates in `Cargo.toml`, and `core::input` unit
tests proving the three branches via injected `is_tty`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pure-transform-utilities/02-CONTEXT.md
@.planning/phases/02-pure-transform-utilities/02-RESEARCH.md
@.planning/phases/02-pure-transform-utilities/02-PATTERNS.md
@.planning/phases/02-pure-transform-utilities/02-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live repo — do not re-explore. -->

Existing TTY-gate shape to mirror, from src/core/output.rs:42-44:
```rust
pub fn color_enabled(no_color_flag: bool) -> bool {
    !no_color_flag && std::env::var_os("NO_COLOR").is_none() && std::io::stdout().is_terminal()
}
```

Existing typed-error enum to extend, from src/core/errors.rs:6-12:
```rust
#[derive(Debug, Error)]
pub enum BoxError {
    #[error("'{cmd}' is not yet implemented — coming in a future release")]
    NotImplemented { cmd: &'static str },
}
```

Existing main.rs Err-arm (the single exit-code owner), src/main.rs:86-94 — currently
maps EVERY Err to exit 1; this plan adds a downcast for MissingInput → 2:
```rust
match result {
    Ok(()) => ExitCode::SUCCESS,
    Err(e) => {
        eprintln!("error: {e:#}");
        ExitCode::from(1)
    }
}
```

core/mod.rs:7-9 module registry (add `pub mod input;`):
```rust
pub mod errors;
pub mod fs;
pub mod output;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the four locked crates and register the input module</name>
  <files>Cargo.toml, src/core/mod.rs</files>
  <read_first>
    - Cargo.toml:11-35 (the section-commented [dependencies] block + existing [dev-dependencies])
    - src/core/mod.rs:1-9 (module doc-comment style + the `pub mod` registry to extend)
    - .planning/phases/02-pure-transform-utilities/02-RESEARCH.md:109-118 (Installation block + chrono/uuid feature notes)
  </read_first>
  <action>
    Add four dependencies to the `[dependencies]` block in Cargo.toml, following the section-comment style already present:
    `uuid = { version = "1.23.3", features = ["v4"] }` (v4 auto-enables rng+std+getrandom — no extra features),
    `base64 = "0.22.1"`,
    `chrono = { version = "0.4.45", default-features = false, features = ["clock", "std"] }` (trims oldtime/wasmbind per D-01 while keeping `Local` — `clock` transitively pulls iana-time-zone),
    `rand = "0.9"`.
    Do NOT add `rand_core` yet — verify whether `use rand::TryRngCore` resolves through rand 0.9 re-exports during the passgen plan (RESEARCH A1); it is not needed in this plan.
    Do NOT re-add any [dev-dependencies] — assert_cmd/predicates/assert_fs/tempfile/trycmd/insta are all already present (Cargo.toml:29-35).
    Register the new input module by adding `pub mod input;` to src/core/mod.rs (after `pub mod fs;`), and extend the module doc-comment with a one-line mention that `input` owns the shared stdin/arg reader, matching the existing doc style.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0 (the four crates resolve against the committed Cargo.lock / crates.io index).
    - `grep -n 'uuid\|base64\|chrono\|rand' Cargo.toml` shows all four under `[dependencies]` with the exact versions/features above (chrono with `default-features = false, features = ["clock","std"]`).
    - `grep -c 'pub mod input' src/core/mod.rs` returns 1.
    - No `rand_core` line in Cargo.toml (deferred to the passgen plan).
  </acceptance_criteria>
  <done>The four Phase-2 crates compile into the binary and `core::input` is a registered module. No later plan needs to touch Cargo.toml.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add BoxError::MissingInput and the main.rs exit-2 downcast</name>
  <files>src/core/errors.rs, src/main.rs</files>
  <read_first>
    - src/core/errors.rs:1-12 (the BoxError enum + thiserror `#[error("…")]` style to extend)
    - src/main.rs:60-95 (the dispatch match + the single Err-arm exit-code owner at :86-94)
    - src/commands/stub.rs:1-10 (how NotImplemented is constructed into anyhow::Error — the typed-variant-into-anyhow pattern)
    - .planning/phases/02-pure-transform-utilities/02-RESEARCH.md:273 (the exit-code landmine note), :390-394 (Pitfall 2)
    - .planning/phases/02-pure-transform-utilities/02-PATTERNS.md:238-247 (the MissingInput→exit-2 net-new dispatch description)
  </read_first>
  <action>
    Add a unit `MissingInput` variant to `BoxError` in errors.rs following the existing `#[error("…")]` style, with the message `no input: pass an argument or pipe data` (D-04 branch 3 wording; Claude's discretion on exact phrasing per D-04, but keep it a usage hint). It carries no fields.
    In main.rs, modify ONLY the `Err(e)` arm of the final `match result` (main.rs:88-93): before printing, downcast the anyhow error to `BoxError` via `e.downcast_ref::<crate::core::errors::BoxError>()` and, when it is `Some(BoxError::MissingInput)`, print `error: {e:#}` to stderr and return `ExitCode::from(2)`; otherwise keep the existing `error: {e:#}` to stderr + `ExitCode::from(1)` behavior unchanged. This makes main.rs the single owner of the exit-2-for-usage mapping (mirrors how NotImplemented→1 is the only other typed mapping). Do NOT use `std::process::exit` and do NOT change the clap pre-dispatch exit-2 path (main.rs:42-51) — that is a separate, already-correct path.
    Add a `#[cfg(test)]` test in main.rs is not possible (binary crate test harness); instead the exit-2 behavior is proved by the input integration test in Task 3. Keep the `not_implemented` import (main.rs:22) — stubs for Phases 3-5 still use it.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -3 && cargo clippy -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'MissingInput' src/core/errors.rs` returns ≥1 (the variant + its `#[error]`).
    - Source assertion: `grep -c 'MissingInput' src/main.rs` returns ≥1 (the downcast match arm).
    - Source assertion: `grep -c 'downcast' src/main.rs` returns ≥1 and the existing `ExitCode::from(2)` count in main.rs is now ≥2 (clap path + new MissingInput path).
    - `cargo clippy -- -D warnings` exits 0 (no unused-variant or dead-code warning — the variant is referenced by main.rs and will be by core::input in Task 3).
  </acceptance_criteria>
  <done>A `BoxError::MissingInput` returned from any command surfaces as exit code 2 with a usage message on stderr; all other errors still map to exit 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement core::input (read_input / read_input_bytes) with unit + integration tests</name>
  <files>src/core/input.rs</files>
  <behavior>
    - read_input(Some("hello")) → Ok("hello") (arg branch, stdin never touched).
    - read_input(Some("-")) with is_tty=false → reads piped stdin to EOF as UTF-8 String.
    - read_input(None) with injected is_tty=true → Err that downcasts to BoxError::MissingInput (no read, no hang).
    - read_input(None) with is_tty=false → reads piped stdin to EOF.
    - read_input_bytes mirrors the three branches but returns Vec<u8> via read_to_end (binary-exact, no UTF-8 validation) — D-05/D-06.
    - The is_tty decision is injectable so branch 3 is unit-testable without a real TTY (RESEARCH:540-543).
  </behavior>
  <read_first>
    - src/core/output.rs:14,42-44 (the `std::io::IsTerminal` + `stdin/stdout().is_terminal()` gate shape to mirror)
    - src/core/errors.rs:1-12 (the MissingInput variant added in Task 2 — the branch-3 return type)
    - .planning/phases/02-pure-transform-utilities/02-RESEARCH.md:233-271 (Pattern 2: read_input/read_input_bytes reference impl), :540-543 (injected-is_tty testing strategy), :402-406 (Pitfall 4: write_stdin bypasses the shell — what the test does/does not prove)
    - .planning/phases/02-pure-transform-utilities/02-PATTERNS.md:249-259 (core::input net-new surface + injection pattern)
  </read_first>
  <action>
    Create src/core/input.rs with two public functions and a shared inner helper so the TTY decision is injectable (Claude's discretion on whether the bytes/string helpers share an inner reader — D-04 discretion note):
    `pub fn read_input(arg: Option<String>) -> anyhow::Result<String>` — UTF-8 text (cowsay, epoch, color).
    `pub fn read_input_bytes(arg: Option<String>) -> anyhow::Result<Vec<u8>>` — binary-exact via `read_to_end` (base64), NO UTF-8 validation (D-05/D-06).
    Layered precedence per D-04: (1) `arg` is `Some(s)` and `s != "-"` → use it directly; (2) `arg` is `None` or `Some("-")` AND `std::io::stdin().is_terminal()` is false (piped) → read stdin to EOF (`read_to_string` for text, `read_to_end` for bytes); (3) `arg` is `None`/`"-"` AND stdin IS an interactive TTY → return `crate::core::errors::BoxError::MissingInput` as `anyhow::Error` (use `.into()`, NOT `anyhow::bail!` — bail produces a plain anyhow error that cannot downcast to BoxError, so main.rs's Task-2 downcast would map it to exit 1 instead of 2). Use `std::io::IsTerminal` and `std::io::Read` (mirror output.rs:14). `.context(...)` the stdin read at the I/O boundary.
    Factor an inner function taking `is_tty: bool` (and optionally a reader) so branch 3 is unit-testable without a real terminal — the public wrappers supply `std::io::stdin().is_terminal()` and the real stdin. Design note for the deferred `--file PATH` (D-06, Phase 3): keep the input-source decision in one place so a file branch slots in ahead of the stdin branch without reshaping the signatures.
    Add `#[cfg(test)] mod tests` covering: arg branch returns the arg verbatim; the inner fn with `is_tty=true` and `arg=None` returns an error that `downcast_ref::<BoxError>()` matches as `MissingInput`; a piped-bytes path (inner fn with a `&[u8]` reader and `is_tty=false`) returns the exact bytes including a non-UTF-8 byte (e.g. `0xFF`) for `read_input_bytes`.
  </action>
  <verify>
    <automated>cargo test input:: 2>&1 | tail -8 && cargo clippy -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test input::` exits 0 with the three unit tests (arg-branch, missing-input-on-tty, piped-bytes-exact) passing.
    - Behavior assertion: the missing-input test asserts the error `downcast_ref::<crate::core::errors::BoxError>()` is `Some(BoxError::MissingInput)` (proving the exit-2 path is reachable — NOT a plain `anyhow::bail!`).
    - Behavior assertion: the bytes test feeds a buffer containing `0xFF` and asserts the returned `Vec<u8>` is byte-identical (proving `read_to_end`, not `read_to_string`).
    - Source assertion: `grep -c 'bail!' src/core/input.rs` returns 0 for branch 3 (MissingInput is returned via `.into()`, not `bail!`).
    - `cargo clippy -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`core::input` provides both reader shapes, never hangs on an interactive TTY, returns the typed exit-2 error on branch 3, and is byte-exact for binary input — verified by unit tests including the downcast and the 0xFF round-trip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin/arg → command | Untrusted text/bytes enter via positional arg or piped stdin; consumed by `core::input`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service (self) | `core::input` branch 3 | mitigate | No-arg on an interactive TTY returns `BoxError::MissingInput` (exit 2) instead of blocking on `read_to_string` — the hang footgun is closed by the `is_terminal()` guard (D-04). |
| T-02-02 | Tampering / data corruption | `read_input_bytes` | mitigate | Binary input read via `read_to_end` into `Vec<u8>` with no UTF-8 validation so bytes are not mangled (D-05/D-06); unit-tested with a `0xFF` byte. |
| T-02-SC | Tampering | crates.io installs (uuid, base64, chrono, rand) | accept | All four are `[VERIFIED: crates.io registry]` canonical crates with slopcheck `[OK]` and a committed Cargo.lock (RESEARCH Package Legitimacy Audit). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages — no blocking human checkpoint required. |

Surface note: this foundation slice has no network, auth, session, or storage; the only real risk is the TTY-hang and byte-mangling footguns, both mitigated above.
</threat_model>

<verification>
- `cargo build` clean with the four new crates.
- `cargo test input::` green (3 unit tests).
- `cargo clippy -- -D warnings` clean; `cargo fmt --check` clean.
- `core::input` branch 3 returns a `BoxError::MissingInput` that main.rs maps to exit 2 (proved structurally by the downcast unit test; an end-to-end exit-2 integration test lands with the first stdin-consuming command in Plan 02 base64).
</verification>

<success_criteria>
- `src/core/input.rs` exists, is registered in `core/mod.rs`, and exposes `read_input` + `read_input_bytes`.
- `BoxError::MissingInput` exists and `main.rs` maps it to exit code 2; all other errors still exit 1.
- The four locked crates (uuid v4, base64, chrono clock/std, rand 0.9) are in Cargo.toml and the binary builds.
- No Cargo.toml edits remain for any Wave-2 plan.
</success_criteria>

<output>
Create `.planning/phases/02-pure-transform-utilities/02-01-SUMMARY.md` when done.
</output>
