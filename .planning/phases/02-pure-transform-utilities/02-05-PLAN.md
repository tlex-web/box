---
phase: 02-pure-transform-utilities
plan: 05
type: execute
wave: 5
depends_on: ["02-01", "02-04"]
files_modified:
  - src/commands/fortune/mod.rs
  - src/commands/eight_ball/mod.rs
  - src/commands/roast/mod.rs
  - src/data/fortunes.txt
  - src/data/roasts.txt
  - src/commands/mod.rs
  - src/cli.rs
  - src/main.rs
  - tests/fortune.rs
  - tests/eight_ball.rs
  - tests/roast.rs
autonomous: true
requirements: ["FORT-01", "8BAL-01", "ROST-01"]
must_haves:
  truths:
    - "User runs `box fortune` and gets a random aphorism from the bundled list, different across repeated runs"
    - "User runs `box 8ball \"Will it work?\"` (question optional) and gets one of the classic 20 Magic 8-Ball answers, different across repeated runs"
    - "User runs `box roast` and gets a random programmer roast, different across repeated runs"
  artifacts:
    - path: "src/commands/fortune/mod.rs"
      provides: "FortuneArgs implementing RunCommand; include_str! list + rand::rng() + IndexedRandom::choose; pub(crate) entries() for membership tests"
      contains: "IndexedRandom"
    - path: "src/commands/eight_ball/mod.rs"
      provides: "EightBallArgs { question } implementing RunCommand; const 20 answers + rand::rng()"
      contains: "EIGHT_BALL_ANSWERS"
    - path: "src/commands/roast/mod.rs"
      provides: "RoastArgs implementing RunCommand; include_str! list + rand::rng()"
      contains: "IndexedRandom"
    - path: "src/data/fortunes.txt"
      provides: "~50-150 CC0/public-domain aphorisms"
      contains: "."
    - path: "src/data/roasts.txt"
      provides: "~30-80 CC0/self-authored programmer one-liners"
      contains: "."
  key_links:
    - from: "src/main.rs"
      to: "fortune/eight_ball/roast .run()"
      via: "Commands::Fortune(args) => args.run(); Commands::EightBall(args) => args.run(); Commands::Roast(args) => args.run()"
      pattern: "Commands::Fortune\\(args\\)|Commands::EightBall\\(args\\)|Commands::Roast\\(args\\)"
    - from: "src/commands/fortune/mod.rs"
      to: "src/data/fortunes.txt"
      via: "include_str!(\"../../data/fortunes.txt\")"
      pattern: "include_str!"
---

<objective>
The three whimsy random-pick slices, grouped because they share one pattern:
`rand::rng()` (OS-seeded ThreadRng, ChaCha12) + `IndexedRandom::choose` over an
embedded list, with non-determinism tested by membership + N-runs-differ
(never exact value). `fortune` and `roast` use `include_str!` text lists;
`8ball` uses a `const &[&str]` of the canonical 20 answers (D-07). Module dir is
`eight_ball/` but the CLI name stays `8ball` (cli.rs:52, already set).

Purpose: these close the phase — the last three success-criteria commands. Each
proves the OS-seeded `rand::rng()` path (distinct from passgen's `OsRng`) and
the embedded-content pattern established by the EFF wordlist in Plan 04.

Output: three working commands, two text data assets, three integration test
files (membership + varies-across-runs), in-module unit tests, and registry
edits. After this plan all 9 Phase-2 stubs are gone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pure-transform-utilities/02-CONTEXT.md
@.planning/phases/02-pure-transform-utilities/02-RESEARCH.md
@.planning/phases/02-pure-transform-utilities/02-PATTERNS.md
@.planning/phases/02-pure-transform-utilities/02-VALIDATION.md
@.planning/phases/02-pure-transform-utilities/02-04-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. From the live repo + RESEARCH. -->

Whimsy RNG + unbiased pick (RESEARCH Pattern 3; note: rand::rng() NOT thread_rng(), choose() NOT % len):
```rust
use rand::seq::IndexedRandom;   // brings .choose() onto slices
let mut rng = rand::rng();      // 0.9: OS-seeded ThreadRng (ChaCha12)
let answer: &str = ANSWERS.choose(&mut rng).expect("non-empty");
```

include_str! path is relative to THE SOURCE FILE (Pitfall 6): from src/commands/fortune/mod.rs →
`include_str!("../../data/fortunes.txt")`; from roast/mod.rs → `include_str!("../../data/roasts.txt")`.

const &[&str] table style for the 20 8ball answers, src/commands/flatten/rename.rs:18-26:
```rust
const RESERVED: &[&str] = &[ "CON", "PRN", /* ... */ ];
```

Non-determinism test pattern (RESEARCH:497-511 / PATTERNS Pattern F) — run N=10, assert ≥2 distinct:
```rust
use std::collections::HashSet;
use assert_cmd::Command;
let mut seen = HashSet::new();
for _ in 0..10 {
    let out = Command::cargo_bin("box").unwrap().args(["8ball","will it work?"]).output().unwrap();
    seen.insert(String::from_utf8_lossy(&out.stdout).trim().to_string());
}
assert!(seen.len() >= 2, "8ball must vary across runs (got {seen:?})");
```

Membership-test exposure: each command exposes `pub(crate) fn entries() -> &'static [&str]` (or the const for 8ball) so unit tests can assert output ∈ list (RESEARCH:495).

cli.rs swaps (src/cli.rs:49-55): `Fortune,`→`Fortune(crate::commands::fortune::FortuneArgs)`, `#[command(name="8ball")] EightBall,`→`EightBall(crate::commands::eight_ball::EightBallArgs)` (keep the `#[command(name="8ball")]` attribute), `Roast,`→`Roast(crate::commands::roast::RoastArgs)`.
main.rs arms (src/main.rs:68-70): `Commands::Fortune(args) => args.run(); Commands::EightBall(args) => args.run(); Commands::Roast(args) => args.run()`.
Args + impl shape, src/commands/flatten/mod.rs:25-46,93-94. Harness tests/flatten.rs:1-24 + tests/cli.rs:42-75.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fortune + roast data assets and commands (FORT-01, ROST-01)</name>
  <files>src/data/fortunes.txt, src/data/roasts.txt, src/commands/fortune/mod.rs, src/commands/roast/mod.rs, src/commands/mod.rs, src/cli.rs, src/main.rs, tests/fortune.rs, tests/roast.rs</files>
  <behavior>
    - `box fortune` → one non-empty aphorism from the bundled list (output ∈ entries()).
    - `box roast` → one non-empty roast from the bundled list (output ∈ entries()).
    - Both vary across runs: N=10 runs yield ≥2 distinct outputs.
    - The parsed fortune list has 50-150 entries; the roast list has 30-80 entries; none empty.
  </behavior>
  <read_first>
    - src/commands/flatten/mod.rs:25-46,93-94,331-365 (Args + impl + pure-fn/test discipline)
    - src/cli.rs:49-55 (Fortune + Roast unit variants), src/main.rs:68,70 (the arms to replace)
    - tests/flatten.rs:1-24, tests/cli.rs:42-75 (harness), RESEARCH.md:497-511 (varies-across-runs)
    - .planning/phases/02-pure-transform-utilities/02-RESEARCH.md:414-418 (Pitfall 6 include_str! path), :612-614 (Open Question 3 fortune fit-to-terminal)
    - .planning/phases/02-pure-transform-utilities/02-PATTERNS.md:291-294,301-304 (fortune/roast pattern assignments), :306-310 (data files NO ANALOG)
    - .planning/phases/02-pure-transform-utilities/02-CONTEXT.md:47-48 (D-09 fortune/roast counts + licensing)
  </read_first>
  <action>
    Create `src/data/fortunes.txt` (~50-150 CC0 / public-domain / original aphorisms, one per line — avoid bundling BSD `fortune` datfiles wholesale, mixed licensing per D-09) and `src/data/roasts.txt` (~30-80 self-authored / CC0 programmer one-liners, one per line). Exact wording and final counts within those ranges are Claude's discretion (D-discretion). No empty lines.
    Create src/commands/fortune/mod.rs: `FortuneArgs {}` (no required flags) implementing `RunCommand`. `include_str!("../../data/fortunes.txt")`, parse into a `&[&str]` (split on lines, filter empties) exposed as `pub(crate) fn entries() -> Vec<&'static str>` (or a cached slice). `run()`: `rand::rng()` + `IndexedRandom::choose` → `println!` the chosen line. For FORT-01 "fits the terminal" (Open Question 3, discretion): if the chosen line exceeds `core::output::terminal_width()`, soft-wrap it (you MAY reuse a wrap helper); otherwise print as-is. Never `% len`; never a fixed seed.
    Create src/commands/roast/mod.rs: identical shape to fortune (`RoastArgs {}`, `include_str!("../../data/roasts.txt")`, `entries()`, `rand::rng()` + `choose`).
    Add `#[cfg(test)] mod tests` in each: `entries()` count within the D-09 range and no empty entry.
    Register `pub mod fortune;` and `pub mod roast;`. Swap cli.rs:49-50 `Fortune,`→`Fortune(crate::commands::fortune::FortuneArgs)` and cli.rs:54-55 `Roast,`→`Roast(crate::commands::roast::RoastArgs)` (keep doc comments verbatim). Replace main.rs:68 with `Commands::Fortune(args) => args.run()` and main.rs:70 with `Commands::Roast(args) => args.run()`.
    Create tests/fortune.rs and tests/roast.rs: assert output is non-empty single record and ∈ the entries list; varies-across-runs (N=10 → HashSet len ≥ 2, RESEARCH Pattern F). NO_COLOR=1.
  </action>
  <verify>
    <automated>cargo test --test fortune 2>&1 | tail -6 && cargo test --test roast 2>&1 | tail -6 && cargo test fortune:: 2>&1 | tail -4 && cargo test roast:: 2>&1 | tail -4 && cargo clippy -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test fortune`, `cargo test --test roast`, `cargo test fortune::`, `cargo test roast::` all exit 0.
    - Unit assertion: fortune `entries()` count ∈ [50,150]; roast `entries()` count ∈ [30,80]; no empty entries.
    - CLI assertion: 10 runs of `box fortune` (and `box roast`) yield ≥2 distinct outputs; each output ∈ the entries list.
    - Source assertion: `grep -c 'thread_rng\|gen_range' src/commands/fortune/mod.rs src/commands/roast/mod.rs` returns 0 (rand 0.9 API only); `grep -c 'include_str!' src/commands/fortune/mod.rs` returns 1. No `not_implemented("fortune")`/`not_implemented("roast")` arms remain.
    - `cargo clippy -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`box fortune` and `box roast` print random, varying, in-list entries from embedded CC0 data; both stubs gone; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 8ball command (eight_ball module) + tests (8BAL-01)</name>
  <files>src/commands/eight_ball/mod.rs, src/commands/mod.rs, src/cli.rs, src/main.rs, tests/eight_ball.rs</files>
  <behavior>
    - `box 8ball "Will it work?"` → one of the canonical 20 Magic 8-Ball answers.
    - `box 8ball` (question optional) → still returns one of the 20 answers.
    - Output varies across runs: N=10 runs yield ≥2 distinct answers.
    - The answer set is exactly 20 (10 affirmative / 5 non-committal / 5 negative), no padding.
  </behavior>
  <read_first>
    - src/commands/flatten/mod.rs:25-46,93-94 (Args + impl), src/commands/flatten/rename.rs:18-26 (the `const &[&str]` table — the model for the 20 answers)
    - src/cli.rs:51-53 (the EightBall variant with `#[command(name="8ball")]` — KEEP that attribute), src/main.rs:69 (the arm to replace)
    - tests/flatten.rs:1-24, tests/cli.rs:42-75 (harness), RESEARCH.md:497-511 (varies-across-runs)
    - .planning/phases/02-pure-transform-utilities/02-PATTERNS.md:296-299 (eight_ball pattern assignment — module dir eight_ball, CLI name 8ball)
    - .planning/phases/02-pure-transform-utilities/02-CONTEXT.md:46 (D-09 8ball 20 answers), :145 (authentic 20)
    - .planning/STATE.md:122 (8ball→eight_ball module-name pitfall)
  </read_first>
  <action>
    Create the module directory `src/commands/eight_ball/` and `src/commands/eight_ball/mod.rs` (module name `eight_ball` — identifiers cannot start with a digit, STATE.md pitfall). `EightBallArgs { question: Option<String> }` implementing `RunCommand`; the question is accepted but does NOT affect the draw (classic 8ball). Define `const EIGHT_BALL_ANSWERS: &[&str] = &[ ...the authentic 20... ]` — 10 affirmative, 5 non-committal, 5 negative (D-09; do not pad beyond 20). `run()`: `rand::rng()` + `IndexedRandom::choose` over `EIGHT_BALL_ANSWERS` → `println!`. Never `% len`; never a fixed seed.
    Add `#[cfg(test)] mod tests`: `EIGHT_BALL_ANSWERS.len() == 20`; no empty answer.
    Register `pub mod eight_ball;` in commands/mod.rs. In cli.rs swap the unit `EightBall,` variant for `EightBall(crate::commands::eight_ball::EightBallArgs)` while KEEPING the existing `#[command(name = "8ball")]` attribute (cli.rs:52) and the `/// Ask the magic 8-ball a question` doc comment verbatim. Replace main.rs:69 `Commands::EightBall => Err(not_implemented("8ball"))` with `Commands::EightBall(args) => args.run()`.
    Create tests/eight_ball.rs: `box 8ball "Will it work?"` and `box 8ball` (no question) each print one of the 20 answers (assert membership); varies-across-runs (N=10 → HashSet len ≥ 2, RESEARCH Pattern F — note P(all 10 identical) ≈ 2e-12). NO_COLOR=1.
  </action>
  <verify>
    <automated>cargo test --test eight_ball 2>&1 | tail -6 && cargo test eight_ball:: 2>&1 | tail -4 && cargo test --test cli 2>&1 | tail -4 && cargo clippy -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test eight_ball`, `cargo test eight_ball::` both exit 0.
    - `cargo test --test cli` exits 0 (help.trycmd still lists `8ball` with its unchanged about text — the `#[command(name="8ball")]` attribute preserved).
    - Unit assertion: `EIGHT_BALL_ANSWERS.len() == 20`, no empty answer.
    - CLI assertion: `box 8ball` (with and without a question) prints a line ∈ the 20; 10 runs yield ≥2 distinct.
    - Source assertion: module path is `src/commands/eight_ball/mod.rs` (not `8ball`); `grep -c 'Commands::EightBall(args) => args.run()' src/main.rs` returns 1; no `not_implemented("8ball")` arm remains.
    - `cargo clippy -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`box 8ball` returns one of the canonical 20 answers (question optional), varies across runs, lives in the `eight_ball` module with the `8ball` CLI name preserved, and the stub is gone. All 9 Phase-2 commands are now functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arg → 8ball question | Question string is accepted but never executed/interpreted — display-only; no injection surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | (none material) | fortune / roast / 8ball | accept | Trivial surface — pure random pick over embedded CC0 lists, no network/auth/storage, no untrusted input that reaches a sink (the 8ball question is ignored for the draw and never interpreted). Output is decorative text to stdout. |
| (note) | Tampering | embedded data licensing | mitigate | fortune/roast content is CC0/public-domain/original (D-09) — no mixed-license BSD datfiles bundled; no third-party attribution gate (unlike the EFF wordlist in Plan 04). |

Surface note: these are the lowest-risk commands in the phase. No CSPRNG requirement (decorative, not security) — `rand::rng()` (OS-seeded ChaCha12) is correct and sufficient (D-08). Unbiased `choose` is still used to avoid skewed distribution, but it is a quality choice, not a security gate.
</threat_model>

<verification>
- `cargo test --test fortune`, `--test roast`, `--test eight_ball`, `cargo test fortune:: roast:: eight_ball::`, `cargo test --test cli` all green.
- `cargo clippy -- -D warnings` clean; `cargo fmt --check` clean.
- `box --help` lists all 23 commands unchanged; `8ball` CLI name preserved via `#[command(name="8ball")]`.
- PHASE GATE (run after this plan): full `cargo test` green + `cargo clippy -- -D warnings` + `cargo fmt --check` clean; regenerate `help.trycmd` with `TRYCMD=overwrite cargo test` and review the diff if any about text drifted. After this plan no `not_implemented` arm remains for any Phase-2 command.
</verification>

<success_criteria>
- fortune/roast print random, varying, in-list entries from embedded CC0 data.
- 8ball returns one of the canonical 20 (question optional), varies across runs, in the eight_ball module with 8ball CLI name.
- All three stubs removed; modules registered and dispatched; membership + varies-across-runs tests green.
- All 9 Phase-2 commands functional — phase complete.
</success_criteria>

<output>
Create `.planning/phases/02-pure-transform-utilities/02-05-SUMMARY.md` when done.
</output>
