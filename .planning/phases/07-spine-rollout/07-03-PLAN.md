---
phase: 07-spine-rollout
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - src/core/output.rs
  - src/commands/json/mod.rs
  - src/commands/qr/mod.rs
  - src/commands/weather/mod.rs
  - tests/json.rs
  - tests/qr.rs
  - tests/weather.rs
autonomous: true
requirements:
  - SPINE-02
  - SPINE-04

must_haves:
  truths:
    - "box json '<doc>' --json emits the parsed document VERBATIM (identity passthrough), NOT wrapped in {results,count}"
    - "box json '<doc>' --clip copies the pretty/compact JSON to the clipboard"
    - "box qr <text> --json emits {text, error_correction} metadata, NOT the half-block glyphs"
    - "box qr <text> --clip copies the SOURCE TEXT, not the rendered glyph block (D-15)"
    - "box weather <loc> --json emits current-only {location, temperature, unit, conditions, …} with unit read from current_units (never hardcoded)"
    - "core::output::clip_feed(&str) tees to CLIP_BUF only under --clip with NO stdout write"
    - "every 7c --json document contains no 0x1B byte and parses as a single value"
  artifacts:
    - path: "src/core/output.rs"
      provides: "NEW clip_feed(&str) primitive (the one sanctioned spine addition this phase, for qr D-15)"
      contains: "fn clip_feed"
    - path: "src/commands/json/mod.rs"
      provides: "D-16 identity passthrough emit_json(&value) + out_line on the plain/compact --clip path"
      contains: "emit_json"
    - path: "src/commands/qr/mod.rs"
      provides: "QrOutput{text, error_correction} + clip_feed(&input) for D-15"
      contains: "clip_feed"
    - path: "src/commands/weather/mod.rs"
      provides: "WeatherOutput from forecast.current with unit from current_units"
      contains: "current_units"
  key_links:
    - from: "src/commands/qr/mod.rs"
      to: "core::output::clip_feed"
      via: "copy source text, print glyphs separately"
      pattern: "clip_feed"
    - from: "src/commands/weather/mod.rs"
      to: "forecast.current_units.temperature_2m"
      via: "authoritative unit label"
      pattern: "current_units"
---

<objective>
Roll the spine across the 3 Wave-7c odd-fit commands — `json`, `qr`, `weather` — and add the ONE sanctioned `core::output` primitive of the whole phase: `clip_feed(&str)`, which `qr` needs for D-15 (print the glyphs to stdout, copy the SOURCE TEXT to the clipboard — something `out_line` cannot express). The `clip_feed` addition is sequenced FIRST in Task 1 so `qr --clip` can depend on it.

Each command is a documented exception to the uniform pilot: `json` is D-16 identity passthrough (emit the parsed Value verbatim, NOT wrapped); `qr` is D-14 metadata (not glyphs) + D-15 clip-copies-text; `weather` is the f64/unit-label watch-item (unit MUST come from `current_units`).

Purpose: SPINE-02 (`--json` on the final 3 of 16 commands) + SPINE-04 (`--clip` on json + qr — the final 2 of 6). Runs AFTER 07-02 so the structural surprises are already behind us.

Output: 1 modified spine file (`core::output.rs`), 3 modified command modules, 3 modified test files, and the phase-final full-suite gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-spine-rollout/07-CONTEXT.md
@.planning/phases/07-spine-rollout/07-PATTERNS.md
@.planning/phases/07-spine-rollout/07-RESEARCH.md

<interfaces>
<!-- Spine primitives + the clip-tee half to mirror for the NEW clip_feed. Extracted from source. -->

From src/core/output.rs (the clip-tee half of out_line to mirror at :143-150):
```rust
pub fn out_line(s: &str) {
    println!("{s}");
    if CLIP_ON.load(Ordering::Relaxed) {            // CLIP_ON atomic; CLIP_BUF Mutex<String> at :85
        let mut b = CLIP_BUF.lock().unwrap();
        b.push_str(s);
        b.push('\n');
    }
}
// out_line_tees unit test at :490-513 — mirror it for clip_feed.
pub fn emit_json<T: serde::Serialize>(value: &T) -> anyhow::Result<()>;  // :124 — tees whole doc under --clip
pub fn is_json_on() -> bool;                          // :93
pub fn is_color_on() -> bool;                          // :34 — under --clip, init_output forces this false
```

Command-local models the executor uses (do NOT recompute):
- json: parses input to `serde_json::Value`; three human paths — compact `to_string()` (:64), colored `colorize` (:69, only when is_color_on), plain `to_string_pretty` (:73); invalid → bail! (:60). The Value IS the document.
- qr: `read_input(self.input)` → `input: String`; `render_qr(&input)` → glyph block (:62-65); error_correction is the fixed literal "M" (:80).
- weather: `ForecastResp { current: Current, current_units: CurrentUnits }` (:284-307); Current.temperature_2m/relative_humidity_2m/wind_speed_10m: f64; unit labels at forecast.current_units.temperature_2m/wind_speed_10m (:106-107); resolved-location echo already on stderr (:94); offline test seam env var BOX_WEATHER_BASE_URL (:36).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NEW core::output::clip_feed primitive + qr (D-14 metadata + D-15 clip-copies-text)</name>
  <files>src/core/output.rs, src/commands/qr/mod.rs, tests/qr.rs</files>
  <read_first>
    - src/core/output.rs (the file being modified — mirror out_line's tee half at :143-150; CLIP_ON/CLIP_BUF at :85; the out_line_tees unit test at :490-513 to copy)
    - src/commands/qr/mod.rs (the file being modified — read_input + render_qr + println! at :62-66; error_correction "M" at :80)
    - src/commands/hash/mod.rs (the scalar serde struct analog)
    - tests/uuid.rs (:135 json_purity, :237 clip_roundtrip templates)
    - tests/qr.rs (the test file being modified)
    - 07-PATTERNS.md § "qr" assignment (D-14/D-15, A2) + Shared Pattern E (the clip_feed addition) + No Analog Found (qr row)
    - 07-RESEARCH.md § Pitfall 4 (qr clip wrong thing), Assumption A2, Open Question 2
    - 07-CONTEXT.md § D-14 (qr --json metadata), D-15 (qr --clip copies source text)
  </read_first>
  <action>
    clip_feed (the ONE sanctioned spine addition — Shared Pattern E): add `pub fn clip_feed(s: &str)` to src/core/output.rs that pushes `s` (plus a trailing '\n' to match out_line's tee shape) into CLIP_BUF ONLY when CLIP_ON is set, with NO stdout write — mirroring the tee half of out_line (:145-149) but omitting the println!. Document that it exists for the qr D-15 "print X, copy Y" case where out_line cannot express the split. Add a unit test `clip_feed_tees_only` in the same #[cfg(test)] module (mirror out_line_tees at :490-513): assert it appends to CLIP_BUF under --clip and writes nothing to stdout, and is a no-op when CLIP_ON is off. (Note: box is binary-only — this unit test runs via `cargo test --bin box`.)

    qr (D-14 + D-15): add `#[derive(serde::Serialize)] struct QrOutput { text: String, error_correction: String }`. Fork on is_json_on() FIRST: under --json emit `QrOutput { text: input.clone(), error_correction: "M".to_string() }` — METADATA only, do NOT emit the glyphs and do not even need render_qr on the json branch (D-14). Else (human path): keep `println!("{rendered}")` for the glyph display (NOT out_line — routing glyphs through out_line would copy garbage), and add `clip_feed(&input)` so that under --clip the SOURCE TEXT is copied, not the glyphs (D-15). Under `--json --clip`, emit_json's own tee copies the JSON document (do not double-feed).

    Tests: in tests/qr.rs add `json_metadata_not_glyphs` — assert `box qr "https://example.com" --json | from_slice` yields `.text == "https://example.com"` and `.error_correction == "M"`, and that stdout contains NO half-block glyph bytes (e.g. assert the parsed doc has only the two metadata fields / no glyph string). Add the `#[ignore]`d `clip_copies_source_text` (D-15 variant of clip_roundtrip from tests/uuid.rs:237): run `box qr <text> --clip`, paste via `box clip --paste`, assert pasted == the INPUT text (NOT the rendered glyph block). Add `json_purity` adapted to the QrOutput shape.
  </action>
  <verify>
    <automated>cargo test --bin box clip_feed_tees_only && cargo test --test qr json_metadata_not_glyphs && cargo test --test qr json_purity</automated>
  </verify>
  <acceptance_criteria>
    - src/core/output.rs contains `pub fn clip_feed` that writes to CLIP_BUF only under --clip and never to stdout; `cargo test --bin box clip_feed_tees_only` exits 0.
    - src/commands/qr/mod.rs contains `struct QrOutput` with `text` and `error_correction` fields and calls `clip_feed(&input)` on the human path.
    - `box qr "https://example.com" --json | ConvertFrom-Json` yields `.text == "https://example.com"` and `.error_correction == "M"` — NO glyphs in the document.
    - The human `box qr <text>` path still prints the glyph block (qr's display is unchanged).
    - tests/qr.rs contains an `#[ignore]`d `clip_copies_source_text` asserting pasted == input.
  </acceptance_criteria>
  <done>The single sanctioned spine addition (clip_feed) is live and unit-tested; qr emits D-14 metadata under --json and copies the source text (not glyphs) under --clip (D-15 / A2 resolved), with qr --clip depending on the now-existing clip_feed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: json identity passthrough (D-16) + json --clip routing</name>
  <files>src/commands/json/mod.rs, tests/json.rs</files>
  <read_first>
    - src/commands/json/mod.rs (the file being modified — from_str::<Value> + bail! on invalid at :60; compact :64, colored :69, plain :73)
    - src/core/output.rs (:93 is_json_on, :124 emit_json, :143 out_line, :34 is_color_on)
    - tests/uuid.rs (:135 json_purity, :237 clip_roundtrip templates)
    - tests/json.rs (the test file being modified)
    - 07-PATTERNS.md § "json" assignment (D-16, the one direct-serde command) + No Analog Found (json row)
    - 07-RESEARCH.md § Wave 7c json row + Pattern 4 (root-rule exceptions)
    - 07-CONTEXT.md § D-16 (json --json identity passthrough, NOT wrapped), D-09 (invalid → exit 1 empty stdout)
  </read_first>
  <action>
    json (D-16 identity passthrough — the ONE sanctioned direct-serde command, root-rule EXCEPTION): under is_json_on() emit the parsed `serde_json::Value` VERBATIM via `emit_json(&value)` (which is pure: no-BOM, trailing \n, and tees the whole document to clip under --json --clip). Do NOT wrap in {results,count}. Invalid JSON still bail!s (exit 1, empty stdout) UNCHANGED at :60 (D-09) — the fork is AFTER the successful parse.

    json --clip WITHOUT --json (SPINE-04): none of the three human paths feed the clip buffer today. Under --clip, init_output forces COLOR_ON=false, so the colorize branch (:69) is never taken — the plain `to_string_pretty` branch (:73) runs. Route THAT plain branch through out_line so --clip tees it. Also route the `--compact` branch (:64) through out_line so `--compact --clip` copies the compact form. (Keep the colored branch as print!/println! — it only runs when color is on, i.e. never under --clip.)

    Tests: in tests/json.rs add `json_identity_passthrough` — assert `box json '{"b":1,"a":2}' --json` emits the parsed document VERBATIM (a top-level object with keys `a` and `b`, NOT a {results,count} wrapper; preserve_order keeps input key order). Add `json_purity` (no 0x1B / no BOM, parses as one value). Add the `#[ignore]`d `clip_roundtrip` (from tests/uuid.rs:237) asserting `box json '<doc>' --clip` copies the pretty form; optionally a `--compact --clip` variant.
  </action>
  <verify>
    <automated>cargo test --test json json_identity_passthrough && cargo test --test json json_purity</automated>
  </verify>
  <acceptance_criteria>
    - src/commands/json/mod.rs under --json calls `emit_json(&value)` on the parsed Value (identity passthrough); the document is NOT wrapped in `{results,count}`.
    - `box json '{"b":1,"a":2}' --json | ConvertFrom-Json` yields an object with members `a` and `b` (no `.results`/`.count` keys).
    - The plain `to_string_pretty` and `--compact` branches route through `out_line` so `--clip` tees them.
    - Invalid JSON still exits 1 with empty stdout (D-09, unchanged).
    - tests/json.rs contains `json_identity_passthrough` (exits 0) and an `#[ignore]`d `clip_roundtrip`.
  </acceptance_criteria>
  <done>json is the documented D-16 identity passthrough — it emits the parsed document verbatim under --json and tees the pretty/compact form under --clip, with invalid input still failing closed (empty stdout, exit 1).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: weather (D-17 current-only, unit from current_units) + phase-final full-suite gate</name>
  <files>src/commands/weather/mod.rs, tests/weather.rs</files>
  <read_first>
    - src/commands/weather/mod.rs (the file being modified — resolved-location stderr echo :94; wmo_to_str + unit labels current_units.temperature_2m/wind_speed_10m :106-107; f64 fields :108; aligned block println! :116-122; ForecastResp/Current/CurrentUnits :284-307; offline seam BOX_WEATHER_BASE_URL :36)
    - src/commands/hash/mod.rs (the scalar serde struct analog)
    - src/core/output.rs (:93 is_json_on, :124 emit_json)
    - tests/uuid.rs (:135 json_purity template)
    - tests/weather.rs (the test file being modified; existing fixtures forecast_imperial.json/forecast_metric.json under tests/fixtures/weather/)
    - 07-PATTERNS.md § "weather" assignment (Pitfall WTHR-3 unit label, f64 watch)
    - 07-RESEARCH.md § Pitfall 2 (float/number), Wave 7c weather row, Wave 0 weather test note (offline seam + fixture)
    - 07-CONTEXT.md § D-17 (weather current-only {location, temperature, unit, conditions, …}; units from current_units, never hardcoded)
  </read_first>
  <action>
    weather (D-17 current-only, NOT in SPINE-04): add `#[derive(serde::Serialize)] struct WeatherOutput { location: String, temperature: f64, unit: String, conditions: String, wind_speed: f64, wind_unit: String, humidity: f64 }` (exact sub-field set is discretion within the D-17 spirit; keep snake_case; build from the already-parsed `forecast`). The `unit`/`wind_unit` fields MUST read from `forecast.current_units.temperature_2m`/`.wind_speed_10m` (:106-107) — NEVER hardcoded (Pitfall WTHR-3: imperial wind is labeled "mp/h", not "mph"). `location` = the resolved label. The f64 fields come straight from forecast.current (real Open-Meteo data is finite — do NOT serialize any hand-computed f64 that could be NaN/Inf, Pitfall 2). Fork on is_json_on() FIRST: under --json emit WeatherOutput; else the existing aligned human block (UNCHANGED). The stderr "Resolved …" echo (:94) is already off the --json stdout channel — leave it.

    Tests: in tests/weather.rs add `json_purity` using the offline seam — set BOX_WEATHER_BASE_URL to a local fixture server / point it at an existing fixture (forecast_imperial.json / forecast_metric.json under tests/fixtures/weather/) so the test is network-free and deterministic. Assert the document is current-only, carries `.temperature`/`.unit`/`.conditions`, and that `.unit` matches the fixture's current_units value (e.g. the imperial fixture's wind unit serializes as the API's label, not a hardcoded "mph"). Assert no 0x1B / no BOM.

    Then run the PHASE-FINAL gate: `cargo test` (all integration) + `cargo test --bin box` (unit) — the entire Phase-7 command set (all 16 --json + 6 --clip + SC4 + clip_feed) must be green. Also confirm the live --clip round-trips run locally once: `cargo test -- --ignored --test-threads=1` (clipboard is shared OS state; documented as a local human-adjacent check).
  </action>
  <verify>
    <automated>cargo test --test weather json_purity && cargo test && cargo test --bin box</automated>
  </verify>
  <acceptance_criteria>
    - src/commands/weather/mod.rs contains a `WeatherOutput` struct whose `unit`/`wind_unit` are read from `forecast.current_units` (grep confirms `current_units` is referenced on the JSON path; no hardcoded "mph"/"°C" literal feeds the JSON unit).
    - `cargo test --test weather json_purity` exits 0 using the BOX_WEATHER_BASE_URL offline fixture (no network).
    - The weather --json document is current-only ({location, temperature, unit, conditions, …}) — no forecast/multi-day fields (those are Phase 10).
    - Phase-final full suite green: `cargo test && cargo test --bin box` exit 0.
  </acceptance_criteria>
  <done>weather emits the current-only D-17 shape with the unit label sourced from current_units (never hardcoded); the complete Phase-7 spine rollout (all 16 --json commands, 6 --clip commands, the SC4 omission, and clip_feed) passes the full integration + unit suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user/network data → JSON | qr input text, weather API f64 fields, json input document cross into serialized output |
| source text vs rendered glyphs → clipboard | qr must copy the useful payload (text), not the visual (glyphs) |
| f64 weather field → JSON number | NaN/Inf would be invalid JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07c-01 | Tampering | qr --clip copying the wrong payload (glyphs instead of source text) | mitigate | D-15: qr uses the NEW clip_feed(&input) for the source text and keeps println! for the glyph display; the `clip_copies_source_text` test asserts pasted == input. |
| T-07c-02 | DoS (invalid JSON) | weather f64 (temperature/wind/humidity) being NaN/Inf | accept | Real Open-Meteo data is finite; the values are taken straight from the parsed response, not hand-computed (no divide-by-zero path). Low risk; documented (Pitfall 2). |
| T-07c-03 | Tampering (terminal escape) | json identity passthrough emitting attacker-controlled control chars | mitigate | emit_json → serde_json escapes control chars in string values on serialize; emit_json never styles; `json_purity` no-0x1B assertion is the backstop. |
| T-07c-04 | Spoofing | weather unit label hardcoded instead of from current_units | mitigate | Pitfall WTHR-3: `unit`/`wind_unit` read from forecast.current_units; the offline-fixture test asserts the label matches the API response (not a hardcoded literal). |
| T-07c-05 | Information Disclosure | clip_feed leaking to clipboard when --clip is off | mitigate | clip_feed gates on CLIP_ON (mirrors out_line); `clip_feed_tees_only` unit test asserts no-op when --clip is off and no stdout write. |
| T-07c-SC | Tampering | npm/pip/cargo installs | accept | Phase 7 installs ZERO packages (confirmed RESEARCH § Package Legitimacy Audit). No install task; gate moot. |
</threat_model>

<verification>
- Per-command quick run after each task: `cargo test --test <cmd>` (json_metadata_not_glyphs / json_identity_passthrough / weather json_purity).
- clip_feed primitive: `cargo test --bin box clip_feed_tees_only`.
- Phase-final gate (Task 3): `cargo test` (all integration) + `cargo test --bin box` (unit) green — the WHOLE Phase-7 set.
- Live --clip round-trips (incl. qr's pasted==input): `cargo test -- --ignored --test-threads=1` locally once.
</verification>

<success_criteria>
- All 3 Wave-7c commands accept `--json` (SPINE-02 complete — 16 of 16).
- json + qr accept `--clip` (SPINE-04 complete — 6 of 6 new this phase).
- `box json --json` is identity passthrough (not wrapped); `box qr --json` is metadata not glyphs; `box weather --json` is current-only with the unit from current_units.
- `core::output::clip_feed` exists, is unit-tested, and is the ONLY spine change this phase; qr --clip copies the source text (D-15 / A2).
- Every 7c `--json` command has a `json_purity` test plus its exception-specific test; full Phase-7 suite green.
</success_criteria>

<output>
Create `.planning/phases/07-spine-rollout/07-03-SUMMARY.md` when done.
</output>
