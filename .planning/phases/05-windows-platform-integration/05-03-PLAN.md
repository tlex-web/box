---
phase: 05-windows-platform-integration
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]   # shares cli.rs/main.rs/commands/mod.rs/Cargo.toml registry — serialize after clip
files_modified:
  - Cargo.toml
  - src/cli.rs
  - src/main.rs
  - src/commands/mod.rs
  - src/commands/pomodoro/mod.rs
  - tests/pomodoro.rs
# autonomous is false: carries a live-countdown + real-toast + Ctrl+C-cancel human-verify checkpoint (POMO-01)
autonomous: false
requirements: [POMO-01]
user_setup: []

must_haves:
  truths:
    - "User runs `box pomodoro` and sees a live in-place `MM:SS` countdown in the terminal (default 25 min)"
    - "`--break` runs 5 minutes, `--long-break` runs 15 minutes, and a positional `[MINUTES]` overrides (D-08)"
    - "When the timer completes a Windows toast notification appears (tauri-winrt-notification, POWERSHELL_APP_ID)"
    - "Ctrl+C / q / Esc cancels cleanly → 'cancelled' to stderr, exit 1, NO toast, terminal restored (D-07)"
    - "Completion → toast fires + exit 0; the terminal is restored on EVERY exit path incl. panic (RAII RawGuard)"
  artifacts:
    - path: "src/commands/pomodoro/mod.rs"
      provides: "PomodoroArgs + RawGuard + poll-as-timer loop + completion toast + pure resolve_duration/fmt_mmss/is_cancel (D-07/D-08/D-09)"
      contains: "RawGuard"
      min_lines: 90
    - path: "tests/pomodoro.rs"
      provides: "POMO-01 smoke: short timer enters/exits non-hanging (non-TTY), no panic"
      contains: "pomodoro"
    - path: "Cargo.toml"
      provides: "tauri-winrt-notification 0.7.2 dependency"
      contains: "tauri-winrt-notification"
  key_links:
    - from: "src/cli.rs"
      to: "crate::commands::pomodoro::PomodoroArgs"
      via: "Pomodoro(PomodoroArgs) variant swap (was unit `Pomodoro,`)"
      pattern: "Pomodoro\\(crate::commands::pomodoro::PomodoroArgs\\)"
    - from: "src/main.rs"
      to: "args.run()"
      via: "Commands::Pomodoro(args) => args.run() (was Err(not_implemented(\"pomodoro\")))"
      pattern: "Commands::Pomodoro\\(args\\)"
    - from: "src/commands/pomodoro/mod.rs"
      to: "tauri_winrt_notification::Toast"
      via: "Toast::new(Toast::POWERSHELL_APP_ID)...show() on COMPLETION only (D-07/D-09)"
      pattern: "POWERSHELL_APP_ID"
    - from: "src/commands/pomodoro/mod.rs"
      to: "crossterm::event::poll"
      via: "poll(min(1s, remaining)) IS the countdown timer AND the keypress gate (D-07)"
      pattern: "event::poll"
---

<objective>
Ship `box pomodoro` as a complete vertical slice (POMO-01): swap the unit `Pomodoro` variant for a real `PomodoroArgs { minutes, break_, long_break }`, delete the `not_implemented("pomodoro")` arm, register `pub mod pomodoro;`, add `tauri-winrt-notification`, and implement a blocking foreground raw-mode countdown loop that REUSES the matrix `RawGuard` + `event::poll`-as-timer + `KeyEventKind::Press` family WHOLESALE. `event::poll(min(1s, remaining))` is BOTH the ~1s countdown tick AND the keypress reader (single-threaded, no background thread, no SIGINT race). Cancel (Ctrl+C-as-KeyEvent / q / Esc) → "cancelled" to stderr + exit 1, NO toast. Completion (loop falls through) → restore terminal, fire the Windows toast, exit 0.

Purpose: The only Phase-5 command that owns terminal raw-mode state — it joins the matrix raw-mode family. The RAII `RawGuard` (Drop = Show + disable_raw_mode) armed the INSTANT raw mode is on (before any fallible `?`) is the real restore path under `panic = "abort"` (matrix CR-01 ordering). The toast crate is the maintained fork of CLAUDE.md's `winrt-notification` (D-09 OVERRIDE) using `POWERSHELL_APP_ID` so no AUMID registration is needed.

Output: A working `box pomodoro` (+ `--break`/`--long-break`/`[MINUTES]`) + `tests/pomodoro.rs` smoke + in-module unit tests (resolve_duration / fmt_mmss / is_cancel) + Cargo.toml toast dep. `pomodoro` stub gone (1 Phase-5 stub remains: weather).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-windows-platform-integration/05-CONTEXT.md
@.planning/phases/05-windows-platform-integration/05-RESEARCH.md
@.planning/phases/05-windows-platform-integration/05-PATTERNS.md
@.planning/phases/05-windows-platform-integration/05-VALIDATION.md

# pomodoro reuses matrix WHOLESALE — read it as the primary analog:
@src/commands/matrix/mod.rs

<interfaces>
<!-- Contracts the executor needs. Use directly. -->

From src/commands/mod.rs:
```rust
pub trait RunCommand { fn run(self) -> anyhow::Result<()>; }
```

From src/commands/matrix/mod.rs:108-116 — the RawGuard to reuse (pomodoro likely keeps the NORMAL screen, so its Drop restores `cursor::Show` + `disable_raw_mode()` ONLY — NO `LeaveAlternateScreen`):
```rust
struct RawGuard;
impl Drop for RawGuard {
    fn drop(&mut self) {
        let mut out = std::io::stdout();
        let _ = crossterm::execute!(out, cursor::Show);
        let _ = disable_raw_mode();
    }
}
```

From src/commands/matrix/mod.rs:118-133 — the guard-armed-pre-execute! ordering (CR-01): `enable_raw_mode()?;` THEN `let _guard = RawGuard;` (armed the INSTANT raw mode is on, BEFORE any further fallible `?`) THEN the `execute!(cursor::Hide)?`.

From src/commands/matrix/mod.rs:307-313 — the quit predicate to copy verbatim (rename `is_quit` → `is_cancel`, identical body):
```rust
fn is_quit(key: &KeyEvent) -> bool {
    if key.kind != KeyEventKind::Press { return false; }   // Windows Press+Release double-fire filter
    matches!(key.code, KeyCode::Char('q') | KeyCode::Esc)
        || (key.code == KeyCode::Char('c') && key.modifiers.contains(KeyModifiers::CONTROL))
}
```

From src/commands/matrix/mod.rs:200-208 — the poll-as-timer + Press-only read shape: `if event::poll(Duration)? { if let Event::Key(key) = event::read()? { if key.kind == KeyEventKind::Press && is_quit(&key) { break; } } }`. For pomodoro, use `poll(min(Duration::from_secs(1), remaining))` so the final partial second is exact (Pitfall POMO-2).

From src/cli.rs:85-92 — swap the unit `Pomodoro,` (line 90). The positional + bool-flag Args style: cowsay/json. ⚠️ `break` is a Rust keyword → `#[arg(long = "break")] pub break_: bool`.

From src/main.rs:95-98 — delete the `Commands::Pomodoro => Err(not_implemented("pomodoro"))` arm; mirror line 80. KEEP the `not_implemented` import (weather still uses it). pomodoro introduces NO exit-2 path; cancel uses `std::process::exit(1)` (or returns an err mapped to exit 1).

⚠️ Cargo.toml release profile is `panic = "abort"` (Cargo.toml:81) — the RawGuard Drop is the REAL restore path. Keep the loop panic-free: no `.unwrap()`/`.expect()` on terminal ops; propagate with `?` (matrix discipline).

tauri-winrt-notification 0.7.2 API (VERIFIED docs.rs for the pinned version):
- `Toast::POWERSHELL_APP_ID: &'static str` — no AUMID registration needed (D-09).
- `Toast::new(app_id: &str) -> Toast`.
- ⚠️ OWNED-self builder (returns `Toast` by value): `Toast::title(self, &str) -> Toast`, `Toast::text1(self, &str) -> Toast`, `Toast::text2(self, &str) -> Toast`.
- `Toast::show(&self) -> Result<()>` — fire-and-return. Fire ONLY on completion, AFTER the RawGuard has restored the terminal. A toast that fails to `show()` after a COMPLETED timer should NOT fail the run — log to stderr and still exit 0 (RESEARCH A3 / D-07 "completion → exit 0").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test scaffold — pure helpers + tests/pomodoro.rs smoke (RED for behavior, GREEN for pure)</name>
  <files>src/commands/pomodoro/mod.rs, src/commands/mod.rs, tests/pomodoro.rs</files>
  <read_first>
    - src/commands/matrix/mod.rs (the FULL analog — RawGuard struct/Drop matrix.rs:108-116, the loop setup matrix.rs:118-133, the `event::poll` loop matrix.rs:149-209, `is_quit` matrix.rs:307-313, and the `#[cfg(test)]` KeyEvent test harness matrix.rs:315-519 — the `key(code, modifiers, kind)` builder + Press-quits + plain-c-not + Release/Repeat-don't-quit cases)
    - tests/matrix.rs (the EXACT smoke-test shape: `write_stdin("q")` + `.timeout(Duration::from_secs(5))` + assert no `"panicked"` in stderr + do NOT pin an exit code)
    - src/commands/mod.rs (add `pub mod pomodoro;` in alpha order — after `passgen`, before `qr`)
    - .planning/phases/05-windows-platform-integration/05-PATTERNS.md § "src/commands/pomodoro/mod.rs" (lines 322-417 — the Args struct, RawGuard adaptation, loop skeleton, is_cancel, and the pure-helper test list)
    - .planning/phases/05-windows-platform-integration/05-RESEARCH.md § Pattern 2 (`fmt_mmss` body line 309), § Pitfalls POMO-1/POMO-2 (lines 370-380)
  </read_first>
  <action>
    Add `pub mod pomodoro;` to `src/commands/mod.rs` in alpha order (after `passgen`, before `qr`). Create `src/commands/pomodoro/mod.rs` with the `//!` doc (POMO-01 + D-07/D-08/D-09), the matrix-subset imports (`std::io::Write`, `std::time::{Duration, Instant}`, `clap::Args`, `crossterm::cursor`, `crossterm::event::{self, Event, KeyCode, KeyEvent, KeyEventKind, KeyModifiers}`, `crossterm::terminal::{disable_raw_mode, enable_raw_mode}`, `crate::commands::RunCommand`, `tauri_winrt_notification::Toast`), and the `PomodoroArgs` struct (`pub minutes: Option<u64>`, `#[arg(long = "break")] pub break_: bool`, `#[arg(long = "long-break")] pub long_break: bool`, each with `///` doc).
    Define the THREE pure testable helpers as free functions (the unit-test seam — extract them so they test without a terminal):
    - `fmt_mmss(secs: u64) -> String` → `format!("{:02}:{:02}", secs / 60, secs % 60)` (RESEARCH:309).
    - `resolve_duration(minutes: Option<u64>, break_: bool, long_break: bool) -> Duration` → positional `minutes` override WINS; else 5 min (`--break`), 15 min (`--long-break`), 25 min (default) (D-08). Decide the break-vs-long-break precedence if both flags are set (document the choice; recommend `--long-break` wins or treat as a documented edge — planner discretion, but make it deterministic and unit-tested).
    - `is_cancel(key: &KeyEvent) -> bool` → copy the matrix `is_quit` body verbatim (Press-only filter, `q`/`Esc`/Ctrl+C-as-KeyEvent).
    Give `PomodoroArgs` a placeholder `RunCommand` impl that `anyhow::bail!("not implemented")` so the crate compiles (the real loop lands in Task 2; the smoke test is RED until then).
    Write the in-module `#[cfg(test)] mod tests`: `fmt_mmss(0)=="00:00"`, `fmt_mmss(65)=="01:05"`, `fmt_mmss(1500)=="25:00"`; `resolve_duration` for all four paths (default 25, break 5, long-break 15, positional override) + override-beats-flag; and the matrix KeyEvent harness for `is_cancel` (Press-quits for q/Esc/Ctrl+C, plain-`c` is NOT cancel, Release/Repeat are NOT cancel). These pure tests should pass immediately (GREEN).
    Write `tests/pomodoro.rs` (matrix.rs smoke shape VERBATIM): `box pomodoro 1` (or a tiny duration) fed a cancel key (`write_stdin("q")`) on piped non-TTY stdin, bounded by `.timeout(Duration::from_secs(5))`, asserting the process terminates non-hanging and stderr carries no `"panicked"` marker. Do NOT pin an exit code (a non-TTY may legitimately error out of raw-mode setup — matrix.rs:39-42). This is RED until Task 2.
    Run `cargo test --bin box pomodoro` (pure tests GREEN). Commit: `test(05-03): add pomodoro duration/format/cancel unit tests + smoke (pure green, behavior red)`.
  </action>
  <verify>
    <automated>cargo build --bin box 2>&1 | tail -5 && cargo test --bin box pomodoro 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/commands/pomodoro/mod.rs` exists with `PomodoroArgs { minutes, break_, long_break }` and the three pure helpers `fmt_mmss`, `resolve_duration`, `is_cancel`.
    - `pub mod pomodoro;` present in alpha order.
    - `#[cfg(test)]` covers: `fmt_mmss` 3 cases, `resolve_duration` 4 paths + override-beats-flag, `is_cancel` Press-quits / plain-c-not / Release-Repeat-not.
    - `tests/pomodoro.rs` exists with the matrix-shape `write_stdin` + `.timeout()` non-hanging smoke.
    - Crate COMPILES; `cargo test --bin box pomodoro` shows the pure helper tests GREEN (real logic).
  </acceptance_criteria>
  <done>Scaffold committed; pure helper tests green; loop behavior RED via placeholder; crate compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pomodoro loop — Cargo toast dep + RawGuard + poll-as-timer + completion toast + wiring (GREEN)</name>
  <files>Cargo.toml, src/commands/pomodoro/mod.rs, src/cli.rs, src/main.rs</files>
  <read_first>
    - src/commands/pomodoro/mod.rs (the scaffold from Task 1 — fill in RawGuard + the real `RunCommand::run`)
    - src/commands/matrix/mod.rs (RawGuard 108-116, the armed-pre-execute! ordering 118-133, the single-flush-per-frame poll loop 149-209 — pomodoro is the same family)
    - src/commands/color/mod.rs (the `is_color_on()`-gated `.truecolor` precedent, color.rs:53-58 — for any OPTIONAL gated countdown-digit color, discretion)
    - src/cli.rs (lines 85-92; mirror line 44 for the Pomodoro swap; the multi-line `///` doc form cli.rs:70-79)
    - src/main.rs (lines 95-98; mirror line 80; KEEP the `not_implemented` import — weather still uses it)
    - Cargo.toml (the `[dependencies]` block + the `panic = "abort"` release profile line 81)
    - .planning/phases/05-windows-platform-integration/05-RESEARCH.md § Code Examples "pomodoro: completion toast" (lines 439-449), § API Verification Toast table (lines 192-198), § Assumptions A3 (toast-fail-still-exit-0)
    - .planning/phases/05-windows-platform-integration/05-PATTERNS.md § pomodoro loop skeleton (lines 367-401)
  </read_first>
  <action>
    Add to `Cargo.toml`: `tauri-winrt-notification = "0.7.2"` with a comment noting it is the maintained drop-in fork of `winrt-notification` (OVERRIDES CLAUDE.md per D-09) and Cargo builds it only on the msvc target. Build to confirm it resolves on `x86_64-pc-windows-msvc`. If it fails to compile, apply the D-09 fallback ladder (→ `winrt-notification` 0.5.1 → `winrt-toast-reborn` 0.3.8 → PowerShell `New-BurntToastNotification` shell-out) — the swap is localized to this module since all hit the same `ToastNotificationManager` API; document which rung was used.
    Implement the `RawGuard` (matrix.rs:108-116, but Drop = `cursor::Show` + `disable_raw_mode()` ONLY — NO `LeaveAlternateScreen` since pomodoro keeps the NORMAL screen for an in-place countdown, RESEARCH:313-326). Implement `PomodoroArgs::run(self)`:
    - `enable_raw_mode()?;` then IMMEDIATELY `let _guard = RawGuard;` (armed before any further `?` — matrix CR-01 ordering), then `let mut out = std::io::stdout(); crossterm::execute!(out, cursor::Hide)?;` (no `EnterAlternateScreen` — in-place countdown is the recommendation, Open Question 1 / discretion).
    - `let total = resolve_duration(self.minutes, self.break_, self.long_break); let end = Instant::now() + total;`
    - Loop: `let remaining = end.saturating_duration_since(Instant::now()); if remaining.is_zero() { break false; }` (COMPLETION). Render the `MM:SS` line via `fmt_mmss(remaining.as_secs())` IN PLACE (single clean write per tick — the matrix flush-once discipline: `queue!`/`write!` the line at a fixed position via `cursor::MoveTo`/carriage-return, then ONE `out.flush()?` — NEVER per char, D-07). The countdown-digit color is OPTIONAL and gated on `is_color_on()` if used (discretion); the format `MM:SS` is discretion. `let tick = remaining.min(Duration::from_secs(1));` then `if event::poll(tick)? { if let Event::Key(key) = event::read()? { if key.kind == KeyEventKind::Press && is_cancel(&key) { break true; } } }` (poll = timer AND keypress gate — Pitfall POMO-2 exact final second).
    - After the loop: `drop(_guard);` to RESTORE the terminal BEFORE any toast/message (D-07). If cancelled: `eprintln!("cancelled");` (message → stderr) then `return Err(...)` mapped to exit 1 OR `std::process::exit(1)` — NO toast (D-07). On completion: fire `Toast::new(Toast::POWERSHELL_APP_ID).title("Pomodoro").text1("Time's up!").show()` (owned-self builder; title/body wording is discretion, D-09). Per RESEARCH A3 / D-07, a toast that FAILS to show after a completed timer must NOT fail the run — log to stderr and still exit 0. Return `Ok(())`.
    - Keep the whole loop panic-free (no `.unwrap()`/`.expect()` on terminal ops) — the RawGuard Drop is the real restore under `panic = "abort"`.
    Swap the cli.rs `Pomodoro,` variant (line 90) for `Pomodoro(crate::commands::pomodoro::PomodoroArgs)` (keep the `///` doc; extend with `--break`/`--long-break` usage if desired). Swap the main.rs arm for `Commands::Pomodoro(args) => args.run()`. Leave the `not_implemented` import.
    Re-run the suite — pure tests + the smoke test MUST pass (GREEN). Run `cargo clippy -- -D warnings` + `cargo fmt --check`. Commit: `feat(05-03): implement box pomodoro (POMO-01) — matrix-family raw-mode countdown + completion toast`.
  </action>
  <verify>
    <automated>cargo test --bin box pomodoro 2>&1 | tail -8 && cargo test --test pomodoro 2>&1 | tail -8 && cargo clippy -- -D warnings 2>&1 | tail -3 && cargo fmt --check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` contains `tauri-winrt-notification = "0.7.2"` (or a documented fallback rung if 0.7.2 failed to build).
    - `RunCommand::run` calls `enable_raw_mode()?` then arms `let _guard = RawGuard;` BEFORE the fallible `execute!(cursor::Hide)?` (CR-01 ordering); the RawGuard Drop restores `Show` + `disable_raw_mode`.
    - The loop uses `event::poll(remaining.min(Duration::from_secs(1)))?` as BOTH the tick timer and keypress gate, with a single `out.flush()?` per tick (no per-char flush).
    - Cancel (`is_cancel` true) → `eprintln!("cancelled")` + exit 1, NO `Toast::...show()` reached on that path.
    - Completion (`remaining.is_zero()`) → `drop(_guard)` restores terminal, THEN `Toast::new(Toast::POWERSHELL_APP_ID)...show()`, exit 0; a `show()` error logs to stderr but still exits 0 (RESEARCH A3).
    - `src/cli.rs` has `Pomodoro(crate::commands::pomodoro::PomodoroArgs)`; `src/main.rs` has `Commands::Pomodoro(args) => args.run()`.
    - `cargo test --bin box pomodoro` (pure helpers) + `cargo test --test pomodoro` (smoke: non-hanging, no panic) both green.
    - `cargo clippy -- -D warnings` + `cargo fmt --check` clean.
  </acceptance_criteria>
  <done>`box pomodoro` live (default 25 / `--break` 5 / `--long-break` 15 / `[MINUTES]`); pure + smoke tests green; clippy + fmt clean; pomodoro stub gone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — live countdown + real toast + clean Ctrl+C cancel (POMO-01 manual)</name>
  <what-built>
    `box pomodoro [MINUTES]` (+ `--break`/`--long-break`): a blocking foreground raw-mode in-place `MM:SS` countdown reusing the matrix `RawGuard` + `event::poll`-as-timer + `KeyEventKind::Press` filter, with a Windows toast on completion (tauri-winrt-notification / POWERSHELL_APP_ID) and a clean Ctrl+C/q/Esc cancel (no toast). The testable invariants (duration math, MM:SS format, quit predicate, enter/exit smoke) are auto-tested; the live render + real WinRT toast + interactive cancel are not auto-snapshotable.
  </what-built>
  <how-to-verify>
    1. In PowerShell 7, run a short timer: `box pomodoro 1` (1 minute) — or use a tiny override if supported.
    2. EXPECTED: a live `MM:SS` countdown updates in place (no scrolling, no flicker, single line redraw).
    3. Let it complete. EXPECTED: a Windows toast notification appears (titled per the implementation, e.g. "Pomodoro" / "Time's up!"), and the command exits 0 with the cursor and terminal restored, no leftover glyphs.
    4. Re-run `box pomodoro 1` and press Ctrl+C mid-countdown. EXPECTED: "cancelled" prints to stderr, the command exits 1, NO toast appears, and the cursor + terminal are fully restored (echo works, no stuck raw mode).
    5. Repeat the cancel test pressing `q`, then `Esc` — each should cancel cleanly with no toast.
    6. Confirm `box pomodoro --break` runs ~5 min and `box pomodoro --long-break` runs ~15 min (you can cancel early to confirm the starting value).
  </how-to-verify>
  <resume-signal>Type "approved" if the live countdown renders cleanly, the completion toast fires, and Ctrl+C/q/Esc each cancel with no toast and a restored terminal — or describe the issue (e.g. "cursor stuck after Ctrl+C", "toast fired on cancel", "countdown flickers").</resume-signal>
  <files>(none — manual live-countdown/toast/cancel verification, no files modified)</files>
  <action>CHECKPOINT (no automation). This is a human-verify gate, not an automated task — the implementation (matrix-family raw-mode loop + completion toast) and its testable invariants (duration/format/cancel units + the non-hanging smoke) were completed in Tasks 1-2. Pause and present the <how-to-verify> steps; do not proceed until the user responds per <resume-signal>. If the cursor is stuck after Ctrl+C, a toast fired on cancel, or the countdown flickers, revisit the RawGuard ordering / the completion-only toast guard / the single-flush-per-tick render in `PomodoroArgs::run`.</action>
  <verify>
    <human-check>`box pomodoro 1` shows a live in-place MM:SS countdown; completion fires a Windows toast and exits 0 with the terminal restored; Ctrl+C / q / Esc each cancel with "cancelled" to stderr, exit 1, NO toast, and a fully restored terminal.</human-check>
  </verify>
  <done>User typed "approved" after the live countdown rendered cleanly, the completion toast fired, and Ctrl+C/q/Esc each cancelled with no toast and a restored terminal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → pomodoro (args/keys) | Positional minutes + flags from clap; keypresses in raw mode |
| pomodoro → terminal (raw mode) | Mutates terminal state (raw mode, cursor) — must restore on every path |
| pomodoro → Windows toast (WinRT) | One-shot toast on completion via POWERSHELL_APP_ID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-POMO-WEDGE | Denial of Service | a wedged raw-mode terminal if the loop panics or early-returns without restoring | mitigate | RAII `RawGuard` armed the INSTANT `enable_raw_mode()?` succeeds, BEFORE any further fallible `?` (matrix CR-01). Drop = `cursor::Show` + `disable_raw_mode()` runs on normal return, `?` early-return, AND unwinding. Under `panic = "abort"` the loop is kept panic-free (no `.unwrap()` on terminal ops) so the Drop guard is the real restore path. This is the realistic failure for this command (per the security requirement). |
| T-05-POMO-TOAST | Spoofing | toast attributes its origin to PowerShell (POWERSHELL_APP_ID) | accept | Documented/accepted (D-09): `POWERSHELL_APP_ID` makes the toast attribute to PowerShell — a known, acceptable cosmetic for a registration-free CLI (avoids AUMID registration). |
| T-05-POMO-CANCEL | Tampering (wrong-state action) | firing a toast on cancel | mitigate | Toast is completion-ONLY: the cancel branch `eprintln!("cancelled")` + exits 1 and NEVER reaches `Toast::...show()` (D-07). Auto-verifiable by code inspection (the show() call is after the `if cancelled { return/exit }` guard). |
| T-05-POMO-SC | Tampering | `tauri-winrt-notification` 0.7.2 supply chain | accept | `[VERIFIED: crates.io]` — slopcheck `status: OK` (0 flags), tauri-apps-maintained canonical repo, current latest stable, modern `windows 0.61` (RESEARCH § Package Legitimacy Audit). No blocking legitimacy checkpoint required. |

No network, no persistence, no secrets. The single genuine control (RAII terminal restore on every path incl. panic) is the realistic failure mode and is mitigated.
</threat_model>

<verification>
- `cargo test --bin box pomodoro` — `fmt_mmss` (3 cases), `resolve_duration` (4 paths + override), `is_cancel` (Press / plain-c / Release-Repeat) green.
- `cargo test --test pomodoro` — smoke: `box pomodoro 1` fed `q` on non-TTY stdin terminates non-hanging within 5s, no panic.
- `cargo test` — full suite green (no regression).
- `cargo clippy -- -D warnings` + `cargo fmt --check` — clean.
- Human-verify: live countdown + completion toast + clean Ctrl+C/q/Esc cancel (no toast, terminal restored) in PS7 (Task 3).
</verification>

<success_criteria>
- `box pomodoro` shows a live in-place countdown; completion fires a Windows toast; Ctrl+C cancels cleanly; `--break`/`--long-break` work (ROADMAP Phase-5 success criterion #3).
- Cancel → exit 1, NO toast; completion → exit 0 with toast (D-07).
- Terminal restored on every exit path incl. panic (RawGuard).
- POMO-01 satisfied; pomodoro `not_implemented` arm gone.
</success_criteria>

<output>
Create `.planning/phases/05-windows-platform-integration/05-03-SUMMARY.md` when done.
</output>
