---
phase: 05-windows-platform-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/cli.rs
  - src/main.rs
  - src/commands/mod.rs
  - src/commands/qr/mod.rs
  - tests/qr.rs
# autonomous is false: carries a qr phone-scan human-verify checkpoint (QR-01)
autonomous: false
requirements: [QR-01]
user_setup: []

must_haves:
  truths:
    - "User runs `box qr \"https://example.com\"` and sees a QR rendered with Unicode half-block glyphs"
    - "The rendered QR is scannable by a phone camera from a dark-themed PowerShell 7 terminal"
    - "`box qr` accepts input as a positional arg OR from piped stdin (the `-`/no-arg precedence)"
    - "`box qr \"x\" > qr.txt` writes clean half-block text with NO ANSI escape sequences"
  artifacts:
    - path: "src/commands/qr/mod.rs"
      provides: "QrArgs + RunCommand impl: read_input → qrcode Dense1x2 render → stdout (D-01/D-02/D-03)"
      contains: "Dense1x2"
      min_lines: 40
    - path: "tests/qr.rs"
      provides: "QR-01 integration: arg + piped render, half-block present, no-ANSI-when-piped"
      contains: "qr"
    - path: "Cargo.toml"
      provides: "qrcode 0.14.1 dependency (default-features = false)"
      contains: "qrcode"
  key_links:
    - from: "src/cli.rs"
      to: "crate::commands::qr::QrArgs"
      via: "Qr(QrArgs) variant swap (was unit `Qr,`)"
      pattern: "Qr\\(crate::commands::qr::QrArgs\\)"
    - from: "src/main.rs"
      to: "args.run()"
      via: "Commands::Qr(args) => args.run() (was Err(not_implemented(\"qr\")))"
      pattern: "Commands::Qr\\(args\\)"
    - from: "src/commands/qr/mod.rs"
      to: "crate::core::input::read_input"
      via: "input acquisition (arg / - stdin / no-arg-TTY→exit-2, D-03)"
      pattern: "read_input"
---

<objective>
Ship `box qr` as a complete vertical slice (QR-01): swap the unit `Qr` variant in `src/cli.rs` for a real `QrArgs` carrying a positional input, delete the `not_implemented("qr")` arm in `src/main.rs`, register `pub mod qr;`, add the `qrcode` crate, and implement `QrArgs::run()` that reads input via `core::input::read_input`, encodes with `qrcode` at `EcLevel::M`, renders pure Unicode half-blocks via the built-in `Dense1x2` renderer, and prints the owned `String` to stdout. No color path (D-03).

Purpose: First of the four Phase-5 platform commands. Establishes the `qrcode` dependency and the monochrome-render slice. After this plan a user can render a scannable QR from arg or stdin, and piping to a file yields clean half-block text with zero ANSI — the exact reason `qr2term` was rejected (D-01).

Output: A working `box qr` command + `tests/qr.rs` integration suite + in-module unit tests + Cargo.toml `qrcode` dep. `qr` stub gone (3 Phase-5 stubs remain: clip/pomodoro/weather).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-windows-platform-integration/05-CONTEXT.md
@.planning/phases/05-windows-platform-integration/05-RESEARCH.md
@.planning/phases/05-windows-platform-integration/05-PATTERNS.md
@.planning/phases/05-windows-platform-integration/05-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — use directly, no exploration needed. -->

From src/commands/mod.rs (the dispatch trait — run takes self by value, returns anyhow::Result<()>, static dispatch):
```rust
pub trait RunCommand {
    fn run(self) -> anyhow::Result<()>;
}
```

From src/core/input.rs:47 (qr's input acquisition — arg / "-" stdin / no-arg-TTY→BoxError::MissingInput exit 2, D-03):
```rust
pub fn read_input(arg: Option<String>) -> anyhow::Result<String>;
```

From src/cli.rs:85-92 — the CURRENT unit variants to swap (qr is line 86, the form `Qr,`). The data-carrying form to mirror is line 44: `Color(crate::commands::color::ColorArgs)`.

From src/main.rs:95-98 — the CURRENT stub arms to delete. The live form to mirror is line 80: `Commands::Color(args) => args.run()`. Note main.rs:37 `use crate::commands::stub::not_implemented;` — this is the LAST phase but THREE stubs (clip/pomodoro/weather) remain after THIS plan, so DO NOT delete the `not_implemented` import here (it is still live for the other three).

qrcode 0.14.1 API (VERIFIED docs.rs for the pinned version):
- `QrCode::with_error_correction_level<D: AsRef<[u8]>>(data, EcLevel::M) -> QrResult<QrCode>` — M is the qrcode default (D-02).
- `QrCode::render::<Dense1x2>() -> Renderer<'_, Dense1x2>` — import `use qrcode::render::unicode::Dense1x2;`.
- `Renderer::quiet_zone(&mut self, bool) -> &mut Self` and `Renderer::build(&self) -> String` (for Dense1x2, `Pixel::Image = String` → owned String, pure half-blocks `▀▄█`/space, NO ANSI). Builder methods take `&mut self`/return `&mut Self`; `.build()` is `&self`, so the full chain compiles as one expression.
- Polarity controls (only if phone-scan fails): `Renderer::dark_color(&mut self, Dense1x2::Dark) -> &mut Self`, `Renderer::light_color(&mut self, Dense1x2::Light) -> &mut Self`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test scaffold — tests/qr.rs + in-module #[cfg(test)] (RED)</name>
  <files>tests/qr.rs, src/commands/qr/mod.rs, src/commands/mod.rs</files>
  <read_first>
    - tests/json.rs (the `json()` write_stdin runner + the no-ANSI byte-scan pattern, json.rs:18-26 and the `!stdout.windows(2).any(|w| w == b"\x1b[")` invariant) — the exact analog for the qr integration runner
    - tests/color.rs (the `color_stdout` exit-0/empty-stderr capture shape, color.rs:21-32)
    - tests/matrix.rs (the smoke-test shape, for reference on assert_cmd + write_stdin)
    - src/commands/mod.rs (the alphabetized `pub mod` list, lines 7-26 — add `pub mod qr;` in alpha order, after `pub mod passgen;`/before `pub mod roast;`)
    - .planning/phases/05-windows-platform-integration/05-VALIDATION.md (the QR-01 rows + Wave 0 Requirements list)
  </read_first>
  <action>
    Create the Wave-0 test scaffold so the slice is test-first. Add `pub mod qr;` to `src/commands/mod.rs` in alpha order (after `passgen`, before `roast`). Create `src/commands/qr/mod.rs` with the `//!` module doc (name QR-01 + D-01/D-02/D-03), the `use` imports (`clap::Args`, `qrcode::render::unicode::Dense1x2`, `qrcode::{EcLevel, QrCode}`, `crate::commands::RunCommand` — NO owo_colors/is_color_on, D-03), a `QrArgs` struct with a single `pub input: Option<String>` positional (`/// Text or URL to encode; omit to read from piped stdin.`), and a `render_qr(input: &str) -> anyhow::Result<String>` pure helper that wraps the qrcode chain (this is the unit-test seam). Stub `render_qr` to `anyhow::bail!("not implemented")` for now and give `QrArgs` a placeholder `RunCommand` impl that calls it — so the crate compiles but the behavior tests are RED.
    Write the in-module `#[cfg(test)] mod tests` asserting `render_qr` (a) returns a non-empty String for a fixed input like `"https://example.com"`, (b) contains NO ANSI (`!s.contains('\u{1b}')` — the D-01 invariant, same assertion as json.rs:69), (c) is deterministic (two calls equal). Write `tests/qr.rs` (mirror the json.rs `write_stdin` runner): a test that `box qr "https://example.com"` exits 0 with non-empty stdout containing a half-block glyph (`▀`/`▄`/`█`); a test that the SAME via piped stdin (`.write_stdin(...)`) also renders; and a no-ANSI-when-piped byte-scan test that runs WITHOUT `NO_COLOR` and asserts stdout never contains `b"\x1b["` (qr must NEVER emit ANSI even without NO_COLOR — the whole reason for rejecting qr2term, D-01).
    Run the suite — the behavior assertions MUST fail (RED). Do NOT swap cli.rs/main.rs yet (the integration tests will fail to find the real command until Task 2; that is expected RED state). Commit: `test(05-01): add failing qr render + integration tests`.
  </action>
  <verify>
    <automated>cargo build --bin box 2>&1 | tail -5 && cargo test --bin box qr 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/commands/qr/mod.rs` exists with `QrArgs { input: Option<String> }`, a `render_qr` helper, and a `#[cfg(test)] mod tests` block.
    - `pub mod qr;` present in `src/commands/mod.rs` in alpha order.
    - `tests/qr.rs` exists with the arg-render, piped-render, and no-ANSI-when-piped tests.
    - The crate COMPILES (`cargo build --bin box` succeeds).
    - `cargo test --bin box qr` shows the render_qr behavior tests FAILING (RED) — proving the tests exercise real behavior, not a tautology.
  </acceptance_criteria>
  <done>Test scaffold committed; behavior tests are RED; crate compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement qr slice — Cargo dep + render chain + registry wiring (GREEN)</name>
  <files>Cargo.toml, src/commands/qr/mod.rs, src/cli.rs, src/main.rs</files>
  <read_first>
    - src/commands/qr/mod.rs (the scaffold from Task 1 — fill in `render_qr` + the real `RunCommand::run`)
    - src/commands/color/mod.rs (the thin-orchestrator + `crate::core::input::read_input(self.input)?` live caller, color.rs:38) — the exact input-acquisition precedent
    - src/commands/cowsay/mod.rs (the no-color render → `println!` precedent)
    - src/cli.rs (lines 85-92 are the unit variants; mirror line 44 `Color(crate::commands::color::ColorArgs)` for the Qr swap)
    - src/main.rs (lines 95-98 are the stub arms; mirror line 80 `Commands::Color(args) => args.run()`; DO NOT delete the `not_implemented` import — 3 stubs remain)
    - Cargo.toml (the `[dependencies]` block; mirror the comment-annotated feature-trim style of `image` lines 57-63 and `chrono` lines 32-34)
    - .planning/phases/05-windows-platform-integration/05-RESEARCH.md § Code Examples "qr: encode → Dense1x2" (lines 405-418) and § API Verification qrcode table (lines 170-181)
  </read_first>
  <action>
    Add to `Cargo.toml` `[dependencies]`: `qrcode = { version = "0.14.1", default-features = false }` with a comment noting `default-features=false` drops the default `["image","svg","pic"]` pull-in and that the `render::unicode::Dense1x2` path is always available without any feature (RESEARCH A2). Run a build to confirm the trimmed feature set resolves the `Dense1x2` path; if it fails to compile, keep full defaults (heavier binary, not a failure — RESEARCH A2).
    Implement `render_qr(input: &str) -> anyhow::Result<String>`: `QrCode::with_error_correction_level(input.as_bytes(), EcLevel::M)?` then `.render::<Dense1x2>().quiet_zone(true).build()` (per D-02 — full ISO-18004 4-module border; M is the qrcode default per D-02). Return the owned `String`. Implement `QrArgs::run(self)`: `let input = crate::core::input::read_input(self.input)?;` (D-03 — arg / `-` stdin / no-arg-TTY→exit-2), `let rendered = render_qr(&input)?;`, `println!("{rendered}");` (data→stdout, no color path D-03). Capacity overflow on oversized input is handled by the `?` on `with_error_correction_level` → clean exit 1, no panic (FOUND-05 / T-05-QR-DoS).
    Swap the cli.rs `Qr,` unit variant (line 86) for `Qr(crate::commands::qr::QrArgs)` (keep the `/// Render a QR code for text or a URL in the terminal` doc-comment). Swap the main.rs `Commands::Qr => Err(not_implemented("qr"))` arm for `Commands::Qr(args) => args.run()`. Leave the `not_implemented` import (clip/pomodoro/weather still use it).
    Re-run the suite — all qr unit + integration tests MUST pass (GREEN). Run `cargo clippy -- -D warnings` and `cargo fmt --check`. Commit: `feat(05-01): implement box qr (QR-01) — qrcode Dense1x2 half-block render`.
    Document the dark-on-light polarity verify step (Pitfall QR-1) in the module doc: verify `Dense1x2`'s default polarity scans on a dark PS background during the human-verify checkpoint; flip `.dark_color(Dense1x2::Dark).light_color(Dense1x2::Light)` ONLY if a phone-scan test fails.
  </action>
  <verify>
    <automated>cargo test --bin box qr 2>&1 | tail -8 && cargo test --test qr 2>&1 | tail -8 && cargo clippy -- -D warnings 2>&1 | tail -3 && cargo fmt --check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` contains `qrcode = { version = "0.14.1", default-features = false }`.
    - `render_qr` calls `QrCode::with_error_correction_level(_, EcLevel::M)` and `.render::<Dense1x2>().quiet_zone(true).build()`.
    - `QrArgs::run` calls `crate::core::input::read_input(self.input)?` and `println!`s the rendered String.
    - `src/cli.rs` has `Qr(crate::commands::qr::QrArgs)`; `src/main.rs` has `Commands::Qr(args) => args.run()`.
    - `cargo test --bin box qr` AND `cargo test --test qr` both pass (GREEN): arg-render, piped-render, no-ANSI-when-piped, and render_qr unit tests all green.
    - `box qr "https://example.com" | cat` output contains a half-block glyph (`▀`/`▄`/`█`) and contains NO `\x1b` byte.
    - `cargo clippy -- -D warnings` and `cargo fmt --check` both clean.
  </acceptance_criteria>
  <done>`box qr` is live; all qr tests green; clippy + fmt clean; qr stub gone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — phone-scan the rendered QR (QR-01 manual)</name>
  <what-built>
    `box qr <text/URL>` rendering a QR with pure Unicode half-block glyphs (no ANSI), via `qrcode` + `Dense1x2` at `EcLevel::M` with `.quiet_zone(true)` (D-01/D-02). Camera-readability is a physical/perceptual property that cannot be auto-tested; the byte-level invariants (half-blocks present, no ANSI, exit 0, deterministic) are already covered by the automated tests in Tasks 1-2.
  </what-built>
  <how-to-verify>
    1. In PowerShell 7 on the default (dark) theme, run: `box qr "https://example.com"`
    2. A QR code rendered with half-block glyphs should appear with a clear border (quiet zone).
    3. Open your phone camera (or a QR scanner app) and point it at the terminal.
    4. EXPECTED: the scanner locks on and resolves to `https://example.com`.
    5. If the scanner will NOT lock or the QR looks inverted (light modules on a dark field): the default `Dense1x2` polarity rendered wrong for the dark theme — flip to `.dark_color(Dense1x2::Dark).light_color(Dense1x2::Light)` in `render_qr` (D-02 / Pitfall QR-1), rebuild, and re-scan.
    6. Also confirm `box qr "test" > qr.txt` then opening `qr.txt` shows clean half-block text with no escape sequences.
  </how-to-verify>
  <resume-signal>Type "approved" if a phone camera reads the QR, or describe the issue (e.g. "scanner won't lock — polarity looks inverted") so the polarity flip can be applied.</resume-signal>
  <files>(none — manual phone-scan verification, no files modified)</files>
  <action>CHECKPOINT (no automation). This is a human-verify gate, not an automated task — there is nothing for the executor to implement. The automation it verifies was completed in Tasks 1-2 (the qr render + the no-ANSI/half-block automated tests). Pause and present the <how-to-verify> steps to the user; do not proceed until the user responds per <resume-signal>. If the user reports the scanner will not lock or the QR looks inverted, apply the D-02 polarity flip (`.dark_color(Dense1x2::Dark).light_color(Dense1x2::Light)` in `render_qr`), rebuild, and re-present.</action>
  <verify>
    <human-check>Phone camera resolves `box qr "https://example.com"` to the URL in PS7 on the dark theme; `box qr "test" > qr.txt` shows clean half-block text with no escape sequences.</human-check>
  </verify>
  <done>User typed "approved" after a phone camera read the rendered QR (or the polarity was flipped and re-verified).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → qr (arg/stdin) | Arbitrary user-supplied text/URL crosses into the QR encoder; may be oversized or contain control bytes |
| qr → terminal (stdout) | Rendered output is written to the user's terminal / a file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-QR-DoS | Denial of Service | `render_qr` on oversized input exceeding qrcode capacity | mitigate | `QrCode::with_error_correction_level(...)?` propagates the capacity error via `?` → clean exit 1, never `.unwrap()`/panic (FOUND-05). No unbounded allocation hand-rolled — qrcode owns the bounds. |
| T-05-QR-INJ | Tampering (output spoofing) | re-emitting arbitrary input as terminal output | mitigate | `Dense1x2.build()` emits ONLY half-block glyphs (`▀▄█`/space) — the renderer produces no control bytes from arbitrary input. No color path (D-03), no ANSI ever (asserted by the no-ANSI-when-piped test). A downstream consumer rendering the output is out of this tool's scope (same posture as `cat`). |
| T-05-QR-SC | Tampering | `qrcode` 0.14.1 supply chain | accept | `[VERIFIED: crates.io]` — slopcheck `status: OK` (0 flags), canonical kennytm repo, current latest stable for the line (RESEARCH § Package Legitimacy Audit). No `[ASSUMED]`/`[SUS]` package → no blocking legitimacy checkpoint required. |

No HIGH-severity threat exists for this command (local, monochrome, no network, no persistence, no secrets). The two genuine controls (no-panic on bad input, no ANSI/no control-byte re-emit) are covered above.
</threat_model>

<verification>
- `cargo test --bin box qr` — render_qr unit tests (non-empty, no-ANSI, deterministic) green.
- `cargo test --test qr` — integration: arg-render, piped-render, no-ANSI-when-piped green.
- `cargo test` — full suite still green (no regression in the 125 prior box-bin unit tests + integration).
- `cargo clippy -- -D warnings` + `cargo fmt --check` — clean.
- Human-verify: phone camera reads the rendered QR in PS7 (Task 3).
</verification>

<success_criteria>
- `box qr "https://example.com"` exits 0 and prints a QR in Unicode half-blocks (ROADMAP Phase-5 success criterion #1).
- The QR is scannable by a phone camera from a dark-themed PowerShell 7 terminal (human-verified).
- Input works as a positional arg AND from piped stdin.
- `box qr "x" > qr.txt` yields clean half-block text with no ANSI escape sequences.
- QR-01 satisfied; qr `not_implemented` arm gone.
</success_criteria>

<output>
Create `.planning/phases/05-windows-platform-integration/05-01-SUMMARY.md` when done.
</output>
