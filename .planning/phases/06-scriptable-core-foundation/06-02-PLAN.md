---
phase: 06-scriptable-core-foundation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/commands/uuid/mod.rs
  - src/commands/hash/mod.rs
  - tests/uuid.rs
  - tests/hash.rs
  - tests/config.rs
autonomous: true
requirements: [SPINE-01, SPINE-03, HASH-V2-01]
nyquist_compliant: true

must_haves:
  truths:
    # ROADMAP success #1 (SPINE-01 / D-01/D-02/D-03)
    - "`box uuid --json` emits one well-formed JSON document `{\"results\":[{\"uuid\":...,\"version\":\"v4\"}],\"count\":N}` on stdout — snake_case, no BOM, no ANSI, no progress chrome"
    - "`box hash file --json` emits `{\"results\":[{\"path\":...,\"algo\":\"blake3\",\"digest\":...}],\"count\":1}` — a one-element results array inside an object, NOT a bare array (D-02, Phase-8-compatible)"
    # ROADMAP success #2 (SPINE-03 / D-07/D-08)
    - "`box uuid --clip` copies the generated UUID AND prints it; `box uuid -n 5 --clip` copies all 5 lines; `box uuid --json --clip` puts the JSON document on the clipboard"
    # ROADMAP success #3 (HASH-V2-01 / D-04)
    - "`box hash file` (no --algo) now emits a 64-hex BLAKE3 digest where v1 emitted SHA-256; `box hash --algo sha256 file` still emits SHA-256"
    # ROADMAP success #4 (HASH-V2-01 + SPINE-05 / D-04)
    - "With config `default_hash_algo = \"sha256\"`, `box hash file` emits SHA-256 again; CLI `--algo blake3` still wins over that config (CLI > env > config > builtin)"
    # D-04 regression backstop + D-05
    - "Bare `box hash --verify <64-hex sha256>` STILL verifies as SHA-256 (algo_from_len table byte-for-byte unchanged — tests/hash.rs::hash_verify_autodetect passes UNCHANGED)"
    - "A 64-hex --verify mismatch with no explicit --algo emits a BLAKE3-fallback diagnostic hint on stderr (decisive if blake3 matches, static otherwise), suppressed under --json, exit STAYS 1 (D-05)"
    # D-09
    - "Under --json, any failure leaves stdout EMPTY with an `error:` line on stderr and exit 1 (runtime) / 2 (usage)"
  artifacts:
    - path: "src/commands/uuid/mod.rs"
      provides: "UuidRow{uuid,version}, UuidOutput{results,count}; is_json_on() fork; out_line for the human path"
      contains: "struct UuidOutput"
    - path: "src/commands/hash/mod.rs"
      provides: "HashRow{path,algo,digest}, HashOutput{results,count}; Algo Serialize+Deserialize lowercase; BLAKE3 compute-default flip with config+env tier; D-05 verify probe"
      contains: "unwrap_or(Algo::Blake3)"
    - path: "tests/uuid.rs"
      provides: "JSON-purity + shape + --clip-capture template (the copy-me template for Phase 7)"
      contains: "fn json_purity"
    - path: "tests/hash.rs"
      provides: "default_is_blake3, json_shape, json_error_empty_stdout, verify_blake3_probe_hint; hash_verify_autodetect UNCHANGED"
      contains: "fn default_is_blake3"
  key_links:
    - from: "src/commands/uuid/mod.rs"
      to: "crate::core::output::{is_json_on, emit_json, out_line}"
      via: "is_json_on() fork: emit_json(&UuidOutput) on JSON path, out_line(&uuid) per row on human path"
      pattern: "is_json_on\\(\\)"
    - from: "src/commands/hash/mod.rs"
      to: "crate::core::config::config().default_hash_algo"
      via: "compute default = self.algo.or(env).or(config).unwrap_or(Blake3) at line 162"
      pattern: "default_hash_algo"
    - from: "src/commands/hash/mod.rs (verify mismatch bail!)"
      to: "blake3 re-open probe"
      via: "re-open the path to compute blake3 when expected.len()==64 && self.algo.is_none() && !is_json_on()"
      pattern: "expected.len\\(\\) == 64"
---

<objective>
Make `uuid` and `hash` the first consumers of the wave-1 spine, locking the D-02 pilot JSON literals as the copy-me template for the other 21 commands. Add the `UuidOutput`/`HashOutput` serde structs (one struct feeds both human and JSON paths — no drift), route the human output through `out_line` (so `--clip` tees), flip the `hash` COMPUTE default to BLAKE3 with the config+env precedence tier, and attach the D-05 BLAKE3-fallback verify probe — while leaving the verify length-table byte-for-byte unchanged so no stored SHA-256 baseline breaks.

Purpose: Prove the entire spine end-to-end on the two cheapest commands and ship the BLAKE3-default breaking change with its config escape hatch live (ROADMAP success criteria #1-#4). The pilot literals and test shapes here become the frozen template Phase 7 copies across 23 commands.

Output: `box uuid --json`/`--clip` and `box hash --json`/`--clip` working per D-01/D-02/D-03/D-07/D-08; BLAKE3-default flipped (D-04) with the SHA-256 verify backstop intact and the D-05 probe live; the reusable JSON-purity + clip-capture + BLAKE3-default + config-precedence test template in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-scriptable-core-foundation/06-CONTEXT.md
@.planning/phases/06-scriptable-core-foundation/06-RESEARCH.md
@.planning/phases/06-scriptable-core-foundation/06-PATTERNS.md
@.planning/phases/06-scriptable-core-foundation/06-VALIDATION.md
@.planning/phases/06-scriptable-core-foundation/06-01-SUMMARY.md

<interfaces>
<!-- The wave-1 spine (06-01) provides these. Use them directly. -->

From src/core/output.rs (added in 06-01):
- `pub fn is_json_on() -> bool`
- `pub fn emit_json<T: serde::Serialize>(value: &T) -> anyhow::Result<()>` — writes one JSON doc to stdout (no BOM, single trailing newline) and tees to CLIP_BUF under --json --clip.
- `pub fn out_line(s: &str)` — println! + tee to CLIP_BUF when --clip is on. THE primary-output primitive.
- (flush_clip runs in main, not called by commands.)

From src/core/config.rs (added in 06-01):
- `pub fn config() -> &'static Config` where `Config { pub default_hash_algo: Option<crate::commands::hash::Algo> }`.

The existing uuid run() to fork (src/commands/uuid/mod.rs:26-35):
```
fn run(self) -> anyhow::Result<()> {
    for _ in 0..self.count {
        println!("{}", format_one(uuid::Uuid::new_v4(), self.upper));
    }
    Ok(())
}
```
The pure `format_one(u, upper) -> String` (uuid/mod.rs:40-47) stays the human renderer and feeds both paths.

The existing hash compute branch to flip (src/commands/hash/mod.rs:159-167), CURRENT:
```
None => {
    let algo = self.algo.unwrap_or(Algo::Sha256);
    let computed = digest_reader(algo, input.reader)?;
    println!("{computed}  {label}");
    Ok(())
}
```
The Algo enum (src/commands/hash/mod.rs:63-73): `pub enum Algo { Sha256, Blake3, Sha512, Md5 }`. (06-01 may have already added `serde::Deserialize` as a minimal edit — check 06-01-SUMMARY; this plan adds `serde::Serialize` + `#[serde(rename_all = "lowercase")]` if not already complete.)

The verify mismatch bail! to extend (src/commands/hash/mod.rs:153-157), CURRENT:
```
} else {
    bail!("hash mismatch for {label}: expected {expected}, got {computed}");
}
```
The algo_from_len table (src/commands/hash/mod.rs:78-85) — 32→md5, 64→sha256, 128→sha512 — MUST stay byte-for-byte unchanged (D-04, the #1 v2 data-risk backstop).

The single-pass reader caveat (src/core/input.rs:121-127): `ResolvedInput.reader` is `Box<dyn Read>`, consumed by digest_reader. The D-05 probe needs the bytes a SECOND time → re-open the path via `read_file_or_stdin(Some(path.clone()))`. When the source is stdin (`label == "-"`) there is NO second read → degrade to the static hint.

The known-answer vectors for `b"box"` (tests/hash.rs:18-21, src/commands/hash/mod.rs:178-181):
- BOX_SHA256 = "26f8567f2569182294c3fa5b9f9cb2270b554eef628b4c149cf82a42888ff4ae"
- BOX_BLAKE3 = "095dfefdedb7f0870e801730da35823caaa8e969078e53b6e262c66f1a5b1c1e"
- BOX_MD5 = "34be958a921e43d813a2075297d8e862"
- BOX_SHA512 = "04bbbafb...29a4d1ef"

tests/hash.rs helpers (lines 23-47): `hash_cmd(args)` (forces NO_COLOR), `box_file(dir)` (writes b"box" to box.bin), `row(hash,label)` (the `<hash>  <label>` two-space coreutils string).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: uuid as first --json/--clip consumer (UuidOutput struct + is_json_on fork + out_line) and the JSON-purity/clip test template</name>
  <files>src/commands/uuid/mod.rs, tests/uuid.rs</files>
  <behavior>
    - `box uuid --json` → stdout parses as exactly one JSON value; `.results` is an array of length 1; `.count == 1`; `.results[0].uuid` is a 36-char lowercase-hyphenated UUID; `.results[0].version == "v4"`; no `0x1B` byte; first 3 bytes ≠ EF BB BF (D-01/D-02/D-03).
    - `box uuid -n 5 --json` → `.count == 5`, `.results` length 5, same element struct.
    - `box uuid` (no flags) → unchanged human output: one UUID per line on stdout (regression — existing behavior preserved).
    - `box uuid --clip` (live, #[ignore]d) → Get-Clipboard equals the printed UUID; `box uuid -n 5 --clip` copies all 5 newline-joined.
  </behavior>
  <read_first>
    - src/commands/uuid/mod.rs (the println! loop at 26-35, the pure format_one at 40-47, the existing unit tests at 49-75)
    - .planning/phases/06-scriptable-core-foundation/06-PATTERNS.md § "src/commands/uuid/mod.rs (command, MODIFIED)" (the verified UuidRow/UuidOutput shape + is_json_on() fork + out_line routing)
    - .planning/phases/06-scriptable-core-foundation/06-RESEARCH.md § Pattern 2 (one serde struct feeds both) + § "pilot JSON literals as test vectors" (the copy-me assertions) + § Pitfall 1 (is_json_on checked FIRST)
    - tests/hash.rs (the assert_cmd helper at 23-34 + the NO_COLOR determinism rule — mirror it for tests/uuid.rs) and tests/clip.rs (the #[ignore]d live-clipboard pattern for the round-trip test)
    - .planning/phases/06-scriptable-core-foundation/06-01-SUMMARY.md (confirm the out_line/emit_json/is_json_on signatures landed as specified)
  </read_first>
  <action>
    1. src/commands/uuid/mod.rs: add two serde structs above/within the module: `#[derive(serde::Serialize)] struct UuidRow { uuid: String, version: &'static str }` and `#[derive(serde::Serialize)] struct UuidOutput { results: Vec<UuidRow>, count: usize }`. Field names are locked by D-03: `uuid`, `version`, `results`, `count`. `version` is the literal `"v4"`.
    2. Rewrite `run(self)`: build `let rows: Vec<UuidRow> = (0..self.count).map(|_| UuidRow { uuid: format_one(uuid::Uuid::new_v4(), self.upper), version: "v4" }).collect();` — `format_one` (the existing pure fn) feeds BOTH paths (no drift, Pattern 2). Then fork on `is_json_on()` FIRST (Pitfall 1 — the only stdout write reachable under --json must be emit_json): if `crate::core::output::is_json_on()` → `let doc = UuidOutput { count: rows.len(), results: rows }; crate::core::output::emit_json(&doc)?;` else → `for r in &rows { crate::core::output::out_line(&r.uuid); }` (out_line, not println!, so --clip tees — D-07). Always-wrapped `{results,count}` even for N=1 (D-01/D-02).
    3. Create/extend tests/uuid.rs. Mirror tests/hash.rs:23-34 for the assert_cmd helper (force NO_COLOR for determinism). Write these named tests (each runnable via `cargo test --test uuid <name>`):
       - `json_purity` — run `box uuid --json`; parse stdout via `serde_json::from_slice::<serde_json::Value>` (must succeed = exactly one JSON value); assert `.get("results").is_some()`, `.get("count") == Some(&json!(1))`, `.results[0].version == "v4"`, `.results[0].uuid` length 36; assert stdout contains no `0x1B`; assert first 3 bytes ≠ `b"\xEF\xBB\xBF"`. This is the copy-me template for Phase 7 — make it clean and self-documenting.
       - `json_count_multi` — `box uuid -n 5 --json` → `.count == 5`, results length 5.
       - `human_output_unchanged` — `box uuid` (no flags) still prints exactly one 36-char UUID line (regression backstop).
       - `clip_roundtrip` marked `#[ignore]` (live Windows clipboard, headless-CI-unsafe — mirror tests/clip.rs): `box uuid --clip` then read the clipboard back; assert it equals the printed UUID. Document it runs via `cargo test --test uuid -- --ignored --test-threads=1`.
    4. Keep the existing format_one unit tests (uuid/mod.rs:49-75) passing unchanged.
  </action>
  <verify>
    <automated>cargo test --test uuid json_purity json_count_multi human_output_unchanged</automated>
    <automated>cargo test --bin box</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test uuid json_purity` passes — stdout is exactly one JSON value, `{results,count}` shape, version "v4", no ANSI, no BOM (SPINE-01, D-01/D-02/D-03, ROADMAP success #1).
    - `cargo test --test uuid json_count_multi` passes — `-n 5 --json` → count 5.
    - `cargo test --test uuid human_output_unchanged` passes — non-JSON path is byte-stable (regression).
    - `box uuid --clip` routes through out_line (source assertion: the human branch calls `out_line`, not `println!`); the #[ignore]d clip_roundtrip exists for the live-verify.
    - One UuidRow/UuidOutput struct feeds both paths (no separate JSON formatting code) — Pattern 2.
    - The existing format_one unit tests still pass under `cargo test --bin box`.
  </acceptance_criteria>
  <done>uuid is a clean --json/--clip consumer hitting the D-02 pilot literal exactly; the JSON-purity + clip-capture template is green and ready for Phase 7 to copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hash BLAKE3-default flip + Algo serde + HashOutput + --json/--clip; verify table UNCHANGED</name>
  <files>src/commands/hash/mod.rs, tests/hash.rs, tests/config.rs</files>
  <behavior>
    - `box hash file` (no --algo, no config) → 64-hex BLAKE3 digest (was SHA-256). For b"box": BOX_BLAKE3 (D-04 compute flip, ROADMAP success #3).
    - `box hash --algo sha256 file` → still BOX_SHA256 (explicit flag wins).
    - `box hash file --json` → `{"results":[{"path":"<file>","algo":"blake3","digest":"<64hex>"}],"count":1}` — one-element results array in an object (D-02), algo serializes lowercase.
    - With config `default_hash_algo = "sha256"` → `box hash file` → SHA-256; `box hash --algo blake3 file` → BLAKE3 (CLI > config, ROADMAP success #4).
    - `box hash --verify <BOX_SHA256> file` (no --algo) → exit 0 (verify table unchanged — 64→sha256).
    - `box hash --verify <BOX_BLAKE3> file` (no --algo) → exit 1 AND a stderr hint mentioning `--algo blake3` (D-05 probe: blake3 matches the file → decisive hint).
    - Under `--json`, a verify mismatch → stdout EMPTY, `error:` on stderr, exit 1; the D-05 hint is SUPPRESSED (D-09/D-05).
  </behavior>
  <read_first>
    - src/commands/hash/mod.rs (the Algo enum 63-73; algo_from_len 78-85 — MUST stay unchanged; the verify branch 135-158 incl. the mismatch bail! at 153-157; the compute branch 159-167 incl. the line-162 default; the digest_reader fn 119-126; the existing unit tests 171-231)
    - src/core/input.rs:113-138 (ResolvedInput.reader is Box<dyn Read>, single-pass — the D-05 re-open caveat) and src/core/input.rs:1-50 (read_file_or_stdin precedence: a real path re-opens, label "-" is stdin)
    - .planning/phases/06-scriptable-core-foundation/06-PATTERNS.md § "src/commands/hash/mod.rs (command, MODIFIED)" (the 4 precise edit points: Algo serde, compute flip + fork, algo_from_len UNCHANGED, D-05 probe with re-read caveat)
    - .planning/phases/06-scriptable-core-foundation/06-RESEARCH.md § Pattern 4 (the .or(env).or(config).unwrap_or precedence chain) + § Pitfall 5 (verify-regression backstop) + § "The D-05 verify-mismatch BLAKE3 probe" code example + § Open Q1 (wire the full env tier now)
    - tests/hash.rs (the helpers 23-47; hash_default_sha256 at 51-59 — this assertion FLIPS to blake3; hash_verify_autodetect at 122-139 — MUST pass UNCHANGED; hash_verify_bad_len_exit2 at 180-186 — the .failure().code(2) idiom)
    - .planning/phases/06-scriptable-core-foundation/06-01-SUMMARY.md (confirm whether Algo::Deserialize already landed in 06-01 and whether the .or(env) tier was wired or deferred here)
  </read_first>
  <action>
    1. Algo enum (hash/mod.rs:63-73): ensure it derives `serde::Serialize` AND `serde::Deserialize` with `#[serde(rename_all = "lowercase")]` so it round-trips `"blake3"`/`"sha256"` for both --json (Serialize) and config/env (Deserialize). (If 06-01 already added Deserialize, just add Serialize + confirm the rename attribute is present.) The spellings already match the ValueEnum variants. Add a single shared parse helper for env+config string→Algo if not already present (e.g. reuse `Algo::from_str`/`ValueEnum::from_str` so env and config use ONE parse path).
    2. Compute default flip (hash/mod.rs:162, the `None =>` no-verify branch): replace `let algo = self.algo.unwrap_or(Algo::Sha256);` with the full precedence chain (D-04 + SPINE-05 + Open Q1 — wire the env tier NOW so Phase 7 never re-touches hash): `let algo = self.algo.or_else(|| std::env::var("BOX_HASH_DEFAULT_ALGO").ok().and_then(|s| <shared parse helper>(&s))).or(crate::core::config::config().default_hash_algo).unwrap_or(Algo::Blake3);`. The env-var spelling `BOX_HASH_DEFAULT_ALGO` (uppercase BOX_<SECTION>_<KEY>) is Claude's Discretion — use it unless 06-01-SUMMARY recorded a different scheme; be consistent.
    3. Add the output structs `#[derive(serde::Serialize)] struct HashRow { path: String, algo: Algo, digest: String }` and `#[derive(serde::Serialize)] struct HashOutput { results: Vec<HashRow>, count: usize }` (D-03 field names: path, algo, digest; algo serializes lowercase via the rename). In the no-verify branch after computing `computed`, fork on `is_json_on()` FIRST: JSON path → `emit_json(&HashOutput { count: 1, results: vec![HashRow { path: label.clone(), algo, digest: computed }] })?`; human path → `crate::core::output::out_line(&format!("{computed}  {label}"))` (keep the two-space coreutils row, D-01; out_line so --clip tees, D-07). Use `label.clone()` / to_string for the path field (non-UTF-8 path policy is `to_string_lossy()` per D-4, but `label` is already a String here — low risk this phase, but never `to_str().unwrap()`).
    4. algo_from_len (hash/mod.rs:78-85): LEAVE BYTE-FOR-BYTE UNCHANGED. Do NOT add a blake3 arm. 64 stays sha256. This is the #1 v2 data-risk backstop (D-04, Pitfall 5).
    5. D-05 verify probe at the mismatch bail! (hash/mod.rs:153-157): before the bail!, add: `if expected.len() == 64 && self.algo.is_none() && !crate::core::output::is_json_on() { ... }`. Inside, when a real path is available (NOT stdin, i.e. label != "-"), re-open it via `read_file_or_stdin(Some(<path>))` and compute its BLAKE3 (`digest_reader(Algo::Blake3, reopened.reader)`); if that blake3 equals `expected` (case-insensitive) → print a DECISIVE stderr hint ("digest mismatch as sha256, but it MATCHES this file's blake3 — re-run with `--algo blake3`"); else → print the STATIC transitional hint ("the default compute algorithm is now blake3; pass `--algo blake3` if this is a blake3 digest"). For stdin (label == "-") the reader is single-pass and cannot be re-read → degrade to the static hint. Hint is `eprintln!` (stderr-only), styling gated on `is_color_on()` (exact wording is Claude's Discretion, concise, box's stderr style). Then the existing `bail!("hash mismatch for {label}: expected {expected}, got {computed}")` runs — exit STAYS 1 (plain anyhow → exit 1, unchanged). NOTE: to re-open you need the original path; `self.path` is consumed by `read_file_or_stdin(self.path)` at line 132 — capture the path string BEFORE that call (e.g. clone `self.path` or derive from `label` when `label != "-"`) so it is available at the probe. Make the "probe only when a real path is available" condition explicit.
    6. Update the in-module unit test `known_answer_per_algo` only if the Algo serde derive changes anything (it should not — derives are additive). Do NOT change algo_from_len_maps_supported_and_rejects_others.
    7. tests/hash.rs edits:
       - FLIP `hash_default_sha256` (51-59): rename to `default_is_blake3` (or update in place) so `box hash <file>` with no --algo asserts the BOX_BLAKE3 row (D-04). This is the ONE existing test whose expectation legitimately changes. Confirm `box hash --algo sha256 file` still yields BOX_SHA256 (add an explicit `--algo sha256 → BOX_SHA256` assertion if the existing arms do not already cover it).
       - Add `json_shape`: `box hash <file> --json` → parse stdout, assert `{results:[{path,algo:"blake3",digest:<64hex>}],count:1}`, no `0x1B`, no BOM. (algo is "blake3" because no --algo + no config → new default.)
       - Add `json_error_empty_stdout`: `box hash --verify <wrong-but-valid-64hex> file --json` (a mismatch) → exit 1, stdout EMPTY (assert stdout is empty bytes), `error:` on stderr (D-09). Use a 64-hex value that is neither the file's sha256 nor blake3 so it mismatches under both.
       - Add `verify_blake3_probe_hint`: `box hash --verify <BOX_BLAKE3> file` (no --algo, file is b"box") → `.failure().code(1)` AND stderr contains `--algo blake3` (D-05 decisive-hint path, blake3 matches the file). Add a second assertion that the SAME invocation WITH `--json` does NOT print the probe hint (suppressed under --json, D-05/D-09) — assert stderr does NOT contain the probe-hint phrasing while still failing exit 1.
       - hash_verify_autodetect (122-139): MUST pass UNCHANGED — do not edit it. If it needs editing, that is the regression red flag (stop and reconcile).
    8. tests/config.rs additions (or strengthen 06-01's hash_default_override): add/replace `hash_default_override` proving the full chain — config `default_hash_algo = "sha256"` (written to the temp APPDATA) → `box hash <file>` emits BOX_SHA256; and `box hash --algo blake3 <file>` (same config) emits BOX_BLAKE3 (CLI > config, ROADMAP success #4). Use the APPDATA-isolation discipline (`cmd.env("APPDATA", temp_dir)`) and NO_COLOR. If 06-01 left a placeholder hash_default_override, replace it with this full assertion now that Algo round-trips.
  </action>
  <verify>
    <automated>cargo test --test hash default_is_blake3 json_shape json_error_empty_stdout verify_blake3_probe_hint hash_verify_autodetect</automated>
    <automated>cargo test --test config hash_default_override</automated>
    <automated>cargo test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test hash default_is_blake3` passes — bare `box hash file` emits 64-hex BLAKE3 (D-04, ROADMAP success #3).
    - `cargo test --test hash json_shape` passes — `{results:[{path,algo:"blake3",digest}],count:1}`, no ANSI, no BOM (SPINE-01, D-02).
    - `cargo test --test hash json_error_empty_stdout` passes — under --json a mismatch leaves stdout EMPTY + `error:` on stderr + exit 1 (D-09).
    - `cargo test --test hash verify_blake3_probe_hint` passes — 64-hex mismatch with no --algo emits the `--algo blake3` hint on stderr, exit STAYS 1; suppressed under --json (D-05).
    - `cargo test --test hash hash_verify_autodetect` passes UNCHANGED — the verify length table is intact, no stored SHA-256 baseline breaks (D-04, Pitfall 5 — the regression backstop).
    - `cargo test --test config hash_default_override` passes — config restores SHA-256, CLI --algo blake3 still wins (CLI > env > config > builtin, ROADMAP success #4).
    - `cargo test` (full suite) is green — including all pre-existing hash tests except the one legitimately-flipped default assertion.
    - Source assertion: hash/mod.rs line ~162 reads `...or(config().default_hash_algo).unwrap_or(Algo::Blake3)`; algo_from_len still returns Sha256 for len 64.
  </acceptance_criteria>
  <done>hash computes BLAKE3 by default with a working config+env+CLI precedence escape hatch, emits the D-02 JSON literal, keeps the SHA-256 verify backstop byte-for-byte, and surfaces the D-05 BLAKE3 probe on mismatch — all four ROADMAP success criteria provably green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI/env/config → algorithm selection | the compute algorithm is resolved from CLI flag, env var, and the untrusted config file |
| filesystem → process | the hashed file (and the re-opened path for the D-05 probe) is untrusted input |
| process → stdout (JSON channel) | `--json` hash/uuid output (incl. the `path` field) is consumed as data downstream |
| process → OS clipboard | `--clip` pushes the digest/UUID payload to the shared Windows clipboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | config-forced `default_hash_algo` overriding intent (hash compute resolution) | mitigate | An explicit CLI `--algo` ALWAYS wins over env+config (precedence by construction: `self.algo.or(env).or(config).unwrap_or(Blake3)`). Config is read only from user-scoped `%APPDATA%\box` (06-01 T-06-01). Tested by `hash_default_override` (CLI --algo blake3 beats config sha256). [SPINE-05] |
| T-06-06 | Spoofing | a 64-hex digest passing the wrong-algorithm verify (the BLAKE3/SHA-256 64-tie) | mitigate | The verify length table is UNCHANGED (64→sha256) so stored SHA-256 baselines never silently break (D-04); a 64-hex blake3 digest MISMATCHES under the default sha256 verify (exit 1) rather than being silently accepted. The D-05 probe only HINTS — it never auto-switches the algorithm. [HASH-V2-01, Pitfall 5] |
| T-06-07 | Information Disclosure | the D-05 probe re-reading a file / leaking into the --json channel | mitigate | The probe is stderr-only, gated on `is_color_on()` for styling, and SUPPRESSED entirely under `--json` (stdout stays pure data); it re-opens only a real path (degrades to a static hint for stdin) and never echoes file contents — only a match/no-match hint. [D-05/D-09] |
| T-06-08 | Tampering | a malicious filename echoed into the `--json` `path` field (ANSI/escape injection) | mitigate | `emit_json` writes via `serde_json`, which escapes control characters in string values; the `path` field is a serde-serialized String, never raw-printed. Non-UTF-8 paths use `to_string_lossy()`, never `to_str().unwrap()` (D-4). [V5; Pitfall 2] |
| T-06-09 | Denial of Service | unbounded buffering while hashing / probing a huge file | accept (mitigated by design) | hashing streams in 64 KiB chunks (`digest_reader`, never `read_to_end`); the D-05 probe likewise streams a second pass via `read_file_or_stdin` + `digest_reader(Blake3, ...)` — no whole-file buffering is introduced. [T-03-03, pre-existing control preserved] |
</threat_model>

<verification>
- `cargo test` — full suite green: uuid (json_purity, json_count_multi, human_output_unchanged), hash (default_is_blake3, json_shape, json_error_empty_stdout, verify_blake3_probe_hint, hash_verify_autodetect UNCHANGED, + the pre-existing explicit-algo/verify tests), config (missing_is_silent, malformed_exit2, hash_default_override).
- `cargo test --bin box` — the in-module unit suites (uuid format_one, hash known_answer_per_algo + algo_from_len) still green.
- Manual PS7 (deferred to phase gate / human-verify): `box uuid --json | ConvertFrom-Json` yields one object; `box uuid --clip` round-trips Unicode-exact and shows the "Copied to clipboard" stderr confirmation on a TTY, suppressed when piped (VALIDATION Manual-Only table).
</verification>

<success_criteria>
- ROADMAP success #1: `box uuid --json` is one well-formed snake_case document, no BOM/ANSI/progress (json_purity green).
- ROADMAP success #2: `box uuid --clip` copies AND prints; `--json --clip` clips the document (out_line/emit_json routing + the #[ignore]d round-trip + the phase-gate human-verify).
- ROADMAP success #3: `box hash file` emits BLAKE3, `--algo sha256` still emits SHA-256 (default_is_blake3 + explicit-algo assertions green).
- ROADMAP success #4: config `default_hash_algo = "sha256"` restores SHA-256, CLI --algo blake3 wins, missing/malformed config handled (hash_default_override + 06-01 config tests green).
- D-04 backstop: hash_verify_autodetect passes UNCHANGED; algo_from_len 64→sha256 intact.
- D-05: 64-hex mismatch emits the BLAKE3 hint on stderr, suppressed under --json, exit 1.
- The pilot literals + JSON-purity/clip-capture/BLAKE3-default/config-precedence test shapes are in place as the frozen Phase-7 template.
</success_criteria>

<output>
Create `.planning/phases/06-scriptable-core-foundation/06-02-SUMMARY.md` when done. Record: the final env-var spelling used and where the env-parse helper lives, the exact path-capture mechanism used for the D-05 re-open probe (clone self.path vs derive from label), the exact D-05 decisive/static hint wording, confirmation that hash_verify_autodetect passed UNCHANGED (the regression backstop), and the final D-02 pilot JSON literals as emitted (paste the actual `box uuid --json` and `box hash file --json` output) so Phase 7 copies the verified shape.
</output>
