---
phase: 10-fun-system-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/cowsay/mod.rs
  - src/commands/eight_ball/mod.rs
  - tests/cowsay.rs
  - tests/eight_ball.rs
autonomous: true
requirements: [COW-V2-01, 8BAL-V2-01]

must_haves:
  truths:
    - "box cowsay --figure dragon \"hi\" renders an alternate figure under the bubble (not the classic cow)"
    - "box cowsay --list-figures lists at least 5 figure names and exits 0"
    - "box cowsay --think \"hi\" renders a thought bubble (rounded ( ) delimiters + o-dot tether) instead of the < > speech bubble"
    - "box cowsay --figure nonesuch exits 2 with a helpful error naming the available figures"
    - "box 8ball renders a compact ASCII 8-ball around the drawn answer with sentiment color when color is on"
    - "box 8ball --json emits a flat {text, sentiment} object where sentiment is one of affirmative|non_committal|negative"
    - "box 8ball --json output contains no ANSI escape (sentiment color is gated on is_color_on(), forced off under --json)"
  artifacts:
    - path: "src/commands/cowsay/mod.rs"
      provides: "figure roster const table, --figure/--list-figures/--think flags, parameterized bubble()"
      contains: "list_figures"
    - path: "src/commands/eight_ball/mod.rs"
      provides: "sentiment index lookup, ASCII 8-ball art, is_color_on()-gated color, {text,sentiment} JSON"
      contains: "sentiment"
    - path: "tests/cowsay.rs"
      provides: "figure-select / list-figures / think / unknown-figure integration coverage"
    - path: "tests/eight_ball.rs"
      provides: "sentiment-field JSON purity + partition-mapping coverage"
  key_links:
    - from: "src/commands/eight_ball/mod.rs"
      to: "EIGHT_BALL_ANSWERS index partition [0..10]/[10..15]/[15..20]"
      via: "sentiment(index) match"
      pattern: "0\\.\\.10|10\\.\\.15|15\\.\\.20"
    - from: "src/commands/cowsay/mod.rs"
      to: "bubble() renderer"
      via: "border/tether glyph parameters for --think"
      pattern: "fn bubble"
---

<objective>
Add the deferred fun-visual depth to `cowsay` (COW-V2-01) and `8ball` (8BAL-V2-01):
a curated built-in figure roster with `--figure`/`--list-figures`, a `--think`
thought bubble, and a compact ASCII 8-ball with tone-mapped sentiment color plus a
scriptable `sentiment` JSON field.

Purpose: Both commands are already shipped with a `{text}` `--json` fork and the
frozen `is_color_on()` gate; this plan grafts additive visual depth onto them
WITHOUT rewriting the pure-ASCII, byte-stable rendering contract.

Output: Extended `cowsay`/`eight_ball` command modules + their integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-fun-system-depth/10-CONTEXT.md
@.planning/phases/10-fun-system-depth/10-PATTERNS.md

<interfaces>
<!-- The frozen spine forks these tasks thread. No exploration needed. -->
From src/core/output.rs:
  pub fn is_json_on() -> bool;         // check FIRST, fork output
  pub fn is_color_on() -> bool;        // gate EVERY colored path; forced false under --json/--clip
  pub fn emit_json<T: Serialize>(value: &T) -> anyhow::Result<()>;
  pub fn out_line(s: &str);
owo_colors::OwoColorize is already imported in eight_ball's sibling modules:
  "text".green() / .yellow() / .red() — the sentiment decoration.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cowsay figure roster + --figure/--list-figures + --think thought bubble (COW-V2-01)</name>
  <files>src/commands/cowsay/mod.rs, tests/cowsay.rs</files>
  <read_first>
    - src/commands/cowsay/mod.rs (the whole module: the COW const at :42, CowsayArgs at :52, the is_json_on() fork at :71, wrap() at :92, bubble() at :142 — the exact reuse seam)
    - tests/cowsay.rs (the existing --json purity harness to extend)
    - .planning/phases/10-fun-system-depth/10-CONTEXT.md §"cowsay (COW-V2-01)" (D-02 curated built-in set / no external .cow I/O; D-03 --think rounded ( ) + o-dot tether)
    - .planning/phases/10-fun-system-depth/10-PATTERNS.md §"src/commands/cowsay/mod.rs" (bubble parameterization guidance)
  </read_first>
  <behavior>
    - box cowsay --figure dragon "hi" prints an alternate figure's ASCII art below the bubble, not COW
    - box cowsay --list-figures prints the roster names (>=5), one per line, exit 0, no bubble
    - box cowsay --figure nonesuch "hi" exits 2 and the stderr error names the available figures
    - box cowsay --think "hi" uses the thought-bubble delimiters (rounded ( )) and the o-dot tether (not < > and not the \ speech tether)
    - box cowsay "hi" (no flags) is byte-identical to today's output (classic cow, < > bubble) — trycmd snapshots stay green
    - box cowsay --figure dragon --json "hi" still emits a flat object with text (bubble/art never serialized, A6); a figure field naming the selection may be added
  </behavior>
  <action>
    Add a `const` roster of pure-ASCII figures (per D-02: `cow` default + at least
    `tux`, `dragon`, `ghost`, `dog`, `stegosaurus` — planner discretion on the exact
    art, keep it small/legible/byte-stable like the existing `COW` const). Represent
    the roster as a name→art lookup (e.g. a `&[(&str, &str)]` table or a small match)
    and add a `list_figures()`-style enumerator. Extend `CowsayArgs` with `--figure
    <name>` (`Option<String>`, resolves to the roster; unknown → exit 2 via a
    `BoxError::MissingInput`-style usage error OR an `anyhow::bail!` mapped to exit 2 —
    prefer returning the existing usage path; the stderr text MUST list the available
    figure names), `--list-figures` (`bool`, fast-return after the `is_json_on()`
    fork), and `--think` (`bool`). Parameterize the existing `bubble()` to accept the
    delimiter/tether glyphs so `--think` renders rounded `( )` borders and the figure's
    tether becomes the `o`-dot per D-03 — do NOT add a second renderer; pass glyphs
    into `bubble()` and swap the figure-art tether char for think mode. NO external
    `.cow` file I/O (D-02 rejects it — the embedded roster is the sole source). Keep
    the flat `{text}` JSON (A6); optionally add a `figure` field (frozen snake_case).
    Preserve the default no-flag output byte-for-byte (the trycmd `cowsay-*.trycmd`
    snapshots are the regression backstop). Extend tests/cowsay.rs with figure-select,
    list-figures, think-bubble, and unknown-figure-exit-2 cases (set NO_COLOR=1).
  </action>
  <verify>
    <automated>cd "C:/Users/aster/projects-source/cli-tools/box" && cargo test --bin box cowsay && cargo test --test cowsay</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test cowsay` exits 0
    - `src/commands/cowsay/mod.rs` contains an enumerator for the figure roster and the roster has >= 5 named figures
    - `box cowsay --list-figures` prints >= 5 lines and exits 0
    - `box cowsay --figure __nope__ "x"` exits 2 with a stderr message that lists the available figure names
    - `box cowsay --think "hi"` output contains a `(` bubble delimiter and does NOT contain the `<` speech delimiter on the content line
    - `box cowsay "hi"` remains byte-identical to the pre-change output (existing trycmd snapshots pass)
  </acceptance_criteria>
  <done>All four cowsay flags behave per the behavior block; default output unchanged; cowsay tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 8ball ASCII art + tone-mapped sentiment color + sentiment JSON field (8BAL-V2-01)</name>
  <files>src/commands/eight_ball/mod.rs, tests/eight_ball.rs</files>
  <read_first>
    - src/commands/eight_ball/mod.rs (the whole module: EIGHT_BALL_ANSWERS const + the 10/5/5 tone partition at :38, the choose() draw at :82, the is_json_on() fork at :90, EightBallOutput at :30, the partition test at :127)
    - src/core/output.rs:285-293 (the canonical "glyph is source of truth, color is decoration" is_color_on()-gated arm to copy)
    - tests/eight_ball.rs (existing harness to extend)
    - .planning/phases/10-fun-system-depth/10-CONTEXT.md §"8ball (8BAL-V2-01)" (D-05 compact ASCII ball + affirmative→green / non_committal→yellow / negative→red, is_color_on()-gated)
  </read_first>
  <behavior>
    - The drawn answer's INDEX maps to sentiment: [0..10]→affirmative, [10..15]→non_committal, [15..20]→negative (pure lookup, no new data)
    - box 8ball (TTY, color on) prints a compact ASCII 8-ball with the answer shown, colored by sentiment
    - box 8ball piped/NO_COLOR prints the same art byte-identical minus ANSI (color gated on is_color_on())
    - box 8ball --json emits {text, sentiment} where sentiment is exactly affirmative | non_committal | negative (snake_case)
    - box 8ball --json stdout contains no ANSI escape and no BOM (init_output forces color off under --json)
  </behavior>
  <action>
    Draw the answer by INDEX (choose over indices, or recover the position) so a pure
    `sentiment(index)` mapper returns one of the three tones via a `match` on the
    documented `[0..10]/[10..15]/[15..20]` boundaries (the boundaries are already
    unit-tested at :127 — no new content). Add a compact, small, legible, pure-ASCII
    8-ball rendering that shows the drawn answer (glyphs are planner discretion; keep
    byte-stable). Color the answer/art `.green()/.yellow()/.red()` ONLY inside an `if
    is_color_on()` arm (copy the arm shape from output.rs:285) so piped/`--json` output
    is byte-identical minus ANSI. Route the plain human render through `out_line`.
    Extend `EightBallOutput` with a derivable `sentiment` field serialized in frozen
    snake_case (`affirmative`/`non_committal`/`negative`) — keep `text` flat (A6, the
    art is a visual, never serialized). Do NOT add `--clip` (8ball is not in SPINE-04).
    Extend tests/eight_ball.rs: a `--json` purity test asserting `.sentiment` is one of
    the three values and stdout has no `0x1B`; a mapping test proving each partition
    band yields its tone (drive it through the pure `sentiment()` fn).
  </action>
  <verify>
    <automated>cd "C:/Users/aster/projects-source/cli-tools/box" && cargo test --bin box eight_ball && cargo test --test eight_ball</automated>
  </verify>
  <acceptance_criteria>
    - `src/commands/eight_ball/mod.rs` contains a `sentiment` fn/mapper referencing the index partition and an `is_color_on()`-gated color arm
    - `box 8ball --json | jq -e '.sentiment' ` succeeds and the value is one of affirmative|non_committal|negative
    - `box 8ball --json` stdout contains no `0x1B` byte (asserted in tests/eight_ball.rs)
    - a unit test proves indices in [0..10]→affirmative, [10..15]→non_committal, [15..20]→negative
    - `cargo test --test eight_ball` exits 0
  </acceptance_criteria>
  <done>8ball renders the ASCII ball with sentiment color when color is on, emits {text,sentiment} JSON with no ANSI, and all 8ball tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg → cowsay figure lookup | `--figure <name>` is untrusted; must resolve against the fixed embedded roster only |
| CLI arg → 8ball question | display-only, never interpreted (existing accepted surface T-02-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-FIG | Tampering | cowsay `--figure` selection | mitigate | Resolve against a fixed `const` roster table; unknown name → exit-2 usage error listing valid names. NO external `.cow` file I/O (D-02) → no path traversal / arbitrary-file read surface. |
| T-10-01-Q | Injection | 8ball question arg | accept | Question is never interpreted or reflected into a shell/eval; display-only (carried T-02-10). |
| T-10-01-ANSI | Info disclosure | sentiment/figure color under --json | mitigate | Color emitted ONLY inside `is_color_on()`; `init_output` forces it false under `--json`/`--clip`, so machine stdout stays ANSI-free (regression test asserts no `0x1B`). |
</threat_model>

<verification>
- `cargo test --bin box cowsay eight_ball` (unit) and `cargo test --test cowsay --test eight_ball` (integration) both green.
- `cargo clippy --all-targets -- -D warnings` clean for the two modules.
- Default `box cowsay "hi"` output unchanged (trycmd snapshots green).
- `box 8ball --json` and `box cowsay --json` stdout contain no ANSI escape / no BOM.
</verification>

<success_criteria>
- SC1 (partial): cowsay offers multiple figures + a think-mode bubble; 8ball renders ASCII art with sentiment color.
- SC4 (partial): 8ball sentiment color is `is_color_on()`-gated; 8ball `--json` carries the new `sentiment` field cleanly.
</success_criteria>

<output>
Create `.planning/phases/10-fun-system-depth/10-01-SUMMARY.md` when done.
</output>
