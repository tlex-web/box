---
phase: 11-meta-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/config.rs
  - src/core/fs.rs
  - src/core/errors.rs
  - src/commands/config/mod.rs
  - src/commands/mod.rs
  - src/cli.rs
  - src/main.rs
  - tests/config_cmd.rs
  - tests/cmd/help.trycmd
autonomous: true
requirements: [CFG-01]

must_haves:
  truths:
    - "`box config show` prints the effective resolved config (human table); `box config show --json | ConvertFrom-Json` yields one clean nested object"
    - "`box config show` (human) and `box config show --json` render the SAME resolved values (D-06 parity) — e.g. both surface `hash.default_algo = blake3` / `weather.units = metric` on an empty config"
    - "`box config get hash.default_algo` on an empty config prints `blake3` and exits 0 (resolved builtin default)"
    - "`box config get weather.location` when unset prints nothing to stdout and exits 1 (git-style not-set signal), silent on stderr"
    - "`box config set weather.units imperial` validates then atomically writes `[weather] units = \"imperial\"`, creating %APPDATA%\\box\\ if absent"
    - "`box config set weather.units kelvin` exits 2 and writes nothing (invalid enum caught by the round-trip)"
    - "`box config set nope.key 1` exits 2, prints the known-key list, and writes nothing"
    - "`box config set hash.default_algo sha256` followed by a bare `box hash <file>` emits SHA-256 (round-trips through the same CLI>env>config>builtin resolver)"
    - "`box config path` prints %APPDATA%\\box\\config.toml to stdout and exits 0 whether or not the file exists"
    - "`box config` is the CLI's first nested `#[command(subcommand)]` — `ConfigArgs`/`ConfigCommand` (Show/Get/Set/Path) slots into cli.rs `Commands` + main.rs dispatch through the standard init→dispatch→0/1/2-exit pipeline (D-01)"
    - "`config set` persists via a typed-struct `toml::to_string` round-trip (Serialize + skip_serializing_if on the leaves), NOT `toml_edit` — reuses the existing `toml`+`serde` derive, no new crate (D-02)"
    - "`config set` validates before it persists: it re-parses the reconstructed doc through the same `toml::from_str::<Config>` startup uses and writes nothing on any failure, so a self-inflicted exit-2 lockout is structurally impossible (D-03)"
    - "Only `hash.default_algo`, `weather.location`, `weather.units` are settable — a closed `SETTABLE_KEYS` registry rejects unknown keys at set time (exit 2, nothing written) (D-04)"
    - "No `color` field is added to `Config` and `color` is not a settable key — the roadmap/SC1 `color` mention is out of scope for Phase 11 (D-05)"
    - "`config get` has three cases — unset-with-builtin-default → resolved default exit 0; unset-no-default (`weather.location`) → empty stdout exit 1; unknown key → exit 2 (D-07)"
    - "`config path` prints the resolved `config_path()` and exits 0 whether or not the file exists — locate, never read (D-08)"
  artifacts:
    - path: "src/commands/config/mod.rs"
      provides: "ConfigArgs + ConfigCommand nested subcommand; show/get/set/path handlers with is_json_on() fork"
      min_lines: 180
    - path: "src/core/config.rs"
      provides: "pub config_path(); Serialize + skip_serializing_if on Config/HashConfig/WeatherConfig; SETTABLE_KEYS registry; pub set_value() validate-before-write"
      contains: "pub fn set_value"
    - path: "src/core/fs.rs"
      provides: "atomic_write(path, contents) — parent-dir create, temp-write, rename"
      contains: "pub fn atomic_write"
    - path: "tests/config_cmd.rs"
      provides: "integration proofs for show/get/set/path incl. the SC2 hash round-trip, the D-06 human/JSON parity check, and the exit-code contract"
      min_lines: 120
  key_links:
    - from: "src/commands/config/mod.rs"
      to: "core::config::set_value"
      via: "set handler delegates write+validate"
      pattern: "config::set_value"
    - from: "src/core/config.rs set_value"
      to: "toml::from_str::<Config>"
      via: "re-parse the reconstructed doc (same as startup load) before writing"
      pattern: "from_str::<Config>|from_str"
    - from: "src/core/config.rs set_value"
      to: "core::fs::atomic_write"
      via: "persist only the validated TOML text"
      pattern: "atomic_write"
    - from: "src/cli.rs Commands::Config"
      to: "src/main.rs dispatch"
      via: "args.run() arm"
      pattern: "Commands::Config"
---

<objective>
Ship `box config` (CFG-01): a nested subcommand with `show | get <key> | set <key> <value> | path` that reads, edits, and locates `%APPDATA%\box\config.toml`, with `--json` on `show`/`get`. It locks against the existing typed nested `Config` schema (D-13) and dogfoods the `--json` spine.

Purpose: Give users a scriptable, safe way to manage the config-file defaults the whole toolkit reads through the SPINE-05 resolver — without ever being able to author a config that bricks startup (D-03 validate-before-write).

Output: The `config` command + its `core::config`/`core::fs` helpers, wired into the CLI/dispatch/exit pipeline, with integration proofs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-meta-commands/11-CONTEXT.md
@.planning/phases/11-meta-commands/11-PATTERNS.md

<interfaces>
<!-- Contracts extracted from the codebase. Use these directly — no exploration needed. -->

From src/core/config.rs (the schema `config` locks against; extend in place):
- `pub struct Config { pub hash: HashConfig, pub weather: WeatherConfig }` — `#[derive(Debug, Default, serde::Deserialize)]`, `#[serde(default, deny_unknown_fields)]`
- `pub struct HashConfig { pub default_algo: Option<Algo> }` (same derives/attrs)
- `pub struct WeatherConfig { pub location: Option<String>, pub units: Option<Units> }` (same derives/attrs)
- `fn config_path() -> Option<std::path::PathBuf>` — %APPDATA%\box\config.toml (%APPDATA% env first, dirs fallback). CURRENTLY PRIVATE — make `pub`.
- `pub fn config() -> &'static Config` — the startup-loaded snapshot (init_config ran in main before dispatch).
- `fn load() -> anyhow::Result<Config>` maps a `toml::from_str` failure to `BoxError::Config { path, message }`.
- Test harness `malformed_maps_to_config_error` (config.rs:230-256) is the exact round-trip validation `set_value` reuses.

From src/commands/hash/mod.rs:
- `pub enum Algo { Sha256, Blake3, Sha512, Md5 }` — `#[derive(..., ValueEnum, serde::Serialize, serde::Deserialize)]`, `#[serde(rename_all = "lowercase")]`. Builtin compute default = `Algo::Blake3`.

From src/commands/weather/mod.rs:
- `pub enum Units { Metric, Imperial }` — same derive/rename shape. Builtin = `Units::Metric`. `weather.location` has NO builtin.

From src/core/output.rs (dogfood the spine):
- `pub fn is_json_on() -> bool`
- `pub fn emit_json<T: serde::Serialize>(value: &T) -> anyhow::Result<()>` — one pretty doc, no BOM/ANSI, trailing `\n` (flushes), tees to --clip.
- `pub fn out_line(s: &str)` — println + --clip tee.

From src/core/errors.rs:
- `pub enum BoxError { MissingInput, UnsupportedHashLength{len}, UnknownFigure{name,available}, Config{path,message}, MissingLocation }`. main.rs downcasts all of these to exit 2.

From src/core/fs.rs (atomic_write host — model on safe_copy's `.context(...)`-per-call style):
- `pub fn safe_copy(src: &Path, dst: &Path) -> anyhow::Result<u64>` (fs.rs:80).

From src/cli.rs:
- `pub enum Commands { ... Weather(...) }` — add `Config(...)` after `Weather`. Doc-comment becomes help text.

From src/main.rs (dispatch match 85-109, exit downcast 133-144):
- `Commands::Weather(args) => args.run(),` — add the `Config` arm after it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: core::config settable-key write path + core::fs atomic_write</name>
  <files>src/core/config.rs, src/core/fs.rs, src/core/errors.rs</files>
  <read_first>
    - src/core/config.rs (the Config/HashConfig/WeatherConfig structs, config_path, load, and the malformed_maps_to_config_error harness at 230-256 — the exact validation to reuse)
    - src/core/fs.rs (safe_copy at fs.rs:80 — the `.context(...)`-per-fallible-call style to mirror)
    - src/core/errors.rs (BoxError variants 45-66; the Config{path,message} variant)
    - src/commands/hash/mod.rs (Algo enum at 114-125 — lowercase serde spelling)
    - src/commands/weather/mod.rs (Units enum at 67-74)
  </read_first>
  <behavior>
    - `set_value("weather.units", "imperial")` → Ok; the returned/written TOML re-parses to `Config` with `weather.units == Some(Units::Imperial)`.
    - `set_value("weather.units", "kelvin")` → Err (a `BoxError::Config`-mapped parse error); NOTHING is written.
    - `set_value("hash.default_algo", "sha256")` → Ok; re-parses to `hash.default_algo == Some(Algo::Sha256)`.
    - `set_value("nope.key", "1")` → Err (`BoxError::ConfigUsage`, message lists the three known keys); NOTHING is written.
    - `set_value("weather.location", "London")` → Ok; re-parses to `weather.location == Some("London")`.
    - `atomic_write(tmpdir/box/config.toml, "…")` creates the missing `box/` parent dir and leaves the exact bytes at the target after a rename.
  </behavior>
  <action>
    Make `config_path` `pub` (signature unchanged). Add `serde::Serialize` to `Config`, `HashConfig`, `WeatherConfig`, and `#[serde(skip_serializing_if = "Option::is_none")]` on the three leaf `Option` fields (`hash.default_algo`, `weather.location`, `weather.units`) so an unset field is never written as an empty key (D-02); leave `deny_unknown_fields`/`default` intact. Also add `#[serde(skip_serializing_if = "…is_empty")]`-equivalent handling for empty sub-tables OR accept the harmless empty `[hash]`/`[weather]` header (Claude's Discretion — both re-parse clean; pick suppression via the leaf skips so a single-key write stays minimal).

    Add a closed settable-key registry as `pub const SETTABLE_KEYS: [&str; 3] = ["hash.default_algo", "weather.location", "weather.units"]` (the D-04/D-13 schema surface, reused by `get`, `set`, and the "did you mean" text).

    Add `pub fn set_value(key: &str, value: &str) -> anyhow::Result<()>` implementing D-03 validate-before-write: (1) reject any `key` not in `SETTABLE_KEYS` with `BoxError::ConfigUsage` whose message names the unknown key and lists `SETTABLE_KEYS` (→ exit 2, nothing written); (2) build the base doc by `toml::to_string(config())` then parsing that into a `toml::Table`, get-or-insert the target sub-table (`hash`/`weather`), and insert `field = toml::Value::String(value)`; (3) re-serialize the table to a TOML string and RE-PARSE it with `toml::from_str::<Config>(&text)` — the SAME parse the startup `load()` uses — mapping any error to `BoxError::Config { path, message }` (→ exit 2, nothing written) exactly like `malformed_maps_to_config_error`; (4) only on a clean re-parse call `crate::core::fs::atomic_write(&config_path()…, &text)`. A `config_path()` of `None` → an `anyhow` error. The typed `Algo`/`Units` enums + `deny_unknown_fields` do the actual value/key validation for free — `set_value` just runs the parse before persisting, so a self-inflicted exit-2 lockout is structurally impossible.

    In `core::errors.rs` add `#[error("{message}")] ConfigUsage { message: String }` for config-command usage errors (unknown key, and any set-time usage message) — routed to exit 2 in Task 2's main.rs downcast; keep `Config{path,message}` for the toml re-parse failures.

    In `core::fs.rs` add `pub fn atomic_write(path: &std::path::Path, contents: &str) -> anyhow::Result<()>`: create the parent dir with `std::fs::create_dir_all` (`.context(...)`), write `contents` to a temp sibling (e.g. a `.tmp` next to `path`) via `std::fs::write` (`.context(...)`), then `std::fs::rename(tmp, path)` (`.context(...)`) — an atomic same-volume replace (Windows `MoveFileEx` semantics; rename OVERWRITES the target, which is correct for a config replace, unlike safe_copy's create_new). Mirror safe_copy's per-call `.context(...)` discipline.

    Add `#[cfg(test)]` unit tests for the six behaviors above (use `tempfile`/an isolated path for atomic_write; drive set_value's validation via the reconstruct+re-parse path — the invalid-enum and unknown-key cases assert Err + no write).
  </action>
  <verify>
    <automated>cargo test --bin box config:: 2>&1 | tail -20 && cargo clippy --bin box --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `config_path` is `pub`; `Config`/`HashConfig`/`WeatherConfig` derive `serde::Serialize` with `skip_serializing_if` on the three leaves (source assertion).
    - `pub const SETTABLE_KEYS` lists exactly `hash.default_algo`, `weather.location`, `weather.units`.
    - `pub fn set_value` and `pub fn atomic_write` exist with the stated signatures.
    - Unit tests pass: valid set round-trips to the typed field; `weather.units=kelvin` and `nope.key=1` both return Err and write nothing; atomic_write creates the parent dir and lands exact bytes.
    - `cargo clippy --all-targets -D warnings` is clean.
  </acceptance_criteria>
  <done>set_value validates via the same toml::from_str::<Config> as startup and writes nothing on any failure; atomic_write persists via temp-write+rename with parent-dir create; ConfigUsage variant exists.</done>
</task>

<task type="auto">
  <name>Task 2: config command + CLI/dispatch/exit wiring + help snapshot</name>
  <files>src/commands/config/mod.rs, src/commands/mod.rs, src/cli.rs, src/main.rs, src/core/errors.rs, tests/cmd/help.trycmd</files>
  <read_first>
    - src/commands/uuid/mod.rs (the frozen is_json_on()-FIRST fork + {results,count} serde template, uuid/mod.rs:116-131 and 42-55)
    - src/commands/weather/mod.rs (config-integrated command shape; resolve_units/resolve_location at 167-176; is_json_on fork inside render() at 270-283; MissingLocation→exit 2 mapping at 106)
    - src/cli.rs (the top-level `#[command(subcommand)]` mechanic at 34-36/40-41 to copy one level down; the Commands enum, add after Weather at line 122)
    - src/main.rs (dispatch match 85-109; exit downcast 133-144 — add the Config arm and the ConfigUsage downcast entry)
    - src/commands/mod.rs (the alphabetical `pub mod` block 7-29; RunCommand trait 34-36)
    - src/core/config.rs (config(), config_path(), SETTABLE_KEYS, set_value — the Task-1 API this consumes)
    - src/core/output.rs (is_json_on/emit_json/out_line at 109-198)
    - tests/cmd/help.trycmd (the full 23-command listing snapshot to extend)
  </read_first>
  <action>
    Create `src/commands/config/mod.rs`. Define `#[derive(Debug, Args)] pub struct ConfigArgs { #[command(subcommand)] pub command: ConfigCommand }` and `#[derive(Debug, Subcommand)] pub enum ConfigCommand { Show, Get { key: String }, Set { key: String, value: String }, Path }` — the CLI's first NESTED subcommand (D-01, novel; copy the mechanic from cli.rs:34-41). Do NOT add a per-subcommand `--json`; read the GLOBAL flag via `crate::core::output::is_json_on()` (already lifted by init_output).

    Implement `impl RunCommand for ConfigArgs` dispatching per variant:
    - `Show`: build an effective view from `crate::core::config::config()` — `hash.default_algo.unwrap_or(Algo::Blake3)`, `weather.units.unwrap_or(Units::Metric)`, `weather.location` (Option, no builtin). Define a local `#[derive(serde::Serialize)]` effective doc shaped `{ hash: { default_algo: Algo }, weather: { location: Option<String>, units: Units } }` (snake_case; `location` serializes to `null` when unset — keep the key for a stable doc). Fork on `is_json_on()` FIRST: true → `emit_json(&effective)`; false → three `out_line` rows (e.g. `hash.default_algo = blake3`, `weather.location = <value or (not set)>`, `weather.units = metric` — exact labels Discretion). Derive the human algo/units strings from the SAME lowercase serde spelling (e.g. via `serde_json::to_value(&algo)`), so human and JSON never drift (D-06 parity — the `show_human_matches_json_parity` test in Task 3 locks this).
    - `Get { key }`: resolve the key against `SETTABLE_KEYS` (D-07): (1) `hash.default_algo` unset → resolved `blake3`, exit 0; `weather.units` unset → `metric`, exit 0; a SET value → that value, exit 0 — print bare via `out_line`, and under `--json` `emit_json` the BARE value (a JSON string). (2) `weather.location` unset (no builtin) → print NOTHING and `std::process::exit(1)` (git-style not-set signal — MUST be silent on stderr, so NOT an `Err` that trips main.rs's always-on `eprintln!("error: …")`); under `--json` `emit_json(&serde_json::Value::Null)` (which flushes the trailing `\n`) THEN `std::process::exit(1)`. (3) an unknown/misspelled key → return `BoxError::ConfigUsage` with the known-key list (→ exit 2). Keep "no value" (exit 1) structurally distinct from "no such key" (exit 2).
    - `Set { key, value }`: call `crate::core::config::set_value(&key, &value)?` (Task 1 does validate-before-write + atomic write); on Ok print a concise TTY-gated stderr confirmation (e.g. `Set {key} = {value}`) and keep stdout empty; exit 0. The `?` propagates `ConfigUsage`/`Config` → exit 2, nothing written.
    - `Path`: `out_line(&config_path()….display().to_string())`, exit 0 whether or not the file exists; a `None` path → an `anyhow` error (exit 1).

    Register `pub mod config;` in `src/commands/mod.rs` (alphabetical, before `cowsay`). Add `/// Read, edit, and locate the box config file` + `Config(crate::commands::config::ConfigArgs),` to `Commands` in cli.rs AFTER `Weather` (no `#[command(name=...)]` needed). Add `Commands::Config(args) => args.run(),` to the main.rs dispatch match after the Weather arm, and add `crate::core::errors::BoxError::ConfigUsage { .. }` to the exit-2 downcast set (beside `Config`/`MissingLocation`). Update `tests/cmd/help.trycmd`: insert the `config       Read, edit, and locate the box config file` line after the `weather` row (the command column stays 11-wide — `bulk-rename` is still the longest — so alignment is unchanged); regenerate/verify with `TRYCMD=overwrite cargo test --test cli trycmd` then eyeball the diff.
  </action>
  <verify>
    <automated>cargo build --bin box 2>&1 | tail -5 && cargo test --test cli trycmd 2>&1 | tail -10 && cargo clippy --bin box --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `ConfigArgs` carries `#[command(subcommand)] command: ConfigCommand` with `Show/Get{key}/Set{key,value}/Path`; `impl RunCommand for ConfigArgs` exists.
    - Source: config/mod.rs `Get` handler reaches `std::process::exit(1)` on the unset-no-default branch (NOT a `BoxError`), and returns `BoxError::ConfigUsage` on the unknown-key branch.
    - Source: `pub mod config;` in commands/mod.rs; `Config(...)` variant in cli.rs after Weather; `Commands::Config(args) => args.run(),` in main.rs; `BoxError::ConfigUsage` in the main.rs exit-2 downcast.
    - `cargo test --test cli trycmd` passes (help.trycmd includes the `config` line, all 24 commands listed).
    - `cargo build` and `cargo clippy --all-targets -D warnings` are clean.
  </acceptance_criteria>
  <done>`box config show/get/set/path` dispatch through RunCommand; get's unset-no-default exits 1 silently; set errors route to exit 2; the help snapshot lists config.</done>
</task>

<task type="auto">
  <name>Task 3: box config integration proofs</name>
  <files>tests/config_cmd.rs</files>
  <read_first>
    - tests/config.rs (the APPDATA-isolation harness: box_cmd/write_config helpers, BOX_SHA256/BOX_BLAKE3 constants, and the exit-2/silent-default patterns — copy this discipline)
    - tests/uuid.rs (json_purity at 208-257 — the no-ANSI/no-BOM/one-document assertion shape to reuse for `config show --json`)
    - src/commands/config/mod.rs (the command under test — Task 2)
  </read_first>
  <action>
    Create `tests/config_cmd.rs` as a black-box `assert_cmd` suite, copying the APPDATA-isolation harness from `tests/config.rs` (each test sets `APPDATA` to its own `assert_fs::TempDir` + `NO_COLOR=1`; a `write_config` helper for pre-seeding). Encode the CONTEXT `<specifics>` round-trip proofs verbatim as tests:
    - `show_json_is_one_clean_object`: `box config show --json` exits 0; stdout parses as EXACTLY one JSON value; it is an object with a `hash` object (`.hash.default_algo == "blake3"`) and a `weather` object (`.weather.units == "metric"`, `.weather.location == null`) on an empty config; NO ANSI (0x1B), NO UTF-8 BOM (reuse the uuid json_purity assertions).
    - `show_human_matches_json_parity` (D-06): on the SAME empty-config fixture, the plain-text `box config show` (no `--json`) exits 0 and its stdout CONTAINS the same resolved values `show_json_is_one_clean_object` asserts for the `--json` branch — i.e. `hash.default_algo = blake3` AND `weather.units = metric` (the human `key = value` rows carry the identical resolved defaults the JSON doc reports). Proves the D-06 human/JSON parity contract: `show` and `show --json` render the same picture, so `config show` can never lie about what `box weather`/`box hash` will consume.
    - `get_hash_default_on_empty_is_blake3`: `box config get hash.default_algo` on an empty config → stdout `blake3`, exit 0.
    - `get_unset_location_is_empty_exit1`: `box config get weather.location` unset → stdout EMPTY, exit 1, stderr EMPTY (the silent not-set signal).
    - `get_unknown_key_exit2`: `box config get nope.key` → exit 2, stderr names the known keys.
    - `set_units_writes_valid_toml`: `box config set weather.units imperial` → exit 0; the on-disk `<APPDATA>/box/config.toml` re-parses (contains `units = "imperial"`); a second `box config get weather.units` → `imperial` exit 0.
    - `set_bad_enum_exit2_nothing_written`: `box config set weather.units kelvin` → exit 2; NO config.toml is created (nothing written).
    - `set_unknown_key_exit2_nothing_written`: `box config set nope.key 1` → exit 2, stderr lists known keys, NO config.toml created.
    - `set_then_hash_roundtrips_sha256` (SC2, offline): in a fresh APPDATA, `box config set hash.default_algo sha256` → exit 0, then `box hash <box.bin>` (a `b"box"` fixture in a SEPARATE temp dir) emits the `BOX_SHA256  <path>` row — proving `config set` feeds the SAME CLI>env>config>builtin resolver `box hash` uses. (Use the hash round-trip as the live SC2 resolver proof; it is fully offline/deterministic, unlike a networked `box weather` — the `weather.units` write is proven at the file/get level above.)
    - `path_prints_config_path`: `box config path` → stdout ends with `box\config.toml` (or contains the isolated APPDATA), exit 0, whether or not the file exists.
  </action>
  <verify>
    <automated>cargo test --test config_cmd 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All listed tests pass via `cargo test --test config_cmd`.
    - `show_human_matches_json_parity` asserts the plain-text `box config show` output carries the same resolved values (`hash.default_algo = blake3`, `weather.units = metric`) as `show --json` for the same empty-config fixture (D-06 human/JSON parity).
    - `set_then_hash_roundtrips_sha256` proves the SC2 write→resolver→consume path offline.
    - `set_bad_enum_exit2_nothing_written` and `set_unknown_key_exit2_nothing_written` assert exit 2 AND absence of a written config.toml (D-03 lockout impossibility).
    - `get_unset_location_is_empty_exit1` asserts empty stdout, empty stderr, exit 1.
    - `show_json_is_one_clean_object` asserts one document, no ANSI, no BOM.
  </acceptance_criteria>
  <done>The CFG-01 behavior contract (show/get/set/path, exit 0/1/2, SC2 round-trip, D-06 human/JSON parity, JSON purity) is locked by green integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → `config set` | untrusted `key`/`value` strings cross into a file write under %APPDATA% |
| `config set` → config.toml | the written file is re-read at EVERY command's startup (a bad write bricks the whole toolkit) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | `core::fs::atomic_write` writing config.toml | mitigate | temp-write + `fs::rename` atomic replace (no torn/partial file on crash); `create_dir_all` for the parent; write ONLY after a clean re-parse |
| T-11-02 | Denial of Service (self-lockout) | `config set` persisting a config that then fails every startup (exit 2) | mitigate | D-03 validate-before-write: reconstruct the doc and re-parse via the SAME `toml::from_str::<Config>` startup uses; on ANY parse/enum/unknown-key error write NOTHING → a self-inflicted exit-2 lockout is structurally impossible |
| T-11-03 | Tampering | arbitrary-key / bad-value write | mitigate | closed `SETTABLE_KEYS` registry (D-04) rejects unknown keys (exit 2, nothing written); typed `Algo`/`Units` enums + `deny_unknown_fields` reject bad values via the round-trip |
| T-11-04 | Information disclosure | `config path` / `config show` printing the config location + values | accept | %APPDATA%\box\config.toml is a standard non-sensitive user path; values (algo/units/location) are user-owned, low-value |
| T-11-05 | Repudiation | no audit trail of who changed config | accept | single-user local personal-toolbox CLI; out of scope |
| T-11-SC | Tampering (supply chain) | new crates | accept | NO new crate in this plan — `config` reuses the already-vetted `toml` 1.1.2 + `serde` derive (D-02; `toml_edit` rejected for the duplicate winnow/toml_datetime) |
</threat_model>

<verification>
- `cargo test --bin box` (unit) + `cargo test --test config_cmd` (integration) + `cargo test --test cli trycmd` (help snapshot) all green.
- `cargo clippy --bin box --all-targets -- -D warnings` clean.
- Manual PS7 spot-check (optional): `box config show`, `box config get hash.default_algo`, `box config path`.
</verification>

<success_criteria>
- CFG-01 delivered: `box config show | get <key> | set <key> <value> | path` reads/edits/locates %APPDATA%\box\config.toml with `--json` on show/get.
- Exit contract honored: usage/config errors → exit 2; `get` unset-no-default → exit 1 (silent stderr); success → exit 0.
- SC1 + SC2 proven: `show --json` emits one clean effective doc; `set hash.default_algo sha256` → bare `box hash` emits SHA-256.
- D-06 parity proven: human `box config show` and `box config show --json` render the same resolved values (`show_human_matches_json_parity`).
- SC4 preserved: a first-time user with no config still runs every command (existing `tests/config.rs::missing_is_silent` unaffected); a malformed config still exits 2.
</success_criteria>

<output>
Create `.planning/phases/11-meta-commands/11-01-SUMMARY.md` when done.
</output>
