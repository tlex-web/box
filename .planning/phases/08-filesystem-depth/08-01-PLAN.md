---
phase: 08-filesystem-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/commands/hash/mod.rs
  - src/commands/flatten/mod.rs
  - src/commands/flatten/rename.rs
  - tests/hash.rs
  - tests/flatten.rs
autonomous: true
requirements: [HASH-V2-02, FLAT-V2-01]

must_haves:
  truths:
    - "box hash a.bin b.bin prints one coreutils `digest␣␣filename` (two-space) line per file"
    - "box hash with one unreadable file among readable ones prints the readable digests, errors per bad file on stderr, and exits 1 (best-effort)"
    - "box hash a.bin b.bin --json emits {results:[…],count:N} with one row per readable file and no ANSI/progress on stdout"
    - "box flatten --extensions jpg,png copies only files whose final extension matches (case-insensitive)"
    - "box flatten --separator - changes the collision-encoding join character"
    - "box flatten --include-hidden copies a dotfile the default walk prunes"
  artifacts:
    - path: "Cargo.toml"
      provides: "indicatif/ignore/globset/windows deps added once for the whole phase"
      contains: "indicatif"
    - path: "src/commands/hash/mod.rs"
      provides: "multi-file hashing into the existing {results,count} HashOutput"
      contains: "Vec<String>"
      min_lines: 380
    - path: "src/commands/flatten/mod.rs"
      provides: "--extensions/--separator/--include-hidden filters folded into build_plan"
      contains: "include_hidden"
      min_lines: 420
    - path: "src/commands/flatten/rename.rs"
      provides: "encode_relative parameterized by the collision-join separator"
      contains: "sep"
  key_links:
    - from: "src/commands/hash/mod.rs"
      to: "core::input::read_file_or_stdin"
      via: "per-path streaming read in the multi-file loop"
      pattern: "read_file_or_stdin"
    - from: "src/commands/flatten/mod.rs"
      to: "build_plan extension/hidden filter"
      via: "filter applied inside the single source-of-truth walk"
      pattern: "extensions"
    - from: "src/commands/hash/mod.rs"
      to: "indicatif stderr progress"
      via: "ProgressDrawTarget::stderr, hidden under is_json_on()"
      pattern: "indicatif|ProgressDrawTarget"
---

<objective>
Add the two non-destructive depth features to `hash` and `flatten`, and add ALL FOUR Phase-8 dependencies to `Cargo.toml` once so the other plans never touch the manifest.

- **HASH-V2-02:** `hash` accepts multiple file arguments and prints one coreutils `digest␣␣filename` (two-space) line per file, with a stderr progress indicator for large inputs and a best-effort partial-failure policy.
- **FLAT-V2-01:** `flatten` gains `--extensions <list>`, `--separator <str>`, `--include-hidden`, and a stderr progress bar.

Purpose: every new field rides the frozen Phase-6/7 spine — one `#[derive(Serialize)]` struct feeds both the human render and `emit_json`, so no-drift is structural. These are the simplest two depth commands, so the dependency wiring and the progress-on-stderr pattern surface here first.

Output: `Cargo.toml` with `indicatif`/`ignore`/`globset`/`windows` added; multi-file `hash`; filtered `flatten` with progress; extended `tests/hash.rs` and `tests/flatten.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-filesystem-depth/08-CONTEXT.md
@.planning/phases/08-filesystem-depth/08-RESEARCH.md
@.planning/phases/08-filesystem-depth/08-VALIDATION.md

<interfaces>
<!-- Frozen spine primitives — consume verbatim, add nothing to core::output. -->
From src/core/output.rs:
  pub fn is_json_on() -> bool;
  pub fn is_color_on() -> bool;
  pub fn emit_json<T: serde::Serialize>(value: &T) -> anyhow::Result<()>;
  pub fn out_line(s: &str);                 // prints + tees to clipboard under --clip

From src/core/input.rs:
  pub struct ResolvedInput { pub reader: Box<dyn Read>, pub label: String }
  pub fn read_file_or_stdin(path: Option<String>) -> anyhow::Result<ResolvedInput>;

From src/core/fs.rs:
  pub fn is_hidden(entry: &walkdir::DirEntry) -> bool;   // root-exempt; dot/HIDDEN prune (D-06)

From src/commands/hash/mod.rs (current single-file shape — extend, do not rewrite):
  pub struct HashArgs { pub algo: Option<Algo>, pub verify: Option<String>, pub path: Option<String> }
  fn digest_reader<R: Read>(algo: Algo, reader: R) -> anyhow::Result<String>;
  struct HashRow { path: String, algo: Algo, digest: String }
  struct HashOutput { results: Vec<HashRow>, count: usize }
  // The two-space coreutils row is emitted at mod.rs:281: out_line(&format!("{computed}  {label}"))

From src/commands/flatten/rename.rs:
  pub fn encode_relative(rel: &Path) -> String;   // currently hardcodes '_' as the join char
  pub fn dedupe(name: &str, occupied: &HashSet<String>) -> String;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase-8 deps to Cargo.toml and scaffold Wave-0 tests (RED)</name>
  <read_first>
    - Cargo.toml (the `[dependencies]` block + the existing comment style)
    - .planning/phases/08-filesystem-depth/08-RESEARCH.md (§ Installation block — exact feature unions for `windows`)
    - tests/hash.rs (the `hash_cmd`/`box_file`/`row` helpers and existing assertions)
    - tests/flatten.rs (the runner shape + existing dry-run/copy assertions)
  </read_first>
  <action>
    Add the four Phase-8 dependencies to `Cargo.toml [dependencies]`, ALL up front so plans 08-02 and 08-03 never edit the manifest (RESEARCH § Primary recommendation): `indicatif = "0.18.4"`; promote `ignore = "0.4"` and `globset = "0.4"` from dev-only to runtime deps; add `windows = { version = "0.61", features = ["Win32_Storage_FileSystem", "Win32_Foundation"] }`. Mirror the existing per-dep comment style; note that `windows 0.61` unifies with the transitive `windows ^0.61` from `tauri-winrt-notification 0.7.2` (D-2) and that `ignore`/`globset` are consumed by 08-02 (tree gitignore, du exclude) and `windows`/`indicatif` by 08-02/08-03/08-01.

    Then scaffold the new integration tests as FAILING (RED) seams:
    - In `tests/hash.rs`: `multi_file_two_space` (two args → two `digest␣␣filename` lines, two spaces each, mapping to the known-answer `box.bin` fixtures), `json_multifile_purity` (`--json` over 2 files → one parseable document with `count==2`, stdout contains no `0x1B` byte and no BOM), `partial_failure_exit1` (one readable + one missing path → readable digest on stdout, an `error:` line on stderr naming the missing path, exit code 1).
    - In `tests/flatten.rs`: `extensions_filter` (`--extensions jpg,png` copies only the matching files; a `.txt` is absent from the output dir), `include_hidden` (a `.dotfile` is copied only with `--include-hidden`), and a unit test entry point reference for `rename::encode_relative_honors_separator` (the unit test itself lands in rename.rs in Task 3).

    These tests MUST compile and FAIL against the current binary (the flags/behaviors do not exist yet).
  </action>
  <acceptance_criteria>
    - `cargo build` succeeds with the four new deps resolved (`cargo tree -i windows` still shows a single `windows v0.61.x`).
    - `Cargo.toml` `[dependencies]` contains `indicatif`, `ignore`, `globset`, and `windows` with the two listed features.
    - `cargo test --test hash multi_file_two_space` and `cargo test --test flatten extensions_filter` COMPILE and FAIL (red) — the behaviors are unimplemented.
    - No existing test regresses to a compile error.
  </acceptance_criteria>
  <verify>
    <automated>cargo build 2>&1 | tail -5</automated>
  </verify>
  <done>Four deps added to Cargo.toml; new failing tests compile and fail in tests/hash.rs and tests/flatten.rs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hash multi-file + coreutils format + stderr progress (HASH-V2-02)</name>
  <read_first>
    - src/commands/hash/mod.rs (the full `run`, the `HashRow`/`HashOutput` structs, the line-281 two-space row, the `digest_reader`/`read_file_or_stdin` flow)
    - src/core/input.rs (`ResolvedInput` is single-pass `Box<dyn Read>`)
    - src/core/output.rs (`is_json_on`, `emit_json`, `out_line`)
    - .planning/phases/08-filesystem-depth/08-RESEARCH.md (§ Code Examples → "Multi-file hash loop"; § Open Questions #1 + Assumption A1)
  </read_first>
  <behavior>
    - `box hash a.bin b.bin` → two lines, each exactly `{digest}  {label}` (TWO spaces), in argument order.
    - No path args + piped stdin → single row labelled `-` (unchanged single-file behavior).
    - One unreadable path among readable ones → readable digests printed, one `error:` per bad file on stderr, exit 1 (coreutils best-effort).
    - `--json` over N readable files → `{results:[{path,algo,digest}×N], count:N}`, one document, no `0x1B`, no BOM. Under a partial failure, emit the document containing only the SUCCESSFUL rows AND exit 1 with per-file errors on stderr (the deliberate partial-success contract — a documented refinement of D-09, A1).
  </behavior>
  <action>
    Change `HashArgs.path: Option<String>` to `paths: Vec<String>` (`#[arg(...)]`; an empty Vec means "read stdin", label `-`). Build the target list: empty → `vec![None]`, else each path mapped to `Some(p)`. Loop each target through `read_file_or_stdin(t)` then `digest_reader(algo, input.reader)`, collecting `HashRow { path: label, algo, digest }`. Resolve `algo` once via the EXISTING CLI>env>config>builtin chain (do not duplicate the resolver). For the human path, keep the line-281 two-space `out_line(&format!("{digest}  {label}"))` per readable file; on a read/hash error, `eprintln!("error: {e:#}")` and set a `had_error` flag (best-effort continue, A1). After the loop, if `is_json_on()` emit one `HashOutput { count: rows.len(), results: rows }`; finally if `had_error` `std::process::exit(1)`.

    Preserve the `--verify` path UNCHANGED — verify stays single-input (the first/only path); multi-file applies to the COMPUTE path only this phase. Add an `indicatif` progress bar to STDERR for the compute loop: construct it with `ProgressDrawTarget::stderr()`, advance once per file, and either hide it (`ProgressBar::hidden()`) or skip construction entirely when `is_json_on()` (Pitfall 2). Pick a sensible "large input" cutoff (e.g. only attach a per-file byte spinner above a threshold, or a file-count bar when `paths.len()` exceeds a small N) — document the chosen cutoff in a doc comment; never draw progress to stdout, never under `--json`.

    Update the multi-file doc comment to record the two-space coreutils format, the best-effort exit-1 policy, and the A1 `--json` partial-success contract.
  </action>
  <acceptance_criteria>
    - `cargo test --test hash multi_file_two_space` passes (two `digest␣␣filename` lines, two spaces).
    - `cargo test --test hash json_multifile_purity` passes (count==2, no 0x1B, no BOM, one parseable value).
    - `cargo test --test hash partial_failure_exit1` passes (readable digest on stdout, stderr error, exit 1).
    - `cargo test --bin box` hash unit tests still pass (known-answer vectors, algo_from_len unchanged).
    - Source: `hash/mod.rs` contains `paths: Vec<String>` and an `indicatif`/`ProgressDrawTarget::stderr` usage guarded by `is_json_on()`.
    - Manual (deferred to verify-work): a large file shows a stderr bar that is absent under `--json`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --test hash 2>&1 | tail -15</automated>
  </verify>
  <done>Multi-file hash prints coreutils two-space rows, best-effort exit 1 on partial failure, clean --json document, stderr-only progress.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: flatten --extensions / --separator / --include-hidden + progress (FLAT-V2-01)</name>
  <read_first>
    - src/commands/flatten/mod.rs (`FlattenArgs`, `build_plan` at 326-399, the `is_hidden` walk filter, the real-run copy loop)
    - src/commands/flatten/rename.rs (`encode_relative` hardcoding `_`, `dedupe`, the existing unit tests)
    - src/core/fs.rs (`is_hidden` — the shared prune the `--include-hidden` flag must bypass)
    - .planning/phases/08-filesystem-depth/08-CONTEXT.md (§ Claude's Discretion → flatten filter semantics)
  </read_first>
  <behavior>
    - `--extensions jpg,png` (comma-separated, case-insensitive, final-extension match) → only matching files appear in the plan/output; non-matching files are simply absent (not skip rows).
    - `--include-hidden` → a `.dotfile` that the default walk prunes is included.
    - `--separator -` → `encode_relative` joins path segments with `-` instead of `_`; `rename::encode_relative_honors_separator` unit test passes.
  </behavior>
  <action>
    Add three fields to `FlattenArgs`: `extensions: Option<String>` (`#[arg(long)]`, comma-separated list), `separator: Option<String>` (`#[arg(long)]`, default applied as `_` when unset), and `include_hidden: bool` (`#[arg(long)]`). Validate `--separator` rejects a value containing `/` or `\` (V5 input validation) → clean `anyhow` error (exit 1), never a panic.

    Thread the filters into `build_plan` (the single source of truth feeding both the human render and `FlattenOutput`/`emit_json`, so no-drift is free):
    - When `include_hidden` is set, drop the `is_hidden` predicate from the `filter_entry` for the walk (still skip the root-exemption-irrelevant symlinks). Keep `is_hidden` exactly as-is when the flag is unset.
    - When `extensions` is set, parse it once into a lowercased set; for each candidate FILE compare its final extension (lowercased via `Path::extension`) against the set, skipping non-matches BEFORE they become plan items (so they never inflate `count`).
    - Parameterize `rename::encode_relative` to take the separator (`encode_relative(rel: &Path, sep: &str)`), replacing the hardcoded `'_'` segment join; pass the resolved separator from `build_plan`. Keep `sanitize_reserved`/`dedupe` semantics intact (dedupe numeric suffix may stay `_` — document the choice). Add the `rename::encode_relative_honors_separator` unit test.

    Add an `indicatif` progress bar to STDERR over the real-run copy loop (`plan.items.len()` is known), advanced once per copied item; hide/skip it when `is_json_on()` (Pitfall 2). Pick and document a "large input" cutoff (e.g. only show the bar above some item-count threshold). Progress is never on stdout and never under `--json`.
  </action>
  <acceptance_criteria>
    - `cargo test --test flatten extensions_filter` passes (only jpg/png copied; txt absent).
    - `cargo test --test flatten include_hidden` passes (dotfile copied only with the flag).
    - `cargo test --bin box rename::encode_relative_honors_separator` passes.
    - `cargo test --test flatten` (the full existing suite) stays green — default behavior (no new flags) is byte-identical to v1.
    - Source: `flatten/mod.rs` contains `include_hidden` and an extension filter inside `build_plan`; `rename.rs` `encode_relative` takes a separator parameter.
    - `box flatten <src> <out> --json` still emits one clean document (no 0x1B) with the filtered `count`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --test flatten 2>&1 | tail -15</automated>
  </verify>
  <done>flatten honors --extensions/--separator/--include-hidden inside build_plan with stderr-only progress; default output unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → filesystem walk | User-supplied `--extensions`/`--separator` values and multiple path args cross into the walk + collision-encoding |
| File contents → hasher | Untrusted file bytes streamed into BLAKE3/RustCrypto (read-only) |
| Dependency install → build | Four crates added to the manifest (supply chain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | Tampering | flatten `--separator` value | mitigate | Reject a separator containing `/` or `\` before it reaches `encode_relative`; existing `encode_relative` guarantees no `\`/`/`/`..` survives in the output name (`tests/flatten::encode_no_separator`) |
| T-8-01-INJ | Tampering | flatten `--extensions` parse | mitigate | Pure string compare against a lowercased set; no glob/regex compiled, no path traversal possible |
| T-8-01-DOS | Denial of Service | non-UTF-8 NTFS path under `--json` | mitigate | `to_string_lossy()` for every path field (D-4); never `to_str().unwrap()` |
| T-8-01-PROG | Information Disclosure | indicatif progress contaminating stdout | mitigate | `ProgressDrawTarget::stderr()`, hidden/skipped under `is_json_on()`; json purity test asserts no `0x1B` on stdout |
| T-8-SC | Tampering | indicatif/ignore/globset/windows install | mitigate | RESEARCH § Package Legitimacy Audit cleared all four as Approved (BurntSushi/console-rs/Microsoft; three already in the build graph). No `[ASSUMED]`/`[SUS]`/`[SLOP]` package → no blocking checkpoint required |
</threat_model>

<verification>
- `cargo test` (full suite) green; `cargo clippy --all-targets -- -D warnings` clean.
- `cargo build` resolves the four new deps; `cargo tree -i windows` shows a single 0.61.x.
- Default `box hash <file>` and `box flatten <src> <out>` output byte-identical to v1 (trycmd snapshots, where present, unchanged).
</verification>

<success_criteria>
- HASH-V2-02 and FLAT-V2-01 marked deliverable: multi-file coreutils hash with best-effort exit 1; flatten filters + stderr progress; both clean under `--json`.
- All four Phase-8 deps live in Cargo.toml so 08-02/08-03 never touch the manifest.
</success_criteria>

<output>
Create `.planning/phases/08-filesystem-depth/08-01-SUMMARY.md` when done.
</output>
