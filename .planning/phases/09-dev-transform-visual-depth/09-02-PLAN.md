---
phase: 09-dev-transform-visual-depth
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - Cargo.toml
  - src/commands/matrix/mod.rs
  - src/commands/qr/mod.rs
  - src/commands/ascii/mod.rs
  - tests/matrix.rs
  - tests/qr.rs
  - tests/ascii.rs
autonomous: true
requirements: [MTRX-V2-01, QR-V2-01, ASCI-V2-01]

must_haves:
  truths:
    - "box matrix honors --color (named preset → head/fade RGB), --speed (slow/normal/fast → poll interval), and --charset (preset name OR a literal custom string); all color stays is_color_on()-gated"
    - "box qr 'x' --save out.png writes a non-empty PNG and out.svg writes an SVG; an unsupported extension exits 1; the terminal glyph block is suppressed under --save with a stderr confirmation"
    - "box qr --error-correction L|M|Q|H changes the EC level (default M) for BOTH the terminal render and the saved file"
    - "box qr 'x' --save out.png --json writes the file AND emits a metadata document carrying saved_path (the two are orthogonal)"
    - "box ascii produces truecolor in a TTY and degrades to the mono RAMP when piped/NO_COLOR/--json; --braille renders the 2x4 dot engine; --invert flips luma at the single seam"
    - "new colored paths (matrix --color, ascii truecolor) emit no 0x1B byte when stdout is redirected (SC4 byte-identical-minus-ANSI)"
  artifacts:
    - path: "Cargo.toml"
      provides: "qrcode image+svg features re-enabled"
      contains: "qrcode"
    - path: "src/commands/matrix/mod.rs"
      provides: "--color/--speed/--charset presets"
      contains: "ValueEnum"
    - path: "src/commands/qr/mod.rs"
      provides: "--save PNG/SVG + --error-correction + saved_path"
      contains: "saved_path"
    - path: "src/commands/ascii/mod.rs"
      provides: "truecolor + braille 2x4 + invert"
      contains: "0x2800"
  key_links:
    - from: "src/commands/qr/mod.rs render_qr"
      to: "both the terminal render and the --save file"
      via: "one EcLevel param feeds both"
      pattern: "with_error_correction_level"
    - from: "src/commands/ascii/mod.rs .truecolor"
      to: "is_color_on() gate"
      via: "the SOLE color gate; plain branch byte-identical-minus-ANSI"
      pattern: "is_color_on\\(\\)"
    - from: "src/commands/qr/mod.rs --save"
      to: "std::fs / ImageBuffer::save with .with_context"
      via: "extension whitelist .png/.svg, bail! on unknown"
      pattern: "with_context"
---

<objective>
Add the three visual depth flags, each additive to its frozen command struct: `matrix` `--color`/`--speed`/`--charset` named presets (MTRX-V2-01); `qr` `--save <file>` PNG/SVG + `--error-correction L|M|Q|H` (QR-V2-01); `ascii` truecolor default-on + `--braille` 2×4 engine + `--invert` (ASCI-V2-01).

Purpose: matrix and ascii add new colored paths that ALL attach in the single `is_color_on()` gate (SC4 byte-identity falls out for free); `qr --save` is the only new filesystem I/O this phase and must error cleanly, never panic.
Output: three extended command modules + their tests; the one manifest edit re-enabling qrcode `["image","svg"]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dev-transform-visual-depth/09-CONTEXT.md
@.planning/phases/09-dev-transform-visual-depth/09-PATTERNS.md
@.planning/phases/09-dev-transform-visual-depth/09-RESEARCH.md
@.planning/phases/09-dev-transform-visual-depth/09-VALIDATION.md

This plan is Wave 2: it depends on 09-01 because both edit Cargo.toml and this plan's `qr --save` code requires the qrcode `["image","svg"]` features compiled in — 09-01 owns Cargo.toml in Wave 1, so this plan's manifest edit lands conflict-free afterward.

matrix and ascii are display-only (SC4 — they never call `is_json_on`/`emit_json`); qr DOES emit a metadata document (D-14). `is_color_on()` is the SOLE color gate; `init_output` forces it false under `--json`/`--clip`/redirect, so piped output is byte-identical minus ANSI. ascii currently imports NEITHER `owo_colors` NOR `is_color_on` — adding color means importing both for the first time (Pitfall 4). Re-enable qrcode features (RESEARCH-locked) — do NOT hand-roll a rasterizer.
</context>

<tasks>

<task type="auto">
  <name>Task 1: matrix --color / --speed / --charset presets</name>
  <read_first>
    - src/commands/matrix/mod.rs (the preset consts :85-94 SPEED_MIN/MAX + HEAD_RGB + FADE_BRIGHT/FADE_DARK, the gated `.truecolor` sites :178 + :188, `katakana_glyphs` :306, the `glyphs` binding :156, `MatrixArgs` :107, the `RawGuard`/`is_quit`/loop :117-322 — UNTOUCHED)
    - src/commands/hash/mod.rs (the `pub` clap `ValueEnum` style — `Algo` — to mirror for the presets)
    - tests/cli.rs (the `display_only_omit_json` SC4 piped-purity template)
    - .planning/phases/09-dev-transform-visual-depth/09-PATTERNS.md (the matrix section)
  </read_first>
  <files>src/commands/matrix/mod.rs, tests/matrix.rs</files>
  <behavior>
    - Each --color preset (Green default / Red / Blue / Cyan / Magenta / Yellow / White) resolves to a deterministic head/fade RGB via a pure helper.
    - Each --speed level (Slow / Normal default / Fast) resolves to a deterministic poll-interval ms / SPEED range via a pure helper.
    - --charset resolves a known preset name (katakana default / ascii / binary / digits) to its glyph table, and a literal custom string to that string's chars.
    - All color stays gated on is_color_on(); a redirected matrix run carries no 0x1B byte and no JSON document.
  </behavior>
  <action>
    matrix: add three flags to `MatrixArgs` (currently empty), matching the `hash::Algo` `pub ValueEnum` style. `--color` = a `pub enum MatrixColor { Green (default), Red, Blue, Cyan, Magenta, Yellow, White }` (ValueEnum) mapping via a pure helper to the head/fade RGB that today are the `HEAD_RGB`/`FADE_*` consts (:90-94) — NOT arbitrary hex. `--speed` = a `pub enum Speed { Slow, Normal (default), Fast }` (ValueEnum) mapping via a pure helper to the `event::poll` interval ms and the `SPEED_MIN/MAX` range (:85-87). `--charset` = a `String` arg that resolves through a pure helper: a known preset name (`katakana` [default] / `ascii` / `binary` / `digits`) → its glyph table, else the literal string's chars become the glyph set — replacing the `glyphs` binding at :156 (today `katakana_glyphs()`). Feed the preset RGB into the EXISTING gated `.truecolor` calls at :178/:188 — keep them gated on `is_color_on()`; add no new ungated color. The `RawGuard`/`is_quit`/`event::poll`/single-flush loop is UNTOUCHED (display-only, SC4). Keep all three resolvers as pure, terminal-free helpers so they unit-test without a terminal.
  </action>
  <verify>
    <automated>cargo test --bin box matrix && cargo test --test matrix</automated>
  </verify>
  <acceptance_criteria>
    - Unit tests assert each `--color` preset → expected head/fade RGB, each `--speed` level → expected poll interval, and `--charset` resolution for a preset name AND a literal custom string (pure helpers, no terminal spawned).
    - `tests/matrix.rs` keeps/extends the `display_only_omit_json`-style assertion that matrix emits no JSON document and a redirected run carries no `0x1B` byte (SC4).
    - `cargo clippy --all-targets -- -D warnings` clean; the loop/quit/RAII code is unchanged.
  </acceptance_criteria>
  <done>matrix accepts --color/--speed/--charset as pure-resolved presets feeding the existing is_color_on()-gated truecolor sites; the RAII loop is untouched, SC4 piped purity holds, and tests + clippy are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: qr --save (PNG/SVG) + --error-correction</name>
  <read_first>
    - src/commands/qr/mod.rs (the pure `render_qr` seam :106-118 with the hardcoded `EcLevel::M` :110, the `is_json_on()` fork + `QrOutput` :63-69 with hardcoded `"M"` :82, the `QrArgs` :53-57, the `println!`/`clip_feed` :91-95)
    - Cargo.toml (the `qrcode` dependency line — currently `default-features = false`)
    - .planning/phases/09-dev-transform-visual-depth/09-RESEARCH.md (§ Code Examples — qr --save PNG and SVG; § Common Pitfalls — Pitfall 3; § Open Questions Q1 — --save + --json orthogonal)
    - .planning/phases/09-dev-transform-visual-depth/09-PATTERNS.md (the qr section — file-I/O discipline)
  </read_first>
  <files>Cargo.toml, src/commands/qr/mod.rs, tests/qr.rs</files>
  <behavior>
    - render_qr honors each EcLevel (L/M/Q/H) — distinct module counts for the same input prove the level is applied.
    - `box qr 'x' --save f.png` writes a file whose first bytes are the PNG magic (0x89 'P' 'N' 'G'); `--save f.svg` writes a file beginning with `<?xml` or `<svg`; both are non-empty.
    - An unsupported extension (`--save f.gif`) exits 1 with a `.png`/`.svg` hint; an unwritable path exits 1 via `.with_context`, never a panic.
    - Under `--save` the terminal glyph block is suppressed and a confirmation is written to stderr.
    - `box qr 'x' --save f.png --json` writes the file AND emits a JSON document with `saved_path` set and `error_correction` reflecting the chosen level.
  </behavior>
  <action>
    Cargo.toml: change the `qrcode` dependency to `default-features = false, features = ["image", "svg"]` (drop `pic`; `image` unifies with the present `image 0.25.10`, `svg` is zero-dep — RESEARCH-locked, do NOT hand-roll a rasterizer).
    qr: add `ec: EcLevel` as a parameter to `render_qr` (default `EcLevel::M`) — replace the hardcoded `EcLevel::M` at :110. Add to `QrArgs`: `--error-correction` as a `pub enum Ec { L, M (default), Q, H }` ValueEnum → `EcLevel`, and `--save: Option<std::path::PathBuf>`. Make `QrOutput.error_correction` reflect the CHOSEN level (replace the hardcoded `"M"` :82) and add `saved_path: Option<String>` with `#[serde(skip_serializing_if = "Option::is_none")]`. On `--save`: infer format from the lowercased extension — `png` → `code.render::<image::Luma<u8>>().quiet_zone(true).build()` then `img.save(path).with_context(...)`; `svg` → `code.render::<qrcode::render::svg::Color>().quiet_zone(true).build()` then `std::fs::write(path, svg).with_context(...)`; any other extension → `bail!("unsupported --save extension '.{other}'; use .png or .svg")` (exit 1, never panic). Suppress the terminal glyph block when `save.is_some()` (gate the `println!("{rendered}")` at :91 behind `if save.is_none()`) and write a stderr confirmation of the written path. `--save` and `--json` are ORTHOGONAL (Open-Q1): under `--save --json` write the file AND emit the metadata document with `saved_path` populated. The `?` on `with_error_correction_level` keeps propagating a capacity overflow → clean exit 1 (T-05-QR-DoS preserved).
  </action>
  <verify>
    <automated>cargo test --bin box render_qr && cargo test --test qr</automated>
  </verify>
  <acceptance_criteria>
    - Unit tests assert `render_qr` produces distinct output per EcLevel for the same input.
    - `tests/qr.rs` uses `assert_fs`/`tempfile` to assert `--save f.png` writes a non-empty file with PNG magic bytes and `--save f.svg` writes a non-empty SVG; a bad extension exits 1; the glyph block is absent from stdout under `--save`.
    - An integration test asserts `--save f.png --json` both writes the file and emits one JSON document containing `"saved_path"`.
    - `cargo clippy --all-targets -- -D warnings` clean.
  </acceptance_criteria>
  <done>qr renders at a selectable EcLevel for both terminal and file, --save writes PNG/SVG via the re-enabled qrcode features (bad extension/unwritable path → clean exit 1, glyphs suppressed + stderr confirmation), and --save+--json is orthogonal with saved_path in the document; qrcode features re-enabled and tests + clippy green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ascii truecolor + --braille (2x4) + --invert</name>
  <read_first>
    - src/commands/ascii/mod.rs (the `image` pipeline + luma seam :77-93, the single `luma_to_char` :104-107, the RAMP const, the imports :32-36 — currently NEITHER owo_colors NOR is_color_on)
    - src/commands/lolcat/mod.rs (the gated per-scalar `.truecolor()` template :100-111 — copy this exact is_color_on() structure)
    - tests/cli.rs (the `display_only_omit_json` SC4 piped-purity template)
    - .planning/phases/09-dev-transform-visual-depth/09-RESEARCH.md (§ Code Examples — ascii braille 2x4 bitmask; § Common Pitfalls — Pitfall 4 SC4 gate)
    - .planning/phases/09-dev-transform-visual-depth/09-PATTERNS.md (the ascii section — three orthogonal additions)
  </read_first>
  <files>src/commands/ascii/mod.rs, tests/ascii.rs</files>
  <behavior>
    - The braille bit-order is locked: for a 2×4 cell, dot i passing the threshold sets bit DOT_BITS[i] (little-endian: dots 1-2-3 → bits 0-1-2 left column, 4-5-6 → bits 3-4-5 right column, 7/8 → bits 6/7 bottom row); the glyph is `char::from_u32(0x2800 + mask)`.
    - --invert applies `255 - luma` at the single luma seam BEFORE the ramp index / braille per-dot threshold.
    - Truecolor is emitted ONLY when is_color_on() is true; a redirected/NO_COLOR/--json run degrades to the mono RAMP and carries no 0x1B byte.
  </behavior>
  <action>
    ascii: add `use owo_colors::OwoColorize;` and `use crate::core::output::is_color_on;` (the module imports NEITHER today — Pitfall 4). Three orthogonal additions: (1) truecolor DEFAULT-ON, gated SOLELY on `is_color_on()` — keep `to_luma8()` for the ramp index but ALSO sample `.to_rgb8()` per cell and emit `.truecolor(r,g,b)` on the ramp glyph only inside the `is_color_on()` branch (copy lolcat's :100-111 structure); plain branch is byte-identical minus the escape; NO `--color` flag. (2) `--braille` (bool) = a hand-rolled 2×4 engine REPLACING the ramp: resize to `2*cols × 4*rows`, build a `u8` mask by setting bit `DOT_BITS[i]` for each of the 8 sub-pixels passing a fixed 50% luma threshold kept as a swappable `const`, glyph = `char::from_u32(0x2800 + mask as u32)`; color in braille mode = average the 8 sub-pixels into one RGB → one gated `.truecolor()` per glyph (braille and color stay orthogonal). Define `const DOT_BITS: [u8; 8]` and lock it with a unit test. (3) `--invert` (bool) = `let luma = if invert { 255 - luma } else { luma };` at the single seam BEFORE `luma_to_char`/the braille threshold. Stays display-only (SC4 — no `--json` document). No crate for braille (the image-only hand-roll exception).
  </action>
  <verify>
    <automated>cargo test --bin box braille && cargo test --test ascii</automated>
  </verify>
  <acceptance_criteria>
    - A unit test locks the braille bit-order (a known sub-pixel pattern → the expected `U+28xx` glyph) and the DOT_BITS mapping.
    - A unit test asserts `--invert` produces `255 - luma` at the seam (extreme/monotonic luma_to_char tests extended).
    - `tests/ascii.rs` asserts (via the SC4 `display_only_omit_json` template + a `> file` scan) that a redirected `box ascii <fixture>` carries no `0x1B` byte while a TTY/forced-color run includes truecolor.
    - `cargo clippy --all-targets -- -D warnings` clean.
  </acceptance_criteria>
  <done>ascii emits is_color_on()-gated truecolor (degrading to the mono RAMP off-TTY), a hand-rolled 2x4 braille engine with a unit-locked bit-order replacing the ramp, and --invert at the single luma seam; the module's new owo_colors/is_color_on imports are added, SC4 piped purity holds, and tests + clippy are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI args → filesystem | `qr --save <path>` is the only new filesystem write; extension + path are user-supplied |
| renderer → stdout/redirect | New colored matrix/ascii paths must not leak ANSI into a pipe/redirect (SC4) |
| user input → QR encoder | Oversized `qr` input can overflow QR capacity (DoS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-SAVE | Tampering/DoS | `qr --save` file write | mitigate | Extension whitelist `.png`/`.svg` only (else `bail!` exit 1); every write `.with_context`-wrapped → clean exit 1, never a panic; user path honored as-is (single-user local CLI, accepted scope) |
| T-09-02-QRDOS | DoS | `qr` QR capacity on oversized `--save` input | mitigate | `with_error_correction_level` returns `Err` → `?`-propagates → clean exit 1 (existing T-05-QR-DoS preserved) |
| T-09-02-ANSI | Tampering/Info disclosure | `matrix --color`, `ascii` truecolor | mitigate | Every new `.truecolor` token reached ONLY after `is_color_on()`; redirected/`NO_COLOR`/`--json` path is byte-identical minus the escape (SC4 `0x1B`-scan test) |
| T-09-02-SC | Tampering (supply chain) | `qrcode` `["image","svg"]` feature re-enable | mitigate | RESEARCH § Package Legitimacy Audit cleared `qrcode` (existing dep, feature re-enable; `image` unifies with the present `image 0.25.10`, `svg` zero-dep). No new package, no `[ASSUMED]`/`[SUS]` → no blocking-human checkpoint required |
| T-09-02-SC2 | Tampering | `qr --save` re-emits rendered content to a file | accept | Output is the QR raster/SVG of the user's own input; no external content; single-user local CLI |
</threat_model>

<verification>
- Per task: `cargo test --bin box <changed-seam>` plus `cargo test --test <cmd>`.
- Plan gate: `cargo test` green for matrix/qr/ascii + `cargo clippy --all-targets -- -D warnings` clean.
- SC4: matrix and ascii each carry a redirected-output `0x1B`-scan assertion (display-only piped purity); qr stays on the `is_json_on()` fork with the metadata document.
</verification>

<success_criteria>
- ROADMAP Phase 9 SC2 satisfied: matrix `--color`/`--speed`/`--charset`; qr `--save` PNG/SVG + `--error-correction`; ascii truecolor + `--braille` + `--invert`.
- ROADMAP Phase 9 SC4 satisfied for matrix --color and ascii truecolor: piped/redirected output byte-identical minus ANSI.
- qrcode `["image","svg"]` features re-enabled and the workspace builds; `qr --save` + `--json` are orthogonal (Open-Q1 locked).
</success_criteria>

<output>
Create `.planning/phases/09-dev-transform-visual-depth/09-02-SUMMARY.md` when done.
</output>
