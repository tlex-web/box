---
phase: 09-dev-transform-visual-depth
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/lolcat/mod.rs
  - tests/lolcat.rs
autonomous: false
requirements: [LOL-V2-01]

must_haves:
  truths:
    - "box lolcat 'text' --animate produces a smooth ~20-FPS animated rainbow in a PS7 TTY and exits cleanly on Ctrl+C / q / Esc with no stuck raw mode"
    - "after the --duration deadline (or a quit key) the alternate screen is left and ONE final colored frame persists in the normal buffer"
    - "box lolcat 'x' --animate | cat (or under --json/--clip, or any non-TTY stdout) degrades to the existing static one-pass render — never enters raw mode, never writes an escape to the pipe"
    - "--freq and --seed visibly change the gradient and ALSO govern the static one-pass render (one gradient path)"
    - "--duration 0 runs until a quit keypress"
  artifacts:
    - path: "src/commands/lolcat/mod.rs"
      provides: "parameterized rgb_at(phase, freq) + bounded alt-screen animate loop + TTY/color AND-gate"
      contains: "is_terminal"
    - path: "tests/lolcat.rs"
      provides: "rgb_at bounds + piped-degrades-to-static + non-hanging smoke"
      contains: "animate"
  key_links:
    - from: "src/commands/lolcat/mod.rs animate entry"
      to: "std::io::stdout().is_terminal() && is_color_on() && !is_json_on() && !is_clip_on()"
      via: "mandatory AND-gate before raw mode; else dispatch to the static renderer"
      pattern: "is_terminal\\(\\)"
    - from: "src/commands/lolcat/mod.rs rgb_at(phase, freq)"
      to: "both the animated frames and the static one-pass render"
      via: "one parameterized gradient function"
      pattern: "rgb_at\\("
    - from: "src/commands/lolcat/mod.rs RawGuard"
      to: "Drop restores cooked mode + leaves alt-screen"
      via: "armed before the fallible alt-screen execute!; panic-free loop"
      pattern: "RawGuard"
---

<objective>
Add the animated `lolcat` path (LOL-V2-01) — the phase headline and the only PS7 human-verify gate. `--animate` runs a bounded alternate-screen loop that reuses `matrix`'s RAII teardown VERBATIM, persists one final colored frame, and degrades to the existing static one-pass renderer whenever stdout is not a real TTY. `--freq`/`--seed` parameterize the gradient and govern BOTH the animated and static paths.

Purpose: terminal-loop correctness (RAII restore, single-flush-per-frame, Press-only quit) and the MANDATORY `is_terminal()` AND-gate that prevents writing raw-mode escapes to a pipe (SC3). The on-screen smoothness and clean restore cannot be proven by automated test alone → a human clears it in PS7.
Output: the extended lolcat module + its automatable tests; a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dev-transform-visual-depth/09-CONTEXT.md
@.planning/phases/09-dev-transform-visual-depth/09-PATTERNS.md
@.planning/phases/09-dev-transform-visual-depth/09-RESEARCH.md
@.planning/phases/09-dev-transform-visual-depth/09-VALIDATION.md

This plan is independent (Wave 1) — lolcat is its own module, uses the already-present `crossterm` (no Cargo.toml change), and shares no files with 09-01/09-02. lolcat is display-only (SC4 — never calls `is_json_on`/`emit_json`). The matrix loop at `matrix/mod.rs:117-322` is the verbatim copy-me template (RawGuard, `event::poll(50ms)` as frame-timer-AND-input-gate, single per-frame `flush()`, `KeyEventKind::Press`-only `is_quit`). lolcat does NOT currently import crossterm — add it.

NON-OBVIOUS GOTCHA (carry into the checkpoint instructions): `read_input` (core/input.rs) gates on STDIN being a TTY and exits 2 (MissingInput) for a no-arg interactive invocation. So `box lolcat --animate` typed with no text exits 2 BEFORE the animate branch — the animate gate is on STDOUT `is_terminal()`, independent of stdin. The human-verify MUST pass text as an arg: `box lolcat "Hello World" --animate`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parameterize rgb_at + bounded alt-screen animate loop with TTY/color AND-gate</name>
  <read_first>
    - src/commands/lolcat/mod.rs (the `rgb_at(phase)` gradient :128-134 with the hardcoded `f=0.1`, the gated per-scalar `.truecolor()` :100-111, the static one-pass render :67-118 with the unconditional ANSI strip :74 and single flush :83-84/:116, `LolcatArgs` :61-65)
    - src/commands/matrix/mod.rs (the VERBATIM template :117-322 — `RawGuard` :117-142, the single-flush-then-`event::poll(50ms)` :205-217, `is_quit` :316-322)
    - .planning/phases/09-dev-transform-visual-depth/09-PATTERNS.md (§ Shared Patterns — the four load-bearing loop pieces; the lolcat section)
    - .planning/phases/09-dev-transform-visual-depth/09-RESEARCH.md (§ Common Pitfalls — Pitfall 1 the headline risk + the read_input vs stdout-TTY subtlety; § Open Questions Q2 the --duration default)
  </read_first>
  <files>src/commands/lolcat/mod.rs</files>
  <behavior>
    - rgb_at(phase, freq) returns r/g/b each in 0..=255 for any phase; changing freq or the initial phase (seed) changes the returned triple (the gradient shifts).
    - The static one-pass render uses the SAME rgb_at(phase, freq) so --freq/--seed affect it too.
    - The animate branch is entered ONLY when std::io::stdout().is_terminal() && is_color_on() && !is_json_on() && !is_clip_on(); otherwise the existing static renderer runs and writes no escape off-TTY.
    - The input ANSI strip (strip_ansi_escapes::strip_str) is applied on BOTH the animated and static paths.
  </behavior>
  <action>
    lolcat: parameterize the gradient — change `rgb_at(phase: f64) -> (u8,u8,u8)` to `rgb_at(phase: f64, freq: f64)` (drop the hardcoded `f = 0.1`; the per-frame phase step stays a tuned flow-speed const). Route BOTH the static one-pass render and the animated frames through this one function (the D-11 single-color-path discipline). Add `use std::io::IsTerminal;` and `use crossterm::...` (the module does not import crossterm today). Extend `LolcatArgs` with `--animate` (bool), `--freq <f64>` (default the prior 0.1), `--seed <f64>` (initial phase offset, default 0.0), and `--duration <u64>` (seconds; small default, e.g. 3 — document in `--help`; `0` = run until keypress). In `run()`, BEFORE the existing static render, branch into the animate loop ONLY when `std::io::stdout().is_terminal() && is_color_on() && !is_json_on() && !is_clip_on()` (the `is_terminal()` AND-gate is MANDATORY — `CLICOLOR_FORCE` can force `is_color_on()` true on a pipe, and a raw-mode escape on a pipe is the SC3-forbidden hazard); otherwise fall through to the EXISTING static renderer (the degrade path, byte-identical minus ANSI). Copy matrix's loop VERBATIM: `enable_raw_mode()?` then arm `let _guard = RawGuard;` BEFORE the fallible `execute!(EnterAlternateScreen, Hide)?`; each frame advance ONE global phase offset (seed = the initial offset), redraw the text colored via `rgb_at(phase, freq)`, `out.flush()?` EXACTLY ONCE per frame, then `event::poll(Duration::from_millis(50))?` as the ~20-FPS frame-timer-AND-input-gate with a `KeyEventKind::Press`-only `is_quit` (q / Esc / Ctrl+C). Run until an `Instant`-based `--duration` deadline OR a quit key (`--duration 0` = run until keypress). After the loop: `execute!(LeaveAlternateScreen, Show)` and reprint ONE final static frame to the normal buffer so the colored text PERSISTS. Keep the loop panic-free (no `.unwrap()` on terminal ops) so the `RawGuard` Drop is the real restore under release `panic="abort"`. Keep the unconditional `strip_ansi_escapes::strip_str` on the input (:74) on BOTH paths (T-04L-01).
  </action>
  <verify>
    <automated>cargo test --bin box rgb_at</automated>
  </verify>
  <acceptance_criteria>
    - A unit test asserts every channel of `rgb_at(phase, freq)` is in 0..=255 across a sweep of phases, and that changing `freq`/seed changes the output triple.
    - A source assertion (grep) confirms the animate branch is guarded by `std::io::stdout().is_terminal()` AND `is_color_on()` AND `!is_json_on()` AND `!is_clip_on()`.
    - A source assertion confirms `RawGuard` is armed before the `EnterAlternateScreen` `execute!` and that there is exactly one `flush()` per frame.
    - `cargo clippy --all-targets -- -D warnings` clean.
  </acceptance_criteria>
  <done>rgb_at is parameterized to (phase, freq) and feeds both the animated and static paths; --animate/--freq/--seed/--duration are wired; the matrix RAII loop is copied verbatim behind the mandatory is_terminal() AND is_color_on() AND-gate (off-TTY degrades to static); the final frame persists; the loop is panic-free and rgb_at + source-gate tests + clippy are green.</done>
</task>

<task type="auto">
  <name>Task 2: automatable lolcat tests — piped degrades to static, non-hanging smoke, byte-identity</name>
  <read_first>
    - src/commands/lolcat/mod.rs (the now-extended args + animate gate from Task 1)
    - tests/lolcat.rs (existing integration assertions to extend)
    - tests/cli.rs (the `display_only_omit_json` SC4 piped-purity template)
    - .planning/phases/09-dev-transform-visual-depth/09-VALIDATION.md (the LOL-V2-01 row — automatable subset vs human-gated)
  </read_first>
  <files>tests/lolcat.rs</files>
  <behavior>
    - A piped/non-TTY `--animate` invocation degrades to the static render: it exits cleanly, emits no 0x1B byte, and equals the static (non-animate) output byte-for-byte.
    - The piped `--animate` invocation terminates promptly (does not block on event::poll without a TTY).
    - Two static runs with different --seed values produce differing output.
  </behavior>
  <action>
    Add integration tests to `tests/lolcat.rs` covering the AUTOMATABLE subset (the on-screen smoothness and restore are deferred to the human-verify in Task 3): (1) piped/non-TTY degrades to static — `echo hi | box lolcat --animate` (and `box lolcat "x" --animate` with stdout captured by the test harness, i.e. not a TTY) does NOT enter raw mode, exits cleanly (status success), and its stdout carries no `0x1B` byte and equals the static render byte-for-byte (use `cli.rs::display_only_omit_json` as the template). (2) non-hanging smoke — the piped `--animate` invocation terminates promptly without a TTY (assert it returns rather than blocking on `event::poll`). (3) `--freq`/`--seed` change the static output (two runs with different seeds differ). Do NOT attempt to assert animation smoothness or terminal restore here — those are the PS7 human gate.
  </action>
  <verify>
    <automated>cargo test --test lolcat</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test lolcat` asserts a piped `--animate` run exits cleanly, emits no `0x1B`, and matches the static render byte-for-byte.
    - A test asserts the piped `--animate` invocation does not hang (completes within the harness without a TTY).
    - A test asserts differing `--seed` values produce differing static output.
  </acceptance_criteria>
  <done>tests/lolcat.rs proves the automatable LOL-V2-01 subset: piped --animate degrades to a byte-identical static render with no 0x1B, the piped invocation does not hang, and --seed changes the static output; cargo test --test lolcat green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: PS7 human-verify — animated lolcat smoothness + clean terminal restore (LOL-V2-01 / SC3)</name>
  <files>src/commands/lolcat/mod.rs (the animated path under verification)</files>
  <read_first>
    - .planning/phases/09-dev-transform-visual-depth/09-VALIDATION.md (§ Manual-Only Verifications — the LOL-V2-01 PS7 instructions)
    - .planning/phases/09-dev-transform-visual-depth/09-RESEARCH.md (§ Common Pitfalls — Pitfall 1, the read_input stdin-TTY gotcha)
  </read_first>
  <action>
    PAUSE for human verification in a real PowerShell 7 terminal. This is the one manual gate of the phase — Claude has already automated and tested everything testable (Tasks 1-2: rgb_at bounds, the is_terminal() AND-gate, piped-degrades-to-static byte-identity, non-hanging smoke, the --seed effect). Present the steps in <how-to-verify> and WAIT for the human's "approved" or a failure description; do NOT auto-advance (workflow.auto_advance is off and this gate is blocking).
  </action>
  <what-built>
    `box lolcat --animate` — a bounded alternate-screen rainbow loop reusing matrix's RAII teardown, with `--freq`/`--seed` parameterizing the gradient, a `--duration` deadline (`0` = until keypress), a final persisted colored frame, and a mandatory `is_terminal()` AND-gate that degrades to the static render off-TTY. Automated tests already proved: the loop never runs off-TTY, piped output is byte-identical-minus-ANSI, the run does not hang, and `--seed` changes the gradient math. What a human must confirm in a real PS7 terminal: smoothness and clean raw-mode restore.
  </what-built>
  <how-to-verify>
    In a real PowerShell 7 terminal (NOT a captured/redirected shell), after `cargo build --release` (or `cargo run --`):
    1. Run `box lolcat "Hello World" --animate`. You MUST pass text as an argument — `box lolcat --animate` with no text exits 2 (read_input gates on STDIN-TTY); that is expected, not a failure.
    2. Confirm a SMOOTH ~20-FPS animated rainbow (no per-character stutter, no flicker).
    3. Press `q` — it exits on the FIRST press (no double-press needed), the cursor is visible, and the prompt is in cooked mode (type a command to confirm no stuck raw mode). Repeat for `Esc` and `Ctrl+C`.
    4. Confirm ONE final colored frame PERSISTS in the normal buffer after exit (the rainbow text remains visible).
    5. Run `box lolcat "Hello World" --animate --freq 0.3` then `--seed 50` — confirm the gradient VISIBLY differs each time.
    6. Run `box lolcat "Hello World" --animate --duration 0` — confirm it runs until you press a quit key.
    7. Run `box lolcat "x" --animate | cat` — confirm it degrades to a static (non-animated) render with no garbage/escape sequences and no hang.
  </how-to-verify>
  <verify>
    <human-check>All seven PS7 checks pass: smooth animation; clean q/Esc/Ctrl+C restore with no stuck raw mode; persisted final frame; visible --freq/--seed effect; --duration 0 runs to keypress; piped run degrades to static.</human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms (types "approved") a smooth ~20-FPS rainbow, clean exit on q / Esc / Ctrl+C with the cursor restored and cooked mode intact, a persisted final colored frame, a visible --freq/--seed gradient change, --duration 0 running until keypress, and a static (no-escape, non-hanging) degrade when piped.
    - Any failure (stuck raw mode, stutter, no persist, unchanged gradient) is filed as a specific defect for gap closure.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all seven checks pass, or describe the specific failure (e.g. stuck raw mode, stutter, no persist, gradient unchanged).</resume-signal>
  <done>A human in PS7 has approved the animated lolcat (smoothness + clean restore + persist + freq/seed effect + piped degrade), or filed specific defects for gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdout TTY vs pipe | Raw-mode escapes must NEVER be written to a non-TTY stdout (the SC3 hazard) |
| user input → recolored output | Input may contain terminal escape sequences that could be re-emitted |
| panic → terminal state | A panic mid-loop must still restore cooked mode (release `panic="abort"`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-PIPE | DoS/corruption | `lolcat --animate` off-TTY | mitigate | MANDATORY `std::io::stdout().is_terminal()` AND-gate before any raw-mode/alt-screen escape — never enter raw mode off-TTY even when `CLICOLOR_FORCE` forces color; off-TTY dispatches to the static renderer (byte-identical-minus-ANSI) |
| T-09-03-INJECT | Tampering | terminal-escape re-injection via input | mitigate | Unconditional `strip_ansi_escapes::strip_str` on the input on BOTH the animated and static paths (T-04L-01) before recolor |
| T-09-03-PANIC | DoS | stuck raw mode after a panic | mitigate | `RawGuard` Drop armed BEFORE the fallible alt-screen `execute!`; loop kept panic-free (no `.unwrap()` on terminal ops) so Drop is the restore backstop under `panic="abort"` |
| T-09-03-SC | Tampering (supply chain) | dependencies | accept | No new crate — `crossterm`/`owo-colors`/`strip-ansi-escapes` are all already in the manifest; nothing to vet |
</threat_model>

<verification>
- Automatable (Tasks 1-2): `cargo test --bin box rgb_at` + `cargo test --test lolcat` (piped-degrades-to-static byte-identity, non-hanging smoke, seed effect) + the source-assertion gates on the AND-gate and RawGuard ordering.
- Phase gate (before /gsd:verify-work): full `cargo test` green + `cargo clippy --all-targets -- -D warnings` clean, THEN the Task 3 PS7 human-verify.
- SC4/SC3: the piped run carries no `0x1B`; the animate branch is unreachable off-TTY.
</verification>

<success_criteria>
- ROADMAP Phase 9 SC3 satisfied (cleared by the human in PS7): smooth animated rainbow, clean Ctrl+C/q/Esc restore with no stuck raw mode, visible `--freq`/`--seed` effect, persisted final frame, static degrade when piped/`--json`.
- ROADMAP Phase 9 SC4 satisfied for animated lolcat: piped/redirected output byte-identical minus ANSI; raw mode never entered off-TTY.
- `rgb_at(phase, freq)` is the single gradient function feeding both the animated and static paths; no Cargo.toml change.
</success_criteria>

<output>
Create `.planning/phases/09-dev-transform-visual-depth/09-03-SUMMARY.md` when done.
</output>
